#%RAML 1.0
title: Amazon Relational Database Service
version: '2014-10-31'
description: '     Amazon Relational Database Service (Amazon RDS) is a web service
  that makes it easier to set up, operate, and scale a relational database in the
  cloud. It provides cost-efficient, resizeable capacity for an industry-standard
  relational database and manages common database administration tasks, freeing up
  developers to focus on what makes their applications and businesses unique.  Amazon
  RDS gives you access to the capabilities of a MySQL, MariaDB, PostgreSQL, Microsoft
  SQL Server, Oracle, or Amazon Aurora database server. These capabilities mean that
  the code, applications, and tools you already use today with your existing databases
  work with Amazon RDS without modification. Amazon RDS automatically backs up your
  database and maintains the database software that powers your DB instance. Amazon
  RDS is flexible: you can scale your database instance''s compute resources and storage
  capacity to meet your application''s demand. As with all Amazon Web Services, there
  are no up-front investments, and you pay only for the resources you use.  This interface
  reference for Amazon RDS contains documentation for a programming or command line
  interface you can use to manage Amazon RDS. Note that Amazon RDS is asynchronous,
  which means that some interfaces might require techniques such as polling or callback
  functions to determine when a command has been applied. In this reference, the parameter
  descriptions indicate whether a command is applied immediately, on the next instance
  reboot, or during the maintenance window. The reference structure is as follows,
  and we list following some related topics from the user guide.   <b>Amazon RDS API
  Reference</b>  <ul> <li>  For the alphabetical list of API actions, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/APIReference/API_Operations.html">API
  Actions</a>. </li> <li>  For the alphabetical list of data types, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/APIReference/API_Types.html">Data
  Types</a>. </li> <li>  For a list of common query parameters, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/APIReference/CommonParameters.html">Common
  Parameters</a>. </li> <li>  For descriptions of the error codes, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/APIReference/CommonErrors.html">Common
  Errors</a>. </li> </ul>   <b>Amazon RDS User Guide</b>  <ul> <li>  For a summary
  of the Amazon RDS interfaces, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Welcome.html#Welcome.Interfaces">Available
  RDS Interfaces</a>. </li> <li>  For more information about how to use the Query
  API, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Using_the_Query_API.html">Using
  the Query API</a>. </li> </ul>'
mediaType:
- application/xml
uses:
  commons: https://raw.githubusercontent.com/apiregistry/commons/master/commons.raml
  extras: https://raw.githubusercontent.com/apiregistry/typesExtras/master/typeExtras.raml
types:
  AccountQuotaList:
    type: array
    items:
      type: AccountQuota
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ApplyMethod:
    type: string
    enum:
    - immediate
    - pending-reboot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  AttributeValueList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  AvailabilityZoneList:
    type: array
    items:
      type: AvailabilityZone
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  AvailabilityZones:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  Boolean:
    type: boolean
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  BooleanOptional:
    type: boolean
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CertificateList:
    type: array
    items:
      type: Certificate
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBClusterList:
    type: array
    items:
      type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBClusterMemberList:
    type: array
    items:
      type: DBClusterMember
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBClusterOptionGroupMemberships:
    type: array
    items:
      type: DBClusterOptionGroupStatus
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBClusterParameterGroupList:
    type: array
    items:
      type: DBClusterParameterGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBClusterSnapshotAttributeList:
    type: array
    items:
      type: DBClusterSnapshotAttribute
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBClusterSnapshotList:
    type: array
    items:
      type: DBClusterSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBEngineVersionList:
    type: array
    items:
      type: DBEngineVersion
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBInstanceList:
    type: array
    items:
      type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBInstanceStatusInfoList:
    type: array
    items:
      type: DBInstanceStatusInfo
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBParameterGroupList:
    type: array
    items:
      type: DBParameterGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBParameterGroupStatusList:
    type: array
    items:
      type: DBParameterGroupStatus
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBSecurityGroupMembershipList:
    type: array
    items:
      type: DBSecurityGroupMembership
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBSecurityGroupNameList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBSecurityGroups:
    type: array
    items:
      type: DBSecurityGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBSnapshotAttributeList:
    type: array
    items:
      type: DBSnapshotAttribute
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBSnapshotList:
    type: array
    items:
      type: DBSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBSubnetGroups:
    type: array
    items:
      type: DBSubnetGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DescribeDBLogFilesList:
    type: array
    items:
      type: DescribeDBLogFilesDetails
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DomainMembershipList:
    type: array
    items:
      type: DomainMembership
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>List of Active Directory Domain membership records associated with a DB instance.</p>
  Double:
    type: number
    format: double
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  EC2SecurityGroupList:
    type: array
    items:
      type: EC2SecurityGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  EventCategoriesList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  EventCategoriesMapList:
    type: array
    items:
      type: EventCategoriesMap
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  EventList:
    type: array
    items:
      type: Event
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  EventSubscriptionsList:
    type: array
    items:
      type: EventSubscription
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  FilterList:
    type: array
    items:
      type: Filter
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  FilterValueList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  IPRangeList:
    type: array
    items:
      type: IPRange
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  Integer:
    type: integer
    format: int
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  IntegerOptional:
    type: integer
    format: int
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  KeyList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  Long:
    type: integer
    format: long
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionConfigurationList:
    type: array
    items:
      type: OptionConfiguration
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionGroupMembershipList:
    type: array
    items:
      type: OptionGroupMembership
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionGroupOptionSettingsList:
    type: array
    items:
      type: OptionGroupOptionSetting
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionGroupOptionVersionsList:
    type: array
    items:
      type: OptionVersion
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionGroupOptionsList:
    type: array
    items:
      type: OptionGroupOption
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>List of available option group options.</p>
  OptionGroupsList:
    type: array
    items:
      type: OptionGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionNamesList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionSettingConfigurationList:
    type: array
    items:
      type: OptionSetting
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionSettingsList:
    type: array
    items:
      type: OptionSetting
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionsConflictsWith:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionsDependedOn:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OptionsList:
    type: array
    items:
      type: Option
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  OrderableDBInstanceOptionsList:
    type: array
    items:
      type: OrderableDBInstanceOption
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ParametersList:
    type: array
    items:
      type: Parameter
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  PendingMaintenanceActionDetails:
    type: array
    items:
      type: PendingMaintenanceAction
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  PendingMaintenanceActions:
    type: array
    items:
      type: ResourcePendingMaintenanceActions
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ReadReplicaDBInstanceIdentifierList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ReadReplicaIdentifierList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RecurringChargeList:
    type: array
    items:
      type: RecurringCharge
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ReservedDBInstanceList:
    type: array
    items:
      type: ReservedDBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ReservedDBInstancesOfferingList:
    type: array
    items:
      type: ReservedDBInstancesOffering
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SourceIdsList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SourceRegionList:
    type: array
    items:
      type: SourceRegion
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SourceType:
    type: string
    enum:
    - db-instance
    - db-parameter-group
    - db-security-group
    - db-snapshot
    - db-cluster
    - db-cluster-snapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  String:
    type: string
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SubnetIdentifierList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SubnetList:
    type: array
    items:
      type: Subnet
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SupportedCharacterSetsList:
    type: array
    items:
      type: CharacterSet
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SupportedTimezonesList:
    type: array
    items:
      type: Timezone
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  TStamp:
    type: datetime
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  TagList:
    type: array
    items:
      type: Tag
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>A list of tags.</p>
  ValidUpgradeTargetList:
    type: array
    items:
      type: UpgradeTarget
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  VpcSecurityGroupIdList:
    type: array
    items:
      type: String
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  VpcSecurityGroupMembershipList:
    type: array
    items:
      type: VpcSecurityGroupMembership
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  AccountAttributesMessage:
    type: object
    properties:
      AccountQuotas?:
        type: AccountQuotaList
        description: <p>A list of <a>AccountQuota</a> objects. Within this list, each quota has a name, a count of usage toward the quota maximum, and a maximum value for the quota.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Data returned by the <b>DescribeAccountAttributes</b> action.</p>
  AccountQuota:
    type: object
    properties:
      AccountQuotaName?:
        type: String
        description: <p>The name of the Amazon RDS quota for this AWS account.</p>
      Used?:
        type: Long
        description: <p>The amount currently used toward the quota maximum.</p>
      Max?:
        type: Long
        description: <p>The maximum allowed value for the quota.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Describes a quota for an AWS account, for example, the number of DB instances allowed.</p>
  AddSourceIdentifierToSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: <p>The name of the RDS event notification subscription you want to add a source identifier to.</p>
      SourceIdentifier:
        type: String
        description: <p>The identifier of the event source to be added. An identifier must begin with a letter and must contain only ASCII letters, digits, and hyphens; it cannot end with a hyphen or contain two consecutive hyphens.</p> <p>Constraints:</p> <ul> <li> <p>If the source type is a DB instance, then a <code>DBInstanceIdentifier</code> must be supplied.</p> </li> <li> <p>If the source type is a DB security group, a <code>DBSecurityGroupName</code> must be supplied.</p> </li> <li> <p>If the source type is a DB parameter group, a <code>DBParameterGroupName</code> must be supplied.</p> </li> <li> <p>If the source type is a DB snapshot, a <code>DBSnapshotIdentifier</code> must be supplied.</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  AddSourceIdentifierToSubscriptionResult:
    type: object
    properties:
      EventSubscription?:
        type: EventSubscription
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  AddTagsToResourceMessage:
    type: object
    properties:
      ResourceName:
        type: String
        description: <p>The Amazon RDS resource the tags will be added to. This value is an Amazon Resource Name (ARN). For information about creating an ARN, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing an RDS Amazon Resource Name (ARN)</a>.</p>
      Tags:
        type: TagList
        description: <p>The tags to be assigned to the Amazon RDS resource.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ApplyPendingMaintenanceActionMessage:
    type: object
    properties:
      ResourceIdentifier:
        type: String
        description: <p>The RDS Amazon Resource Name (ARN) of the resource that the pending maintenance action applies to. For information about creating an ARN, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing an RDS Amazon Resource Name (ARN)</a>.</p>
      ApplyAction:
        type: String
        description: '<p>The pending maintenance action to apply to this resource.</p>
          <p>Valid values: <code>system-update</code>, <code>db-upgrade</code> </p>'
      OptInType:
        type: String
        description: <p>A value that specifies the type of opt-in request, or undoes an opt-in request. An opt-in request of type <code>immediate</code> cannot be undone.</p> <p>Valid values:</p> <ul> <li> <p> <code>immediate</code> - Apply the maintenance action immediately.</p> </li> <li> <p> <code>next-maintenance</code> - Apply the maintenance action during the next maintenance window for the resource.</p> </li> <li> <p> <code>undo-opt-in</code> - Cancel any existing <code>next-maintenance</code> opt-in requests.</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ApplyPendingMaintenanceActionResult:
    type: object
    properties:
      ResourcePendingMaintenanceActions?:
        type: ResourcePendingMaintenanceActions
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  AuthorizeDBSecurityGroupIngressMessage:
    type: object
    properties:
      DBSecurityGroupName:
        type: String
        description: <p>The name of the DB security group to add authorization to.</p>
      CIDRIP?:
        type: String
        description: <p>The IP range to authorize.</p>
      EC2SecurityGroupName?:
        type: String
        description: <p> Name of the EC2 security group to authorize. For VPC DB security groups, <code>EC2SecurityGroupId</code> must be provided. Otherwise, <code>EC2SecurityGroupOwnerId</code> and either <code>EC2SecurityGroupName</code> or <code>EC2SecurityGroupId</code> must be provided. </p>
      EC2SecurityGroupId?:
        type: String
        description: <p> Id of the EC2 security group to authorize. For VPC DB security groups, <code>EC2SecurityGroupId</code> must be provided. Otherwise, <code>EC2SecurityGroupOwnerId</code> and either <code>EC2SecurityGroupName</code> or <code>EC2SecurityGroupId</code> must be provided. </p>
      EC2SecurityGroupOwnerId?:
        type: String
        description: <p> AWS account number of the owner of the EC2 security group specified in the <code>EC2SecurityGroupName</code> parameter. The AWS Access Key ID is not an acceptable value. For VPC DB security groups, <code>EC2SecurityGroupId</code> must be provided. Otherwise, <code>EC2SecurityGroupOwnerId</code> and either <code>EC2SecurityGroupName</code> or <code>EC2SecurityGroupId</code> must be provided. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  AuthorizeDBSecurityGroupIngressResult:
    type: object
    properties:
      DBSecurityGroup?:
        type: DBSecurityGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  AvailabilityZone:
    type: object
    properties:
      Name?:
        type: String
        description: <p>The name of the availability zone.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains Availability Zone information.</p> <p> This data type is used as an element in the following data type:</p> <ul> <li> <p> <a>OrderableDBInstanceOption</a> </p> </li> </ul>
  Certificate:
    type: object
    properties:
      CertificateIdentifier?:
        type: String
        description: <p>The unique key that identifies a certificate.</p>
      CertificateType?:
        type: String
        description: <p>The type of the certificate.</p>
      Thumbprint?:
        type: String
        description: <p>The thumbprint of the certificate.</p>
      ValidFrom?:
        type: TStamp
        description: <p>The starting date from which the certificate is valid.</p>
      ValidTill?:
        type: TStamp
        description: <p>The final date that the certificate continues to be valid.</p>
      CertificateArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the certificate.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>A CA certificate for an AWS account.</p>
  CertificateMessage:
    type: object
    properties:
      Certificates?:
        type: CertificateList
        description: <p>The list of <a>Certificate</a> objects for the AWS account.</p>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <a>DescribeCertificates</a> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Data returned by the <b>DescribeCertificates</b> action.</p>
  CharacterSet:
    type: object
    properties:
      CharacterSetName?:
        type: String
        description: <p>The name of the character set.</p>
      CharacterSetDescription?:
        type: String
        description: <p>The description of the character set.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the action <a>DescribeDBEngineVersions</a>. </p>
  CopyDBClusterParameterGroupMessage:
    type: object
    properties:
      SourceDBClusterParameterGroupIdentifier:
        type: String
        description: <p>The identifier or Amazon Resource Name (ARN) for the source DB cluster parameter group. For information about creating an ARN, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing an RDS Amazon Resource Name (ARN)</a>. </p> <p>Constraints:</p> <ul> <li> <p>Must specify a valid DB cluster parameter group.</p> </li> <li> <p>If the source DB cluster parameter group is in the same region as the copy, specify a valid DB parameter group identifier, for example <code>my-db-cluster-param-group</code>, or a valid ARN.</p> </li> <li> <p>If the source DB parameter group is in a different region than the copy, specify a valid DB cluster parameter group ARN, for example <code>arn:aws:rds:us-east-1:123456789012:cluster-pg:custom-cluster-group1</code>.</p> </li> </ul>
      TargetDBClusterParameterGroupIdentifier:
        type: String
        description: '<p>The identifier for the copied DB cluster parameter group.</p>
          <p>Constraints:</p> <ul> <li> <p>Cannot be null, empty, or blank</p> </li>
          <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens</p>
          </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot
          end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <p>Example:
          <code>my-cluster-param-group1</code> </p>'
      TargetDBClusterParameterGroupDescription:
        type: String
        description: <p>A description for the copied DB cluster parameter group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CopyDBClusterParameterGroupResult:
    type: object
    properties:
      DBClusterParameterGroup?:
        type: DBClusterParameterGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CopyDBClusterSnapshotMessage:
    type: object
    properties:
      SourceDBClusterSnapshotIdentifier:
        type: String
        description: '<p>The identifier of the DB cluster snapshot to copy. This parameter
          is not case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must contain
          from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>First
          character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or
          contain two consecutive hyphens.</p> </li> </ul> <p>Example: <code>my-cluster-snapshot1</code>
          </p>'
      TargetDBClusterSnapshotIdentifier:
        type: String
        description: '<p>The identifier of the new DB cluster snapshot to create from
          the source DB cluster snapshot. This parameter is not case-sensitive.</p>
          <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric
          characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p>
          </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p>
          </li> </ul> <p>Example: <code>my-cluster-snapshot2</code> </p>'
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CopyDBClusterSnapshotResult:
    type: object
    properties:
      DBClusterSnapshot?:
        type: DBClusterSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CopyDBParameterGroupMessage:
    type: object
    properties:
      SourceDBParameterGroupIdentifier:
        type: String
        description: <p> The identifier or ARN for the source DB parameter group. For information about creating an ARN, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing an RDS Amazon Resource Name (ARN)</a>. </p> <p>Constraints:</p> <ul> <li> <p>Must specify a valid DB parameter group.</p> </li> <li> <p> Must specify a valid DB parameter group identifier, for example <code>my-db-param-group</code>, or a valid ARN.</p> </li> </ul>
      TargetDBParameterGroupIdentifier:
        type: String
        description: '<p>The identifier for the copied DB parameter group.</p> <p>Constraints:</p>
          <ul> <li> <p>Cannot be null, empty, or blank</p> </li> <li> <p>Must contain
          from 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First
          character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or
          contain two consecutive hyphens</p> </li> </ul> <p>Example: <code>my-db-parameter-group</code>
          </p>'
      TargetDBParameterGroupDescription:
        type: String
        description: <p>A description for the copied DB parameter group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CopyDBParameterGroupResult:
    type: object
    properties:
      DBParameterGroup?:
        type: DBParameterGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CopyDBSnapshotMessage:
    type: object
    properties:
      SourceDBSnapshotIdentifier:
        type: String
        description: '<p>The identifier for the source DB snapshot.</p> <p>If you
          are copying from a shared manual DB snapshot, this must be the ARN of the
          shared DB snapshot.</p> <p>Constraints:</p> <ul> <li> <p>Must specify a
          valid system snapshot in the "available" state.</p> </li> <li> <p>If the
          source snapshot is in the same region as the copy, specify a valid DB snapshot
          identifier.</p> </li> <li> <p>If the source snapshot is in a different region
          than the copy, specify a valid DB snapshot ARN. For more information, go
          to <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_CopySnapshot.html">
          Copying a DB Snapshot</a>.</p> </li> </ul> <p>Example: <code>rds:mydb-2012-04-02-00-01</code>
          </p> <p>Example: <code>arn:aws:rds:rr-regn-1:123456789012:snapshot:mysql-instance1-snapshot-20130805</code>
          </p>'
      TargetDBSnapshotIdentifier:
        type: String
        description: '<p>The identifier for the copied snapshot.</p> <p>Constraints:</p>
          <ul> <li> <p>Cannot be null, empty, or blank</p> </li> <li> <p>Must contain
          from 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First
          character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or
          contain two consecutive hyphens</p> </li> </ul> <p>Example: <code>my-db-snapshot</code>
          </p>'
      KmsKeyId?:
        type: String
        description: <p>The AWS Key Management Service (AWS KMS) key identifier for an encrypted DB snapshot. The KMS key identifier is the Amazon Resource Name (ARN) or the KMS key alias for the KMS encryption key.</p> <p>If you copy an unencrypted DB snapshot and specify a value for the <code>KmsKeyId</code> parameter, Amazon RDS encrypts the target DB snapshot using the specified KMS encryption key.</p> <p>If you copy an encrypted DB snapshot from your AWS account, you can specify a value for <code>KmsKeyId</code> to encrypt the copy with a new KMS encryption key. If you don't specify a value for <code>KmsKeyId</code> then the copy of the DB snapshot is encrypted with the same KMS key as the source DB snapshot. </p> <p>If you copy an encrypted DB snapshot that is shared from another AWS account, then you must specify a value for <code>KmsKeyId</code>.</p>
      Tags?:
        type: TagList
      CopyTags?:
        type: BooleanOptional
        description: <p>True to copy all tags from the source DB snapshot to the target DB snapshot; otherwise false. The default is false.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CopyDBSnapshotResult:
    type: object
    properties:
      DBSnapshot?:
        type: DBSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CopyOptionGroupMessage:
    type: object
    properties:
      SourceOptionGroupIdentifier:
        type: String
        description: <p>The identifier or ARN for the source option group. For information about creating an ARN, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing an RDS Amazon Resource Name (ARN)</a>. </p> <p>Constraints:</p> <ul> <li> <p>Must specify a valid option group.</p> </li> <li> <p>If the source option group is in the same region as the copy, specify a valid option group identifier, for example <code>my-option-group</code>, or a valid ARN.</p> </li> <li> <p>If the source option group is in a different region than the copy, specify a valid option group ARN, for example <code>arn:aws:rds:us-west-2:123456789012:og:special-options</code>.</p> </li> </ul>
      TargetOptionGroupIdentifier:
        type: String
        description: '<p>The identifier for the copied option group.</p> <p>Constraints:</p>
          <ul> <li> <p>Cannot be null, empty, or blank</p> </li> <li> <p>Must contain
          from 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First
          character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or
          contain two consecutive hyphens</p> </li> </ul> <p>Example: <code>my-option-group</code>
          </p>'
      TargetOptionGroupDescription:
        type: String
        description: <p>The description for the copied option group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CopyOptionGroupResult:
    type: object
    properties:
      OptionGroup?:
        type: OptionGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBClusterMessage:
    type: object
    properties:
      AvailabilityZones?:
        type: AvailabilityZones
        description: <p>A list of EC2 Availability Zones that instances in the DB cluster can be created in. For information on regions and Availability Zones, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.RegionsAndAvailabilityZones.html">Regions and Availability Zones</a>. </p>
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days for which automated backups are retained.
          You must specify a minimum value of 1.</p> <p>Default: 1</p> <p>Constraints:</p>
          <ul> <li> <p>Must be a value from 1 to 35</p> </li> </ul>'
      CharacterSetName?:
        type: String
        description: <p>A value that indicates that the DB cluster should be associated with the specified CharacterSet.</p>
      DatabaseName?:
        type: String
        description: <p>The name for your database of up to 8 alpha-numeric characters. If you do not provide a name, Amazon RDS will not create a database in the DB cluster you are creating.</p>
      DBClusterIdentifier:
        type: String
        description: '<p>The DB cluster identifier. This parameter is stored as a
          lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from
          1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>First character
          must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain
          two consecutive hyphens.</p> </li> </ul> <p>Example: <code>my-cluster1</code>
          </p>'
      DBClusterParameterGroupName?:
        type: String
        description: <p> The name of the DB cluster parameter group to associate with this DB cluster. If this argument is omitted, <code>default.aurora5.6</code> will be used. </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: <p>A list of EC2 VPC security groups to associate with this DB cluster.</p>
      DBSubnetGroupName?:
        type: String
        description: '<p>A DB subnet group to associate with this DB cluster.</p>
          <p>Constraints: Must contain no more than 255 alphanumeric characters, periods,
          underscores, spaces, or hyphens. Must not be default.</p> <p>Example: <code>mySubnetgroup</code>
          </p>'
      Engine:
        type: String
        description: '<p>The name of the database engine to be used for this DB cluster.</p>
          <p>Valid Values: <code>aurora</code> </p>'
      EngineVersion?:
        type: String
        description: '<p>The version number of the database engine to use.</p> <p>
          <b>Aurora</b> </p> <p>Example: <code>5.6.10a</code> </p>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the instances in the DB cluster
          accept connections.</p> <p> Default: <code>3306</code> </p>'
      MasterUsername?:
        type: String
        description: <p>The name of the master user for the DB cluster.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 16 alphanumeric characters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot be a reserved word for the chosen database engine.</p> </li> </ul>
      MasterUserPassword?:
        type: String
        description: '<p>The password for the master database user. This password
          can contain any printable ASCII character except "/", """, or "@".</p> <p>Constraints:
          Must contain from 8 to 41 characters.</p>'
      OptionGroupName?:
        type: String
        description: <p>A value that indicates that the DB cluster should be associated with the specified option group.</p> <p>Permanent options cannot be removed from an option group. The option group cannot be removed from a DB cluster once it is associated with a DB cluster.</p>
      PreferredBackupWindow?:
        type: String
        description: '<p>The daily time range during which automated backups are created
          if automated backups are enabled using the <code>BackupRetentionPeriod</code>
          parameter. </p> <p>Default: A 30-minute window selected at random from an
          8-hour block of time per region. To see the time blocks available, see <a
          href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Constraints:</p> <ul> <li> <p>Must be in the format <code>hh24:mi-hh24:mi</code>.</p>
          </li> <li> <p>Times should be in Universal Coordinated Time (UTC).</p> </li>
          <li> <p>Must not conflict with the preferred maintenance window.</p> </li>
          <li> <p>Must be at least 30 minutes.</p> </li> </ul>'
      PreferredMaintenanceWindow?:
        type: String
        description: '<p>The weekly time range during which system maintenance can
          occur, in Universal Coordinated Time (UTC).</p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code>
          </p> <p>Default: A 30-minute window selected at random from an 8-hour block
          of time per region, occurring on a random day of the week. To see the time
          blocks available, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Valid Days: Mon, Tue, Wed, Thu, Fri, Sat, Sun</p> <p>Constraints:
          Minimum 30-minute window.</p>'
      ReplicationSourceIdentifier?:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the source DB cluster if this DB cluster is created as a Read Replica.</p>
      Tags?:
        type: TagList
      StorageEncrypted?:
        type: BooleanOptional
        description: <p>Specifies whether the DB cluster is encrypted.</p>
      KmsKeyId?:
        type: String
        description: <p>The KMS key identifier for an encrypted DB cluster.</p> <p>The KMS key identifier is the Amazon Resource Name (ARN) for the KMS encryption key. If you are creating a DB cluster with the same AWS account that owns the KMS encryption key used to encrypt the new DB cluster, then you can use the KMS key alias instead of the ARN for the KM encryption key.</p> <p>If the <code>StorageEncrypted</code> parameter is true, and you do not specify a value for the <code>KmsKeyId</code> parameter, then Amazon RDS will use your default encryption key. AWS KMS creates the default encryption key for your AWS account. Your AWS account has a different default encryption key for each AWS region.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBClusterParameterGroupMessage:
    type: object
    properties:
      DBClusterParameterGroupName:
        type: String
        description: <p>The name of the DB cluster parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <note> <p>This value is stored as a lowercase string.</p> </note>
      DBParameterGroupFamily:
        type: String
        description: <p>The DB cluster parameter group family name. A DB cluster parameter group can be associated with one and only one DB cluster parameter group family, and can be applied only to a DB cluster running a database engine and engine version compatible with that DB cluster parameter group family.</p>
      Description:
        type: String
        description: <p>The description for the DB cluster parameter group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBClusterParameterGroupResult:
    type: object
    properties:
      DBClusterParameterGroup?:
        type: DBClusterParameterGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBClusterResult:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBClusterSnapshotMessage:
    type: object
    properties:
      DBClusterSnapshotIdentifier:
        type: String
        description: '<p>The identifier of the DB cluster snapshot. This parameter
          is stored as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must
          contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li>
          <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a
          hyphen or contain two consecutive hyphens.</p> </li> </ul> <p>Example: <code>my-cluster1-snapshot1</code>
          </p>'
      DBClusterIdentifier:
        type: String
        description: '<p>The identifier of the DB cluster to create a snapshot for.
          This parameter is not case-sensitive.</p> <p>Constraints:</p> <ul> <li>
          <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li>
          <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with
          a hyphen or contain two consecutive hyphens.</p> </li> </ul> <p>Example:
          <code>my-cluster1</code> </p>'
      Tags?:
        type: TagList
        description: <p>The tags to be assigned to the DB cluster snapshot.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBClusterSnapshotResult:
    type: object
    properties:
      DBClusterSnapshot?:
        type: DBClusterSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBInstanceMessage:
    type: object
    properties:
      DBName?:
        type: String
        description: '<p>The meaning of this parameter differs according to the database
          engine you use.</p> <p>Type: String</p> <p> <b>MySQL</b> </p> <p>The name
          of the database to create when the DB instance is created. If this parameter
          is not specified, no database is created in the DB instance.</p> <p>Constraints:</p>
          <ul> <li> <p>Must contain 1 to 64 alphanumeric characters</p> </li> <li>
          <p>Cannot be a word reserved by the specified database engine</p> </li>
          </ul> <p> <b>MariaDB</b> </p> <p>The name of the database to create when
          the DB instance is created. If this parameter is not specified, no database
          is created in the DB instance.</p> <p>Constraints:</p> <ul> <li> <p>Must
          contain 1 to 64 alphanumeric characters</p> </li> <li> <p>Cannot be a word
          reserved by the specified database engine</p> </li> </ul> <p> <b>PostgreSQL</b>
          </p> <p>The name of the database to create when the DB instance is created.
          If this parameter is not specified, the default "postgres" database is created
          in the DB instance.</p> <p>Constraints:</p> <ul> <li> <p>Must contain 1
          to 63 alphanumeric characters</p> </li> <li> <p>Must begin with a letter
          or an underscore. Subsequent characters can be letters, underscores, or
          digits (0-9).</p> </li> <li> <p>Cannot be a word reserved by the specified
          database engine</p> </li> </ul> <p> <b>Oracle</b> </p> <p>The Oracle System
          ID (SID) of the created DB instance.</p> <p>Default: <code>ORCL</code> </p>
          <p>Constraints:</p> <ul> <li> <p>Cannot be longer than 8 characters</p>
          </li> </ul> <p> <b>SQL Server</b> </p> <p>Not applicable. Must be null.</p>
          <p> <b>Amazon Aurora</b> </p> <p>The name of the database to create when
          the primary instance of the DB cluster is created. If this parameter is
          not specified, no database is created in the DB instance.</p> <p>Constraints:</p>
          <ul> <li> <p>Must contain 1 to 64 alphanumeric characters</p> </li> <li>
          <p>Cannot be a word reserved by the specified database engine</p> </li>
          </ul>'
      DBInstanceIdentifier:
        type: String
        description: '<p>The DB instance identifier. This parameter is stored as a
          lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from
          1 to 63 alphanumeric characters or hyphens (1 to 15 for SQL Server).</p>
          </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot
          end with a hyphen or contain two consecutive hyphens.</p> </li> </ul> <p>Example:
          <code>mydbinstance</code> </p>'
      AllocatedStorage?:
        type: IntegerOptional
        description: '<p>The amount of storage (in gigabytes) to be initially allocated
          for the database instance.</p> <p>Type: Integer</p> <p> <b>MySQL</b> </p>
          <p>Constraints: Must be an integer from 5 to 6144.</p> <p> <b>MariaDB</b>
          </p> <p>Constraints: Must be an integer from 5 to 6144.</p> <p> <b>PostgreSQL</b>
          </p> <p>Constraints: Must be an integer from 5 to 6144.</p> <p> <b>Oracle</b>
          </p> <p>Constraints: Must be an integer from 10 to 6144.</p> <p> <b>SQL
          Server</b> </p> <p>Constraints: Must be an integer from 200 to 4096 (Standard
          Edition and Enterprise Edition) or from 20 to 4096 (Express Edition and
          Web Edition)</p>'
      DBInstanceClass:
        type: String
        description: '<p>The compute and memory capacity of the DB instance.</p> <p>
          Valid Values: <code>db.t1.micro | db.m1.small | db.m1.medium | db.m1.large
          | db.m1.xlarge | db.m2.xlarge |db.m2.2xlarge | db.m2.4xlarge | db.m3.medium
          | db.m3.large | db.m3.xlarge | db.m3.2xlarge | db.m4.large | db.m4.xlarge
          | db.m4.2xlarge | db.m4.4xlarge | db.m4.10xlarge | db.r3.large | db.r3.xlarge
          | db.r3.2xlarge | db.r3.4xlarge | db.r3.8xlarge | db.t2.micro | db.t2.small
          | db.t2.medium | db.t2.large</code> </p>'
      Engine:
        type: String
        description: '<p>The name of the database engine to be used for this instance.</p>
          <p> Valid Values: <code>mysql</code> | <code>mariadb</code> | <code>oracle-se1</code>
          | <code>oracle-se2</code> | <code>oracle-se</code> | <code>oracle-ee</code>
          | <code>sqlserver-ee</code> | <code>sqlserver-se</code> | <code>sqlserver-ex</code>
          | <code>sqlserver-web</code> | <code>postgres</code> | <code>aurora</code>
          </p> <p>Not every database engine is available for every AWS region.</p>'
      MasterUsername?:
        type: String
        description: '<p>The name of master user for the client DB instance.</p> <p>
          <b>MySQL</b> </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 16 alphanumeric
          characters.</p> </li> <li> <p>First character must be a letter.</p> </li>
          <li> <p>Cannot be a reserved word for the chosen database engine.</p> </li>
          </ul> <p> <b>MariaDB</b> </p> <p>Constraints:</p> <ul> <li> <p>Must be 1
          to 16 alphanumeric characters.</p> </li> <li> <p>Cannot be a reserved word
          for the chosen database engine.</p> </li> </ul> <p>Type: String</p> <p>
          <b>Oracle</b> </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 30 alphanumeric
          characters.</p> </li> <li> <p>First character must be a letter.</p> </li>
          <li> <p>Cannot be a reserved word for the chosen database engine.</p> </li>
          </ul> <p> <b>SQL Server</b> </p> <p>Constraints:</p> <ul> <li> <p>Must be
          1 to 128 alphanumeric characters.</p> </li> <li> <p>First character must
          be a letter.</p> </li> <li> <p>Cannot be a reserved word for the chosen
          database engine.</p> </li> </ul> <p> <b>PostgreSQL</b> </p> <p>Constraints:</p>
          <ul> <li> <p>Must be 1 to 63 alphanumeric characters.</p> </li> <li> <p>First
          character must be a letter.</p> </li> <li> <p>Cannot be a reserved word
          for the chosen database engine.</p> </li> </ul>'
      MasterUserPassword?:
        type: String
        description: '<p>The password for the master database user. Can be any printable
          ASCII character except "/", """, or "@".</p> <p>Type: String</p> <p> <b>MySQL</b>
          </p> <p>Constraints: Must contain from 8 to 41 characters.</p> <p> <b>MariaDB</b>
          </p> <p>Constraints: Must contain from 8 to 41 characters.</p> <p> <b>Oracle</b>
          </p> <p>Constraints: Must contain from 8 to 30 characters.</p> <p> <b>SQL
          Server</b> </p> <p>Constraints: Must contain from 8 to 128 characters.</p>
          <p> <b>PostgreSQL</b> </p> <p>Constraints: Must contain from 8 to 128 characters.</p>
          <p> <b>Amazon Aurora</b> </p> <p>Constraints: Must contain from 8 to 41
          characters.</p>'
      DBSecurityGroups?:
        type: DBSecurityGroupNameList
        description: '<p>A list of DB security groups to associate with this DB instance.</p>
          <p>Default: The default DB security group for the database engine.</p>'
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: '<p>A list of EC2 VPC security groups to associate with this
          DB instance.</p> <p>Default: The default EC2 VPC security group for the
          DB subnet group''s VPC.</p>'
      AvailabilityZone?:
        type: String
        description: '<p> The EC2 Availability Zone that the database instance will
          be created in. For information on regions and Availability Zones, see <a
          href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.RegionsAndAvailabilityZones.html">Regions
          and Availability Zones</a>. </p> <p>Default: A random, system-chosen Availability
          Zone in the endpoint''s region.</p> <p> Example: <code>us-east-1d</code>
          </p> <p> Constraint: The AvailabilityZone parameter cannot be specified
          if the MultiAZ parameter is set to <code>true</code>. The specified Availability
          Zone must be in the same region as the current endpoint. </p>'
      DBSubnetGroupName?:
        type: String
        description: <p>A DB subnet group to associate with this DB instance.</p> <p>If there is no DB subnet group, then it is a non-VPC DB instance.</p>
      PreferredMaintenanceWindow?:
        type: String
        description: '<p> The weekly time range during which system maintenance can
          occur, in Universal Coordinated Time (UTC). For more information, see <a
          href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.DBMaintenance.html">DB
          Instance Maintenance</a>. </p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code>
          </p> <p> Default: A 30-minute window selected at random from an 8-hour block
          of time per region, occurring on a random day of the week. To see the time
          blocks available, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Valid Days: Mon, Tue, Wed, Thu, Fri, Sat, Sun</p> <p>Constraints:
          Minimum 30-minute window.</p>'
      DBParameterGroupName?:
        type: String
        description: <p>The name of the DB parameter group to associate with this DB instance. If this argument is omitted, the default DBParameterGroup for the specified engine will be used.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days for which automated backups are retained.
          Setting this parameter to a positive number enables backups. Setting this
          parameter to 0 disables automated backups.</p> <p>Default: 1</p> <p>Constraints:</p>
          <ul> <li> <p>Must be a value from 0 to 35</p> </li> <li> <p>Cannot be set
          to 0 if the DB instance is a source to Read Replicas</p> </li> </ul>'
      PreferredBackupWindow?:
        type: String
        description: '<p> The daily time range during which automated backups are
          created if automated backups are enabled, using the <code>BackupRetentionPeriod</code>
          parameter. For more information, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Overview.BackingUpAndRestoringAmazonRDSInstances.html">DB
          Instance Backups</a>. </p> <p> Default: A 30-minute window selected at random
          from an 8-hour block of time per region. To see the time blocks available,
          see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Constraints:</p> <ul> <li> <p>Must be in the format <code>hh24:mi-hh24:mi</code>.</p>
          </li> <li> <p>Times should be in Universal Coordinated Time (UTC).</p> </li>
          <li> <p>Must not conflict with the preferred maintenance window.</p> </li>
          <li> <p>Must be at least 30 minutes.</p> </li> </ul>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the database accepts connections.</p>
          <p> <b>MySQL</b> </p> <p> Default: <code>3306</code> </p> <p> Valid Values:
          <code>1150-65535</code> </p> <p>Type: Integer</p> <p> <b>MariaDB</b> </p>
          <p> Default: <code>3306</code> </p> <p> Valid Values: <code>1150-65535</code>
          </p> <p>Type: Integer</p> <p> <b>PostgreSQL</b> </p> <p> Default: <code>5432</code>
          </p> <p> Valid Values: <code>1150-65535</code> </p> <p>Type: Integer</p>
          <p> <b>Oracle</b> </p> <p> Default: <code>1521</code> </p> <p> Valid Values:
          <code>1150-65535</code> </p> <p> <b>SQL Server</b> </p> <p> Default: <code>1433</code>
          </p> <p> Valid Values: <code>1150-65535</code> except for <code>1434</code>,
          <code>3389</code>, <code>47001</code>, <code>49152</code>, and <code>49152</code>
          through <code>49156</code>. </p> <p> <b>Amazon Aurora</b> </p> <p> Default:
          <code>3306</code> </p> <p> Valid Values: <code>1150-65535</code> </p> <p>Type:
          Integer</p>'
      MultiAZ?:
        type: BooleanOptional
        description: <p>Specifies if the DB instance is a Multi-AZ deployment. You cannot set the AvailabilityZone parameter if the MultiAZ parameter is set to true.</p>
      EngineVersion?:
        type: String
        description: <p>The version number of the database engine to use.</p> <p>The following are the database engines and major and minor versions that are available with Amazon RDS. Not every database engine is available for every AWS region.</p> <p> <b>Amazon Aurora</b> </p> <ul> <li> <p> <b>Version 5.6 (only available in AWS regions ap-northeast-1, ap-northeast-2, ap-south-1, ap-southeast-2, eu-west-1, us-east-1, us-west-2):</b> <code> 5.6.10a</code> </p> </li> </ul> <p> <b>MariaDB</b> </p> <ul> <li> <p> <b>Version 10.1 (available in all AWS regions except us-gov-west-1):</b> <code> 10.1.14</code> </p> </li> <li> <p> <b>Version 10.0 (available in all AWS regions):</b> <code> 10.0.17 | 10.0.24</code> </p> </li> </ul> <p> <b>Microsoft SQL Server Enterprise Edition (sqlserver-ee)</b> </p> <ul> <li> <p> <b>Version 11.00 (available in all AWS regions):</b> <code> 11.00.2100.60.v1 | 11.00.5058.0.v1 | 11.00.6020.0.v1</code> </p> </li> <li> <p> <b>Version 10.50 (available in all AWS regions):</b> <code> 10.50.2789.0.v1 | 10.50.6000.34.v1 | 10.50.6529.0.v1</code> </p> </li> </ul> <p> <b>Microsoft SQL Server Express Edition (sqlserver-ex)</b> </p> <ul> <li> <p> <b>Version 12.00 (available in all AWS regions):</b> <code> 12.00.4422.0.v1</code> </p> </li> <li> <p> <b>Version 11.00 (available in all AWS regions):</b> <code> 11.00.2100.60.v1 | 11.00.5058.0.v1 | 11.00.6020.0.v1</code> </p> </li> <li> <p> <b>Version 10.50 (available in all AWS regions):</b> <code> 10.50.2789.0.v1 | 10.50.6000.34.v1 | 10.50.6529.0.v1</code> </p> </li> </ul> <p> <b>Microsoft SQL Server Standard Edition (sqlserver-se)</b> </p> <ul> <li> <p> <b>Version 12.00 (available in all AWS regions):</b> <code> 12.00.4422.0.v1</code> </p> </li> <li> <p> <b>Version 11.00 (available in all AWS regions):</b> <code> 11.00.2100.60.v1 | 11.00.5058.0.v1 | 11.00.6020.0.v1</code> </p> </li> <li> <p> <b>Version 10.50 (available in all AWS regions):</b> <code> 10.50.2789.0.v1 | 10.50.6000.34.v1 | 10.50.6529.0.v1</code> </p> </li> </ul> <p> <b>Microsoft SQL Server Web Edition (sqlserver-web)</b> </p> <ul> <li> <p> <b>Version 12.00 (available in all AWS regions):</b> <code> 12.00.4422.0.v1</code> </p> </li> <li> <p> <b>Version 11.00 (available in all AWS regions):</b> <code> 11.00.2100.60.v1 | 11.00.5058.0.v1 | 11.00.6020.0.v1</code> </p> </li> <li> <p> <b>Version 10.50 (available in all AWS regions):</b> <code> 10.50.2789.0.v1 | 10.50.6000.34.v1 | 10.50.6529.0.v1</code> </p> </li> </ul> <p> <b>MySQL</b> </p> <ul> <li> <p> <b>Version 5.7 (available in all AWS regions):</b> <code> 5.7.10 | 5.7.11</code> </p> </li> <li> <p> <b>Version 5.6 (available in all AWS regions except ap-south-1, ap-northeast-2):</b> <code> 5.6.19a | 5.6.19b | 5.6.21 | 5.6.21b | 5.6.22</code> </p> </li> <li> <p> <b>Version 5.6 (available in all AWS regions except ap-south-1):</b> <code> 5.6.23</code> </p> </li> <li> <p> <b>Version 5.6 (available in all AWS regions):</b> <code> 5.6.27 | 5.6.29</code> </p> </li> <li> <p> <b>Version 5.5 (only available in AWS regions ap-northeast-1, ap-southeast-1, ap-southeast-2, eu-west-1, sa-east-1, us-east-1, us-gov-west-1, us-west-1, us-west-2):</b> <code> 5.5.40 | 5.5.40a</code> </p> </li> <li> <p> <b>Version 5.5 (available in all AWS regions except ap-south-1, ap-northeast-2):</b> <code> 5.5.40b | 5.5.41</code> </p> </li> <li> <p> <b>Version 5.5 (available in all AWS regions except ap-south-1):</b> <code> 5.5.42</code> </p> </li> <li> <p> <b>Version 5.5 (available in all AWS regions):</b> <code> 5.5.46</code> </p> </li> </ul> <p> <b>Oracle Database Enterprise Edition (oracle-ee)</b> </p> <ul> <li> <p> <b>Version 12.1 (available in all AWS regions except ap-south-1, ap-northeast-2):</b> <code> 12.1.0.1.v1 | 12.1.0.1.v2</code> </p> </li> <li> <p> <b>Version 12.1 (only available in AWS regions ap-northeast-1, ap-southeast-1, ap-southeast-2, eu-central-1, eu-west-1, sa-east-1, us-east-1, us-west-1, us-west-2):</b> <code> 12.1.0.1.v3 | 12.1.0.1.v4 | 12.1.0.1.v5</code> </p> </li> <li> <p> <b>Version 12.1 (available in all AWS regions):</b> <code> 12.1.0.2.v1</code> </p> </li> <li> <p> <b>Version 12.1 (available in all AWS regions except us-gov-west-1):</b> <code> 12.1.0.2.v2 | 12.1.0.2.v3 | 12.1.0.2.v4</code> </p> </li> <li> <p> <b>Version 11.2 (available in all AWS regions):</b> <code> 11.2.0.4.v1 | 11.2.0.4.v3 | 11.2.0.4.v4</code> </p> </li> <li> <p> <b>Version 11.2 (available in all AWS regions except us-gov-west-1):</b> <code> 11.2.0.4.v5 | 11.2.0.4.v6 | 11.2.0.4.v7 | 11.2.0.4.v8</code> </p> </li> </ul> <p> <b>Oracle Database Standard Edition (oracle-se)</b> </p> <ul> <li> <p> <b>Version 12.1 (available in all AWS regions except ap-south-1, ap-northeast-2):</b> <code> 12.1.0.1.v1 | 12.1.0.1.v2</code> </p> </li> <li> <p> <b>Version 12.1 (only available in AWS regions ap-northeast-1, ap-southeast-1, ap-southeast-2, eu-central-1, eu-west-1, sa-east-1, us-east-1, us-west-1, us-west-2):</b> <code> 12.1.0.1.v3 | 12.1.0.1.v4 | 12.1.0.1.v5</code> </p> </li> <li> <p> <b>Version 11.2 (available in all AWS regions):</b> <code> 11.2.0.4.v1 | 11.2.0.4.v3 | 11.2.0.4.v4</code> </p> </li> <li> <p> <b>Version 11.2 (available in all AWS regions except us-gov-west-1):</b> <code> 11.2.0.4.v5 | 11.2.0.4.v6 | 11.2.0.4.v7 | 11.2.0.4.v8</code> </p> </li> </ul> <p> <b>Oracle Database Standard Edition One (oracle-se1)</b> </p> <ul> <li> <p> <b>Version 12.1 (available in all AWS regions except ap-south-1, ap-northeast-2):</b> <code> 12.1.0.1.v1 | 12.1.0.1.v2</code> </p> </li> <li> <p> <b>Version 12.1 (only available in AWS regions ap-northeast-1, ap-southeast-1, ap-southeast-2, eu-central-1, eu-west-1, sa-east-1, us-east-1, us-west-1, us-west-2):</b> <code> 12.1.0.1.v3 | 12.1.0.1.v4 | 12.1.0.1.v5</code> </p> </li> <li> <p> <b>Version 11.2 (available in all AWS regions):</b> <code> 11.2.0.4.v1 | 11.2.0.4.v3 | 11.2.0.4.v4</code> </p> </li> <li> <p> <b>Version 11.2 (available in all AWS regions except us-gov-west-1):</b> <code> 11.2.0.4.v5 | 11.2.0.4.v6 | 11.2.0.4.v7 | 11.2.0.4.v8</code> </p> </li> </ul> <p> <b>Oracle Database Standard Edition Two (oracle-se2)</b> </p> <ul> <li> <p> <b>Version 12.1 (available in all AWS regions except us-gov-west-1):</b> <code> 12.1.0.2.v2 | 12.1.0.2.v3 | 12.1.0.2.v4</code> </p> </li> </ul> <p> <b>PostgreSQL</b> </p> <ul> <li> <p> <b>Version 9.5 (available in all AWS regions except us-gov-west-1):</b> <code> 9.5.2</code> </p> </li> <li> <p> <b>Version 9.4 (available in all AWS regions except ap-south-1):</b> <code> 9.4.1 | 9.4.4</code> </p> </li> <li> <p> <b>Version 9.4 (available in all AWS regions):</b> <code> 9.4.5</code> </p> </li> <li> <p> <b>Version 9.4 (available in all AWS regions except us-gov-west-1):</b> <code> 9.4.7</code> </p> </li> <li> <p> <b>Version 9.3 (only available in AWS regions ap-northeast-1, ap-southeast-1, ap-southeast-2, eu-west-1, sa-east-1, us-east-1, us-gov-west-1, us-west-1, us-west-2):</b> <code> 9.3.1 | 9.3.2</code> </p> </li> <li> <p> <b>Version 9.3 (available in all AWS regions except ap-south-1, ap-northeast-2):</b> <code> 9.3.10 | 9.3.3 | 9.3.5 | 9.3.6 | 9.3.9</code> </p> </li> <li> <p> <b>Version 9.3 (only available in AWS regions ap-northeast-1, ap-southeast-1, ap-southeast-2, eu-central-1, eu-west-1, sa-east-1, us-east-1, us-west-1, us-west-2):</b> <code> 9.3.12</code> </p> </li> </ul>
      AutoMinorVersionUpgrade?:
        type: BooleanOptional
        description: '<p>Indicates that minor engine upgrades will be applied automatically
          to the DB instance during the maintenance window.</p> <p>Default: <code>true</code>
          </p>'
      LicenseModel?:
        type: String
        description: '<p>License model information for this DB instance.</p> <p> Valid
          values: <code>license-included</code> | <code>bring-your-own-license</code>
          | <code>general-public-license</code> </p>'
      Iops?:
        type: IntegerOptional
        description: '<p>The amount of Provisioned IOPS (input/output operations per
          second) to be initially allocated for the DB instance.</p> <p>Constraints:
          Must be a multiple between 3 and 10 of the storage amount for the DB instance.
          Must also be an integer multiple of 1000. For example, if the size of your
          DB instance is 500 GB, then your <code>Iops</code> value can be 2000, 3000,
          4000, or 5000. </p>'
      OptionGroupName?:
        type: String
        description: <p>Indicates that the DB instance should be associated with the specified option group.</p> <p>Permanent options, such as the TDE option for Oracle Advanced Security TDE, cannot be removed from an option group, and that option group cannot be removed from a DB instance once it is associated with a DB instance</p>
      CharacterSetName?:
        type: String
        description: <p>For supported engines, indicates that the DB instance should be associated with the specified CharacterSet.</p>
      PubliclyAccessible?:
        type: BooleanOptional
        description: '<p>Specifies the accessibility options for the DB instance.
          A value of true specifies an Internet-facing instance with a publicly resolvable
          DNS name, which resolves to a public IP address. A value of false specifies
          an internal instance with a DNS name that resolves to a private IP address.</p>
          <p>Default: The default behavior varies depending on whether a VPC has been
          requested or not. The following list shows the default behavior in each
          case.</p> <ul> <li> <p> <b>Default VPC:</b> true</p> </li> <li> <p> <b>VPC:</b>
          false</p> </li> </ul> <p>If no DB subnet group has been specified as part
          of the request and the PubliclyAccessible value has not been set, the DB
          instance will be publicly accessible. If a specific DB subnet group has
          been specified as part of the request and the PubliclyAccessible value has
          not been set, the DB instance will be private.</p>'
      Tags?:
        type: TagList
      DBClusterIdentifier?:
        type: String
        description: '<p>The identifier of the DB cluster that the instance will belong
          to.</p> <p>For information on creating a DB cluster, see <a>CreateDBCluster</a>.</p>
          <p>Type: String</p>'
      StorageType?:
        type: String
        description: '<p>Specifies the storage type to be associated with the DB instance.</p>
          <p> Valid values: <code>standard | gp2 | io1</code> </p> <p> If you specify
          <code>io1</code>, you must also include a value for the <code>Iops</code>
          parameter. </p> <p> Default: <code>io1</code> if the <code>Iops</code> parameter
          is specified; otherwise <code>standard</code> </p>'
      TdeCredentialArn?:
        type: String
        description: <p>The ARN from the Key Store with which to associate the instance for TDE encryption.</p>
      TdeCredentialPassword?:
        type: String
        description: <p>The password for the given ARN from the Key Store in order to access the device.</p>
      StorageEncrypted?:
        type: BooleanOptional
        description: '<p>Specifies whether the DB instance is encrypted.</p> <p>Default:
          false</p>'
      KmsKeyId?:
        type: String
        description: <p>The KMS key identifier for an encrypted DB instance.</p> <p>The KMS key identifier is the Amazon Resource Name (ARN) for the KMS encryption key. If you are creating a DB instance with the same AWS account that owns the KMS encryption key used to encrypt the new DB instance, then you can use the KMS key alias instead of the ARN for the KM encryption key.</p> <p>If the <code>StorageEncrypted</code> parameter is true, and you do not specify a value for the <code>KmsKeyId</code> parameter, then Amazon RDS will use your default encryption key. AWS KMS creates the default encryption key for your AWS account. Your AWS account has a different default encryption key for each AWS region.</p>
      Domain?:
        type: String
        description: <p>Specify the Active Directory Domain to create the instance in.</p>
      CopyTagsToSnapshot?:
        type: BooleanOptional
        description: <p>True to copy all tags from the DB instance to snapshots of the DB instance; otherwise false. The default is false.</p>
      MonitoringInterval?:
        type: IntegerOptional
        description: '<p>The interval, in seconds, between points when Enhanced Monitoring
          metrics are collected for the DB instance. To disable collecting Enhanced
          Monitoring metrics, specify 0. The default is 0.</p> <p>If <code>MonitoringRoleArn</code>
          is specified, then you must also set <code>MonitoringInterval</code> to
          a value other than 0.</p> <p>Valid Values: <code>0, 1, 5, 10, 15, 30, 60</code>
          </p>'
      MonitoringRoleArn?:
        type: String
        description: <p>The ARN for the IAM role that permits RDS to send enhanced monitoring metrics to CloudWatch Logs. For example, <code>arn:aws:iam:123456789012:role/emaccess</code>. For information on creating a monitoring role, go to <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Monitoring.html#USER_Monitoring.OS.IAMRole">To create an IAM role for Amazon RDS Enhanced Monitoring</a>.</p> <p>If <code>MonitoringInterval</code> is set to a value other than 0, then you must supply a <code>MonitoringRoleArn</code> value.</p>
      DomainIAMRoleName?:
        type: String
        description: <p>Specify the name of the IAM role to be used when making API calls to the Directory Service.</p>
      PromotionTier?:
        type: IntegerOptional
        description: '<p>A value that specifies the order in which an Aurora Replica
          is promoted to the primary instance after a failure of the existing primary
          instance. For more information, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Aurora.Managing.html#Aurora.Managing.FaultTolerance">
          Fault Tolerance for an Aurora DB Cluster</a>. </p> <p>Default: 1</p> <p>Valid
          Values: 0 - 15</p>'
      Timezone?:
        type: String
        description: <p>The time zone of the DB instance. The time zone parameter is currently supported only by <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_SQLServer.html#SQLServer.Concepts.General.TimeZone">Microsoft SQL Server</a>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBInstanceReadReplicaMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: <p>The DB instance identifier of the Read Replica. This identifier is the unique key that identifies a DB instance. This parameter is stored as a lowercase string.</p>
      SourceDBInstanceIdentifier:
        type: String
        description: <p>The identifier of the DB instance that will act as the source for the Read Replica. Each DB instance can have up to five Read Replicas.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier of an existing MySQL, MariaDB, or PostgreSQL DB instance.</p> </li> <li> <p>Can specify a DB instance that is a MySQL Read Replica only if the source is running MySQL 5.6.</p> </li> <li> <p>Can specify a DB instance that is a PostgreSQL Read Replica only if the source is running PostgreSQL 9.3.5.</p> </li> <li> <p>The specified DB instance must have automatic backups enabled, its backup retention period must be greater than 0.</p> </li> <li> <p>If the source DB instance is in the same region as the Read Replica, specify a valid DB instance identifier.</p> </li> <li> <p>If the source DB instance is in a different region than the Read Replica, specify a valid DB instance ARN. For more information, go to <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing a Amazon RDS Amazon Resource Name (ARN)</a>.</p> </li> </ul>
      DBInstanceClass?:
        type: String
        description: '<p>The compute and memory capacity of the Read Replica.</p>
          <p> Valid Values: <code>db.m1.small | db.m1.medium | db.m1.large | db.m1.xlarge
          | db.m2.xlarge |db.m2.2xlarge | db.m2.4xlarge | db.m3.medium | db.m3.large
          | db.m3.xlarge | db.m3.2xlarge | db.m4.large | db.m4.xlarge | db.m4.2xlarge
          | db.m4.4xlarge | db.m4.10xlarge | db.r3.large | db.r3.xlarge | db.r3.2xlarge
          | db.r3.4xlarge | db.r3.8xlarge | db.t2.micro | db.t2.small | db.t2.medium
          | db.t2.large</code> </p> <p>Default: Inherits from the source DB instance.</p>'
      AvailabilityZone?:
        type: String
        description: '<p>The Amazon EC2 Availability Zone that the Read Replica will
          be created in.</p> <p>Default: A random, system-chosen Availability Zone
          in the endpoint''s region.</p> <p> Example: <code>us-east-1d</code> </p>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number that the DB instance uses for connections.</p>
          <p>Default: Inherits from the source DB instance</p> <p>Valid Values: <code>1150-65535</code>
          </p>'
      AutoMinorVersionUpgrade?:
        type: BooleanOptional
        description: '<p>Indicates that minor engine upgrades will be applied automatically
          to the Read Replica during the maintenance window.</p> <p>Default: Inherits
          from the source DB instance</p>'
      Iops?:
        type: IntegerOptional
        description: <p>The amount of Provisioned IOPS (input/output operations per second) to be initially allocated for the DB instance.</p>
      OptionGroupName?:
        type: String
        description: <p>The option group the DB instance will be associated with. If omitted, the default option group for the engine specified will be used.</p>
      PubliclyAccessible?:
        type: BooleanOptional
        description: '<p>Specifies the accessibility options for the DB instance.
          A value of true specifies an Internet-facing instance with a publicly resolvable
          DNS name, which resolves to a public IP address. A value of false specifies
          an internal instance with a DNS name that resolves to a private IP address.</p>
          <p>Default: The default behavior varies depending on whether a VPC has been
          requested or not. The following list shows the default behavior in each
          case.</p> <ul> <li> <p> <b>Default VPC:</b>true</p> </li> <li> <p> <b>VPC:</b>false</p>
          </li> </ul> <p>If no DB subnet group has been specified as part of the request
          and the PubliclyAccessible value has not been set, the DB instance will
          be publicly accessible. If a specific DB subnet group has been specified
          as part of the request and the PubliclyAccessible value has not been set,
          the DB instance will be private.</p>'
      Tags?:
        type: TagList
      DBSubnetGroupName?:
        type: String
        description: '<p>Specifies a DB subnet group for the DB instance. The new
          DB instance will be created in the VPC associated with the DB subnet group.
          If no DB subnet group is specified, then the new DB instance is not created
          in a VPC.</p> <p>Constraints:</p> <ul> <li> <p>Can only be specified if
          the source DB instance identifier specifies a DB instance in another region.</p>
          </li> <li> <p>The specified DB subnet group must be in the same region in
          which the operation is running.</p> </li> <li> <p>All Read Replicas in one
          region that are created from the same source DB instance must either:&gt;</p>
          <ul> <li> <p>Specify DB subnet groups from the same VPC. All these Read
          Replicas will be created in the same VPC.</p> </li> <li> <p>Not specify
          a DB subnet group. All these Read Replicas will be created outside of any
          VPC.</p> </li> </ul> </li> </ul> <p>Constraints: Must contain no more than
          255 alphanumeric characters, periods, underscores, spaces, or hyphens. Must
          not be default.</p> <p>Example: <code>mySubnetgroup</code> </p>'
      StorageType?:
        type: String
        description: '<p>Specifies the storage type to be associated with the Read
          Replica.</p> <p> Valid values: <code>standard | gp2 | io1</code> </p> <p>
          If you specify <code>io1</code>, you must also include a value for the <code>Iops</code>
          parameter. </p> <p> Default: <code>io1</code> if the <code>Iops</code> parameter
          is specified; otherwise <code>standard</code> </p>'
      CopyTagsToSnapshot?:
        type: BooleanOptional
        description: <p>True to copy all tags from the Read Replica to snapshots of the Read Replica; otherwise false. The default is false.</p>
      MonitoringInterval?:
        type: IntegerOptional
        description: '<p>The interval, in seconds, between points when Enhanced Monitoring
          metrics are collected for the Read Replica. To disable collecting Enhanced
          Monitoring metrics, specify 0. The default is 0.</p> <p>If <code>MonitoringRoleArn</code>
          is specified, then you must also set <code>MonitoringInterval</code> to
          a value other than 0.</p> <p>Valid Values: <code>0, 1, 5, 10, 15, 30, 60</code>
          </p>'
      MonitoringRoleArn?:
        type: String
        description: <p>The ARN for the IAM role that permits RDS to send enhanced monitoring metrics to CloudWatch Logs. For example, <code>arn:aws:iam:123456789012:role/emaccess</code>. For information on creating a monitoring role, go to <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Monitoring.html#USER_Monitoring.OS.IAMRole">To create an IAM role for Amazon RDS Enhanced Monitoring</a>.</p> <p>If <code>MonitoringInterval</code> is set to a value other than 0, then you must supply a <code>MonitoringRoleArn</code> value.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBInstanceReadReplicaResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBInstanceResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBParameterGroupMessage:
    type: object
    properties:
      DBParameterGroupName:
        type: String
        description: <p>The name of the DB parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <note> <p>This value is stored as a lowercase string.</p> </note>
      DBParameterGroupFamily:
        type: String
        description: <p>The DB parameter group family name. A DB parameter group can be associated with one and only one DB parameter group family, and can be applied only to a DB instance running a database engine and engine version compatible with that DB parameter group family.</p>
      Description:
        type: String
        description: <p>The description for the DB parameter group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBParameterGroupResult:
    type: object
    properties:
      DBParameterGroup?:
        type: DBParameterGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBSecurityGroupMessage:
    type: object
    properties:
      DBSecurityGroupName:
        type: String
        description: '<p>The name for the DB security group. This value is stored
          as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to
          255 alphanumeric characters</p> </li> <li> <p>First character must be a
          letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive
          hyphens</p> </li> <li> <p>Must not be "Default"</p> </li> </ul> <p>Example:
          <code>mysecuritygroup</code> </p>'
      DBSecurityGroupDescription:
        type: String
        description: <p>The description for the DB security group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBSecurityGroupResult:
    type: object
    properties:
      DBSecurityGroup?:
        type: DBSecurityGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBSnapshotMessage:
    type: object
    properties:
      DBSnapshotIdentifier:
        type: String
        description: '<p>The identifier for the DB snapshot.</p> <p>Constraints:</p>
          <ul> <li> <p>Cannot be null, empty, or blank</p> </li> <li> <p>Must contain
          from 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First
          character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or
          contain two consecutive hyphens</p> </li> </ul> <p>Example: <code>my-snapshot-id</code>
          </p>'
      DBInstanceIdentifier:
        type: String
        description: <p>The DB instance identifier. This is the unique key that identifies a DB instance.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBSnapshotResult:
    type: object
    properties:
      DBSnapshot?:
        type: DBSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateDBSubnetGroupMessage:
    type: object
    properties:
      DBSubnetGroupName:
        type: String
        description: '<p>The name for the DB subnet group. This value is stored as
          a lowercase string.</p> <p>Constraints: Must contain no more than 255 alphanumeric
          characters, periods, underscores, spaces, or hyphens. Must not be default.</p>
          <p>Example: <code>mySubnetgroup</code> </p>'
      DBSubnetGroupDescription:
        type: String
        description: <p>The description for the DB subnet group.</p>
      SubnetIds:
        type: SubnetIdentifierList
        description: <p>The EC2 Subnet IDs for the DB subnet group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateDBSubnetGroupResult:
    type: object
    properties:
      DBSubnetGroup?:
        type: DBSubnetGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateEventSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: '<p>The name of the subscription.</p> <p>Constraints: The name
          must be less than 255 characters.</p>'
      SnsTopicArn:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the SNS topic created for event notification. The ARN is created by Amazon SNS when you create a topic and subscribe to it.</p>
      SourceType?:
        type: String
        description: '<p>The type of source that will be generating the events. For
          example, if you want to be notified of events generated by a DB instance,
          you would set this parameter to db-instance. if this value is not specified,
          all events are returned.</p> <p>Valid values: <code>db-instance</code> |
          <code>db-cluster</code> | <code>db-parameter-group</code> | <code>db-security-group</code>
          | <code>db-snapshot</code> | <code>db-cluster-snapshot</code> </p>'
      EventCategories?:
        type: EventCategoriesList
        description: <p> A list of event categories for a SourceType that you want to subscribe to. You can see a list of the categories for a given SourceType in the <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Events.html">Events</a> topic in the Amazon RDS User Guide or by using the <b>DescribeEventCategories</b> action. </p>
      SourceIds?:
        type: SourceIdsList
        description: <p>The list of identifiers of the event sources for which events will be returned. If not specified, then all sources are included in the response. An identifier must begin with a letter and must contain only ASCII letters, digits, and hyphens; it cannot end with a hyphen or contain two consecutive hyphens.</p> <p>Constraints:</p> <ul> <li> <p>If SourceIds are supplied, SourceType must also be provided.</p> </li> <li> <p>If the source type is a DB instance, then a <code>DBInstanceIdentifier</code> must be supplied.</p> </li> <li> <p>If the source type is a DB security group, a <code>DBSecurityGroupName</code> must be supplied.</p> </li> <li> <p>If the source type is a DB parameter group, a <code>DBParameterGroupName</code> must be supplied.</p> </li> <li> <p>If the source type is a DB snapshot, a <code>DBSnapshotIdentifier</code> must be supplied.</p> </li> </ul>
      Enabled?:
        type: BooleanOptional
        description: <p> A Boolean value; set to <b>true</b> to activate the subscription, set to <b>false</b> to create the subscription but not active it. </p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateEventSubscriptionResult:
    type: object
    properties:
      EventSubscription?:
        type: EventSubscription
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  CreateOptionGroupMessage:
    type: object
    properties:
      OptionGroupName:
        type: String
        description: '<p>Specifies the name of the option group to be created.</p>
          <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters
          or hyphens</p> </li> <li> <p>First character must be a letter</p> </li>
          <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p>
          </li> </ul> <p>Example: <code>myoptiongroup</code> </p>'
      EngineName:
        type: String
        description: <p>Specifies the name of the engine that this option group should be associated with.</p>
      MajorEngineVersion:
        type: String
        description: <p>Specifies the major version of the engine that this option group should be associated with.</p>
      OptionGroupDescription:
        type: String
        description: <p>The description of the option group.</p>
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  CreateOptionGroupResult:
    type: object
    properties:
      OptionGroup?:
        type: OptionGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DBCluster:
    type: object
    properties:
      AllocatedStorage?:
        type: IntegerOptional
        description: <p>Specifies the allocated storage size in gigabytes (GB).</p>
      AvailabilityZones?:
        type: AvailabilityZones
        description: <p>Provides the list of EC2 Availability Zones that instances in the DB cluster can be created in.</p>
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: <p>Specifies the number of days for which automatic DB snapshots are retained.</p>
      CharacterSetName?:
        type: String
        description: <p>If present, specifies the name of the character set that this cluster is associated with.</p>
      DatabaseName?:
        type: String
        description: <p>Contains the name of the initial database of this DB cluster that was provided at create time, if one was specified when the DB cluster was created. This same name is returned for the life of the DB cluster.</p>
      DBClusterIdentifier?:
        type: String
        description: <p>Contains a user-supplied DB cluster identifier. This identifier is the unique key that identifies a DB cluster.</p>
      DBClusterParameterGroup?:
        type: String
        description: <p>Specifies the name of the DB cluster parameter group for the DB cluster.</p>
      DBSubnetGroup?:
        type: String
        description: <p>Specifies information on the subnet group associated with the DB cluster, including the name, description, and subnets in the subnet group.</p>
      Status?:
        type: String
        description: <p>Specifies the current state of this DB cluster.</p>
      PercentProgress?:
        type: String
        description: <p>Specifies the progress of the operation as a percentage.</p>
      EarliestRestorableTime?:
        type: TStamp
        description: <p>Specifies the earliest time to which a database can be restored with point-in-time restore.</p>
      Endpoint?:
        type: String
        description: <p>Specifies the connection endpoint for the primary instance of the DB cluster.</p>
      ReaderEndpoint?:
        type: String
        description: <p>The reader endpoint for the DB cluster. The reader endpoint for a DB cluster load-balances connections across the Aurora Replicas that are available in a DB cluster. As clients request new connections to the reader endpoint, Aurora distributes the connection requests among the Aurora Replicas in the DB cluster. This functionality can help balance your read workload across multiple Aurora Replicas in your DB cluster. </p> <p>If a failover occurs, and the Aurora Replica that you are connected to is promoted to be the primary instance, your connection will be dropped. To continue sending your read workload to other Aurora Replicas in the cluster, you can then recoonect to the reader endpoint.</p>
      Engine?:
        type: String
        description: <p>Provides the name of the database engine to be used for this DB cluster.</p>
      EngineVersion?:
        type: String
        description: <p>Indicates the database engine version.</p>
      LatestRestorableTime?:
        type: TStamp
        description: <p>Specifies the latest time to which a database can be restored with point-in-time restore.</p>
      Port?:
        type: IntegerOptional
        description: <p>Specifies the port that the database engine is listening on.</p>
      MasterUsername?:
        type: String
        description: <p>Contains the master username for the DB cluster.</p>
      DBClusterOptionGroupMemberships?:
        type: DBClusterOptionGroupMemberships
        description: <p>Provides the list of option group memberships for this DB cluster.</p>
      PreferredBackupWindow?:
        type: String
        description: <p>Specifies the daily time range during which automated backups are created if automated backups are enabled, as determined by the <code>BackupRetentionPeriod</code>. </p>
      PreferredMaintenanceWindow?:
        type: String
        description: <p>Specifies the weekly time range during which system maintenance can occur, in Universal Coordinated Time (UTC).</p>
      ReplicationSourceIdentifier?:
        type: String
        description: <p>Contains the identifier of the source DB cluster if this DB cluster is a Read Replica.</p>
      ReadReplicaIdentifiers?:
        type: ReadReplicaIdentifierList
        description: <p>Contains one or more identifiers of the Read Replicas associated with this DB cluster.</p>
      DBClusterMembers?:
        type: DBClusterMemberList
        description: <p>Provides the list of instances that make up the DB cluster.</p>
      VpcSecurityGroups?:
        type: VpcSecurityGroupMembershipList
        description: <p>Provides a list of VPC security groups that the DB cluster belongs to.</p>
      HostedZoneId?:
        type: String
        description: <p>Specifies the ID that Amazon Route 53 assigns when you create a hosted zone.</p>
      StorageEncrypted?:
        type: Boolean
        description: <p>Specifies whether the DB cluster is encrypted.</p>
      KmsKeyId?:
        type: String
        description: <p>If <code>StorageEncrypted</code> is true, the KMS key identifier for the encrypted DB cluster.</p>
      DbClusterResourceId?:
        type: String
        description: <p>The region-unique, immutable identifier for the DB cluster. This identifier is found in AWS CloudTrail log entries whenever the KMS key for the DB cluster is accessed.</p>
      DBClusterArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB cluster.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the following actions:</p> <ul> <li> <p> <a>CreateDBCluster</a> </p> </li> <li> <p> <a>DeleteDBCluster</a> </p> </li> <li> <p> <a>FailoverDBCluster</a> </p> </li> <li> <p> <a>ModifyDBCluster</a> </p> </li> <li> <p> <a>RestoreDBClusterFromSnapshot</a> </p> </li> <li> <p> <a>RestoreDBClusterToPointInTime</a> </p> </li> </ul> <p>This data type is used as a response element in the <a>DescribeDBClusters</a> action.</p>
  DBClusterMember:
    type: object
    properties:
      DBInstanceIdentifier?:
        type: String
        description: <p>Specifies the instance identifier for this member of the DB cluster.</p>
      IsClusterWriter?:
        type: Boolean
        description: <p>Value that is <code>true</code> if the cluster member is the primary instance for the DB cluster and <code>false</code> otherwise.</p>
      DBClusterParameterGroupStatus?:
        type: String
        description: <p>Specifies the status of the DB cluster parameter group for this member of the DB cluster.</p>
      PromotionTier?:
        type: IntegerOptional
        description: <p>A value that specifies the order in which an Aurora Replica is promoted to the primary instance after a failure of the existing primary instance. For more information, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Aurora.Managing.html#Aurora.Managing.FaultTolerance"> Fault Tolerance for an Aurora DB Cluster</a>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains information about an instance that is part of a DB cluster.</p>
  DBClusterMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A pagination token that can be used in a subsequent DescribeDBClusters request.</p>
      DBClusters?:
        type: DBClusterList
        description: <p>Contains a list of DB clusters for the user.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the <a>DescribeDBClusters</a> action.</p>
  DBClusterOptionGroupStatus:
    type: object
    properties:
      DBClusterOptionGroupName?:
        type: String
        description: <p>Specifies the name of the DB cluster option group.</p>
      Status?:
        type: String
        description: <p>Specifies the status of the DB cluster option group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains status information for a DB cluster option group.</p>
  DBClusterParameterGroup:
    type: object
    properties:
      DBClusterParameterGroupName?:
        type: String
        description: <p>Provides the name of the DB cluster parameter group.</p>
      DBParameterGroupFamily?:
        type: String
        description: <p>Provides the name of the DB parameter group family that this DB cluster parameter group is compatible with.</p>
      Description?:
        type: String
        description: <p>Provides the customer-specified description for this DB cluster parameter group.</p>
      DBClusterParameterGroupArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB cluster parameter group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the <a>CreateDBClusterParameterGroup</a> or <a>CopyDBClusterParameterGroup</a> action. </p> <p>This data type is used as a request parameter in the <a>DeleteDBClusterParameterGroup</a> action, and as a response element in the <a>DescribeDBClusterParameterGroups</a> action. </p>
  DBClusterParameterGroupDetails:
    type: object
    properties:
      Parameters?:
        type: ParametersList
        description: <p>Provides a list of parameters for the DB cluster parameter group.</p>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous DescribeDBClusterParameters request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Provides details about a DB cluster parameter group including the parameters in the DB cluster parameter group.</p>
  DBClusterParameterGroupNameMessage:
    type: object
    properties:
      DBClusterParameterGroupName?:
        type: String
        description: <p>The name of the DB cluster parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <note> <p>This value is stored as a lowercase string.</p> </note>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DBClusterParameterGroupsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBClusterParameterGroups</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DBClusterParameterGroups?:
        type: DBClusterParameterGroupList
        description: <p>A list of DB cluster parameter groups.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DBClusterSnapshot:
    type: object
    properties:
      AvailabilityZones?:
        type: AvailabilityZones
        description: <p>Provides the list of EC2 Availability Zones that instances in the DB cluster snapshot can be restored in.</p>
      DBClusterSnapshotIdentifier?:
        type: String
        description: <p>Specifies the identifier for the DB cluster snapshot.</p>
      DBClusterIdentifier?:
        type: String
        description: <p>Specifies the DB cluster identifier of the DB cluster that this DB cluster snapshot was created from.</p>
      SnapshotCreateTime?:
        type: TStamp
        description: <p>Provides the time when the snapshot was taken, in Universal Coordinated Time (UTC).</p>
      Engine?:
        type: String
        description: <p>Specifies the name of the database engine.</p>
      AllocatedStorage?:
        type: Integer
        description: <p>Specifies the allocated storage size in gigabytes (GB).</p>
      Status?:
        type: String
        description: <p>Specifies the status of this DB cluster snapshot.</p>
      Port?:
        type: Integer
        description: <p>Specifies the port that the DB cluster was listening on at the time of the snapshot.</p>
      VpcId?:
        type: String
        description: <p>Provides the VPC ID associated with the DB cluster snapshot.</p>
      ClusterCreateTime?:
        type: TStamp
        description: <p>Specifies the time when the DB cluster was created, in Universal Coordinated Time (UTC).</p>
      MasterUsername?:
        type: String
        description: <p>Provides the master username for the DB cluster snapshot.</p>
      EngineVersion?:
        type: String
        description: <p>Provides the version of the database engine for this DB cluster snapshot.</p>
      LicenseModel?:
        type: String
        description: <p>Provides the license model information for this DB cluster snapshot.</p>
      SnapshotType?:
        type: String
        description: <p>Provides the type of the DB cluster snapshot.</p>
      PercentProgress?:
        type: Integer
        description: <p>Specifies the percentage of the estimated data that has been transferred.</p>
      StorageEncrypted?:
        type: Boolean
        description: <p>Specifies whether the DB cluster snapshot is encrypted.</p>
      KmsKeyId?:
        type: String
        description: <p>If <code>StorageEncrypted</code> is true, the KMS key identifier for the encrypted DB cluster snapshot.</p>
      DBClusterSnapshotArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB cluster snapshot.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the following actions:</p> <ul> <li> <p> <a>CreateDBClusterSnapshot</a> </p> </li> <li> <p> <a>DeleteDBClusterSnapshot</a> </p> </li> </ul> <p>This data type is used as a response element in the <a>DescribeDBClusterSnapshots</a> action.</p>
  DBClusterSnapshotAttribute:
    type: object
    properties:
      AttributeName?:
        type: String
        description: <p>The name of the manual DB cluster snapshot attribute.</p> <p>The attribute named <code>restore</code> refers to the list of AWS accounts that have permission to copy or restore the manual DB cluster snapshot. For more information, see the <a>ModifyDBClusterSnapshotAttribute</a> API action.</p>
      AttributeValues?:
        type: AttributeValueList
        description: <p>The value(s) for the manual DB cluster snapshot attribute.</p> <p>If the <code>AttributeName</code> field is set to <code>restore</code>, then this element returns a list of IDs of the AWS accounts that are authorized to copy or restore the manual DB cluster snapshot. If a value of <code>all</code> is in the list, then the manual DB cluster snapshot is public and available for any AWS account to copy or restore.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the name and values of a manual DB cluster snapshot attribute.</p> <p>Manual DB cluster snapshot attributes are used to authorize other AWS accounts to restore a manual DB cluster snapshot. For more information, see the <a>ModifyDBClusterSnapshotAttribute</a> API action.</p>
  DBClusterSnapshotAttributesResult:
    type: object
    properties:
      DBClusterSnapshotIdentifier?:
        type: String
        description: <p>The identifier of the manual DB cluster snapshot that the attributes apply to.</p>
      DBClusterSnapshotAttributes?:
        type: DBClusterSnapshotAttributeList
        description: <p>The list of attributes and values for the manual DB cluster snapshot.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the results of a successful call to the <a>DescribeDBClusterSnapshotAttributes</a> API action.</p> <p>Manual DB cluster snapshot attributes are used to authorize other AWS accounts to copy or restore a manual DB cluster snapshot. For more information, see the <a>ModifyDBClusterSnapshotAttribute</a> API action.</p>
  DBClusterSnapshotMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <a>DescribeDBClusterSnapshots</a> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DBClusterSnapshots?:
        type: DBClusterSnapshotList
        description: <p>Provides a list of DB cluster snapshots for the user.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Provides a list of DB cluster snapshots for the user as the result of a call to the <a>DescribeDBClusterSnapshots</a> action. </p>
  DBEngineVersion:
    type: object
    properties:
      Engine?:
        type: String
        description: <p>The name of the database engine.</p>
      EngineVersion?:
        type: String
        description: <p>The version number of the database engine.</p>
      DBParameterGroupFamily?:
        type: String
        description: <p>The name of the DB parameter group family for the database engine.</p>
      DBEngineDescription?:
        type: String
        description: <p>The description of the database engine.</p>
      DBEngineVersionDescription?:
        type: String
        description: <p>The description of the database engine version.</p>
      DefaultCharacterSet?:
        type: CharacterSet
        description: <p> The default character set for new instances of this engine version, if the <code>CharacterSetName</code> parameter of the CreateDBInstance API is not specified. </p>
      SupportedCharacterSets?:
        type: SupportedCharacterSetsList
        description: <p> A list of the character sets supported by this engine for the <code>CharacterSetName</code> parameter of the <code>CreateDBInstance</code> action. </p>
      ValidUpgradeTarget?:
        type: ValidUpgradeTargetList
        description: <p>A list of engine versions that this database engine version can be upgraded to.</p>
      SupportedTimezones?:
        type: SupportedTimezonesList
        description: <p>A list of the time zones supported by this engine for the <code>Timezone</code> parameter of the <code>CreateDBInstance</code> action. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the action <a>DescribeDBEngineVersions</a>. </p>
  DBEngineVersionMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DBEngineVersions?:
        type: DBEngineVersionList
        description: <p> A list of <code>DBEngineVersion</code> elements. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeDBEngineVersions</a> action. </p>
  DBInstance:
    type: object
    properties:
      DBInstanceIdentifier?:
        type: String
        description: <p>Contains a user-supplied database identifier. This identifier is the unique key that identifies a DB instance.</p>
      DBInstanceClass?:
        type: String
        description: <p>Contains the name of the compute and memory capacity class of the DB instance.</p>
      Engine?:
        type: String
        description: <p>Provides the name of the database engine to be used for this DB instance.</p>
      DBInstanceStatus?:
        type: String
        description: <p>Specifies the current state of this database.</p>
      MasterUsername?:
        type: String
        description: <p>Contains the master username for the DB instance.</p>
      DBName?:
        type: String
        description: '<p>The meaning of this parameter differs according to the database
          engine you use. For example, this value returns MySQL, MariaDB, or PostgreSQL
          information when returning values from CreateDBInstanceReadReplica since
          Read Replicas are only supported for these engines.</p> <p> <b>MySQL, MariaDB,
          SQL Server, PostgreSQL, Amazon Aurora</b> </p> <p>Contains the name of the
          initial database of this instance that was provided at create time, if one
          was specified when the DB instance was created. This same name is returned
          for the life of the DB instance.</p> <p>Type: String</p> <p> <b>Oracle</b>
          </p> <p>Contains the Oracle System ID (SID) of the created DB instance.
          Not shown when the returned parameters do not apply to an Oracle DB instance.</p>'
      Endpoint?:
        type: Endpoint
        description: <p>Specifies the connection endpoint.</p>
      AllocatedStorage?:
        type: Integer
        description: <p>Specifies the allocated storage size specified in gigabytes.</p>
      InstanceCreateTime?:
        type: TStamp
        description: <p>Provides the date and time the DB instance was created.</p>
      PreferredBackupWindow?:
        type: String
        description: <p> Specifies the daily time range during which automated backups are created if automated backups are enabled, as determined by the <code>BackupRetentionPeriod</code>. </p>
      BackupRetentionPeriod?:
        type: Integer
        description: <p>Specifies the number of days for which automatic DB snapshots are retained.</p>
      DBSecurityGroups?:
        type: DBSecurityGroupMembershipList
        description: <p> Provides List of DB security group elements containing only <code>DBSecurityGroup.Name</code> and <code>DBSecurityGroup.Status</code> subelements. </p>
      VpcSecurityGroups?:
        type: VpcSecurityGroupMembershipList
        description: <p>Provides a list of VPC security group elements that the DB instance belongs to.</p>
      DBParameterGroups?:
        type: DBParameterGroupStatusList
        description: <p>Provides the list of DB parameter groups applied to this DB instance.</p>
      AvailabilityZone?:
        type: String
        description: <p>Specifies the name of the Availability Zone the DB instance is located in.</p>
      DBSubnetGroup?:
        type: DBSubnetGroup
        description: <p>Specifies information on the subnet group associated with the DB instance, including the name, description, and subnets in the subnet group.</p>
      PreferredMaintenanceWindow?:
        type: String
        description: <p>Specifies the weekly time range during which system maintenance can occur, in Universal Coordinated Time (UTC).</p>
      PendingModifiedValues?:
        type: PendingModifiedValues
        description: <p>Specifies that changes to the DB instance are pending. This element is only included when changes are pending. Specific changes are identified by subelements.</p>
      LatestRestorableTime?:
        type: TStamp
        description: <p>Specifies the latest time to which a database can be restored with point-in-time restore.</p>
      MultiAZ?:
        type: Boolean
        description: <p>Specifies if the DB instance is a Multi-AZ deployment.</p>
      EngineVersion?:
        type: String
        description: <p>Indicates the database engine version.</p>
      AutoMinorVersionUpgrade?:
        type: Boolean
        description: <p>Indicates that minor version patches are applied automatically.</p>
      ReadReplicaSourceDBInstanceIdentifier?:
        type: String
        description: <p>Contains the identifier of the source DB instance if this DB instance is a Read Replica.</p>
      ReadReplicaDBInstanceIdentifiers?:
        type: ReadReplicaDBInstanceIdentifierList
        description: <p>Contains one or more identifiers of the Read Replicas associated with this DB instance.</p>
      LicenseModel?:
        type: String
        description: <p>License model information for this DB instance.</p>
      Iops?:
        type: IntegerOptional
        description: <p>Specifies the Provisioned IOPS (I/O operations per second) value.</p>
      OptionGroupMemberships?:
        type: OptionGroupMembershipList
        description: <p>Provides the list of option group memberships for this DB instance.</p>
      CharacterSetName?:
        type: String
        description: <p>If present, specifies the name of the character set that this instance is associated with.</p>
      SecondaryAvailabilityZone?:
        type: String
        description: <p>If present, specifies the name of the secondary Availability Zone for a DB instance with multi-AZ support.</p>
      PubliclyAccessible?:
        type: Boolean
        description: '<p>Specifies the accessibility options for the DB instance.
          A value of true specifies an Internet-facing instance with a publicly resolvable
          DNS name, which resolves to a public IP address. A value of false specifies
          an internal instance with a DNS name that resolves to a private IP address.</p>
          <p>Default: The default behavior varies depending on whether a VPC has been
          requested or not. The following list shows the default behavior in each
          case.</p> <ul> <li> <p> <b>Default VPC:</b>true</p> </li> <li> <p> <b>VPC:</b>false</p>
          </li> </ul> <p>If no DB subnet group has been specified as part of the request
          and the PubliclyAccessible value has not been set, the DB instance will
          be publicly accessible. If a specific DB subnet group has been specified
          as part of the request and the PubliclyAccessible value has not been set,
          the DB instance will be private.</p>'
      StatusInfos?:
        type: DBInstanceStatusInfoList
        description: <p>The status of a Read Replica. If the instance is not a Read Replica, this will be blank.</p>
      StorageType?:
        type: String
        description: <p>Specifies the storage type associated with DB instance.</p>
      TdeCredentialArn?:
        type: String
        description: <p>The ARN from the key store with which the instance is associated for TDE encryption.</p>
      DbInstancePort?:
        type: Integer
        description: <p>Specifies the port that the DB instance listens on. If the DB instance is part of a DB cluster, this can be a different port than the DB cluster port.</p>
      DBClusterIdentifier?:
        type: String
        description: <p>If the DB instance is a member of a DB cluster, contains the name of the DB cluster that the DB instance is a member of.</p>
      StorageEncrypted?:
        type: Boolean
        description: <p>Specifies whether the DB instance is encrypted.</p>
      KmsKeyId?:
        type: String
        description: <p> If <code>StorageEncrypted</code> is true, the KMS key identifier for the encrypted DB instance. </p>
      DbiResourceId?:
        type: String
        description: <p>The region-unique, immutable identifier for the DB instance. This identifier is found in AWS CloudTrail log entries whenever the KMS key for the DB instance is accessed.</p>
      CACertificateIdentifier?:
        type: String
        description: <p>The identifier of the CA certificate for this DB instance.</p>
      DomainMemberships?:
        type: DomainMembershipList
        description: <p>The Active Directory Domain membership records associated with the DB instance.</p>
      CopyTagsToSnapshot?:
        type: Boolean
        description: <p>Specifies whether tags are copied from the DB instance to snapshots of the DB instance.</p>
      MonitoringInterval?:
        type: IntegerOptional
        description: <p>The interval, in seconds, between points when Enhanced Monitoring metrics are collected for the DB instance.</p>
      EnhancedMonitoringResourceArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the Amazon CloudWatch Logs log stream that receives the Enhanced Monitoring metrics data for the DB instance.</p>
      MonitoringRoleArn?:
        type: String
        description: <p>The ARN for the IAM role that permits RDS to send Enhanced Monitoring metrics to CloudWatch Logs.</p>
      PromotionTier?:
        type: IntegerOptional
        description: <p>A value that specifies the order in which an Aurora Replica is promoted to the primary instance after a failure of the existing primary instance. For more information, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Aurora.Managing.html#Aurora.Managing.FaultTolerance"> Fault Tolerance for an Aurora DB Cluster</a>. </p>
      DBInstanceArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB instance.</p>
      Timezone?:
        type: String
        description: <p>The time zone of the DB instance. In most cases, the <code>Timezone</code> element is empty. <code>Timezone</code> content appears only for Microsoft SQL Server DB instances that were created with a time zone specified. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the following actions:</p> <ul> <li> <p> <a>CreateDBInstance</a> </p> </li> <li> <p> <a>DeleteDBInstance</a> </p> </li> <li> <p> <a>ModifyDBInstance</a> </p> </li> </ul> <p>This data type is used as a response element in the <a>DescribeDBInstances</a> action.</p>
  DBInstanceMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
      DBInstances?:
        type: DBInstanceList
        description: <p> A list of <a>DBInstance</a> instances. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeDBInstances</a> action. </p>
  DBInstanceStatusInfo:
    type: object
    properties:
      StatusType?:
        type: String
        description: <p>This value is currently "read replication."</p>
      Normal?:
        type: Boolean
        description: <p>Boolean value that is true if the instance is operating normally, or false if the instance is in an error state.</p>
      Status?:
        type: String
        description: <p>Status of the DB instance. For a StatusType of read replica, the values can be replicating, error, stopped, or terminated.</p>
      Message?:
        type: String
        description: <p>Details of the error if there is an error for the instance. If the instance is not in an error state, this value is blank.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Provides a list of status information for a DB instance.</p>
  DBParameterGroup:
    type: object
    properties:
      DBParameterGroupName?:
        type: String
        description: <p>Provides the name of the DB parameter group.</p>
      DBParameterGroupFamily?:
        type: String
        description: <p>Provides the name of the DB parameter group family that this DB parameter group is compatible with.</p>
      Description?:
        type: String
        description: <p>Provides the customer-specified description for this DB parameter group.</p>
      DBParameterGroupArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB parameter group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the <a>CreateDBParameterGroup</a> action. </p> <p>This data type is used as a request parameter in the <a>DeleteDBParameterGroup</a> action, and as a response element in the <a>DescribeDBParameterGroups</a> action. </p>
  DBParameterGroupDetails:
    type: object
    properties:
      Parameters?:
        type: ParametersList
        description: <p> A list of <a>Parameter</a> values. </p>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeDBParameters</a> action. </p>
  DBParameterGroupNameMessage:
    type: object
    properties:
      DBParameterGroupName?:
        type: String
        description: <p>Provides the name of the DB parameter group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>ModifyDBParameterGroup</a> or <a>ResetDBParameterGroup</a> action. </p>
  DBParameterGroupStatus:
    type: object
    properties:
      DBParameterGroupName?:
        type: String
        description: <p>The name of the DP parameter group.</p>
      ParameterApplyStatus?:
        type: String
        description: <p>The status of parameter updates.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>The status of the DB parameter group.</p> <p>This data type is used as a response element in the following actions:</p> <ul> <li> <p> <a>CreateDBInstance</a> </p> </li> <li> <p> <a>CreateDBInstanceReadReplica</a> </p> </li> <li> <p> <a>DeleteDBInstance</a> </p> </li> <li> <p> <a>ModifyDBInstance</a> </p> </li> <li> <p> <a>RebootDBInstance</a> </p> </li> <li> <p> <a>RestoreDBInstanceFromDBSnapshot</a> </p> </li> </ul>
  DBParameterGroupsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DBParameterGroups?:
        type: DBParameterGroupList
        description: <p> A list of <a>DBParameterGroup</a> instances. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeDBParameterGroups</a> action. </p>
  DBSecurityGroup:
    type: object
    properties:
      OwnerId?:
        type: String
        description: <p>Provides the AWS ID of the owner of a specific DB security group.</p>
      DBSecurityGroupName?:
        type: String
        description: <p>Specifies the name of the DB security group.</p>
      DBSecurityGroupDescription?:
        type: String
        description: <p>Provides the description of the DB security group.</p>
      VpcId?:
        type: String
        description: <p>Provides the VpcId of the DB security group.</p>
      EC2SecurityGroups?:
        type: EC2SecurityGroupList
        description: <p> Contains a list of <a>EC2SecurityGroup</a> elements. </p>
      IPRanges?:
        type: IPRangeList
        description: <p> Contains a list of <a>IPRange</a> elements. </p>
      DBSecurityGroupArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB security group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the following actions:</p> <ul> <li> <p> <a>DescribeDBSecurityGroups</a> </p> </li> <li> <p> <a>AuthorizeDBSecurityGroupIngress</a> </p> </li> <li> <p> <a>CreateDBSecurityGroup</a> </p> </li> <li> <p> <a>RevokeDBSecurityGroupIngress</a> </p> </li> </ul> <p>This data type is used as a response element in the <a>DescribeDBSecurityGroups</a> action.</p>
  DBSecurityGroupMembership:
    type: object
    properties:
      DBSecurityGroupName?:
        type: String
        description: <p>The name of the DB security group.</p>
      Status?:
        type: String
        description: <p>The status of the DB security group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>This data type is used as a response element in the following actions:</p> <ul> <li> <p> <a>ModifyDBInstance</a> </p> </li> <li> <p> <a>RebootDBInstance</a> </p> </li> <li> <p> <a>RestoreDBInstanceFromDBSnapshot</a> </p> </li> <li> <p> <a>RestoreDBInstanceToPointInTime</a> </p> </li> </ul>
  DBSecurityGroupMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DBSecurityGroups?:
        type: DBSecurityGroups
        description: <p> A list of <a>DBSecurityGroup</a> instances. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeDBSecurityGroups</a> action. </p>
  DBSnapshot:
    type: object
    properties:
      DBSnapshotIdentifier?:
        type: String
        description: <p>Specifies the identifier for the DB snapshot.</p>
      DBInstanceIdentifier?:
        type: String
        description: <p>Specifies the DB instance identifier of the DB instance this DB snapshot was created from.</p>
      SnapshotCreateTime?:
        type: TStamp
        description: <p>Provides the time when the snapshot was taken, in Universal Coordinated Time (UTC).</p>
      Engine?:
        type: String
        description: <p>Specifies the name of the database engine.</p>
      AllocatedStorage?:
        type: Integer
        description: <p>Specifies the allocated storage size in gigabytes (GB).</p>
      Status?:
        type: String
        description: <p>Specifies the status of this DB snapshot.</p>
      Port?:
        type: Integer
        description: <p>Specifies the port that the database engine was listening on at the time of the snapshot.</p>
      AvailabilityZone?:
        type: String
        description: <p>Specifies the name of the Availability Zone the DB instance was located in at the time of the DB snapshot.</p>
      VpcId?:
        type: String
        description: <p>Provides the VPC ID associated with the DB snapshot.</p>
      InstanceCreateTime?:
        type: TStamp
        description: <p>Specifies the time when the snapshot was taken, in Universal Coordinated Time (UTC).</p>
      MasterUsername?:
        type: String
        description: <p>Provides the master username for the DB snapshot.</p>
      EngineVersion?:
        type: String
        description: <p>Specifies the version of the database engine.</p>
      LicenseModel?:
        type: String
        description: <p>License model information for the restored DB instance.</p>
      SnapshotType?:
        type: String
        description: <p>Provides the type of the DB snapshot.</p>
      Iops?:
        type: IntegerOptional
        description: <p>Specifies the Provisioned IOPS (I/O operations per second) value of the DB instance at the time of the snapshot.</p>
      OptionGroupName?:
        type: String
        description: <p>Provides the option group name for the DB snapshot.</p>
      PercentProgress?:
        type: Integer
        description: <p>The percentage of the estimated data that has been transferred.</p>
      SourceRegion?:
        type: String
        description: <p>The region that the DB snapshot was created in or copied from.</p>
      SourceDBSnapshotIdentifier?:
        type: String
        description: <p>The DB snapshot Arn that the DB snapshot was copied from. It only has value in case of cross customer or cross region copy.</p>
      StorageType?:
        type: String
        description: <p>Specifies the storage type associated with DB snapshot.</p>
      TdeCredentialArn?:
        type: String
        description: <p>The ARN from the key store with which to associate the instance for TDE encryption.</p>
      Encrypted?:
        type: Boolean
        description: <p>Specifies whether the DB snapshot is encrypted.</p>
      KmsKeyId?:
        type: String
        description: <p> If <code>Encrypted</code> is true, the KMS key identifier for the encrypted DB snapshot. </p>
      DBSnapshotArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB snapshot.</p>
      Timezone?:
        type: String
        description: <p>The time zone of the DB snapshot. In most cases, the <code>Timezone</code> element is empty. <code>Timezone</code> content appears only for snapshots taken from Microsoft SQL Server DB instances that were created with a time zone specified. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the following actions:</p> <ul> <li> <p> <a>CreateDBSnapshot</a> </p> </li> <li> <p> <a>DeleteDBSnapshot</a> </p> </li> </ul> <p>This data type is used as a response element in the <a>DescribeDBSnapshots</a> action.</p>
  DBSnapshotAttribute:
    type: object
    properties:
      AttributeName?:
        type: String
        description: <p>The name of the manual DB snapshot attribute.</p> <p>The attribute named <code>restore</code> refers to the list of AWS accounts that have permission to copy or restore the manual DB cluster snapshot. For more information, see the <a>ModifyDBSnapshotAttribute</a> API action.</p>
      AttributeValues?:
        type: AttributeValueList
        description: <p>The value or values for the manual DB snapshot attribute.</p> <p>If the <code>AttributeName</code> field is set to <code>restore</code>, then this element returns a list of IDs of the AWS accounts that are authorized to copy or restore the manual DB snapshot. If a value of <code>all</code> is in the list, then the manual DB snapshot is public and available for any AWS account to copy or restore.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the name and values of a manual DB snapshot attribute</p> <p>Manual DB snapshot attributes are used to authorize other AWS accounts to restore a manual DB snapshot. For more information, see the <a>ModifyDBSnapshotAttribute</a> API.</p>
  DBSnapshotAttributesResult:
    type: object
    properties:
      DBSnapshotIdentifier?:
        type: String
        description: <p>The identifier of the manual DB snapshot that the attributes apply to.</p>
      DBSnapshotAttributes?:
        type: DBSnapshotAttributeList
        description: <p>The list of attributes and values for the manual DB snapshot.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the results of a successful call to the <a>DescribeDBSnapshotAttributes</a> API action.</p> <p>Manual DB snapshot attributes are used to authorize other AWS accounts to copy or restore a manual DB snapshot. For more information, see the <a>ModifyDBSnapshotAttribute</a> API action.</p>
  DBSnapshotMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DBSnapshots?:
        type: DBSnapshotList
        description: <p> A list of <a>DBSnapshot</a> instances. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeDBSnapshots</a> action. </p>
  DBSubnetGroup:
    type: object
    properties:
      DBSubnetGroupName?:
        type: String
        description: <p>The name of the DB subnet group.</p>
      DBSubnetGroupDescription?:
        type: String
        description: <p>Provides the description of the DB subnet group.</p>
      VpcId?:
        type: String
        description: <p>Provides the VpcId of the DB subnet group.</p>
      SubnetGroupStatus?:
        type: String
        description: <p>Provides the status of the DB subnet group.</p>
      Subnets?:
        type: SubnetList
        description: <p> Contains a list of <a>Subnet</a> elements. </p>
      DBSubnetGroupArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the DB subnet group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the following actions:</p> <ul> <li> <p> <a>CreateDBSubnetGroup</a> </p> </li> <li> <p> <a>ModifyDBSubnetGroup</a> </p> </li> <li> <p> <a>DescribeDBSubnetGroups</a> </p> </li> <li> <p> <a>DeleteDBSubnetGroup</a> </p> </li> </ul> <p>This data type is used as a response element in the <a>DescribeDBSubnetGroups</a> action.</p>
  DBSubnetGroupMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DBSubnetGroups?:
        type: DBSubnetGroups
        description: <p> A list of <a>DBSubnetGroup</a> instances. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeDBSubnetGroups</a> action. </p>
  DeleteDBClusterMessage:
    type: object
    properties:
      DBClusterIdentifier:
        type: String
        description: <p>The DB cluster identifier for the DB cluster to be deleted. This parameter isn't case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      SkipFinalSnapshot?:
        type: Boolean
        description: '<p> Determines whether a final DB cluster snapshot is created
          before the DB cluster is deleted. If <code>true</code> is specified, no
          DB cluster snapshot is created. If <code>false</code> is specified, a DB
          cluster snapshot is created before the DB cluster is deleted. </p> <note>
          <p>You must specify a <code>FinalDBSnapshotIdentifier</code> parameter if
          <code>SkipFinalSnapshot</code> is <code>false</code>.</p> </note> <p>Default:
          <code>false</code> </p>'
      FinalDBSnapshotIdentifier?:
        type: String
        description: <p> The DB cluster snapshot identifier of the new DB cluster snapshot created when <code>SkipFinalSnapshot</code> is set to <code>false</code>. </p> <note> <p> Specifying this parameter and also setting the <code>SkipFinalShapshot</code> parameter to true results in an error. </p> </note> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteDBClusterParameterGroupMessage:
    type: object
    properties:
      DBClusterParameterGroupName:
        type: String
        description: <p>The name of the DB cluster parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be the name of an existing DB cluster parameter group.</p> </li> <li> <p>You cannot delete a default DB cluster parameter group.</p> </li> <li> <p>Cannot be associated with any DB clusters.</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteDBClusterResult:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DeleteDBClusterSnapshotMessage:
    type: object
    properties:
      DBClusterSnapshotIdentifier:
        type: String
        description: '<p>The identifier of the DB cluster snapshot to delete.</p>
          <p>Constraints: Must be the name of an existing DB cluster snapshot in the
          <code>available</code> state.</p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteDBClusterSnapshotResult:
    type: object
    properties:
      DBClusterSnapshot?:
        type: DBClusterSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DeleteDBInstanceMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: <p>The DB instance identifier for the DB instance to be deleted. This parameter isn't case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      SkipFinalSnapshot?:
        type: Boolean
        description: '<p> Determines whether a final DB snapshot is created before
          the DB instance is deleted. If <code>true</code> is specified, no DBSnapshot
          is created. If <code>false</code> is specified, a DB snapshot is created
          before the DB instance is deleted. </p> <p>Note that when a DB instance
          is in a failure state and has a status of ''failed'', ''incompatible-restore'',
          or ''incompatible-network'', it can only be deleted when the SkipFinalSnapshot
          parameter is set to "true".</p> <p>Specify <code>true</code> when deleting
          a Read Replica.</p> <note> <p>The FinalDBSnapshotIdentifier parameter must
          be specified if SkipFinalSnapshot is <code>false</code>.</p> </note> <p>Default:
          <code>false</code> </p>'
      FinalDBSnapshotIdentifier?:
        type: String
        description: <p> The DBSnapshotIdentifier of the new DBSnapshot created when SkipFinalSnapshot is set to <code>false</code>. </p> <note> <p>Specifying this parameter and also setting the SkipFinalShapshot parameter to true results in an error.</p> </note> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> <li> <p>Cannot be specified when deleting a Read Replica.</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteDBInstanceResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DeleteDBParameterGroupMessage:
    type: object
    properties:
      DBParameterGroupName:
        type: String
        description: <p>The name of the DB parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be the name of an existing DB parameter group</p> </li> <li> <p>You cannot delete a default DB parameter group</p> </li> <li> <p>Cannot be associated with any DB instances</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteDBSecurityGroupMessage:
    type: object
    properties:
      DBSecurityGroupName:
        type: String
        description: <p>The name of the DB security group to delete.</p> <note> <p>You cannot delete the default DB security group.</p> </note> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> <li> <p>Must not be "Default"</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteDBSnapshotMessage:
    type: object
    properties:
      DBSnapshotIdentifier:
        type: String
        description: '<p>The DBSnapshot identifier.</p> <p>Constraints: Must be the
          name of an existing DB snapshot in the <code>available</code> state.</p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteDBSnapshotResult:
    type: object
    properties:
      DBSnapshot?:
        type: DBSnapshot
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DeleteDBSubnetGroupMessage:
    type: object
    properties:
      DBSubnetGroupName:
        type: String
        description: '<p>The name of the database subnet group to delete.</p> <note>
          <p>You cannot delete the default subnet group.</p> </note> <p>Constraints:</p>
          <p>Constraints: Must contain no more than 255 alphanumeric characters, periods,
          underscores, spaces, or hyphens. Must not be default.</p> <p>Example: <code>mySubnetgroup</code>
          </p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteEventSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: <p>The name of the RDS event notification subscription you want to delete.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DeleteEventSubscriptionResult:
    type: object
    properties:
      EventSubscription?:
        type: EventSubscription
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DeleteOptionGroupMessage:
    type: object
    properties:
      OptionGroupName:
        type: String
        description: <p>The name of the option group to be deleted.</p> <note> <p>You cannot delete default option groups.</p> </note>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeAccountAttributesMessage:
    type: object
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeCertificatesMessage:
    type: object
    properties:
      CertificateIdentifier?:
        type: String
        description: <p>The user-supplied certificate identifier. If this parameter is specified, information for only the identified certificate is returned. This parameter isn't case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <a>DescribeCertificates</a> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBClusterParameterGroupsMessage:
    type: object
    properties:
      DBClusterParameterGroupName?:
        type: String
        description: <p>The name of a specific DB cluster parameter group to return details for.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBClusterParameterGroups</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBClusterParametersMessage:
    type: object
    properties:
      DBClusterParameterGroupName:
        type: String
        description: <p>The name of a specific DB cluster parameter group to return parameter details for.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Source?:
        type: String
        description: <p> A value that indicates to return only parameters for a specific source. Parameter sources can be <code>engine</code>, <code>service</code>, or <code>customer</code>. </p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBClusterParameters</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBClusterSnapshotAttributesMessage:
    type: object
    properties:
      DBClusterSnapshotIdentifier:
        type: String
        description: <p>The identifier for the DB cluster snapshot to describe the attributes for.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBClusterSnapshotAttributesResult:
    type: object
    properties:
      DBClusterSnapshotAttributesResult?:
        type: DBClusterSnapshotAttributesResult
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DescribeDBClusterSnapshotsMessage:
    type: object
    properties:
      DBClusterIdentifier?:
        type: String
        description: <p>The ID of the DB cluster to retrieve the list of DB cluster snapshots for. This parameter cannot be used in conjunction with the <code>DBClusterSnapshotIdentifier</code> parameter. This parameter is not case-sensitive. </p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      DBClusterSnapshotIdentifier?:
        type: String
        description: <p>A specific DB cluster snapshot identifier to describe. This parameter cannot be used in conjunction with the <code>DBClusterIdentifier</code> parameter. This value is stored as a lowercase string. </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> <li> <p>If this identifier is for an automated snapshot, the <code>SnapshotType</code> parameter must also be specified.</p> </li> </ul>
      SnapshotType?:
        type: String
        description: <p>The type of DB cluster snapshots to be returned. You can specify one of the following values:</p> <ul> <li> <p> <code>automated</code> - Return all DB cluster snapshots that have been automatically taken by Amazon RDS for my AWS account.</p> </li> <li> <p> <code>manual</code> - Return all DB cluster snapshots that have been taken by my AWS account.</p> </li> <li> <p> <code>shared</code> - Return all manual DB cluster snapshots that have been shared to my AWS account.</p> </li> <li> <p> <code>public</code> - Return all DB cluster snapshots that have been marked as public.</p> </li> </ul> <p>If you don't specify a <code>SnapshotType</code> value, then both automated and manual DB cluster snapshots are returned. You can include shared DB cluster snapshots with these results by setting the <code>IncludeShared</code> parameter to <code>true</code>. You can include public DB cluster snapshots with these results by setting the <code>IncludePublic</code> parameter to <code>true</code>.</p> <p>The <code>IncludeShared</code> and <code>IncludePublic</code> parameters don't apply for <code>SnapshotType</code> values of <code>manual</code> or <code>automated</code>. The <code>IncludePublic</code> parameter doesn't apply when <code>SnapshotType</code> is set to <code>shared</code>. The <code>IncludeShared</code> parameter doesn't apply when <code>SnapshotType</code> is set to <code>public</code>.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional pagination token provided by a previous <code>DescribeDBClusterSnapshots</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      IncludeShared?:
        type: Boolean
        description: <p>Set this value to <code>true</code> to include shared manual DB cluster snapshots from other AWS accounts that this AWS account has been given permission to copy or restore, otherwise set this value to <code>false</code>. The default is <code>false</code>.</p> <p>You can give an AWS account permission to restore a manual DB cluster snapshot from another AWS account by the <a>ModifyDBClusterSnapshotAttribute</a> API action.</p>
      IncludePublic?:
        type: Boolean
        description: <p>Set this value to <code>true</code> to include manual DB cluster snapshots that are public and can be copied or restored by any AWS account, otherwise set this value to <code>false</code>. The default is <code>false</code>. The default is false.</p> <p>You can share a manual DB cluster snapshot as public by using the <a>ModifyDBClusterSnapshotAttribute</a> API action.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBClustersMessage:
    type: object
    properties:
      DBClusterIdentifier?:
        type: String
        description: <p>The user-supplied DB cluster identifier. If this parameter is specified, information from only the specific DB cluster is returned. This parameter isn't case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <a>DescribeDBClusters</a> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBEngineVersionsMessage:
    type: object
    properties:
      Engine?:
        type: String
        description: <p>The database engine to return.</p>
      EngineVersion?:
        type: String
        description: '<p>The database engine version to return.</p> <p>Example: <code>5.1.49</code>
          </p>'
      DBParameterGroupFamily?:
        type: String
        description: <p>The name of a specific DB parameter group family to return details for.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Filters?:
        type: FilterList
        description: <p>Not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more than the <code>MaxRecords</code> value is available, a pagination
          token called a marker is included in the response so that the following
          results can be retrieved. </p> <p>Default: 100</p> <p>Constraints: Minimum
          20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      DefaultOnly?:
        type: Boolean
        description: <p>Indicates that only the default version of the specified engine or engine and major version combination is returned.</p>
      ListSupportedCharacterSets?:
        type: BooleanOptional
        description: <p>If this parameter is specified and the requested engine supports the <code>CharacterSetName</code> parameter for <code>CreateDBInstance</code>, the response includes a list of supported character sets for each engine version. </p>
      ListSupportedTimezones?:
        type: BooleanOptional
        description: <p>If this parameter is specified and the requested engine supports the <code>TimeZone</code> parameter for <code>CreateDBInstance</code>, the response includes a list of supported time zones for each engine version. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DescribeDBInstancesMessage:
    type: object
    properties:
      DBInstanceIdentifier?:
        type: String
        description: <p>The user-supplied instance identifier. If this parameter is specified, information from only the specific DB instance is returned. This parameter isn't case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBInstances</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBLogFilesDetails:
    type: object
    properties:
      LogFileName?:
        type: String
        description: <p>The name of the log file for the specified DB instance.</p>
      LastWritten?:
        type: Long
        description: <p>A POSIX timestamp when the last log entry was written.</p>
      Size?:
        type: Long
        description: <p>The size, in bytes, of the log file for the specified DB instance.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>This data type is used as a response element to <a>DescribeDBLogFiles</a>.</p>
  DescribeDBLogFilesMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: <p>The customer-assigned name of the DB instance that contains the log files you want to list.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      FilenameContains?:
        type: String
        description: <p>Filters the available log files for log file names that contain the specified string.</p>
      FileLastWritten?:
        type: Long
        description: <p>Filters the available log files for files written since the specified date, in POSIX timestamp format with milliseconds.</p>
      FileSize?:
        type: Long
        description: <p>Filters the available log files for files larger than the specified size.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: <p>The maximum number of records to include in the response. If more records exist than the specified MaxRecords value, a pagination token called a marker is included in the response so that the remaining results can be retrieved.</p>
      Marker?:
        type: String
        description: <p>The pagination token provided in the previous request. If this parameter is specified the response includes only records beyond the marker, up to MaxRecords.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBLogFilesResponse:
    type: object
    properties:
      DescribeDBLogFiles?:
        type: DescribeDBLogFilesList
        description: <p>The DB log files returned.</p>
      Marker?:
        type: String
        description: <p>A pagination token that can be used in a subsequent DescribeDBLogFiles request.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> The response from a call to <a>DescribeDBLogFiles</a>. </p>
  DescribeDBParameterGroupsMessage:
    type: object
    properties:
      DBParameterGroupName?:
        type: String
        description: <p>The name of a specific DB parameter group to return details for.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBParameterGroups</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBParametersMessage:
    type: object
    properties:
      DBParameterGroupName:
        type: String
        description: <p>The name of a specific DB parameter group to return details for.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Source?:
        type: String
        description: '<p>The parameter types to return.</p> <p>Default: All parameter
          types returned</p> <p>Valid Values: <code>user | system | engine-default</code>
          </p>'
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBParameters</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DescribeDBSecurityGroupsMessage:
    type: object
    properties:
      DBSecurityGroupName?:
        type: String
        description: <p>The name of the DB security group to return details for.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBSecurityGroups</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBSnapshotAttributesMessage:
    type: object
    properties:
      DBSnapshotIdentifier:
        type: String
        description: <p>The identifier for the DB snapshot to describe the attributes for.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBSnapshotAttributesResult:
    type: object
    properties:
      DBSnapshotAttributesResult?:
        type: DBSnapshotAttributesResult
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DescribeDBSnapshotsMessage:
    type: object
    properties:
      DBInstanceIdentifier?:
        type: String
        description: <p>The ID of the DB instance to retrieve the list of DB snapshots for. This parameter cannot be used in conjunction with <code>DBSnapshotIdentifier</code>. This parameter is not case-sensitive. </p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      DBSnapshotIdentifier?:
        type: String
        description: <p> A specific DB snapshot identifier to describe. This parameter cannot be used in conjunction with <code>DBInstanceIdentifier</code>. This value is stored as a lowercase string. </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>If this identifier is for an automated snapshot, the <code>SnapshotType</code> parameter must also be specified.</p> </li> </ul>
      SnapshotType?:
        type: String
        description: <p>The type of snapshots to be returned. You can specify one of the following values:</p> <ul> <li> <p> <code>automated</code> - Return all DB snapshots that have been automatically taken by Amazon RDS for my AWS account.</p> </li> <li> <p> <code>manual</code> - Return all DB snapshots that have been taken by my AWS account.</p> </li> <li> <p> <code>shared</code> - Return all manual DB snapshots that have been shared to my AWS account.</p> </li> <li> <p> <code>public</code> - Return all DB snapshots that have been marked as public.</p> </li> </ul> <p>If you don't specify a <code>SnapshotType</code> value, then both automated and manual snapshots are returned. Shared and public DB snapshots are not included in the returned results by default. You can include shared snapshots with these results by setting the <code>IncludeShared</code> parameter to <code>true</code>. You can include public snapshots with these results by setting the <code>IncludePublic</code> parameter to <code>true</code>.</p> <p>The <code>IncludeShared</code> and <code>IncludePublic</code> parameters don't apply for <code>SnapshotType</code> values of <code>manual</code> or <code>automated</code>. The <code>IncludePublic</code> parameter doesn't apply when <code>SnapshotType</code> is set to <code>shared</code>. The <code>IncludeShared</code> parameter doesn't apply when <code>SnapshotType</code> is set to <code>public</code>.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeDBSnapshots</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      IncludeShared?:
        type: Boolean
        description: <p>Set this value to <code>true</code> to include shared manual DB snapshots from other AWS accounts that this AWS account has been given permission to copy or restore, otherwise set this value to <code>false</code>. The default is <code>false</code>.</p> <p>You can give an AWS account permission to restore a manual DB snapshot from another AWS account by using the <a>ModifyDBSnapshotAttribute</a> API action.</p>
      IncludePublic?:
        type: Boolean
        description: <p>Set this value to <code>true</code> to include manual DB snapshots that are public and can be copied or restored by any AWS account, otherwise set this value to <code>false</code>. The default is <code>false</code>.</p> <p>You can share a manual DB snapshot as public by using the <a>ModifyDBSnapshotAttribute</a> API.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeDBSubnetGroupsMessage:
    type: object
    properties:
      DBSubnetGroupName?:
        type: String
        description: <p>The name of the DB subnet group to return details for.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous DescribeDBSubnetGroups request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeEngineDefaultClusterParametersMessage:
    type: object
    properties:
      DBParameterGroupFamily:
        type: String
        description: <p>The name of the DB cluster parameter group family to return engine parameter information for.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeEngineDefaultClusterParameters</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeEngineDefaultClusterParametersResult:
    type: object
    properties:
      EngineDefaults?:
        type: EngineDefaults
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DescribeEngineDefaultParametersMessage:
    type: object
    properties:
      DBParameterGroupFamily:
        type: String
        description: <p>The name of the DB parameter group family.</p>
      Filters?:
        type: FilterList
        description: <p>Not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribeEngineDefaultParameters</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeEngineDefaultParametersResult:
    type: object
    properties:
      EngineDefaults?:
        type: EngineDefaults
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  DescribeEventCategoriesMessage:
    type: object
    properties:
      SourceType?:
        type: String
        description: '<p>The type of source that will be generating the events.</p>
          <p>Valid values: db-instance | db-parameter-group | db-security-group |
          db-snapshot</p>'
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeEventSubscriptionsMessage:
    type: object
    properties:
      SubscriptionName?:
        type: String
        description: <p>The name of the RDS event notification subscription you want to describe.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous DescribeOrderableDBInstanceOptions request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeEventsMessage:
    type: object
    properties:
      SourceIdentifier?:
        type: String
        description: <p>The identifier of the event source for which events will be returned. If not specified, then all sources are included in the response.</p> <p>Constraints:</p> <ul> <li> <p>If SourceIdentifier is supplied, SourceType must also be provided.</p> </li> <li> <p>If the source type is <code>DBInstance</code>, then a <code>DBInstanceIdentifier</code> must be supplied.</p> </li> <li> <p>If the source type is <code>DBSecurityGroup</code>, a <code>DBSecurityGroupName</code> must be supplied.</p> </li> <li> <p>If the source type is <code>DBParameterGroup</code>, a <code>DBParameterGroupName</code> must be supplied.</p> </li> <li> <p>If the source type is <code>DBSnapshot</code>, a <code>DBSnapshotIdentifier</code> must be supplied.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
      SourceType?:
        type: SourceType
        description: <p>The event source to retrieve events for. If no value is specified, all events are returned.</p>
      StartTime?:
        type: TStamp
        description: '<p> The beginning of the time interval to retrieve events for,
          specified in ISO 8601 format. For more information about ISO 8601, go to
          the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a>
          </p> <p>Example: 2009-07-08T18:00Z</p>'
      EndTime?:
        type: TStamp
        description: '<p> The end of the time interval for which to retrieve events,
          specified in ISO 8601 format. For more information about ISO 8601, go to
          the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a>
          </p> <p>Example: 2009-07-08T18:00Z</p>'
      Duration?:
        type: IntegerOptional
        description: '<p>The number of minutes to retrieve events for.</p> <p>Default:
          60</p>'
      EventCategories?:
        type: EventCategoriesList
        description: <p>A list of event categories that trigger notifications for a event notification subscription.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous DescribeEvents request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeOptionGroupOptionsMessage:
    type: object
    properties:
      EngineName:
        type: String
        description: <p>A required parameter. Options available for the given engine name will be described.</p>
      MajorEngineVersion?:
        type: String
        description: <p>If specified, filters the results to include only options for the specified major engine version.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeOptionGroupsMessage:
    type: object
    properties:
      OptionGroupName?:
        type: String
        description: <p>The name of the option group to describe. Cannot be supplied together with EngineName or MajorEngineVersion.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous DescribeOptionGroups request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      EngineName?:
        type: String
        description: <p>Filters the list of option groups to only include groups associated with a specific database engine.</p>
      MajorEngineVersion?:
        type: String
        description: <p>Filters the list of option groups to only include groups associated with a specific database engine version. If specified, then EngineName must also be specified.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeOrderableDBInstanceOptionsMessage:
    type: object
    properties:
      Engine:
        type: String
        description: <p>The name of the engine to retrieve DB instance options for.</p>
      EngineVersion?:
        type: String
        description: <p>The engine version filter value. Specify this parameter to show only the available offerings matching the specified engine version.</p>
      DBInstanceClass?:
        type: String
        description: <p>The DB instance class filter value. Specify this parameter to show only the available offerings matching the specified DB instance class.</p>
      LicenseModel?:
        type: String
        description: <p>The license model filter value. Specify this parameter to show only the available offerings matching the specified license model.</p>
      Vpc?:
        type: BooleanOptional
        description: <p>The VPC filter value. Specify this parameter to show only the available VPC or non-VPC offerings.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous DescribeOrderableDBInstanceOptions request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribePendingMaintenanceActionsMessage:
    type: object
    properties:
      ResourceIdentifier?:
        type: String
        description: <p>The ARN of a resource to return pending maintenance actions for.</p>
      Filters?:
        type: FilterList
        description: <p>A filter that specifies one or more resources to return pending maintenance actions for.</p> <p>Supported filters:</p> <ul> <li> <p> <code>db-instance-id</code> - Accepts DB instance identifiers and DB instance Amazon Resource Names (ARNs). The results list will only include pending maintenance actions for the DB instances identified by these ARNs.</p> </li> </ul>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribePendingMaintenanceActions</code> request. If this parameter is specified, the response includes only records beyond the marker, up to a number of records specified by <code>MaxRecords</code>. </p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeReservedDBInstancesMessage:
    type: object
    properties:
      ReservedDBInstanceId?:
        type: String
        description: <p>The reserved DB instance identifier filter value. Specify this parameter to show only the reservation that matches the specified reservation ID.</p>
      ReservedDBInstancesOfferingId?:
        type: String
        description: <p>The offering identifier filter value. Specify this parameter to show only purchased reservations matching the specified offering identifier.</p>
      DBInstanceClass?:
        type: String
        description: <p>The DB instance class filter value. Specify this parameter to show only those reservations matching the specified DB instances class.</p>
      Duration?:
        type: String
        description: '<p>The duration filter value, specified in years or seconds.
          Specify this parameter to show only reservations for this duration.</p>
          <p>Valid Values: <code>1 | 3 | 31536000 | 94608000</code> </p>'
      ProductDescription?:
        type: String
        description: <p>The product description filter value. Specify this parameter to show only those reservations matching the specified product description.</p>
      OfferingType?:
        type: String
        description: '<p>The offering type filter value. Specify this parameter to
          show only the available offerings matching the specified offering type.</p>
          <p>Valid Values: <code>"Partial Upfront" | "All Upfront" | "No Upfront"
          </code> </p>'
      MultiAZ?:
        type: BooleanOptional
        description: <p>The Multi-AZ filter value. Specify this parameter to show only those reservations matching the specified Multi-AZ parameter.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more than the <code>MaxRecords</code> value is available, a pagination
          token called a marker is included in the response so that the following
          results can be retrieved. </p> <p>Default: 100</p> <p>Constraints: Minimum
          20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeReservedDBInstancesOfferingsMessage:
    type: object
    properties:
      ReservedDBInstancesOfferingId?:
        type: String
        description: '<p>The offering identifier filter value. Specify this parameter
          to show only the available offering that matches the specified reservation
          identifier.</p> <p>Example: <code>438012d3-4052-4cc7-b2e3-8d3372e0e706</code>
          </p>'
      DBInstanceClass?:
        type: String
        description: <p>The DB instance class filter value. Specify this parameter to show only the available offerings matching the specified DB instance class.</p>
      Duration?:
        type: String
        description: '<p>Duration filter value, specified in years or seconds. Specify
          this parameter to show only reservations for this duration.</p> <p>Valid
          Values: <code>1 | 3 | 31536000 | 94608000</code> </p>'
      ProductDescription?:
        type: String
        description: <p>Product description filter value. Specify this parameter to show only the available offerings matching the specified product description.</p>
      OfferingType?:
        type: String
        description: '<p>The offering type filter value. Specify this parameter to
          show only the available offerings matching the specified offering type.</p>
          <p>Valid Values: <code>"Partial Upfront" | "All Upfront" | "No Upfront"
          </code> </p>'
      MultiAZ?:
        type: BooleanOptional
        description: <p>The Multi-AZ filter value. Specify this parameter to show only the available offerings matching the specified Multi-AZ parameter.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p> The maximum number of records to include in the response.
          If more than the <code>MaxRecords</code> value is available, a pagination
          token called a marker is included in the response so that the following
          results can be retrieved. </p> <p>Default: 100</p> <p>Constraints: Minimum
          20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DescribeSourceRegionsMessage:
    type: object
    properties:
      RegionName?:
        type: String
        description: <p>The source region name. For example, <code>us-east-1</code>.</p> <p>Constraints:</p> <ul> <li> <p>Must specify a valid AWS Region name.</p> </li> </ul>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of records to include in the response.
          If more records exist than the specified <code>MaxRecords</code> value,
          a pagination token called a marker is included in the response so that the
          remaining results can be retrieved. </p> <p>Default: 100</p> <p>Constraints:
          Minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <a>DescribeSourceRegions</a> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  DomainMembership:
    type: object
    properties:
      Domain?:
        type: String
        description: <p>The identifier of the Active Directory Domain.</p>
      Status?:
        type: String
        description: <p>The status of the DB instance's Active Directory Domain membership, such as joined, pending-join, failed etc).</p>
      FQDN?:
        type: String
        description: <p>The fully qualified domain name of the Active Directory Domain.</p>
      IAMRoleName?:
        type: String
        description: <p>The name of the IAM role to be used when making API calls to the Directory Service.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>An Active Directory Domain membership record associated with the DB instance.</p>
  DownloadDBLogFilePortionDetails:
    type: object
    properties:
      LogFileData?:
        type: String
        description: <p>Entries from the specified log file.</p>
      Marker?:
        type: String
        description: <p>A pagination token that can be used in a subsequent DownloadDBLogFilePortion request.</p>
      AdditionalDataPending?:
        type: Boolean
        description: <p>Boolean value that if true, indicates there is more data to be downloaded.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>This data type is used as a response element to <a>DownloadDBLogFilePortion</a>.</p>
  DownloadDBLogFilePortionMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: <p>The customer-assigned name of the DB instance that contains the log files you want to list.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      LogFileName:
        type: String
        description: <p>The name of the log file to be downloaded.</p>
      Marker?:
        type: String
        description: <p>The pagination token provided in the previous request or "0". If the Marker parameter is specified the response includes only records beyond the marker until the end of the file or up to NumberOfLines.</p>
      NumberOfLines?:
        type: Integer
        description: <p>The number of lines to download. If the number of lines specified results in a file over 1 MB in size, the file will be truncated at 1 MB in size.</p> <p>If the NumberOfLines parameter is specified, then the block of lines returned can be from the beginning or the end of the log file, depending on the value of the Marker parameter.</p> <ul> <li> <p>If neither Marker or NumberOfLines are specified, the entire log file is returned up to a maximum of 10000 lines, starting with the most recent log entries first.</p> </li> <li> <p>If NumberOfLines is specified and Marker is not specified, then the most recent lines from the end of the log file are returned.</p> </li> <li> <p>If Marker is specified as "0", then the specified number of lines from the beginning of the log file are returned.</p> </li> <li> <p>You can download the log file in blocks of lines by specifying the size of the block using the NumberOfLines parameter, and by specifying a value of "0" for the Marker parameter in your first request. Include the Marker value returned in the response as the Marker value for the next request, continuing until the AdditionalDataPending response element returns false.</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  EC2SecurityGroup:
    type: object
    properties:
      Status?:
        type: String
        description: <p>Provides the status of the EC2 security group. Status can be "authorizing", "authorized", "revoking", and "revoked".</p>
      EC2SecurityGroupName?:
        type: String
        description: <p>Specifies the name of the EC2 security group.</p>
      EC2SecurityGroupId?:
        type: String
        description: <p>Specifies the id of the EC2 security group.</p>
      EC2SecurityGroupOwnerId?:
        type: String
        description: <p> Specifies the AWS ID of the owner of the EC2 security group specified in the <code>EC2SecurityGroupName</code> field. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>This data type is used as a response element in the following actions:</p> <ul> <li> <p> <a>AuthorizeDBSecurityGroupIngress</a> </p> </li> <li> <p> <a>DescribeDBSecurityGroups</a> </p> </li> <li> <p> <a>RevokeDBSecurityGroupIngress</a> </p> </li> </ul>
  Endpoint:
    type: object
    properties:
      Address?:
        type: String
        description: <p>Specifies the DNS address of the DB instance.</p>
      Port?:
        type: Integer
        description: <p>Specifies the port that the database engine is listening on.</p>
      HostedZoneId?:
        type: String
        description: <p>Specifies the ID that Amazon Route 53 assigns when you create a hosted zone.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>This data type is used as a response element in the following actions:</p> <ul> <li> <p> <a>CreateDBInstance</a> </p> </li> <li> <p> <a>DescribeDBInstances</a> </p> </li> <li> <p> <a>DeleteDBInstance</a> </p> </li> </ul>
  EngineDefaults:
    type: object
    properties:
      DBParameterGroupFamily?:
        type: String
        description: <p>Specifies the name of the DB parameter group family that the engine default parameters apply to.</p>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous EngineDefaults request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
      Parameters?:
        type: ParametersList
        description: <p>Contains a list of engine default parameters.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeEngineDefaultParameters</a> action. </p>
  Event:
    type: object
    properties:
      SourceIdentifier?:
        type: String
        description: <p>Provides the identifier for the source of the event.</p>
      SourceType?:
        type: SourceType
        description: <p>Specifies the source type for this event.</p>
      Message?:
        type: String
        description: <p>Provides the text of this event.</p>
      EventCategories?:
        type: EventCategoriesList
        description: <p>Specifies the category for the event.</p>
      Date?:
        type: TStamp
        description: <p>Specifies the date and time of the event.</p>
      SourceArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the event.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the <a>DescribeEvents</a> action. </p>
  EventCategoriesMap:
    type: object
    properties:
      SourceType?:
        type: String
        description: <p>The source type that the returned categories belong to</p>
      EventCategories?:
        type: EventCategoriesList
        description: <p>The event categories for the specified source type</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the results of a successful invocation of the <a>DescribeEventCategories</a> action.</p>
  EventCategoriesMessage:
    type: object
    properties:
      EventCategoriesMapList?:
        type: EventCategoriesMapList
        description: <p>A list of EventCategoriesMap data types.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Data returned from the <b>DescribeEventCategories</b> action.</p>
  EventSubscription:
    type: object
    properties:
      CustomerAwsId?:
        type: String
        description: <p>The AWS customer account associated with the RDS event notification subscription.</p>
      CustSubscriptionId?:
        type: String
        description: <p>The RDS event notification subscription Id.</p>
      SnsTopicArn?:
        type: String
        description: <p>The topic ARN of the RDS event notification subscription.</p>
      Status?:
        type: String
        description: '<p>The status of the RDS event notification subscription.</p>
          <p>Constraints:</p> <p>Can be one of the following: creating | modifying
          | deleting | active | no-permission | topic-not-exist</p> <p>The status
          "no-permission" indicates that RDS no longer has permission to post to the
          SNS topic. The status "topic-not-exist" indicates that the topic was deleted
          after the subscription was created.</p>'
      SubscriptionCreationTime?:
        type: String
        description: <p>The time the RDS event notification subscription was created.</p>
      SourceType?:
        type: String
        description: <p>The source type for the RDS event notification subscription.</p>
      SourceIdsList?:
        type: SourceIdsList
        description: <p>A list of source IDs for the RDS event notification subscription.</p>
      EventCategoriesList?:
        type: EventCategoriesList
        description: <p>A list of event categories for the RDS event notification subscription.</p>
      Enabled?:
        type: Boolean
        description: <p>A Boolean value indicating if the subscription is enabled. True indicates the subscription is enabled.</p>
      EventSubscriptionArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the event subscription.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the results of a successful invocation of the <a>DescribeEventSubscriptions</a> action.</p>
  EventSubscriptionsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous DescribeOrderableDBInstanceOptions request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      EventSubscriptionsList?:
        type: EventSubscriptionsList
        description: <p>A list of EventSubscriptions data types.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Data returned by the <b>DescribeEventSubscriptions</b> action.</p>
  EventsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous Events request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
      Events?:
        type: EventList
        description: <p> A list of <a>Event</a> instances. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeEvents</a> action. </p>
  FailoverDBClusterMessage:
    type: object
    properties:
      DBClusterIdentifier?:
        type: String
        description: <p>A DB cluster identifier to force a failover for. This parameter is not case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      TargetDBInstanceIdentifier?:
        type: String
        description: <p>The name of the instance to promote to the primary instance.</p> <p>You must specify the instance identifier for an Aurora Replica in the DB cluster. For example, <code>mydbcluster-replica1</code>.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  FailoverDBClusterResult:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  Filter:
    type: object
    properties:
      Name:
        type: String
        description: <p>This parameter is not currently supported.</p>
      Values:
        type: FilterValueList
        description: <p>This parameter is not currently supported.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>This type is not currently supported.</p>
  IPRange:
    type: object
    properties:
      Status?:
        type: String
        description: <p>Specifies the status of the IP range. Status can be "authorizing", "authorized", "revoking", and "revoked".</p>
      CIDRIP?:
        type: String
        description: <p>Specifies the IP range.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the <a>DescribeDBSecurityGroups</a> action. </p>
  ListTagsForResourceMessage:
    type: object
    properties:
      ResourceName:
        type: String
        description: <p>The Amazon RDS resource with tags to be listed. This value is an Amazon Resource Name (ARN). For information about creating an ARN, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing an RDS Amazon Resource Name (ARN)</a>.</p>
      Filters?:
        type: FilterList
        description: <p>This parameter is not currently supported.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBClusterMessage:
    type: object
    properties:
      DBClusterIdentifier:
        type: String
        description: <p>The DB cluster identifier for the cluster being modified. This parameter is not case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier for an existing DB cluster.</p> </li> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
      NewDBClusterIdentifier?:
        type: String
        description: '<p>The new DB cluster identifier for the DB cluster when renaming
          a DB cluster. This value is stored as a lowercase string.</p> <p>Constraints:</p>
          <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p>
          </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot
          end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <p>Example:
          <code>my-cluster2</code> </p>'
      ApplyImmediately?:
        type: Boolean
        description: '<p>A value that specifies whether the modifications in this
          request and any pending modifications are asynchronously applied as soon
          as possible, regardless of the <code>PreferredMaintenanceWindow</code> setting
          for the DB cluster. If this parameter is set to <code>false</code>, changes
          to the DB cluster are applied during the next maintenance window.</p> <p>The
          <code>ApplyImmediately</code> parameter only affects the <code>NewDBClusterIdentifier</code>
          and <code>MasterUserPassword</code> values. If you set the <code>ApplyImmediately</code>
          parameter value to false, then changes to the <code>NewDBClusterIdentifier</code>
          and <code>MasterUserPassword</code> values are applied during the next maintenance
          window. All other changes are applied immediately, regardless of the value
          of the <code>ApplyImmediately</code> parameter.</p> <p>Default: <code>false</code>
          </p>'
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days for which automated backups are retained.
          You must specify a minimum value of 1.</p> <p>Default: 1</p> <p>Constraints:</p>
          <ul> <li> <p>Must be a value from 1 to 35</p> </li> </ul>'
      DBClusterParameterGroupName?:
        type: String
        description: <p>The name of the DB cluster parameter group to use for the DB cluster.</p>
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: <p>A lst of VPC security groups that the DB cluster will belong to.</p>
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the DB cluster accepts connections.</p>
          <p>Constraints: Value must be <code>1150-65535</code> </p> <p>Default: The
          same port as the original DB cluster.</p>'
      MasterUserPassword?:
        type: String
        description: '<p>The new password for the master database user. This password
          can contain any printable ASCII character except "/", """, or "@".</p> <p>Constraints:
          Must contain from 8 to 41 characters.</p>'
      OptionGroupName?:
        type: String
        description: <p>A value that indicates that the DB cluster should be associated with the specified option group. Changing this parameter does not result in an outage except in the following case, and the change is applied during the next maintenance window unless the <code>ApplyImmediately</code> parameter is set to <code>true</code> for this request. If the parameter change results in an option group that enables OEM, this change can cause a brief (sub-second) period during which new connections are rejected but existing connections are not interrupted. </p> <p>Permanent options cannot be removed from an option group. The option group cannot be removed from a DB cluster once it is associated with a DB cluster.</p>
      PreferredBackupWindow?:
        type: String
        description: '<p>The daily time range during which automated backups are created
          if automated backups are enabled, using the <code>BackupRetentionPeriod</code>
          parameter. </p> <p>Default: A 30-minute window selected at random from an
          8-hour block of time per region. To see the time blocks available, see <a
          href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Constraints:</p> <ul> <li> <p>Must be in the format <code>hh24:mi-hh24:mi</code>.</p>
          </li> <li> <p>Times should be in Universal Coordinated Time (UTC).</p> </li>
          <li> <p>Must not conflict with the preferred maintenance window.</p> </li>
          <li> <p>Must be at least 30 minutes.</p> </li> </ul>'
      PreferredMaintenanceWindow?:
        type: String
        description: '<p>The weekly time range during which system maintenance can
          occur, in Universal Coordinated Time (UTC).</p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code>
          </p> <p>Default: A 30-minute window selected at random from an 8-hour block
          of time per region, occurring on a random day of the week. To see the time
          blocks available, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Valid Days: Mon, Tue, Wed, Thu, Fri, Sat, Sun</p> <p>Constraints:
          Minimum 30-minute window.</p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBClusterParameterGroupMessage:
    type: object
    properties:
      DBClusterParameterGroupName:
        type: String
        description: <p>The name of the DB cluster parameter group to modify.</p>
      Parameters:
        type: ParametersList
        description: <p>A list of parameters in the DB cluster parameter group to modify.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBClusterResult:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ModifyDBClusterSnapshotAttributeMessage:
    type: object
    properties:
      DBClusterSnapshotIdentifier:
        type: String
        description: <p>The identifier for the DB cluster snapshot to modify the attributes for.</p>
      AttributeName:
        type: String
        description: <p>The name of the DB cluster snapshot attribute to modify.</p> <p>To manage authorization for other AWS accounts to copy or restore a manual DB cluster snapshot, set this value to <code>restore</code>.</p>
      ValuesToAdd?:
        type: AttributeValueList
        description: <p>A list of DB cluster snapshot attributes to add to the attribute specified by <code>AttributeName</code>.</p> <p>To authorize other AWS accounts to copy or restore a manual DB cluster snapshot, set this list to include one or more AWS account IDs, or <code>all</code> to make the manual DB cluster snapshot restorable by any AWS account. Do not add the <code>all</code> value for any manual DB cluster snapshots that contain private information that you don't want available to all AWS accounts.</p>
      ValuesToRemove?:
        type: AttributeValueList
        description: <p>A list of DB cluster snapshot attributes to remove from the attribute specified by <code>AttributeName</code>.</p> <p>To remove authorization for other AWS accounts to copy or restore a manual DB cluster snapshot, set this list to include one or more AWS account identifiers, or <code>all</code> to remove authorization for any AWS account to copy or restore the DB cluster snapshot. If you specify <code>all</code>, an AWS account whose account ID is explicitly added to the <code>restore</code> attribute can still copy or restore a manual DB cluster snapshot.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBClusterSnapshotAttributeResult:
    type: object
    properties:
      DBClusterSnapshotAttributesResult?:
        type: DBClusterSnapshotAttributesResult
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ModifyDBInstanceMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: <p>The DB instance identifier. This value is stored as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier for an existing DB instance</p> </li> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      AllocatedStorage?:
        type: IntegerOptional
        description: '<p> The new storage capacity of the RDS instance. Changing this
          setting does not result in an outage and the change is applied during the
          next maintenance window unless <code>ApplyImmediately</code> is set to <code>true</code>
          for this request. </p> <p> <b>MySQL</b> </p> <p>Default: Uses existing setting</p>
          <p>Valid Values: 5-6144</p> <p>Constraints: Value supplied must be at least
          10% greater than the current value. Values that are not at least 10% greater
          than the existing value are rounded up so that they are 10% greater than
          the current value.</p> <p>Type: Integer</p> <p> <b>MariaDB</b> </p> <p>Default:
          Uses existing setting</p> <p>Valid Values: 5-6144</p> <p>Constraints: Value
          supplied must be at least 10% greater than the current value. Values that
          are not at least 10% greater than the existing value are rounded up so that
          they are 10% greater than the current value.</p> <p>Type: Integer</p> <p>
          <b>PostgreSQL</b> </p> <p>Default: Uses existing setting</p> <p>Valid Values:
          5-6144</p> <p>Constraints: Value supplied must be at least 10% greater than
          the current value. Values that are not at least 10% greater than the existing
          value are rounded up so that they are 10% greater than the current value.</p>
          <p>Type: Integer</p> <p> <b>Oracle</b> </p> <p>Default: Uses existing setting</p>
          <p>Valid Values: 10-6144</p> <p>Constraints: Value supplied must be at least
          10% greater than the current value. Values that are not at least 10% greater
          than the existing value are rounded up so that they are 10% greater than
          the current value.</p> <p> <b>SQL Server</b> </p> <p>Cannot be modified.</p>
          <p>If you choose to migrate your DB instance from using standard storage
          to using Provisioned IOPS, or from using Provisioned IOPS to using standard
          storage, the process can take time. The duration of the migration depends
          on several factors such as database load, storage size, storage type (standard
          or Provisioned IOPS), amount of IOPS provisioned (if any), and the number
          of prior scale storage operations. Typical migration times are under 24
          hours, but the process can take up to several days in some cases. During
          the migration, the DB instance will be available for use, but might experience
          performance degradation. While the migration takes place, nightly backups
          for the instance will be suspended. No other Amazon RDS operations can take
          place for the instance, including modifying the instance, rebooting the
          instance, deleting the instance, creating a Read Replica for the instance,
          and creating a DB snapshot of the instance.</p>'
      DBInstanceClass?:
        type: String
        description: '<p> The new compute and memory capacity of the DB instance.
          To determine the instance classes that are available for a particular DB
          engine, use the <a>DescribeOrderableDBInstanceOptions</a> action. </p> <p>
          Passing a value for this setting causes an outage during the change and
          is applied during the next maintenance window, unless <code>ApplyImmediately</code>
          is specified as <code>true</code> for this request. </p> <p>Default: Uses
          existing setting</p> <p>Valid Values: <code>db.t1.micro | db.m1.small |
          db.m1.medium | db.m1.large | db.m1.xlarge | db.m2.xlarge | db.m2.2xlarge
          | db.m2.4xlarge | db.m3.medium | db.m3.large | db.m3.xlarge | db.m3.2xlarge
          | db.m4.large | db.m4.xlarge | db.m4.2xlarge | db.m4.4xlarge | db.m4.10xlarge
          | db.r3.large | db.r3.xlarge | db.r3.2xlarge | db.r3.4xlarge | db.r3.8xlarge
          | db.t2.micro | db.t2.small | db.t2.medium | db.t2.large</code> </p>'
      DBSubnetGroupName?:
        type: String
        description: '<p>The new DB subnet group for the DB instance. You can use
          this parameter to move your DB instance to a different VPC, or to a different
          subnet group in the same VPC. If your DB instance is not in a VPC, you can
          also use this parameter to move your DB instance into a VPC. For more information,
          see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_VPC.WorkingWithRDSInstanceinaVPC.html#USER_VPC.Non-VPC2VPC">Updating
          the VPC for a DB Instance</a>. </p> <p>Changing the subnet group causes
          an outage during the change. The change is applied during the next maintenance
          window, unless you specify <code>true</code> for the <code>ApplyImmediately</code>
          parameter. </p> <p>Constraints: Must contain no more than 255 alphanumeric
          characters, periods, underscores, spaces, or hyphens.</p> <p>Example: <code>mySubnetGroup</code>
          </p>'
      DBSecurityGroups?:
        type: DBSecurityGroupNameList
        description: <p>A list of DB security groups to authorize on this DB instance. Changing this setting does not result in an outage and the change is asynchronously applied as soon as possible.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: <p>A list of EC2 VPC security groups to authorize on this DB instance. This change is asynchronously applied as soon as possible.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      ApplyImmediately?:
        type: Boolean
        description: '<p>Specifies whether the modifications in this request and any
          pending modifications are asynchronously applied as soon as possible, regardless
          of the <code>PreferredMaintenanceWindow</code> setting for the DB instance.
          </p> <p> If this parameter is set to <code>false</code>, changes to the
          DB instance are applied during the next maintenance window. Some parameter
          changes can cause an outage and will be applied on the next call to <a>RebootDBInstance</a>,
          or the next failure reboot. Review the table of parameters in <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Overview.DBInstance.Modifying.html">Modifying
          a DB Instance and Using the Apply Immediately Parameter</a> to see the impact
          that setting <code>ApplyImmediately</code> to <code>true</code> or <code>false</code>
          has for each modified parameter and to determine when the changes will be
          applied. </p> <p>Default: <code>false</code> </p>'
      MasterUserPassword?:
        type: String
        description: '<p>The new password for the DB instance master user. Can be
          any printable ASCII character except "/", """, or "@".</p> <p> Changing
          this parameter does not result in an outage and the change is asynchronously
          applied as soon as possible. Between the time of the request and the completion
          of the request, the <code>MasterUserPassword</code> element exists in the
          <code>PendingModifiedValues</code> element of the operation response. </p>
          <p>Default: Uses existing setting</p> <p>Constraints: Must be 8 to 41 alphanumeric
          characters (MySQL, MariaDB, and Amazon Aurora), 8 to 30 alphanumeric characters
          (Oracle), or 8 to 128 alphanumeric characters (SQL Server).</p> <note> <p>Amazon
          RDS API actions never return the password, so this action provides a way
          to regain access to a primary instance user if the password is lost. This
          includes restoring privileges that might have been accidentally revoked.</p>
          </note>'
      DBParameterGroupName?:
        type: String
        description: '<p>The name of the DB parameter group to apply to the DB instance.
          Changing this setting does not result in an outage. The parameter group
          name itself is changed immediately, but the actual parameter changes are
          not applied until you reboot the instance without failover. The db instance
          will NOT be rebooted automatically and the parameter changes will NOT be
          applied during the next maintenance window.</p> <p>Default: Uses existing
          setting</p> <p>Constraints: The DB parameter group must be in the same DB
          parameter group family as this DB instance.</p>'
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days to retain automated backups. Setting this
          parameter to a positive number enables backups. Setting this parameter to
          0 disables automated backups.</p> <p>Changing this parameter can result
          in an outage if you change from 0 to a non-zero value or from a non-zero
          value to 0. These changes are applied during the next maintenance window
          unless the <code>ApplyImmediately</code> parameter is set to <code>true</code>
          for this request. If you change the parameter from one non-zero value to
          another non-zero value, the change is asynchronously applied as soon as
          possible.</p> <p>Default: Uses existing setting</p> <p>Constraints:</p>
          <ul> <li> <p>Must be a value from 0 to 35</p> </li> <li> <p>Can be specified
          for a MySQL Read Replica only if the source is running MySQL 5.6</p> </li>
          <li> <p>Can be specified for a PostgreSQL Read Replica only if the source
          is running PostgreSQL 9.3.5</p> </li> <li> <p>Cannot be set to 0 if the
          DB instance is a source to Read Replicas</p> </li> </ul>'
      PreferredBackupWindow?:
        type: String
        description: <p> The daily time range during which automated backups are created if automated backups are enabled, as determined by the <code>BackupRetentionPeriod</code> parameter. Changing this parameter does not result in an outage and the change is asynchronously applied as soon as possible. </p> <p>Constraints:</p> <ul> <li> <p>Must be in the format hh24:mi-hh24:mi</p> </li> <li> <p>Times should be in Universal Time Coordinated (UTC)</p> </li> <li> <p>Must not conflict with the preferred maintenance window</p> </li> <li> <p>Must be at least 30 minutes</p> </li> </ul>
      PreferredMaintenanceWindow?:
        type: String
        description: '<p>The weekly time range (in UTC) during which system maintenance
          can occur, which might result in an outage. Changing this parameter does
          not result in an outage, except in the following situation, and the change
          is asynchronously applied as soon as possible. If there are pending actions
          that cause a reboot, and the maintenance window is changed to include the
          current time, then changing this parameter will cause a reboot of the DB
          instance. If moving this window to the current time, there must be at least
          30 minutes between the current time and end of the window to ensure pending
          changes are applied.</p> <p>Default: Uses existing setting</p> <p>Format:
          ddd:hh24:mi-ddd:hh24:mi</p> <p>Valid Days: Mon | Tue | Wed | Thu | Fri |
          Sat | Sun</p> <p>Constraints: Must be at least 30 minutes</p>'
      MultiAZ?:
        type: BooleanOptional
        description: '<p> Specifies if the DB instance is a Multi-AZ deployment. Changing
          this parameter does not result in an outage and the change is applied during
          the next maintenance window unless the <code>ApplyImmediately</code> parameter
          is set to <code>true</code> for this request. </p> <p>Constraints: Cannot
          be specified if the DB instance is a Read Replica.</p>'
      EngineVersion?:
        type: String
        description: <p> The version number of the database engine to upgrade to. Changing this parameter results in an outage and the change is applied during the next maintenance window unless the <code>ApplyImmediately</code> parameter is set to <code>true</code> for this request. </p> <p>For major version upgrades, if a non-default DB parameter group is currently in use, a new DB parameter group in the DB parameter group family for the new engine version must be specified. The new DB parameter group can be the default for that DB parameter group family.</p> <p>For a list of valid engine versions, see <a>CreateDBInstance</a>.</p>
      AllowMajorVersionUpgrade?:
        type: Boolean
        description: '<p>Indicates that major version upgrades are allowed. Changing
          this parameter does not result in an outage and the change is asynchronously
          applied as soon as possible.</p> <p>Constraints: This parameter must be
          set to true when specifying a value for the EngineVersion parameter that
          is a different major version than the DB instance''s current version.</p>'
      AutoMinorVersionUpgrade?:
        type: BooleanOptional
        description: <p> Indicates that minor version upgrades will be applied automatically to the DB instance during the maintenance window. Changing this parameter does not result in an outage except in the following case and the change is asynchronously applied as soon as possible. An outage will result if this parameter is set to <code>true</code> during the maintenance window, and a newer minor version is available, and RDS has enabled auto patching for that engine version. </p>
      LicenseModel?:
        type: String
        description: '<p>The license model for the DB instance.</p> <p>Valid values:
          <code>license-included</code> | <code>bring-your-own-license</code> | <code>general-public-license</code>
          </p>'
      Iops?:
        type: IntegerOptional
        description: '<p> The new Provisioned IOPS (I/O operations per second) value
          for the RDS instance. Changing this setting does not result in an outage
          and the change is applied during the next maintenance window unless the
          <code>ApplyImmediately</code> parameter is set to <code>true</code> for
          this request. </p> <p>Default: Uses existing setting</p> <p>Constraints:
          Value supplied must be at least 10% greater than the current value. Values
          that are not at least 10% greater than the existing value are rounded up
          so that they are 10% greater than the current value. If you are migrating
          from Provisioned IOPS to standard storage, set this value to 0. The DB instance
          will require a reboot for the change in storage type to take effect.</p>
          <p> <b>SQL Server</b> </p> <p>Setting the IOPS value for the SQL Server
          database engine is not supported.</p> <p>Type: Integer</p> <p>If you choose
          to migrate your DB instance from using standard storage to using Provisioned
          IOPS, or from using Provisioned IOPS to using standard storage, the process
          can take time. The duration of the migration depends on several factors
          such as database load, storage size, storage type (standard or Provisioned
          IOPS), amount of IOPS provisioned (if any), and the number of prior scale
          storage operations. Typical migration times are under 24 hours, but the
          process can take up to several days in some cases. During the migration,
          the DB instance will be available for use, but might experience performance
          degradation. While the migration takes place, nightly backups for the instance
          will be suspended. No other Amazon RDS operations can take place for the
          instance, including modifying the instance, rebooting the instance, deleting
          the instance, creating a Read Replica for the instance, and creating a DB
          snapshot of the instance.</p>'
      OptionGroupName?:
        type: String
        description: <p> Indicates that the DB instance should be associated with the specified option group. Changing this parameter does not result in an outage except in the following case and the change is applied during the next maintenance window unless the <code>ApplyImmediately</code> parameter is set to <code>true</code> for this request. If the parameter change results in an option group that enables OEM, this change can cause a brief (sub-second) period during which new connections are rejected but existing connections are not interrupted. </p> <p>Permanent options, such as the TDE option for Oracle Advanced Security TDE, cannot be removed from an option group, and that option group cannot be removed from a DB instance once it is associated with a DB instance</p>
      NewDBInstanceIdentifier?:
        type: String
        description: <p> The new DB instance identifier for the DB instance when renaming a DB instance. When you change the DB instance identifier, an instance reboot will occur immediately if you set <code>Apply Immediately</code> to true, or will occur during the next maintenance window if <code>Apply Immediately</code> to false. This value is stored as a lowercase string. </p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      StorageType?:
        type: String
        description: '<p>Specifies the storage type to be associated with the DB instance.</p>
          <p> Valid values: <code>standard | gp2 | io1</code> </p> <p> If you specify
          <code>io1</code>, you must also include a value for the <code>Iops</code>
          parameter. </p> <p> Default: <code>io1</code> if the <code>Iops</code> parameter
          is specified; otherwise <code>standard</code> </p>'
      TdeCredentialArn?:
        type: String
        description: <p>The ARN from the Key Store with which to associate the instance for TDE encryption.</p>
      TdeCredentialPassword?:
        type: String
        description: <p>The password for the given ARN from the Key Store in order to access the device.</p>
      CACertificateIdentifier?:
        type: String
        description: <p>Indicates the certificate that needs to be associated with the instance.</p>
      Domain?:
        type: String
        description: <p>The Active Directory Domain to move the instance to. Specify <code>none</code> to remove the instance from its current domain. The domain must be created prior to this operation. Currently only a Microsoft SQL Server instance can be created in a Active Directory Domain. </p>
      CopyTagsToSnapshot?:
        type: BooleanOptional
        description: <p>True to copy all tags from the DB instance to snapshots of the DB instance; otherwise false. The default is false.</p>
      MonitoringInterval?:
        type: IntegerOptional
        description: '<p>The interval, in seconds, between points when Enhanced Monitoring
          metrics are collected for the DB instance. To disable collecting Enhanced
          Monitoring metrics, specify 0. The default is 0.</p> <p>If <code>MonitoringRoleArn</code>
          is specified, then you must also set <code>MonitoringInterval</code> to
          a value other than 0.</p> <p>Valid Values: <code>0, 1, 5, 10, 15, 30, 60</code>
          </p>'
      DBPortNumber?:
        type: IntegerOptional
        description: '<p>The port number on which the database accepts connections.</p>
          <p>The value of the <code>DBPortNumber</code> parameter must not match any
          of the port values specified for options in the option group for the DB
          instance.</p> <p>Your database will restart when you change the <code>DBPortNumber</code>
          value regardless of the value of the <code>ApplyImmediately</code> parameter.</p>
          <p> <b>MySQL</b> </p> <p> Default: <code>3306</code> </p> <p> Valid Values:
          <code>1150-65535</code> </p> <p> <b>MariaDB</b> </p> <p> Default: <code>3306</code>
          </p> <p> Valid Values: <code>1150-65535</code> </p> <p> <b>PostgreSQL</b>
          </p> <p> Default: <code>5432</code> </p> <p> Valid Values: <code>1150-65535</code>
          </p> <p>Type: Integer</p> <p> <b>Oracle</b> </p> <p> Default: <code>1521</code>
          </p> <p> Valid Values: <code>1150-65535</code> </p> <p> <b>SQL Server</b>
          </p> <p> Default: <code>1433</code> </p> <p> Valid Values: <code>1150-65535</code>
          except for <code>1434</code>, <code>3389</code>, <code>47001</code>, <code>49152</code>,
          and <code>49152</code> through <code>49156</code>. </p> <p> <b>Amazon Aurora</b>
          </p> <p> Default: <code>3306</code> </p> <p> Valid Values: <code>1150-65535</code>
          </p>'
      PubliclyAccessible?:
        type: BooleanOptional
        description: '<p>Boolean value that indicates if the DB instance has a publicly
          resolvable DNS name. Set to <code>True</code> to make the DB instance Internet-facing
          with a publicly resolvable DNS name, which resolves to a public IP address.
          Set to <code>False</code> to make the DB instance internal with a DNS name
          that resolves to a private IP address. </p> <p> <code>PubliclyAccessible</code>
          only applies to DB instances in a VPC. The DB instance must be part of a
          public subnet and <code>PubliclyAccessible</code> must be true in order
          for it to be publicly accessible. </p> <p>Changes to the <code>PubliclyAccessible</code>
          parameter are applied immediately regardless of the value of the <code>ApplyImmediately</code>
          parameter.</p> <p>Default: false</p>'
      MonitoringRoleArn?:
        type: String
        description: <p>The ARN for the IAM role that permits RDS to send enhanced monitoring metrics to CloudWatch Logs. For example, <code>arn:aws:iam:123456789012:role/emaccess</code>. For information on creating a monitoring role, go to <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Monitoring.html#USER_Monitoring.OS.IAMRole">To create an IAM role for Amazon RDS Enhanced Monitoring</a>.</p> <p>If <code>MonitoringInterval</code> is set to a value other than 0, then you must supply a <code>MonitoringRoleArn</code> value.</p>
      DomainIAMRoleName?:
        type: String
        description: <p>The name of the IAM role to use when making API calls to the Directory Service.</p>
      PromotionTier?:
        type: IntegerOptional
        description: '<p>A value that specifies the order in which an Aurora Replica
          is promoted to the primary instance after a failure of the existing primary
          instance. For more information, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Aurora.Managing.html#Aurora.Managing.FaultTolerance">
          Fault Tolerance for an Aurora DB Cluster</a>. </p> <p>Default: 1</p> <p>Valid
          Values: 0 - 15</p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBInstanceResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ModifyDBParameterGroupMessage:
    type: object
    properties:
      DBParameterGroupName:
        type: String
        description: <p>The name of the DB parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be the name of an existing DB parameter group</p> </li> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Parameters:
        type: ParametersList
        description: '<p>An array of parameter names, values, and the apply method
          for the parameter update. At least one parameter name, value, and apply
          method must be supplied; subsequent arguments are optional. A maximum of
          20 parameters can be modified in a single request.</p> <p>Valid Values (for
          the application method): <code>immediate | pending-reboot</code> </p> <note>
          <p>You can use the immediate value with dynamic parameters only. You can
          use the pending-reboot value for both dynamic and static parameters, and
          changes are applied when you reboot the DB instance without failover.</p>
          </note>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBSnapshotAttributeMessage:
    type: object
    properties:
      DBSnapshotIdentifier:
        type: String
        description: <p>The identifier for the DB snapshot to modify the attributes for.</p>
      AttributeName:
        type: String
        description: <p>The name of the DB snapshot attribute to modify.</p> <p>To manage authorization for other AWS accounts to copy or restore a manual DB snapshot, set this value to <code>restore</code>.</p>
      ValuesToAdd?:
        type: AttributeValueList
        description: <p>A list of DB snapshot attributes to add to the attribute specified by <code>AttributeName</code>.</p> <p>To authorize other AWS accounts to copy or restore a manual snapshot, set this list to include one or more AWS account IDs, or <code>all</code> to make the manual DB snapshot restorable by any AWS account. Do not add the <code>all</code> value for any manual DB snapshots that contain private information that you don't want available to all AWS accounts.</p>
      ValuesToRemove?:
        type: AttributeValueList
        description: <p>A list of DB snapshot attributes to remove from the attribute specified by <code>AttributeName</code>.</p> <p>To remove authorization for other AWS accounts to copy or restore a manual snapshot, set this list to include one or more AWS account identifiers, or <code>all</code> to remove authorization for any AWS account to copy or restore the DB snapshot. If you specify <code>all</code>, an AWS account whose account ID is explicitly added to the <code>restore</code> attribute can still copy or restore the manual DB snapshot.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBSnapshotAttributeResult:
    type: object
    properties:
      DBSnapshotAttributesResult?:
        type: DBSnapshotAttributesResult
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ModifyDBSubnetGroupMessage:
    type: object
    properties:
      DBSubnetGroupName:
        type: String
        description: '<p>The name for the DB subnet group. This value is stored as
          a lowercase string.</p> <p>Constraints: Must contain no more than 255 alphanumeric
          characters, periods, underscores, spaces, or hyphens. Must not be default.</p>
          <p>Example: <code>mySubnetgroup</code> </p>'
      DBSubnetGroupDescription?:
        type: String
        description: <p>The description for the DB subnet group.</p>
      SubnetIds:
        type: SubnetIdentifierList
        description: <p>The EC2 subnet IDs for the DB subnet group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyDBSubnetGroupResult:
    type: object
    properties:
      DBSubnetGroup?:
        type: DBSubnetGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ModifyEventSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: <p>The name of the RDS event notification subscription.</p>
      SnsTopicArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the SNS topic created for event notification. The ARN is created by Amazon SNS when you create a topic and subscribe to it.</p>
      SourceType?:
        type: String
        description: '<p>The type of source that will be generating the events. For
          example, if you want to be notified of events generated by a DB instance,
          you would set this parameter to db-instance. if this value is not specified,
          all events are returned.</p> <p>Valid values: db-instance | db-parameter-group
          | db-security-group | db-snapshot</p>'
      EventCategories?:
        type: EventCategoriesList
        description: <p> A list of event categories for a SourceType that you want to subscribe to. You can see a list of the categories for a given SourceType in the <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Events.html">Events</a> topic in the Amazon RDS User Guide or by using the <b>DescribeEventCategories</b> action. </p>
      Enabled?:
        type: BooleanOptional
        description: <p> A Boolean value; set to <b>true</b> to activate the subscription. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyEventSubscriptionResult:
    type: object
    properties:
      EventSubscription?:
        type: EventSubscription
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  ModifyOptionGroupMessage:
    type: object
    properties:
      OptionGroupName:
        type: String
        description: <p>The name of the option group to be modified.</p> <p>Permanent options, such as the TDE option for Oracle Advanced Security TDE, cannot be removed from an option group, and that option group cannot be removed from a DB instance once it is associated with a DB instance</p>
      OptionsToInclude?:
        type: OptionConfigurationList
        description: <p>Options in this list are added to the option group or, if already present, the specified configuration is used to update the existing configuration.</p>
      OptionsToRemove?:
        type: OptionNamesList
        description: <p>Options in this list are removed from the option group.</p>
      ApplyImmediately?:
        type: Boolean
        description: <p>Indicates whether the changes should be applied immediately, or during the next maintenance window for each instance associated with the option group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ModifyOptionGroupResult:
    type: object
    properties:
      OptionGroup?:
        type: OptionGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  Option:
    type: object
    properties:
      OptionName?:
        type: String
        description: <p>The name of the option.</p>
      OptionDescription?:
        type: String
        description: <p>The description of the option.</p>
      Persistent?:
        type: Boolean
        description: <p>Indicate if this option is persistent.</p>
      Permanent?:
        type: Boolean
        description: <p>Indicate if this option is permanent.</p>
      Port?:
        type: IntegerOptional
        description: <p>If required, the port configured for this option to use.</p>
      OptionVersion?:
        type: String
        description: <p>The version of the option.</p>
      OptionSettings?:
        type: OptionSettingConfigurationList
        description: <p>The option settings for this option.</p>
      DBSecurityGroupMemberships?:
        type: DBSecurityGroupMembershipList
        description: <p>If the option requires access to a port, then this DB security group allows access to the port.</p>
      VpcSecurityGroupMemberships?:
        type: VpcSecurityGroupMembershipList
        description: <p>If the option requires access to a port, then this VPC security group allows access to the port.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Option details.</p>
  OptionConfiguration:
    type: object
    properties:
      OptionName:
        type: String
        description: <p>The configuration of options to include in a group.</p>
      Port?:
        type: IntegerOptional
        description: <p>The optional port for the option.</p>
      OptionVersion?:
        type: String
        description: <p>The version for the option.</p>
      DBSecurityGroupMemberships?:
        type: DBSecurityGroupNameList
        description: <p>A list of DBSecurityGroupMemebrship name strings used for this option.</p>
      VpcSecurityGroupMemberships?:
        type: VpcSecurityGroupIdList
        description: <p>A list of VpcSecurityGroupMemebrship name strings used for this option.</p>
      OptionSettings?:
        type: OptionSettingsList
        description: <p>The option settings to include in an option group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>A list of all available options</p>
  OptionGroup:
    type: object
    properties:
      OptionGroupName?:
        type: String
        description: <p>Specifies the name of the option group.</p>
      OptionGroupDescription?:
        type: String
        description: <p>Provides a description of the option group.</p>
      EngineName?:
        type: String
        description: <p>Indicates the name of the engine that this option group can be applied to.</p>
      MajorEngineVersion?:
        type: String
        description: <p>Indicates the major engine version associated with this option group.</p>
      Options?:
        type: OptionsList
        description: <p>Indicates what options are available in the option group.</p>
      AllowsVpcAndNonVpcInstanceMemberships?:
        type: Boolean
        description: <p>Indicates whether this option group can be applied to both VPC and non-VPC instances. The value <code>true</code> indicates the option group can be applied to both VPC and non-VPC instances. </p>
      VpcId?:
        type: String
        description: <p>If <b>AllowsVpcAndNonVpcInstanceMemberships</b> is <code>false</code>, this field is blank. If <b>AllowsVpcAndNonVpcInstanceMemberships</b> is <code>true</code> and this field is blank, then this option group can be applied to both VPC and non-VPC instances. If this field contains a value, then this option group can only be applied to instances that are in the VPC indicated by this field. </p>
      OptionGroupArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the option group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  OptionGroupMembership:
    type: object
    properties:
      OptionGroupName?:
        type: String
        description: <p>The name of the option group that the instance belongs to.</p>
      Status?:
        type: String
        description: '<p>The status of the DB instance''s option group membership.
          Valid values are: <code>in-sync</code>, <code>pending-apply</code>, <code>pending-removal</code>,
          <code>pending-maintenance-apply</code>, <code>pending-maintenance-removal</code>,
          <code>applying</code>, <code>removing</code>, and <code>failed</code>. </p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Provides information on the option groups the DB instance is a member of.</p>
  OptionGroupOption:
    type: object
    properties:
      Name?:
        type: String
        description: <p>The name of the option.</p>
      Description?:
        type: String
        description: <p>The description of the option.</p>
      EngineName?:
        type: String
        description: <p>The name of the engine that this option can be applied to.</p>
      MajorEngineVersion?:
        type: String
        description: <p>Indicates the major engine version that the option is available for.</p>
      MinimumRequiredMinorEngineVersion?:
        type: String
        description: <p>The minimum required engine version for the option to be applied.</p>
      PortRequired?:
        type: Boolean
        description: <p>Specifies whether the option requires a port.</p>
      DefaultPort?:
        type: IntegerOptional
        description: <p>If the option requires a port, specifies the default port for the option.</p>
      OptionsDependedOn?:
        type: OptionsDependedOn
        description: <p>The options that are prerequisites for this option.</p>
      OptionsConflictsWith?:
        type: OptionsConflictsWith
        description: <p>The options that conflict with this option.</p>
      Persistent?:
        type: Boolean
        description: <p>Persistent options can't be removed from an option group while DB instances are associated with the option group. If you disassociate all DB instances from the option group, your can remove the persistent option from the option group.</p>
      Permanent?:
        type: Boolean
        description: <p>Permanent options can never be removed from an option group. An option group containing a permanent option can't be removed from a DB instance.</p>
      OptionGroupOptionSettings?:
        type: OptionGroupOptionSettingsList
        description: <p>The option settings that are available (and the default value) for each option in an option group.</p>
      OptionGroupOptionVersions?:
        type: OptionGroupOptionVersionsList
        description: <p>The versions that are available for the option.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Available option.</p>
  OptionGroupOptionSetting:
    type: object
    properties:
      SettingName?:
        type: String
        description: <p>The name of the option group option.</p>
      SettingDescription?:
        type: String
        description: <p>The description of the option group option.</p>
      DefaultValue?:
        type: String
        description: <p>The default value for the option group option.</p>
      ApplyType?:
        type: String
        description: <p>The DB engine specific parameter type for the option group option.</p>
      AllowedValues?:
        type: String
        description: <p>Indicates the acceptable values for the option group option.</p>
      IsModifiable?:
        type: Boolean
        description: <p>Boolean value where true indicates that this option group option can be changed from the default value.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Option group option settings are used to display settings available for each option with their default values and other information. These values are used with the DescribeOptionGroupOptions action.</p>
  OptionGroupOptionsMessage:
    type: object
    properties:
      OptionGroupOptions?:
        type: OptionGroupOptionsList
      Marker?:
        type: String
        description: <p>An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  OptionGroups:
    type: object
    properties:
      OptionGroupsList?:
        type: OptionGroupsList
        description: <p>List of option groups.</p>
      Marker?:
        type: String
        description: <p>An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>List of option groups.</p>
  OptionSetting:
    type: object
    properties:
      Name?:
        type: String
        description: <p>The name of the option that has settings that you can set.</p>
      Value?:
        type: String
        description: <p>The current value of the option setting.</p>
      DefaultValue?:
        type: String
        description: <p>The default value of the option setting.</p>
      Description?:
        type: String
        description: <p>The description of the option setting.</p>
      ApplyType?:
        type: String
        description: <p>The DB engine specific parameter type.</p>
      DataType?:
        type: String
        description: <p>The data type of the option setting.</p>
      AllowedValues?:
        type: String
        description: <p>The allowed values of the option setting.</p>
      IsModifiable?:
        type: Boolean
        description: <p>A Boolean value that, when true, indicates the option setting can be modified from the default.</p>
      IsCollection?:
        type: Boolean
        description: <p>Indicates if the option setting is part of a collection.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Option settings are the actual settings being applied or configured for that option. It is used when you modify an option group or describe option groups. For example, the NATIVE_NETWORK_ENCRYPTION option has a setting called SQLNET.ENCRYPTION_SERVER that can have several different values.</p>
  OptionVersion:
    type: object
    properties:
      Version?:
        type: String
        description: <p>The version of the option.</p>
      IsDefault?:
        type: Boolean
        description: <p>True if the version is the default version of the option; otherwise, false.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>The version for an option. Option group option versions are returned by the <a>DescribeOptionGroupOptions</a> action.</p>
  OrderableDBInstanceOption:
    type: object
    properties:
      Engine?:
        type: String
        description: <p>The engine type of the orderable DB instance.</p>
      EngineVersion?:
        type: String
        description: <p>The engine version of the orderable DB instance.</p>
      DBInstanceClass?:
        type: String
        description: <p>The DB instance class for the orderable DB instance.</p>
      LicenseModel?:
        type: String
        description: <p>The license model for the orderable DB instance.</p>
      AvailabilityZones?:
        type: AvailabilityZoneList
        description: <p>A list of Availability Zones for the orderable DB instance.</p>
      MultiAZCapable?:
        type: Boolean
        description: <p>Indicates whether this orderable DB instance is multi-AZ capable.</p>
      ReadReplicaCapable?:
        type: Boolean
        description: <p>Indicates whether this orderable DB instance can have a Read Replica.</p>
      Vpc?:
        type: Boolean
        description: <p>Indicates whether this is a VPC orderable DB instance.</p>
      SupportsStorageEncryption?:
        type: Boolean
        description: <p>Indicates whether this orderable DB instance supports encrypted storage.</p>
      StorageType?:
        type: String
        description: <p>Indicates the storage type for this orderable DB instance.</p>
      SupportsIops?:
        type: Boolean
        description: <p>Indicates whether this orderable DB instance supports provisioned IOPS.</p>
      SupportsEnhancedMonitoring?:
        type: Boolean
        description: <p>Indicates whether the DB instance supports enhanced monitoring at intervals from 1 to 60 seconds.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains a list of available options for a DB instance</p> <p> This data type is used as a response element in the <a>DescribeOrderableDBInstanceOptions</a> action. </p>
  OrderableDBInstanceOptionsMessage:
    type: object
    properties:
      OrderableDBInstanceOptions?:
        type: OrderableDBInstanceOptionsList
        description: <p>An <a>OrderableDBInstanceOption</a> structure containing information about orderable options for the DB instance.</p>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous OrderableDBInstanceOptions request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code> . </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeOrderableDBInstanceOptions</a> action. </p>
  Parameter:
    type: object
    properties:
      ParameterName?:
        type: String
        description: <p>Specifies the name of the parameter.</p>
      ParameterValue?:
        type: String
        description: <p>Specifies the value of the parameter.</p>
      Description?:
        type: String
        description: <p>Provides a description of the parameter.</p>
      Source?:
        type: String
        description: <p>Indicates the source of the parameter value.</p>
      ApplyType?:
        type: String
        description: <p>Specifies the engine specific parameters type.</p>
      DataType?:
        type: String
        description: <p>Specifies the valid data type for the parameter.</p>
      AllowedValues?:
        type: String
        description: <p>Specifies the valid range of values for the parameter.</p>
      IsModifiable?:
        type: Boolean
        description: <p> Indicates whether (<code>true</code>) or not (<code>false</code>) the parameter can be modified. Some parameters have security or operational implications that prevent them from being changed. </p>
      MinimumEngineVersion?:
        type: String
        description: <p>The earliest engine version to which the parameter can apply.</p>
      ApplyMethod?:
        type: ApplyMethod
        description: <p>Indicates when to apply parameter updates.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a request parameter in the <a>ModifyDBParameterGroup</a> and <a>ResetDBParameterGroup</a> actions. </p> <p>This data type is used as a response element in the <a>DescribeEngineDefaultParameters</a> and <a>DescribeDBParameters</a> actions.</p>
  PendingMaintenanceAction:
    type: object
    properties:
      Action?:
        type: String
        description: <p>The type of pending maintenance action that is available for the resource.</p>
      AutoAppliedAfterDate?:
        type: TStamp
        description: <p>The date of the maintenance window when the action will be applied. The maintenance action will be applied to the resource during its first maintenance window after this date. If this date is specified, any <code>next-maintenance</code> opt-in requests are ignored.</p>
      ForcedApplyDate?:
        type: TStamp
        description: <p>The date when the maintenance action will be automatically applied. The maintenance action will be applied to the resource on this date regardless of the maintenance window for the resource. If this date is specified, any <code>immediate</code> opt-in requests are ignored.</p>
      OptInStatus?:
        type: String
        description: <p>Indicates the type of opt-in request that has been received for the resource.</p>
      CurrentApplyDate?:
        type: TStamp
        description: <p>The effective date when the pending maintenance action will be applied to the resource. This date takes into account opt-in requests received from the <a>ApplyPendingMaintenanceAction</a> API, the <code>AutoAppliedAfterDate</code>, and the <code>ForcedApplyDate</code>. This value is blank if an opt-in request has not been received and nothing has been specified as <code>AutoAppliedAfterDate</code> or <code>ForcedApplyDate</code>.</p>
      Description?:
        type: String
        description: <p>A description providing more detail about the maintenance action.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Provides information about a pending maintenance action for a resource.</p>
  PendingMaintenanceActionsMessage:
    type: object
    properties:
      PendingMaintenanceActions?:
        type: PendingMaintenanceActions
        description: <p>A list of the pending maintenance actions for the resource.</p>
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous <code>DescribePendingMaintenanceActions</code> request. If this parameter is specified, the response includes only records beyond the marker, up to a number of records specified by <code>MaxRecords</code>. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Data returned from the <b>DescribePendingMaintenanceActions</b> action.</p>
  PendingModifiedValues:
    type: object
    properties:
      DBInstanceClass?:
        type: String
        description: <p> Contains the new <code>DBInstanceClass</code> for the DB instance that will be applied or is in progress. </p>
      AllocatedStorage?:
        type: IntegerOptional
        description: <p> Contains the new <code>AllocatedStorage</code> size for the DB instance that will be applied or is in progress. </p>
      MasterUserPassword?:
        type: String
        description: <p>Contains the pending or in-progress change of the master credentials for the DB instance.</p>
      Port?:
        type: IntegerOptional
        description: <p>Specifies the pending port for the DB instance.</p>
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: <p>Specifies the pending number of days for which automated backups are retained.</p>
      MultiAZ?:
        type: BooleanOptional
        description: <p>Indicates that the Single-AZ DB instance is to change to a Multi-AZ deployment.</p>
      EngineVersion?:
        type: String
        description: <p>Indicates the database engine version.</p>
      LicenseModel?:
        type: String
        description: '<p>The license model for the DB instance.</p> <p>Valid values:
          <code>license-included</code> | <code>bring-your-own-license</code> | <code>general-public-license</code>
          </p>'
      Iops?:
        type: IntegerOptional
        description: <p>Specifies the new Provisioned IOPS value for the DB instance that will be applied or is being applied.</p>
      DBInstanceIdentifier?:
        type: String
        description: <p> Contains the new <code>DBInstanceIdentifier</code> for the DB instance that will be applied or is in progress. </p>
      StorageType?:
        type: String
        description: <p>Specifies the storage type to be associated with the DB instance.</p>
      CACertificateIdentifier?:
        type: String
        description: <p>Specifies the identifier of the CA certificate for the DB instance.</p>
      DBSubnetGroupName?:
        type: String
        description: <p>The new DB subnet group for the DB instance. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the <a>ModifyDBInstance</a> action. </p>
  PromoteReadReplicaDBClusterMessage:
    type: object
    properties:
      DBClusterIdentifier:
        type: String
        description: '<p>The identifier of the DB cluster Read Replica to promote.
          This parameter is not case-sensitive. </p> <p>Constraints:</p> <ul> <li>
          <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li>
          <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with
          a hyphen or contain two consecutive hyphens.</p> </li> </ul> <p>Example:
          <code>my-cluster-replica1</code> </p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  PromoteReadReplicaDBClusterResult:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  PromoteReadReplicaMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: '<p>The DB instance identifier. This value is stored as a lowercase
          string.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier for
          an existing Read Replica DB instance</p> </li> <li> <p>Must contain from
          1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character
          must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two
          consecutive hyphens</p> </li> </ul> <p>Example: <code>mydbinstance</code>
          </p>'
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days to retain automated backups. Setting this
          parameter to a positive number enables backups. Setting this parameter to
          0 disables automated backups.</p> <p>Default: 1</p> <p>Constraints:</p>
          <ul> <li> <p>Must be a value from 0 to 8</p> </li> </ul>'
      PreferredBackupWindow?:
        type: String
        description: '<p> The daily time range during which automated backups are
          created if automated backups are enabled, using the <code>BackupRetentionPeriod</code>
          parameter. </p> <p> Default: A 30-minute window selected at random from
          an 8-hour block of time per region. To see the time blocks available, see
          <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Constraints:</p> <ul> <li> <p>Must be in the format <code>hh24:mi-hh24:mi</code>.</p>
          </li> <li> <p>Times should be in Universal Coordinated Time (UTC).</p> </li>
          <li> <p>Must not conflict with the preferred maintenance window.</p> </li>
          <li> <p>Must be at least 30 minutes.</p> </li> </ul>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  PromoteReadReplicaResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  PurchaseReservedDBInstancesOfferingMessage:
    type: object
    properties:
      ReservedDBInstancesOfferingId:
        type: String
        description: '<p>The ID of the Reserved DB instance offering to purchase.</p>
          <p>Example: 438012d3-4052-4cc7-b2e3-8d3372e0e706</p>'
      ReservedDBInstanceId?:
        type: String
        description: '<p>Customer-specified identifier to track this reservation.</p>
          <p>Example: myreservationID</p>'
      DBInstanceCount?:
        type: IntegerOptional
        description: '<p>The number of instances to reserve.</p> <p>Default: <code>1</code>
          </p>'
      Tags?:
        type: TagList
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  PurchaseReservedDBInstancesOfferingResult:
    type: object
    properties:
      ReservedDBInstance?:
        type: ReservedDBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RebootDBInstanceMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: <p>The DB instance identifier. This parameter is stored as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      ForceFailover?:
        type: BooleanOptional
        description: '<p> When <code>true</code>, the reboot will be conducted through
          a MultiAZ failover. </p> <p>Constraint: You cannot specify <code>true</code>
          if the instance is not configured for MultiAZ.</p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  RebootDBInstanceResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RecurringCharge:
    type: object
    properties:
      RecurringChargeAmount?:
        type: Double
        description: <p>The amount of the recurring charge.</p>
      RecurringChargeFrequency?:
        type: String
        description: <p>The frequency of the recurring charge.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the <a>DescribeReservedDBInstances</a> and <a>DescribeReservedDBInstancesOfferings</a> actions. </p>
  RemoveSourceIdentifierFromSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: <p>The name of the RDS event notification subscription you want to remove a source identifier from.</p>
      SourceIdentifier:
        type: String
        description: <p> The source identifier to be removed from the subscription, such as the <b>DB instance identifier</b> for a DB instance or the name of a security group. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  RemoveSourceIdentifierFromSubscriptionResult:
    type: object
    properties:
      EventSubscription?:
        type: EventSubscription
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RemoveTagsFromResourceMessage:
    type: object
    properties:
      ResourceName:
        type: String
        description: <p>The Amazon RDS resource the tags will be removed from. This value is an Amazon Resource Name (ARN). For information about creating an ARN, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Tagging.ARN.html#USER_Tagging.ARN.Constructing"> Constructing an RDS Amazon Resource Name (ARN)</a>.</p>
      TagKeys:
        type: KeyList
        description: <p>The tag key (name) of the tag to be removed.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ReservedDBInstance:
    type: object
    properties:
      ReservedDBInstanceId?:
        type: String
        description: <p>The unique identifier for the reservation.</p>
      ReservedDBInstancesOfferingId?:
        type: String
        description: <p>The offering identifier.</p>
      DBInstanceClass?:
        type: String
        description: <p>The DB instance class for the reserved DB instance.</p>
      StartTime?:
        type: TStamp
        description: <p>The time the reservation started.</p>
      Duration?:
        type: Integer
        description: <p>The duration of the reservation in seconds.</p>
      FixedPrice?:
        type: Double
        description: <p>The fixed price charged for this reserved DB instance.</p>
      UsagePrice?:
        type: Double
        description: <p>The hourly price charged for this reserved DB instance.</p>
      CurrencyCode?:
        type: String
        description: <p>The currency code for the reserved DB instance.</p>
      DBInstanceCount?:
        type: Integer
        description: <p>The number of reserved DB instances.</p>
      ProductDescription?:
        type: String
        description: <p>The description of the reserved DB instance.</p>
      OfferingType?:
        type: String
        description: <p>The offering type of this reserved DB instance.</p>
      MultiAZ?:
        type: Boolean
        description: <p>Indicates if the reservation applies to Multi-AZ deployments.</p>
      State?:
        type: String
        description: <p>The state of the reserved DB instance.</p>
      RecurringCharges?:
        type: RecurringChargeList
        description: <p>The recurring price charged to run this reserved DB instance.</p>
      ReservedDBInstanceArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for the reserved DB instance.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the <a>DescribeReservedDBInstances</a> and <a>PurchaseReservedDBInstancesOffering</a> actions. </p>
  ReservedDBInstanceMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      ReservedDBInstances?:
        type: ReservedDBInstanceList
        description: <p>A list of reserved DB instances.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeReservedDBInstances</a> action. </p>
  ReservedDBInstancesOffering:
    type: object
    properties:
      ReservedDBInstancesOfferingId?:
        type: String
        description: <p>The offering identifier.</p>
      DBInstanceClass?:
        type: String
        description: <p>The DB instance class for the reserved DB instance.</p>
      Duration?:
        type: Integer
        description: <p>The duration of the offering in seconds.</p>
      FixedPrice?:
        type: Double
        description: <p>The fixed price charged for this offering.</p>
      UsagePrice?:
        type: Double
        description: <p>The hourly price charged for this offering.</p>
      CurrencyCode?:
        type: String
        description: <p>The currency code for the reserved DB instance offering.</p>
      ProductDescription?:
        type: String
        description: <p>The database engine used by the offering.</p>
      OfferingType?:
        type: String
        description: <p>The offering type.</p>
      MultiAZ?:
        type: Boolean
        description: <p>Indicates if the offering applies to Multi-AZ deployments.</p>
      RecurringCharges?:
        type: RecurringChargeList
        description: <p>The recurring price charged to run this reserved DB instance.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the <a>DescribeReservedDBInstancesOfferings</a> action. </p>
  ReservedDBInstancesOfferingMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      ReservedDBInstancesOfferings?:
        type: ReservedDBInstancesOfferingList
        description: <p>A list of reserved DB instance offerings.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> Contains the result of a successful invocation of the <a>DescribeReservedDBInstancesOfferings</a> action. </p>
  ResetDBClusterParameterGroupMessage:
    type: object
    properties:
      DBClusterParameterGroupName:
        type: String
        description: <p>The name of the DB cluster parameter group to reset.</p>
      ResetAllParameters?:
        type: Boolean
        description: <p>A value that is set to <code>true</code> to reset all parameters in the DB cluster parameter group to their default values, and <code>false</code> otherwise. You cannot use this parameter if there is a list of parameter names specified for the <code>Parameters</code> parameter.</p>
      Parameters?:
        type: ParametersList
        description: <p>A list of parameter names in the DB cluster parameter group to reset to the default values. You cannot use this parameter if the <code>ResetAllParameters</code> parameter is set to <code>true</code>.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ResetDBParameterGroupMessage:
    type: object
    properties:
      DBParameterGroupName:
        type: String
        description: <p>The name of the DB parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      ResetAllParameters?:
        type: Boolean
        description: '<p> Specifies whether (<code>true</code>) or not (<code>false</code>)
          to reset all parameters in the DB parameter group to default values. </p>
          <p>Default: <code>true</code> </p>'
      Parameters?:
        type: ParametersList
        description: '<p>An array of parameter names, values, and the apply method
          for the parameter update. At least one parameter name, value, and apply
          method must be supplied; subsequent arguments are optional. A maximum of
          20 parameters can be modified in a single request.</p> <p> <b>MySQL</b>
          </p> <p>Valid Values (for Apply method): <code>immediate</code> | <code>pending-reboot</code>
          </p> <p>You can use the immediate value with dynamic parameters only. You
          can use the <code>pending-reboot</code> value for both dynamic and static
          parameters, and changes are applied when DB instance reboots.</p> <p> <b>MariaDB</b>
          </p> <p>Valid Values (for Apply method): <code>immediate</code> | <code>pending-reboot</code>
          </p> <p>You can use the immediate value with dynamic parameters only. You
          can use the <code>pending-reboot</code> value for both dynamic and static
          parameters, and changes are applied when DB instance reboots.</p> <p> <b>Oracle</b>
          </p> <p>Valid Values (for Apply method): <code>pending-reboot</code> </p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  ResourcePendingMaintenanceActions:
    type: object
    properties:
      ResourceIdentifier?:
        type: String
        description: <p>The ARN of the resource that has pending maintenance actions.</p>
      PendingMaintenanceActionDetails?:
        type: PendingMaintenanceActionDetails
        description: <p>A list that provides details about the pending maintenance actions for the resource.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Describes the pending maintenance actions for a resource.</p>
  RestoreDBClusterFromS3Message:
    type: object
    properties:
      AvailabilityZones?:
        type: AvailabilityZones
        description: <p>A list of EC2 Availability Zones that instances in the restored DB cluster can be created in.</p>
      BackupRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days for which automated backups of the restored
          DB cluster are retained. You must specify a minimum value of 1.</p> <p>Default:
          1</p> <p>Constraints:</p> <ul> <li> <p>Must be a value from 1 to 35</p>
          </li> </ul>'
      CharacterSetName?:
        type: String
        description: <p>A value that indicates that the restored DB cluster should be associated with the specified CharacterSet.</p>
      DatabaseName?:
        type: String
        description: <p>The database name for the restored DB cluster.</p>
      DBClusterIdentifier:
        type: String
        description: '<p>The name of the DB cluster to create from the source data
          in the S3 bucket. This parameter is isn''t case-sensitive.</p> <p>Constraints:</p>
          <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p>
          </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot
          end with a hyphen or contain two consecutive hyphens.</p> </li> </ul> <p>Example:
          <code>my-cluster1</code> </p>'
      DBClusterParameterGroupName?:
        type: String
        description: <p>The name of the DB cluster parameter group to associate with the restored DB cluster. If this argument is omitted, <code>default.aurora5.6</code> will be used. </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: <p>A list of EC2 VPC security groups to associate with the restored DB cluster.</p>
      DBSubnetGroupName?:
        type: String
        description: '<p>A DB subnet group to associate with the restored DB cluster.</p>
          <p>Constraints: Must contain no more than 255 alphanumeric characters, periods,
          underscores, spaces, or hyphens. Must not be default.</p> <p>Example: <code>mySubnetgroup</code>
          </p>'
      Engine:
        type: String
        description: '<p>The name of the database engine to be used for the restored
          DB cluster.</p> <p>Valid Values: <code>aurora</code> </p>'
      EngineVersion?:
        type: String
        description: '<p>The version number of the database engine to use.</p> <p>
          <b>Aurora</b> </p> <p>Example: <code>5.6.10a</code> </p>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the instances in the restored DB
          cluster accept connections.</p> <p> Default: <code>3306</code> </p>'
      MasterUsername:
        type: String
        description: <p>The name of the master user for the restored DB cluster.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 16 alphanumeric characters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot be a reserved word for the chosen database engine.</p> </li> </ul>
      MasterUserPassword:
        type: String
        description: '<p>The password for the master database user. This password
          can contain any printable ASCII character except "/", """, or "@".</p> <p>Constraints:
          Must contain from 8 to 41 characters.</p>'
      OptionGroupName?:
        type: String
        description: <p>A value that indicates that the restored DB cluster should be associated with the specified option group.</p> <p>Permanent options cannot be removed from an option group. An option group cannot be removed from a DB cluster once it is associated with a DB cluster.</p>
      PreferredBackupWindow?:
        type: String
        description: '<p>The daily time range during which automated backups are created
          if automated backups are enabled using the <code>BackupRetentionPeriod</code>
          parameter. </p> <p>Default: A 30-minute window selected at random from an
          8-hour block of time per region. To see the time blocks available, see <a
          href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Constraints:</p> <ul> <li> <p>Must be in the format <code>hh24:mi-hh24:mi</code>.</p>
          </li> <li> <p>Times should be in Universal Coordinated Time (UTC).</p> </li>
          <li> <p>Must not conflict with the preferred maintenance window.</p> </li>
          <li> <p>Must be at least 30 minutes.</p> </li> </ul>'
      PreferredMaintenanceWindow?:
        type: String
        description: '<p>The weekly time range during which system maintenance can
          occur, in Universal Coordinated Time (UTC).</p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code>
          </p> <p>Default: A 30-minute window selected at random from an 8-hour block
          of time per region, occurring on a random day of the week. To see the time
          blocks available, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/AdjustingTheMaintenanceWindow.html">
          Adjusting the Preferred Maintenance Window</a> in the <i>Amazon RDS User
          Guide.</i> </p> <p>Valid Days: Mon, Tue, Wed, Thu, Fri, Sat, Sun</p> <p>Constraints:
          Minimum 30-minute window.</p>'
      Tags?:
        type: TagList
      StorageEncrypted?:
        type: BooleanOptional
        description: <p>Specifies whether the restored DB cluster is encrypted.</p>
      KmsKeyId?:
        type: String
        description: <p>The KMS key identifier for an encrypted DB cluster.</p> <p>The KMS key identifier is the Amazon Resource Name (ARN) for the KMS encryption key. If you are creating a DB cluster with the same AWS account that owns the KMS encryption key used to encrypt the new DB cluster, then you can use the KMS key alias instead of the ARN for the KM encryption key.</p> <p>If the <code>StorageEncrypted</code> parameter is true, and you do not specify a value for the <code>KmsKeyId</code> parameter, then Amazon RDS will use your default encryption key. AWS KMS creates the default encryption key for your AWS account. Your AWS account has a different default encryption key for each AWS region.</p>
      SourceEngine:
        type: String
        description: '<p>The identifier for the database engine that was backed up
          to create the files stored in the Amazon S3 bucket. </p> <p>Valid values:
          <code>mysql</code> </p>'
      SourceEngineVersion:
        type: String
        description: '<p>The version of the database that the backup files were created
          from.</p> <p>MySQL version 5.5 and 5.6 are supported. </p> <p>Example: <code>5.6.22</code>
          </p>'
      S3BucketName:
        type: String
        description: <p>The name of the Amazon S3 bucket that contains the data used to create the Amazon Aurora DB cluster.</p>
      S3Prefix?:
        type: String
        description: <p>The prefix for all of the file names that contain the data used to create the Amazon Aurora DB cluster. If you do not specify a <b>SourceS3Prefix</b> value, then the Amazon Aurora DB cluster is created by using all of the files in the Amazon S3 bucket.</p>
      S3IngestionRoleArn:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that authorizes Amazon RDS to access the Amazon S3 bucket on your behalf.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RestoreDBClusterFromS3Result:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RestoreDBClusterFromSnapshotMessage:
    type: object
    properties:
      AvailabilityZones?:
        type: AvailabilityZones
        description: <p>Provides the list of EC2 Availability Zones that instances in the restored DB cluster can be created in.</p>
      DBClusterIdentifier:
        type: String
        description: '<p>The name of the DB cluster to create from the DB cluster
          snapshot. This parameter isn''t case-sensitive.</p> <p>Constraints:</p>
          <ul> <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens</p>
          </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot
          end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <p>Example:
          <code>my-snapshot-id</code> </p>'
      SnapshotIdentifier:
        type: String
        description: <p>The identifier for the DB cluster snapshot to restore from.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      Engine:
        type: String
        description: '<p>The database engine to use for the new DB cluster.</p> <p>Default:
          The same as source</p> <p>Constraint: Must be compatible with the engine
          of the source</p>'
      EngineVersion?:
        type: String
        description: <p>The version of the database engine to use for the new DB cluster.</p>
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the new DB cluster accepts connections.</p>
          <p>Constraints: Value must be <code>1150-65535</code> </p> <p>Default: The
          same port as the original DB cluster.</p>'
      DBSubnetGroupName?:
        type: String
        description: '<p>The name of the DB subnet group to use for the new DB cluster.</p>
          <p>Constraints: Must contain no more than 255 alphanumeric characters, periods,
          underscores, spaces, or hyphens. Must not be default.</p> <p>Example: <code>mySubnetgroup</code>
          </p>'
      DatabaseName?:
        type: String
        description: <p>The database name for the restored DB cluster.</p>
      OptionGroupName?:
        type: String
        description: <p>The name of the option group to use for the restored DB cluster.</p>
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: <p>A list of VPC security groups that the new DB cluster will belong to.</p>
      Tags?:
        type: TagList
        description: <p>The tags to be assigned to the restored DB cluster.</p>
      KmsKeyId?:
        type: String
        description: <p>The KMS key identifier to use when restoring an encrypted DB cluster from a DB cluster snapshot.</p> <p>The KMS key identifier is the Amazon Resource Name (ARN) for the KMS encryption key. If you are restoring a DB cluster with the same AWS account that owns the KMS encryption key used to encrypt the new DB cluster, then you can use the KMS key alias instead of the ARN for the KMS encryption key.</p> <p>If you do not specify a value for the <code>KmsKeyId</code> parameter, then the following will occur:</p> <ul> <li> <p>If the DB cluster snapshot is encrypted, then the restored DB cluster is encrypted using the KMS key that was used to encrypt the DB cluster snapshot.</p> </li> <li> <p>If the DB cluster snapshot is not encrypted, then the restored DB cluster is encrypted using the specified encryption key.</p> </li> </ul>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  RestoreDBClusterFromSnapshotResult:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RestoreDBClusterToPointInTimeMessage:
    type: object
    properties:
      DBClusterIdentifier:
        type: String
        description: <p>The name of the new DB cluster to be created.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      SourceDBClusterIdentifier:
        type: String
        description: <p>The identifier of the source DB cluster from which to restore.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier of an existing database instance</p> </li> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      RestoreToTime?:
        type: TStamp
        description: '<p>The date and time to restore the DB cluster to.</p> <p>Valid
          Values: Value must be a time in Universal Coordinated Time (UTC) format</p>
          <p>Constraints:</p> <ul> <li> <p>Must be before the latest restorable time
          for the DB instance</p> </li> <li> <p>Cannot be specified if <code>UseLatestRestorableTime</code>
          parameter is true</p> </li> </ul> <p>Example: <code>2015-03-07T23:45:00Z</code>
          </p>'
      UseLatestRestorableTime?:
        type: Boolean
        description: '<p>A value that is set to <code>true</code> to restore the DB
          cluster to the latest restorable backup time, and <code>false</code> otherwise.
          </p> <p>Default: <code>false</code> </p> <p>Constraints: Cannot be specified
          if <code>RestoreToTime</code> parameter is provided.</p>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the new DB cluster accepts connections.</p>
          <p>Constraints: Value must be <code>1150-65535</code> </p> <p>Default: The
          same port as the original DB cluster.</p>'
      DBSubnetGroupName?:
        type: String
        description: '<p>The DB subnet group name to use for the new DB cluster.</p>
          <p>Constraints: Must contain no more than 255 alphanumeric characters, periods,
          underscores, spaces, or hyphens. Must not be default.</p> <p>Example: <code>mySubnetgroup</code>
          </p>'
      OptionGroupName?:
        type: String
        description: <p>The name of the option group for the new DB cluster.</p>
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: <p>A lst of VPC security groups that the new DB cluster belongs to.</p>
      Tags?:
        type: TagList
      KmsKeyId?:
        type: String
        description: <p>The KMS key identifier to use when restoring an encrypted DB cluster from an encrypted DB cluster.</p> <p>The KMS key identifier is the Amazon Resource Name (ARN) for the KMS encryption key. If you are restoring a DB cluster with the same AWS account that owns the KMS encryption key used to encrypt the new DB cluster, then you can use the KMS key alias instead of the ARN for the KMS encryption key.</p> <p>You can restore to a new DB cluster and encrypt the new DB cluster with a KMS key that is different than the KMS key used to encrypt the source DB cluster. The new DB cluster will be encrypted with the KMS key identified by the <code>KmsKeyId</code> parameter.</p> <p>If you do not specify a value for the <code>KmsKeyId</code> parameter, then the following will occur:</p> <ul> <li> <p>If the DB cluster is encrypted, then the restored DB cluster is encrypted using the KMS key that was used to encrypt the source DB cluster.</p> </li> <li> <p>If the DB cluster is not encrypted, then the restored DB cluster is not encrypted.</p> </li> </ul> <p>If <code>DBClusterIdentifier</code> refers to a DB cluster that is note encrypted, then the restore request is rejected.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  RestoreDBClusterToPointInTimeResult:
    type: object
    properties:
      DBCluster?:
        type: DBCluster
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RestoreDBInstanceFromDBSnapshotMessage:
    type: object
    properties:
      DBInstanceIdentifier:
        type: String
        description: '<p>Name of the DB instance to create from the DB snapshot. This
          parameter isn''t case-sensitive.</p> <p>Constraints:</p> <ul> <li> <p>Must
          contain from 1 to 63 alphanumeric characters or hyphens (1 to 15 for SQL
          Server)</p> </li> <li> <p>First character must be a letter</p> </li> <li>
          <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li>
          </ul> <p>Example: <code>my-snapshot-id</code> </p>'
      DBSnapshotIdentifier:
        type: String
        description: <p>The identifier for the DB snapshot to restore from.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <p>If you are restoring from a shared manual DB snapshot, the <code>DBSnapshotIdentifier</code> must be the ARN of the shared DB snapshot.</p>
      DBInstanceClass?:
        type: String
        description: '<p>The compute and memory capacity of the Amazon RDS DB instance.</p>
          <p>Valid Values: <code>db.t1.micro | db.m1.small | db.m1.medium | db.m1.large
          | db.m1.xlarge | db.m2.2xlarge | db.m2.4xlarge | db.m3.medium | db.m3.large
          | db.m3.xlarge | db.m3.2xlarge | db.m4.large | db.m4.xlarge | db.m4.2xlarge
          | db.m4.4xlarge | db.m4.10xlarge | db.r3.large | db.r3.xlarge | db.r3.2xlarge
          | db.r3.4xlarge | db.r3.8xlarge | db.t2.micro | db.t2.small | db.t2.medium
          | db.t2.large</code> </p>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the database accepts connections.</p>
          <p>Default: The same port as the original DB instance</p> <p>Constraints:
          Value must be <code>1150-65535</code> </p>'
      AvailabilityZone?:
        type: String
        description: '<p>The EC2 Availability Zone that the database instance will
          be created in.</p> <p>Default: A random, system-chosen Availability Zone.</p>
          <p>Constraint: You cannot specify the AvailabilityZone parameter if the
          MultiAZ parameter is set to <code>true</code>.</p> <p>Example: <code>us-east-1a</code>
          </p>'
      DBSubnetGroupName?:
        type: String
        description: '<p>The DB subnet group name to use for the new instance.</p>
          <p>Constraints: Must contain no more than 255 alphanumeric characters, periods,
          underscores, spaces, or hyphens. Must not be default.</p> <p>Example: <code>mySubnetgroup</code>
          </p>'
      MultiAZ?:
        type: BooleanOptional
        description: '<p>Specifies if the DB instance is a Multi-AZ deployment.</p>
          <p>Constraint: You cannot specify the AvailabilityZone parameter if the
          MultiAZ parameter is set to <code>true</code>.</p>'
      PubliclyAccessible?:
        type: BooleanOptional
        description: '<p>Specifies the accessibility options for the DB instance.
          A value of true specifies an Internet-facing instance with a publicly resolvable
          DNS name, which resolves to a public IP address. A value of false specifies
          an internal instance with a DNS name that resolves to a private IP address.</p>
          <p>Default: The default behavior varies depending on whether a VPC has been
          requested or not. The following list shows the default behavior in each
          case.</p> <ul> <li> <p> <b>Default VPC:</b> true</p> </li> <li> <p> <b>VPC:</b>
          false</p> </li> </ul> <p>If no DB subnet group has been specified as part
          of the request and the PubliclyAccessible value has not been set, the DB
          instance will be publicly accessible. If a specific DB subnet group has
          been specified as part of the request and the PubliclyAccessible value has
          not been set, the DB instance will be private.</p>'
      AutoMinorVersionUpgrade?:
        type: BooleanOptional
        description: <p>Indicates that minor version upgrades will be applied automatically to the DB instance during the maintenance window.</p>
      LicenseModel?:
        type: String
        description: '<p>License model information for the restored DB instance.</p>
          <p>Default: Same as source.</p> <p> Valid values: <code>license-included</code>
          | <code>bring-your-own-license</code> | <code>general-public-license</code>
          </p>'
      DBName?:
        type: String
        description: <p>The database name for the restored DB instance.</p> <note> <p>This parameter doesn't apply to the MySQL, PostgreSQL, or MariaDB engines.</p> </note>
      Engine?:
        type: String
        description: '<p>The database engine to use for the new instance.</p> <p>Default:
          The same as source</p> <p>Constraint: Must be compatible with the engine
          of the source</p> <p> Valid Values: <code>MySQL</code> | <code>mariadb</code>
          | <code>oracle-se1</code> | <code>oracle-se</code> | <code>oracle-ee</code>
          | <code>sqlserver-ee</code> | <code>sqlserver-se</code> | <code>sqlserver-ex</code>
          | <code>sqlserver-web</code> | <code>postgres</code> | <code>aurora</code>
          </p>'
      Iops?:
        type: IntegerOptional
        description: '<p>Specifies the amount of provisioned IOPS for the DB instance,
          expressed in I/O operations per second. If this parameter is not specified,
          the IOPS value will be taken from the backup. If this parameter is set to
          0, the new instance will be converted to a non-PIOPS instance, which will
          take additional time, though your DB instance will be available for connections
          before the conversion starts.</p> <p>Constraints: Must be an integer greater
          than 1000.</p> <p> <b>SQL Server</b> </p> <p>Setting the IOPS value for
          the SQL Server database engine is not supported.</p>'
      OptionGroupName?:
        type: String
        description: <p>The name of the option group to be used for the restored DB instance.</p> <p>Permanent options, such as the TDE option for Oracle Advanced Security TDE, cannot be removed from an option group, and that option group cannot be removed from a DB instance once it is associated with a DB instance</p>
      Tags?:
        type: TagList
      StorageType?:
        type: String
        description: '<p>Specifies the storage type to be associated with the DB instance.</p>
          <p> Valid values: <code>standard | gp2 | io1</code> </p> <p> If you specify
          <code>io1</code>, you must also include a value for the <code>Iops</code>
          parameter. </p> <p> Default: <code>io1</code> if the <code>Iops</code> parameter
          is specified; otherwise <code>standard</code> </p>'
      TdeCredentialArn?:
        type: String
        description: <p>The ARN from the Key Store with which to associate the instance for TDE encryption.</p>
      TdeCredentialPassword?:
        type: String
        description: <p>The password for the given ARN from the Key Store in order to access the device.</p>
      Domain?:
        type: String
        description: <p>Specify the Active Directory Domain to restore the instance in.</p>
      CopyTagsToSnapshot?:
        type: BooleanOptional
        description: <p>True to copy all tags from the restored DB instance to snapshots of the DB instance; otherwise false. The default is false.</p>
      DomainIAMRoleName?:
        type: String
        description: <p>Specify the name of the IAM role to be used when making API calls to the Directory Service.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  RestoreDBInstanceFromDBSnapshotResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RestoreDBInstanceToPointInTimeMessage:
    type: object
    properties:
      SourceDBInstanceIdentifier:
        type: String
        description: <p>The identifier of the source DB instance from which to restore.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier of an existing database instance</p> </li> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      TargetDBInstanceIdentifier:
        type: String
        description: <p>The name of the new database instance to be created.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      RestoreTime?:
        type: TStamp
        description: '<p>The date and time to restore from.</p> <p>Valid Values: Value
          must be a time in Universal Coordinated Time (UTC) format</p> <p>Constraints:</p>
          <ul> <li> <p>Must be before the latest restorable time for the DB instance</p>
          </li> <li> <p>Cannot be specified if UseLatestRestorableTime parameter is
          true</p> </li> </ul> <p>Example: <code>2009-09-07T23:45:00Z</code> </p>'
      UseLatestRestorableTime?:
        type: Boolean
        description: '<p> Specifies whether (<code>true</code>) or not (<code>false</code>)
          the DB instance is restored from the latest backup time. </p> <p>Default:
          <code>false</code> </p> <p>Constraints: Cannot be specified if RestoreTime
          parameter is provided.</p>'
      DBInstanceClass?:
        type: String
        description: '<p>The compute and memory capacity of the Amazon RDS DB instance.</p>
          <p>Valid Values: <code>db.t1.micro | db.m1.small | db.m1.medium | db.m1.large
          | db.m1.xlarge | db.m2.2xlarge | db.m2.4xlarge | db.m3.medium | db.m3.large
          | db.m3.xlarge | db.m3.2xlarge | db.m4.large | db.m4.xlarge | db.m4.2xlarge
          | db.m4.4xlarge | db.m4.10xlarge | db.r3.large | db.r3.xlarge | db.r3.2xlarge
          | db.r3.4xlarge | db.r3.8xlarge | db.t2.micro | db.t2.small | db.t2.medium
          | db.t2.large</code> </p> <p>Default: The same DBInstanceClass as the original
          DB instance.</p>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the database accepts connections.</p>
          <p>Constraints: Value must be <code>1150-65535</code> </p> <p>Default: The
          same port as the original DB instance.</p>'
      AvailabilityZone?:
        type: String
        description: '<p>The EC2 Availability Zone that the database instance will
          be created in.</p> <p>Default: A random, system-chosen Availability Zone.</p>
          <p>Constraint: You cannot specify the AvailabilityZone parameter if the
          MultiAZ parameter is set to true.</p> <p>Example: <code>us-east-1a</code>
          </p>'
      DBSubnetGroupName?:
        type: String
        description: '<p>The DB subnet group name to use for the new instance.</p>
          <p>Constraints: Must contain no more than 255 alphanumeric characters, periods,
          underscores, spaces, or hyphens. Must not be default.</p> <p>Example: <code>mySubnetgroup</code>
          </p>'
      MultiAZ?:
        type: BooleanOptional
        description: '<p>Specifies if the DB instance is a Multi-AZ deployment.</p>
          <p>Constraint: You cannot specify the AvailabilityZone parameter if the
          MultiAZ parameter is set to <code>true</code>.</p>'
      PubliclyAccessible?:
        type: BooleanOptional
        description: '<p>Specifies the accessibility options for the DB instance.
          A value of true specifies an Internet-facing instance with a publicly resolvable
          DNS name, which resolves to a public IP address. A value of false specifies
          an internal instance with a DNS name that resolves to a private IP address.</p>
          <p>Default: The default behavior varies depending on whether a VPC has been
          requested or not. The following list shows the default behavior in each
          case.</p> <ul> <li> <p> <b>Default VPC:</b>true</p> </li> <li> <p> <b>VPC:</b>false</p>
          </li> </ul> <p>If no DB subnet group has been specified as part of the request
          and the PubliclyAccessible value has not been set, the DB instance will
          be publicly accessible. If a specific DB subnet group has been specified
          as part of the request and the PubliclyAccessible value has not been set,
          the DB instance will be private.</p>'
      AutoMinorVersionUpgrade?:
        type: BooleanOptional
        description: <p>Indicates that minor version upgrades will be applied automatically to the DB instance during the maintenance window.</p>
      LicenseModel?:
        type: String
        description: '<p>License model information for the restored DB instance.</p>
          <p>Default: Same as source.</p> <p> Valid values: <code>license-included</code>
          | <code>bring-your-own-license</code> | <code>general-public-license</code>
          </p>'
      DBName?:
        type: String
        description: <p>The database name for the restored DB instance.</p> <note> <p>This parameter is not used for the MySQL or MariaDB engines.</p> </note>
      Engine?:
        type: String
        description: '<p>The database engine to use for the new instance.</p> <p>Default:
          The same as source</p> <p>Constraint: Must be compatible with the engine
          of the source</p> <p> Valid Values: <code>MySQL</code> | <code>mariadb</code>
          | <code>oracle-se1</code> | <code>oracle-se</code> | <code>oracle-ee</code>
          | <code>sqlserver-ee</code> | <code>sqlserver-se</code> | <code>sqlserver-ex</code>
          | <code>sqlserver-web</code> | <code>postgres</code> | <code>aurora</code>
          </p>'
      Iops?:
        type: IntegerOptional
        description: '<p>The amount of Provisioned IOPS (input/output operations per
          second) to be initially allocated for the DB instance.</p> <p>Constraints:
          Must be an integer greater than 1000.</p> <p> <b>SQL Server</b> </p> <p>Setting
          the IOPS value for the SQL Server database engine is not supported.</p>'
      OptionGroupName?:
        type: String
        description: <p>The name of the option group to be used for the restored DB instance.</p> <p>Permanent options, such as the TDE option for Oracle Advanced Security TDE, cannot be removed from an option group, and that option group cannot be removed from a DB instance once it is associated with a DB instance</p>
      CopyTagsToSnapshot?:
        type: BooleanOptional
        description: <p>True to copy all tags from the restored DB instance to snapshots of the DB instance; otherwise false. The default is false.</p>
      Tags?:
        type: TagList
      StorageType?:
        type: String
        description: '<p>Specifies the storage type to be associated with the DB instance.</p>
          <p> Valid values: <code>standard | gp2 | io1</code> </p> <p> If you specify
          <code>io1</code>, you must also include a value for the <code>Iops</code>
          parameter. </p> <p> Default: <code>io1</code> if the <code>Iops</code> parameter
          is specified; otherwise <code>standard</code> </p>'
      TdeCredentialArn?:
        type: String
        description: <p>The ARN from the Key Store with which to associate the instance for TDE encryption.</p>
      TdeCredentialPassword?:
        type: String
        description: <p>The password for the given ARN from the Key Store in order to access the device.</p>
      Domain?:
        type: String
        description: <p>Specify the Active Directory Domain to restore the instance in.</p>
      DomainIAMRoleName?:
        type: String
        description: <p>Specify the name of the IAM role to be used when making API calls to the Directory Service.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  RestoreDBInstanceToPointInTimeResult:
    type: object
    properties:
      DBInstance?:
        type: DBInstance
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  RevokeDBSecurityGroupIngressMessage:
    type: object
    properties:
      DBSecurityGroupName:
        type: String
        description: <p>The name of the DB security group to revoke ingress from.</p>
      CIDRIP?:
        type: String
        description: <p> The IP range to revoke access from. Must be a valid CIDR range. If <code>CIDRIP</code> is specified, <code>EC2SecurityGroupName</code>, <code>EC2SecurityGroupId</code> and <code>EC2SecurityGroupOwnerId</code> cannot be provided. </p>
      EC2SecurityGroupName?:
        type: String
        description: <p> The name of the EC2 security group to revoke access from. For VPC DB security groups, <code>EC2SecurityGroupId</code> must be provided. Otherwise, EC2SecurityGroupOwnerId and either <code>EC2SecurityGroupName</code> or <code>EC2SecurityGroupId</code> must be provided. </p>
      EC2SecurityGroupId?:
        type: String
        description: <p> The id of the EC2 security group to revoke access from. For VPC DB security groups, <code>EC2SecurityGroupId</code> must be provided. Otherwise, EC2SecurityGroupOwnerId and either <code>EC2SecurityGroupName</code> or <code>EC2SecurityGroupId</code> must be provided. </p>
      EC2SecurityGroupOwnerId?:
        type: String
        description: <p> The AWS Account Number of the owner of the EC2 security group specified in the <code>EC2SecurityGroupName</code> parameter. The AWS Access Key ID is not an acceptable value. For VPC DB security groups, <code>EC2SecurityGroupId</code> must be provided. Otherwise, EC2SecurityGroupOwnerId and either <code>EC2SecurityGroupName</code> or <code>EC2SecurityGroupId</code> must be provided. </p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  RevokeDBSecurityGroupIngressResult:
    type: object
    properties:
      DBSecurityGroup?:
        type: DBSecurityGroup
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
  SourceRegion:
    type: object
    properties:
      RegionName?:
        type: String
        description: <p>The source region name.</p>
      Endpoint?:
        type: String
        description: <p>The source region endpoint.</p>
      Status?:
        type: String
        description: <p>The status of the source region.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains an AWS Region name as the result of a successful call to the <a>DescribeSourceRegions</a> action.</p>
  SourceRegionMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p> An optional pagination token provided by a previous request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by <code>MaxRecords</code>. </p>
      SourceRegions?:
        type: SourceRegionList
        description: <p>A list of SourceRegion instances that contains each source AWS Region that the current region can get a Read Replica or a DB snapshot from.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Contains the result of a successful invocation of the <a>DescribeSourceRegions</a> action.</p>
  Subnet:
    type: object
    properties:
      SubnetIdentifier?:
        type: String
        description: <p>Specifies the identifier of the subnet.</p>
      SubnetAvailabilityZone?:
        type: AvailabilityZone
      SubnetStatus?:
        type: String
        description: <p>Specifies the status of the subnet.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p> This data type is used as a response element in the <a>DescribeDBSubnetGroups</a> action. </p>
  Tag:
    type: object
    properties:
      Key?:
        type: String
        description: '<p>A key is the required name of the tag. The string value can
          be from 1 to 128 Unicode characters in length and cannot be prefixed with
          "aws:" or "rds:". The string can only contain only the set of Unicode letters,
          digits, white-space, ''_'', ''.'', ''/'', ''='', ''+'', ''-'' (Java regex:
          "^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-]*)$").</p>'
      Value?:
        type: String
        description: '<p>A value is the optional value of the tag. The string value
          can be from 1 to 256 Unicode characters in length and cannot be prefixed
          with "aws:" or "rds:". The string can only contain only the set of Unicode
          letters, digits, white-space, ''_'', ''.'', ''/'', ''='', ''+'', ''-'' (Java
          regex: "^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-]*)$").</p>'
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>Metadata assigned to an Amazon RDS resource consisting of a key-value pair.</p>
  TagListMessage:
    type: object
    properties:
      TagList?:
        type: TagList
        description: <p>List of tags returned by the ListTagsForResource operation.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p/>
  Timezone:
    type: object
    properties:
      TimezoneName?:
        type: String
        description: <p>The name of the time zone.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>A time zone associated with a <a>DBInstance</a> or a <a>DBSnapshot</a>. This data type is an element in the response to the <a>DescribeDBInstances</a>, the <a>DescribeDBSnapshots</a>, and the <a>DescribeDBEngineVersions</a> actions. </p>
  UpgradeTarget:
    type: object
    properties:
      Engine?:
        type: String
        description: <p>The name of the upgrade target database engine.</p>
      EngineVersion?:
        type: String
        description: <p>The version number of the upgrade target database engine.</p>
      Description?:
        type: String
        description: <p>The version of the database engine that a DB instance can be upgraded to.</p>
      AutoUpgrade?:
        type: Boolean
        description: <p>A value that indicates whether the target version will be applied to any source DB instances that have AutoMinorVersionUpgrade set to true.</p>
      IsMajorVersionUpgrade?:
        type: Boolean
        description: <p>A value that indicates whether a database engine will be upgraded to a major version.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>The version of the database engine that a DB instance can be upgraded to.</p>
  VpcSecurityGroupMembership:
    type: object
    properties:
      VpcSecurityGroupId?:
        type: String
        description: <p>The name of the VPC security group.</p>
      Status?:
        type: String
        description: <p>The status of the VPC security group.</p>
    xml:
      namespace: http://rds.amazonaws.com/doc/2014-10-31/
    description: <p>This data type is used as a response element for queries on VPC security group membership.</p>
/{AddSourceIdentifierToSubscription}:
  uriParameters:
    AddSourceIdentifierToSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Add Source Identifier To Subscription
  post:
    description: Adds a source identifier to an existing RDS event notification subscription.
    displayName: Add Source Identifier To Subscription
    queryParameters:
      Action:
        type: string
        enum:
        - AddSourceIdentifierToSubscription
    body:
      application/xml: AddSourceIdentifierToSubscriptionMessage
    responses:
      200:
        body:
          application/xml: AddSourceIdentifierToSubscriptionResult
      404:
        description: SubscriptionNotFound
/{AddTagsToResource}:
  uriParameters:
    AddTagsToResource?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Add Tags To Resource
  post:
    description: Adds metadata tags to an Amazon RDS resource. These tags can also be used with cost allocation reporting to track cost associated with Amazon RDS resources, or used in a Condition statement in an IAM policy for Amazon RDS. For an overview on tagging Amazon RDS resources, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Overview.Tagging.html">Tagging Amazon RDS Resources</a>.
    displayName: Add Tags To Resource
    queryParameters:
      Action:
        type: string
        enum:
        - AddTagsToResource
    body:
      application/xml: AddTagsToResourceMessage
    responses:
      200:
        body:
          application/xml: null
      404:
        description: DBInstanceNotFound
/{ApplyPendingMaintenanceAction}:
  uriParameters:
    ApplyPendingMaintenanceAction?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Apply Pending Maintenance Action
  post:
    description: Applies a pending maintenance action to a resource (for example, to a DB instance).
    displayName: Apply Pending Maintenance Action
    queryParameters:
      Action:
        type: string
        enum:
        - ApplyPendingMaintenanceAction
    body:
      application/xml: ApplyPendingMaintenanceActionMessage
    responses:
      200:
        body:
          application/xml: ApplyPendingMaintenanceActionResult
      404:
        description: ResourceNotFoundFault
/{AuthorizeDBSecurityGroupIngress}:
  uriParameters:
    AuthorizeDBSecurityGroupIngress?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Authorize D BSecurity Group Ingress
  post:
    description: 'Enables ingress to a DBSecurityGroup using one of two forms of authorization.
      First, EC2 or VPC security groups can be added to the DBSecurityGroup if the
      application using the database is running on EC2 or VPC instances. Second, IP
      ranges are available if the application accessing your database is running on
      the Internet. Required parameters for this API are one of CIDR range, EC2SecurityGroupId
      for VPC, or (EC2SecurityGroupOwnerId and either EC2SecurityGroupName or EC2SecurityGroupId
      for non-VPC). <note> You cannot authorize ingress from an EC2 security group
      in one region to an Amazon RDS DB instance in another. You cannot authorize
      ingress from a VPC security group in one VPC to an Amazon RDS DB instance in
      another. </note> For an overview of CIDR ranges, go to the <a href="http://en.wikipedia.org/wiki/Classless_Inter-Domain_Routing">Wikipedia
      Tutorial</a>. '
    displayName: Authorize D BSecurity Group Ingress
    queryParameters:
      Action:
        type: string
        enum:
        - AuthorizeDBSecurityGroupIngress
    body:
      application/xml: AuthorizeDBSecurityGroupIngressMessage
    responses:
      200:
        body:
          application/xml: AuthorizeDBSecurityGroupIngressResult
      404:
        description: DBSecurityGroupNotFound
      400:
        description: InvalidDBSecurityGroupState
/{CopyDBClusterParameterGroup}:
  uriParameters:
    CopyDBClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Copy D BCluster Parameter Group
  post:
    description: Copies the specified DB cluster parameter group.
    displayName: Copy D BCluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - CopyDBClusterParameterGroup
    body:
      application/xml: CopyDBClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: CopyDBClusterParameterGroupResult
      404:
        description: DBParameterGroupNotFound
      400:
        description: DBParameterGroupQuotaExceeded
/{CopyDBClusterSnapshot}:
  uriParameters:
    CopyDBClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Copy D BCluster Snapshot
  post:
    description: 'Creates a snapshot of a DB cluster. For more information on Amazon
      Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Copy D BCluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - CopyDBClusterSnapshot
    body:
      application/xml: CopyDBClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: CopyDBClusterSnapshotResult
      400:
        description: DBClusterSnapshotAlreadyExistsFault
      404:
        description: DBClusterSnapshotNotFoundFault
/{CopyDBParameterGroup}:
  uriParameters:
    CopyDBParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Copy D BParameter Group
  post:
    description: Copies the specified DB parameter group.
    displayName: Copy D BParameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - CopyDBParameterGroup
    body:
      application/xml: CopyDBParameterGroupMessage
    responses:
      200:
        body:
          application/xml: CopyDBParameterGroupResult
      404:
        description: DBParameterGroupNotFound
      400:
        description: DBParameterGroupAlreadyExists
/{CopyDBSnapshot}:
  uriParameters:
    CopyDBSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Copy D BSnapshot
  post:
    description: Copies the specified DB snapshot. The source DB snapshot must be in the "available" state. If you are copying from a shared manual DB snapshot, the <code>SourceDBSnapshotIdentifier</code> must be the ARN of the shared DB snapshot.
    displayName: Copy D BSnapshot
    queryParameters:
      Action:
        type: string
        enum:
        - CopyDBSnapshot
    body:
      application/xml: CopyDBSnapshotMessage
    responses:
      200:
        body:
          application/xml: CopyDBSnapshotResult
      400:
        description: DBSnapshotAlreadyExists
      404:
        description: DBSnapshotNotFound
/{CopyOptionGroup}:
  uriParameters:
    CopyOptionGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Copy Option Group
  post:
    description: Copies the specified option group.
    displayName: Copy Option Group
    queryParameters:
      Action:
        type: string
        enum:
        - CopyOptionGroup
    body:
      application/xml: CopyOptionGroupMessage
    responses:
      200:
        body:
          application/xml: CopyOptionGroupResult
      400:
        description: OptionGroupAlreadyExistsFault
      404:
        description: OptionGroupNotFoundFault
/{CreateDBCluster}:
  uriParameters:
    CreateDBCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BCluster
  post:
    description: 'Creates a new Amazon Aurora DB cluster. You can use the <code>ReplicationSourceIdentifier</code>
      parameter to create the DB cluster as a Read Replica of another DB cluster.
      For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Create D BCluster
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBCluster
    body:
      application/xml: CreateDBClusterMessage
    responses:
      200:
        body:
          application/xml: CreateDBClusterResult
      400:
        description: DBClusterAlreadyExistsFault
      403:
        description: DBClusterQuotaExceededFault
      404:
        description: DBSubnetGroupNotFoundFault
/{CreateDBClusterParameterGroup}:
  uriParameters:
    CreateDBClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BCluster Parameter Group
  post:
    description: 'Creates a new DB cluster parameter group. Parameters in a DB cluster
      parameter group apply to all of the instances in a DB cluster.  A DB cluster
      parameter group is initially created with the default parameters for the database
      engine used by instances in the DB cluster. To provide custom values for any
      of the parameters, you must modify the group after creating it using <a>ModifyDBClusterParameterGroup</a>.
      Once you''ve created a DB cluster parameter group, you need to associate it
      with your DB cluster using <a>ModifyDBCluster</a>. When you associate a new
      DB cluster parameter group with a running DB cluster, you need to reboot the
      DB instances in the DB cluster without failover for the new DB cluster parameter
      group and associated settings to take effect.  <important> After you create
      a DB cluster parameter group, you should wait at least 5 minutes before creating
      your first DB cluster that uses that DB cluster parameter group as the default
      parameter group. This allows Amazon RDS to fully complete the create action
      before the DB cluster parameter group is used as the default for a new DB cluster.
      This is especially important for parameters that are critical when creating
      the default database for a DB cluster, such as the character set for the default
      database defined by the <code>character_set_database</code> parameter. You can
      use the <i>Parameter Groups</i> option of the <a href="https://console.aws.amazon.com/rds/">Amazon
      RDS console</a> or the <a>DescribeDBClusterParameters</a> command to verify
      that your DB cluster parameter group has been created or modified. </important>
      For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Create D BCluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBClusterParameterGroup
    body:
      application/xml: CreateDBClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: CreateDBClusterParameterGroupResult
      400:
        description: DBParameterGroupQuotaExceeded
/{CreateDBClusterSnapshot}:
  uriParameters:
    CreateDBClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BCluster Snapshot
  post:
    description: 'Creates a snapshot of a DB cluster. For more information on Amazon
      Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Create D BCluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBClusterSnapshot
    body:
      application/xml: CreateDBClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: CreateDBClusterSnapshotResult
      400:
        description: DBClusterSnapshotAlreadyExistsFault
      404:
        description: DBClusterNotFoundFault
/{CreateDBInstance}:
  uriParameters:
    CreateDBInstance?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BInstance
  post:
    description: Creates a new DB instance.
    displayName: Create D BInstance
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBInstance
    body:
      application/xml: CreateDBInstanceMessage
    responses:
      200:
        body:
          application/xml: CreateDBInstanceResult
      400:
        description: DBInstanceAlreadyExists
      404:
        description: DBParameterGroupNotFound
/{CreateDBInstanceReadReplica}:
  uriParameters:
    CreateDBInstanceReadReplica?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BInstance ReadReplica
  post:
    description: Creates a DB instance for a DB instance running MySQL, MariaDB, or PostgreSQL that acts as a Read Replica of a source DB instance. All Read Replica DB instances are created as Single-AZ deployments with backups disabled. All other DB instance attributes (including DB security groups and DB parameter groups) are inherited from the source DB instance, except as specified below. <important> The source DB instance must have backup retention enabled. </important>
    displayName: Create D BInstance ReadReplica
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBInstanceReadReplica
    body:
      application/xml: CreateDBInstanceReadReplicaMessage
    responses:
      200:
        body:
          application/xml: CreateDBInstanceReadReplicaResult
      400:
        description: DBInstanceAlreadyExists
      404:
        description: DBParameterGroupNotFound
/{CreateDBParameterGroup}:
  uriParameters:
    CreateDBParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BParameter Group
  post:
    description: Creates a new DB parameter group.  A DB parameter group is initially created with the default parameters for the database engine used by the DB instance. To provide custom values for any of the parameters, you must modify the group after creating it using <i>ModifyDBParameterGroup</i>. Once you've created a DB parameter group, you need to associate it with your DB instance using <i>ModifyDBInstance</i>. When you associate a new DB parameter group with a running DB instance, you need to reboot the DB instance without failover for the new DB parameter group and associated settings to take effect.  <important> After you create a DB parameter group, you should wait at least 5 minutes before creating your first DB instance that uses that DB parameter group as the default parameter group. This allows Amazon RDS to fully complete the create action before the parameter group is used as the default for a new DB instance. This is especially important for parameters that are critical when creating the default database for a DB instance, such as the character set for the default database defined by the <code>character_set_database</code> parameter. You can use the <i>Parameter Groups</i> option of the <a href="https://console.aws.amazon.com/rds/">Amazon RDS console</a> or the <i>DescribeDBParameters</i> command to verify that your DB parameter group has been created or modified. </important>
    displayName: Create D BParameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBParameterGroup
    body:
      application/xml: CreateDBParameterGroupMessage
    responses:
      200:
        body:
          application/xml: CreateDBParameterGroupResult
      400:
        description: DBParameterGroupQuotaExceeded
/{CreateDBSecurityGroup}:
  uriParameters:
    CreateDBSecurityGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BSecurity Group
  post:
    description: Creates a new DB security group. DB security groups control access to a DB instance.
    displayName: Create D BSecurity Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBSecurityGroup
    body:
      application/xml: CreateDBSecurityGroupMessage
    responses:
      200:
        body:
          application/xml: CreateDBSecurityGroupResult
      400:
        description: DBSecurityGroupAlreadyExists
/{CreateDBSnapshot}:
  uriParameters:
    CreateDBSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BSnapshot
  post:
    description: Creates a DBSnapshot. The source DBInstance must be in "available" state.
    displayName: Create D BSnapshot
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBSnapshot
    body:
      application/xml: CreateDBSnapshotMessage
    responses:
      200:
        body:
          application/xml: CreateDBSnapshotResult
      400:
        description: DBSnapshotAlreadyExists
      404:
        description: DBInstanceNotFound
/{CreateDBSubnetGroup}:
  uriParameters:
    CreateDBSubnetGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create D BSubnet Group
  post:
    description: Creates a new DB subnet group. DB subnet groups must contain at least one subnet in at least two AZs in the region.
    displayName: Create D BSubnet Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateDBSubnetGroup
    body:
      application/xml: CreateDBSubnetGroupMessage
    responses:
      200:
        body:
          application/xml: CreateDBSubnetGroupResult
      400:
        description: DBSubnetGroupAlreadyExists
/{CreateEventSubscription}:
  uriParameters:
    CreateEventSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Event Subscription
  post:
    description: Creates an RDS event notification subscription. This action requires a topic ARN (Amazon Resource Name) created by either the RDS console, the SNS console, or the SNS API. To obtain an ARN with SNS, you must create a topic in Amazon SNS and subscribe to the topic. The ARN is displayed in the SNS console. You can specify the type of source (SourceType) you want to be notified of, provide a list of RDS sources (SourceIds) that triggers the events, and provide a list of event categories (EventCategories) for events you want to be notified of. For example, you can specify SourceType = db-instance, SourceIds = mydbinstance1, mydbinstance2 and EventCategories = Availability, Backup. If you specify both the SourceType and SourceIds, such as SourceType = db-instance and SourceIdentifier = myDBInstance1, you will be notified of all the db-instance events for the specified source. If you specify a SourceType but do not specify a SourceIdentifier, you will receive notice of the events for that source type for all your RDS sources. If you do not specify either the SourceType nor the SourceIdentifier, you will be notified of events generated from all RDS sources belonging to your customer account.
    displayName: Create Event Subscription
    queryParameters:
      Action:
        type: string
        enum:
        - CreateEventSubscription
    body:
      application/xml: CreateEventSubscriptionMessage
    responses:
      200:
        body:
          application/xml: CreateEventSubscriptionResult
      400:
        description: EventSubscriptionQuotaExceeded
      404:
        description: SNSTopicArnNotFound
/{CreateOptionGroup}:
  uriParameters:
    CreateOptionGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Option Group
  post:
    description: Creates a new option group. You can create up to 20 option groups.
    displayName: Create Option Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateOptionGroup
    body:
      application/xml: CreateOptionGroupMessage
    responses:
      200:
        body:
          application/xml: CreateOptionGroupResult
      400:
        description: OptionGroupAlreadyExistsFault
/{DeleteDBCluster}:
  uriParameters:
    DeleteDBCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BCluster
  post:
    description: 'The DeleteDBCluster action deletes a previously provisioned DB cluster.
      When you delete a DB cluster, all automated backups for that DB cluster are
      deleted and cannot be recovered. Manual DB cluster snapshots of the specified
      DB cluster are not deleted. <p/> For more information on Amazon Aurora, see
      <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Delete D BCluster
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBCluster
    body:
      application/xml: DeleteDBClusterMessage
    responses:
      200:
        body:
          application/xml: DeleteDBClusterResult
      404:
        description: DBClusterNotFoundFault
      400:
        description: InvalidDBClusterStateFault
/{DeleteDBClusterParameterGroup}:
  uriParameters:
    DeleteDBClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BCluster Parameter Group
  post:
    description: 'Deletes a specified DB cluster parameter group. The DB cluster parameter
      group to be deleted cannot be associated with any DB clusters. For more information
      on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Delete D BCluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBClusterParameterGroup
    body:
      application/xml: DeleteDBClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidDBParameterGroupState
      404:
        description: DBParameterGroupNotFound
/{DeleteDBClusterSnapshot}:
  uriParameters:
    DeleteDBClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BCluster Snapshot
  post:
    description: 'Deletes a DB cluster snapshot. If the snapshot is being copied,
      the copy operation is terminated. <note> The DB cluster snapshot must be in
      the <code>available</code> state to be deleted. </note> For more information
      on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Delete D BCluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBClusterSnapshot
    body:
      application/xml: DeleteDBClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: DeleteDBClusterSnapshotResult
      400:
        description: InvalidDBClusterSnapshotStateFault
      404:
        description: DBClusterSnapshotNotFoundFault
/{DeleteDBInstance}:
  uriParameters:
    DeleteDBInstance?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BInstance
  post:
    description: 'The DeleteDBInstance action deletes a previously provisioned DB
      instance. When you delete a DB instance, all automated backups for that instance
      are deleted and cannot be recovered. Manual DB snapshots of the DB instance
      to be deleted by <code>DeleteDBInstance</code> are not deleted.  If you request
      a final DB snapshot the status of the Amazon RDS DB instance is <code>deleting</code>
      until the DB snapshot is created. The API action <code>DescribeDBInstance</code>
      is used to monitor the status of this operation. The action cannot be canceled
      or reverted once submitted.  Note that when a DB instance is in a failure state
      and has a status of <code>failed</code>, <code>incompatible-restore</code>,
      or <code>incompatible-network</code>, you can only delete it when the <code>SkipFinalSnapshot</code>
      parameter is set to <code>true</code>. If the specified DB instance is part
      of an Amazon Aurora DB cluster, you cannot delete the DB instance if the following
      are true: <ul> <li> The DB cluster is a Read Replica of another Amazon Aurora
      DB cluster. </li> <li> The DB instance is the only instance in the DB cluster.
      </li> </ul> To delete a DB instance in this case, first call the <a>PromoteReadReplicaDBCluster</a>
      API action to promote the DB cluster so it''s no longer a Read Replica. After
      the promotion completes, then call the <code>DeleteDBInstance</code> API action
      to delete the final instance in the DB cluster.'
    displayName: Delete D BInstance
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBInstance
    body:
      application/xml: DeleteDBInstanceMessage
    responses:
      200:
        body:
          application/xml: DeleteDBInstanceResult
      404:
        description: DBInstanceNotFound
      400:
        description: InvalidDBInstanceState
/{DeleteDBParameterGroup}:
  uriParameters:
    DeleteDBParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BParameter Group
  post:
    description: Deletes a specified DBParameterGroup. The DBParameterGroup to be deleted cannot be associated with any DB instances.
    displayName: Delete D BParameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBParameterGroup
    body:
      application/xml: DeleteDBParameterGroupMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidDBParameterGroupState
      404:
        description: DBParameterGroupNotFound
/{DeleteDBSecurityGroup}:
  uriParameters:
    DeleteDBSecurityGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BSecurity Group
  post:
    description: Deletes a DB security group. <note> The specified DB security group must not be associated with any DB instances. </note>
    displayName: Delete D BSecurity Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBSecurityGroup
    body:
      application/xml: DeleteDBSecurityGroupMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidDBSecurityGroupState
      404:
        description: DBSecurityGroupNotFound
/{DeleteDBSnapshot}:
  uriParameters:
    DeleteDBSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BSnapshot
  post:
    description: Deletes a DBSnapshot. If the snapshot is being copied, the copy operation is terminated. <note> The DBSnapshot must be in the <code>available</code> state to be deleted. </note>
    displayName: Delete D BSnapshot
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBSnapshot
    body:
      application/xml: DeleteDBSnapshotMessage
    responses:
      200:
        body:
          application/xml: DeleteDBSnapshotResult
      400:
        description: InvalidDBSnapshotState
      404:
        description: DBSnapshotNotFound
/{DeleteDBSubnetGroup}:
  uriParameters:
    DeleteDBSubnetGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete D BSubnet Group
  post:
    description: Deletes a DB subnet group. <note> The specified database subnet group must not be associated with any DB instances. </note>
    displayName: Delete D BSubnet Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteDBSubnetGroup
    body:
      application/xml: DeleteDBSubnetGroupMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidDBSubnetGroupStateFault
      404:
        description: DBSubnetGroupNotFoundFault
/{DeleteEventSubscription}:
  uriParameters:
    DeleteEventSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Event Subscription
  post:
    description: Deletes an RDS event notification subscription.
    displayName: Delete Event Subscription
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteEventSubscription
    body:
      application/xml: DeleteEventSubscriptionMessage
    responses:
      200:
        body:
          application/xml: DeleteEventSubscriptionResult
      404:
        description: SubscriptionNotFound
      400:
        description: InvalidEventSubscriptionState
/{DeleteOptionGroup}:
  uriParameters:
    DeleteOptionGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Option Group
  post:
    description: Deletes an existing option group.
    displayName: Delete Option Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteOptionGroup
    body:
      application/xml: DeleteOptionGroupMessage
    responses:
      200:
        body:
          application/xml: null
      404:
        description: OptionGroupNotFoundFault
      400:
        description: InvalidOptionGroupStateFault
/{DescribeAccountAttributes}:
  uriParameters:
    DescribeAccountAttributes?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Account Attributes
  post:
    description: Lists all of the attributes for a customer account. The attributes include Amazon RDS quotas for the account, such as the number of DB instances allowed. The description for a quota includes the quota name, current usage toward that quota, and the quota's maximum value. This command does not take any parameters.
    displayName: Describe Account Attributes
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeAccountAttributes
    body:
      application/xml: DescribeAccountAttributesMessage
    responses:
      200:
        body:
          application/xml: AccountAttributesMessage
/{DescribeCertificates}:
  uriParameters:
    DescribeCertificates?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Certificates
  post:
    description: Lists the set of CA certificates provided by Amazon RDS for this AWS account.
    displayName: Describe Certificates
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeCertificates
    body:
      application/xml: DescribeCertificatesMessage
    responses:
      200:
        body:
          application/xml: CertificateMessage
      404:
        description: CertificateNotFound
/{DescribeDBClusterParameterGroups}:
  uriParameters:
    DescribeDBClusterParameterGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BCluster Parameter Groups
  post:
    description: ' Returns a list of <code>DBClusterParameterGroup</code> descriptions.
      If a <code>DBClusterParameterGroupName</code> parameter is specified, the list
      will contain only the description of the specified DB cluster parameter group.  For
      more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Describe D BCluster Parameter Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBClusterParameterGroups
    body:
      application/xml: DescribeDBClusterParameterGroupsMessage
    responses:
      200:
        body:
          application/xml: DBClusterParameterGroupsMessage
      404:
        description: DBParameterGroupNotFound
/{DescribeDBClusterParameters}:
  uriParameters:
    DescribeDBClusterParameters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BCluster Parameters
  post:
    description: 'Returns the detailed parameter list for a particular DB cluster
      parameter group. For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Describe D BCluster Parameters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBClusterParameters
    body:
      application/xml: DescribeDBClusterParametersMessage
    responses:
      200:
        body:
          application/xml: DBClusterParameterGroupDetails
      404:
        description: DBParameterGroupNotFound
/{DescribeDBClusterSnapshotAttributes}:
  uriParameters:
    DescribeDBClusterSnapshotAttributes?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BCluster Snapshot Attributes
  post:
    description: Returns a list of DB cluster snapshot attribute names and values for a manual DB cluster snapshot. When sharing snapshots with other AWS accounts, <code>DescribeDBClusterSnapshotAttributes</code> returns the <code>restore</code> attribute and a list of IDs for the AWS accounts that are authorized to copy or restore the manual DB cluster snapshot. If <code>all</code> is included in the list of values for the <code>restore</code> attribute, then the manual DB cluster snapshot is public and can be copied or restored by all AWS accounts. To add or remove access for an AWS account to copy or restore a manual DB cluster snapshot, or to make the manual DB cluster snapshot public or private, use the <a>ModifyDBClusterSnapshotAttribute</a> API action.
    displayName: Describe D BCluster Snapshot Attributes
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBClusterSnapshotAttributes
    body:
      application/xml: DescribeDBClusterSnapshotAttributesMessage
    responses:
      200:
        body:
          application/xml: DescribeDBClusterSnapshotAttributesResult
      404:
        description: DBClusterSnapshotNotFoundFault
/{DescribeDBClusterSnapshots}:
  uriParameters:
    DescribeDBClusterSnapshots?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BCluster Snapshots
  post:
    description: 'Returns information about DB cluster snapshots. This API action
      supports pagination. For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Describe D BCluster Snapshots
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBClusterSnapshots
    body:
      application/xml: DescribeDBClusterSnapshotsMessage
    responses:
      200:
        body:
          application/xml: DBClusterSnapshotMessage
      404:
        description: DBClusterSnapshotNotFoundFault
/{DescribeDBClusters}:
  uriParameters:
    DescribeDBClusters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BClusters
  post:
    description: 'Returns information about provisioned Aurora DB clusters. This API
      supports pagination. For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Describe D BClusters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBClusters
    body:
      application/xml: DescribeDBClustersMessage
    responses:
      200:
        body:
          application/xml: DBClusterMessage
      404:
        description: DBClusterNotFoundFault
/{DescribeDBEngineVersions}:
  uriParameters:
    DescribeDBEngineVersions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BEngine Versions
  post:
    description: Returns a list of the available DB engines.
    displayName: Describe D BEngine Versions
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBEngineVersions
    body:
      application/xml: DescribeDBEngineVersionsMessage
    responses:
      200:
        body:
          application/xml: DBEngineVersionMessage
/{DescribeDBInstances}:
  uriParameters:
    DescribeDBInstances?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BInstances
  post:
    description: Returns information about provisioned RDS instances. This API supports pagination.
    displayName: Describe D BInstances
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBInstances
    body:
      application/xml: DescribeDBInstancesMessage
    responses:
      200:
        body:
          application/xml: DBInstanceMessage
      404:
        description: DBInstanceNotFound
/{DescribeDBLogFiles}:
  uriParameters:
    DescribeDBLogFiles?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BLogFiles
  post:
    description: Returns a list of DB log files for the DB instance.
    displayName: Describe D BLogFiles
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBLogFiles
    body:
      application/xml: DescribeDBLogFilesMessage
    responses:
      200:
        body:
          application/xml: DescribeDBLogFilesResponse
      404:
        description: DBInstanceNotFound
/{DescribeDBParameterGroups}:
  uriParameters:
    DescribeDBParameterGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BParameter Groups
  post:
    description: ' Returns a list of <code>DBParameterGroup</code> descriptions. If
      a <code>DBParameterGroupName</code> is specified, the list will contain only
      the description of the specified DB parameter group. '
    displayName: Describe D BParameter Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBParameterGroups
    body:
      application/xml: DescribeDBParameterGroupsMessage
    responses:
      200:
        body:
          application/xml: DBParameterGroupsMessage
      404:
        description: DBParameterGroupNotFound
/{DescribeDBParameters}:
  uriParameters:
    DescribeDBParameters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BParameters
  post:
    description: Returns the detailed parameter list for a particular DB parameter group.
    displayName: Describe D BParameters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBParameters
    body:
      application/xml: DescribeDBParametersMessage
    responses:
      200:
        body:
          application/xml: DBParameterGroupDetails
      404:
        description: DBParameterGroupNotFound
/{DescribeDBSecurityGroups}:
  uriParameters:
    DescribeDBSecurityGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BSecurity Groups
  post:
    description: ' Returns a list of <code>DBSecurityGroup</code> descriptions. If
      a <code>DBSecurityGroupName</code> is specified, the list will contain only
      the descriptions of the specified DB security group. '
    displayName: Describe D BSecurity Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBSecurityGroups
    body:
      application/xml: DescribeDBSecurityGroupsMessage
    responses:
      200:
        body:
          application/xml: DBSecurityGroupMessage
      404:
        description: DBSecurityGroupNotFound
/{DescribeDBSnapshotAttributes}:
  uriParameters:
    DescribeDBSnapshotAttributes?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BSnapshot Attributes
  post:
    description: Returns a list of DB snapshot attribute names and values for a manual DB snapshot. When sharing snapshots with other AWS accounts, <code>DescribeDBSnapshotAttributes</code> returns the <code>restore</code> attribute and a list of IDs for the AWS accounts that are authorized to copy or restore the manual DB snapshot. If <code>all</code> is included in the list of values for the <code>restore</code> attribute, then the manual DB snapshot is public and can be copied or restored by all AWS accounts. To add or remove access for an AWS account to copy or restore a manual DB snapshot, or to make the manual DB snapshot public or private, use the <a>ModifyDBSnapshotAttribute</a> API action.
    displayName: Describe D BSnapshot Attributes
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBSnapshotAttributes
    body:
      application/xml: DescribeDBSnapshotAttributesMessage
    responses:
      200:
        body:
          application/xml: DescribeDBSnapshotAttributesResult
      404:
        description: DBSnapshotNotFound
/{DescribeDBSnapshots}:
  uriParameters:
    DescribeDBSnapshots?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BSnapshots
  post:
    description: Returns information about DB snapshots. This API action supports pagination.
    displayName: Describe D BSnapshots
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBSnapshots
    body:
      application/xml: DescribeDBSnapshotsMessage
    responses:
      200:
        body:
          application/xml: DBSnapshotMessage
      404:
        description: DBSnapshotNotFound
/{DescribeDBSubnetGroups}:
  uriParameters:
    DescribeDBSubnetGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe D BSubnet Groups
  post:
    description: 'Returns a list of DBSubnetGroup descriptions. If a DBSubnetGroupName
      is specified, the list will contain only the descriptions of the specified DBSubnetGroup.
      For an overview of CIDR ranges, go to the <a href="http://en.wikipedia.org/wiki/Classless_Inter-Domain_Routing">Wikipedia
      Tutorial</a>. '
    displayName: Describe D BSubnet Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDBSubnetGroups
    body:
      application/xml: DescribeDBSubnetGroupsMessage
    responses:
      200:
        body:
          application/xml: DBSubnetGroupMessage
      404:
        description: DBSubnetGroupNotFoundFault
/{DescribeEngineDefaultClusterParameters}:
  uriParameters:
    DescribeEngineDefaultClusterParameters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Engine Default Cluster Parameters
  post:
    description: 'Returns the default engine and system parameter information for
      the cluster database engine. For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Describe Engine Default Cluster Parameters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEngineDefaultClusterParameters
    body:
      application/xml: DescribeEngineDefaultClusterParametersMessage
    responses:
      200:
        body:
          application/xml: DescribeEngineDefaultClusterParametersResult
/{DescribeEngineDefaultParameters}:
  uriParameters:
    DescribeEngineDefaultParameters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Engine Default Parameters
  post:
    description: Returns the default engine and system parameter information for the specified database engine.
    displayName: Describe Engine Default Parameters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEngineDefaultParameters
    body:
      application/xml: DescribeEngineDefaultParametersMessage
    responses:
      200:
        body:
          application/xml: DescribeEngineDefaultParametersResult
/{DescribeEventCategories}:
  uriParameters:
    DescribeEventCategories?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Event Categories
  post:
    description: 'Displays a list of categories for all event source types, or, if
      specified, for a specified source type. You can see a list of the event categories
      and source types in the <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Events.html">
      Events</a> topic in the <i>Amazon RDS User Guide.</i> '
    displayName: Describe Event Categories
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEventCategories
    body:
      application/xml: DescribeEventCategoriesMessage
    responses:
      200:
        body:
          application/xml: EventCategoriesMessage
/{DescribeEventSubscriptions}:
  uriParameters:
    DescribeEventSubscriptions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Event Subscriptions
  post:
    description: Lists all the subscription descriptions for a customer account. The description for a subscription includes SubscriptionName, SNSTopicARN, CustomerID, SourceType, SourceID, CreationTime, and Status. If you specify a SubscriptionName, lists the description for that subscription.
    displayName: Describe Event Subscriptions
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEventSubscriptions
    body:
      application/xml: DescribeEventSubscriptionsMessage
    responses:
      200:
        body:
          application/xml: EventSubscriptionsMessage
      404:
        description: SubscriptionNotFound
/{DescribeEvents}:
  uriParameters:
    DescribeEvents?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Events
  post:
    description: Returns events related to DB instances, DB security groups, DB snapshots, and DB parameter groups for the past 14 days. Events specific to a particular DB instance, DB security group, database snapshot, or DB parameter group can be obtained by providing the name as a parameter. By default, the past hour of events are returned.
    displayName: Describe Events
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEvents
    body:
      application/xml: DescribeEventsMessage
    responses:
      200:
        body:
          application/xml: EventsMessage
/{DescribeOptionGroupOptions}:
  uriParameters:
    DescribeOptionGroupOptions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Option Group Options
  post:
    description: Describes all available options.
    displayName: Describe Option Group Options
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeOptionGroupOptions
    body:
      application/xml: DescribeOptionGroupOptionsMessage
    responses:
      200:
        body:
          application/xml: OptionGroupOptionsMessage
/{DescribeOptionGroups}:
  uriParameters:
    DescribeOptionGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Option Groups
  post:
    description: Describes the available option groups.
    displayName: Describe Option Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeOptionGroups
    body:
      application/xml: DescribeOptionGroupsMessage
    responses:
      200:
        body:
          application/xml: OptionGroups
      404:
        description: OptionGroupNotFoundFault
/{DescribeOrderableDBInstanceOptions}:
  uriParameters:
    DescribeOrderableDBInstanceOptions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Orderable D B Instance Options
  post:
    description: Returns a list of orderable DB instance options for the specified engine.
    displayName: Describe Orderable D B Instance Options
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeOrderableDBInstanceOptions
    body:
      application/xml: DescribeOrderableDBInstanceOptionsMessage
    responses:
      200:
        body:
          application/xml: OrderableDBInstanceOptionsMessage
/{DescribePendingMaintenanceActions}:
  uriParameters:
    DescribePendingMaintenanceActions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Pending Maintenance Actions
  post:
    description: Returns a list of resources (for example, DB instances) that have at least one pending maintenance action.
    displayName: Describe Pending Maintenance Actions
    queryParameters:
      Action:
        type: string
        enum:
        - DescribePendingMaintenanceActions
    body:
      application/xml: DescribePendingMaintenanceActionsMessage
    responses:
      200:
        body:
          application/xml: PendingMaintenanceActionsMessage
      404:
        description: ResourceNotFoundFault
/{DescribeReservedDBInstances}:
  uriParameters:
    DescribeReservedDBInstances?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Reserved D B Instances
  post:
    description: Returns information about reserved DB instances for this account, or about a specified reserved DB instance.
    displayName: Describe Reserved D B Instances
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeReservedDBInstances
    body:
      application/xml: DescribeReservedDBInstancesMessage
    responses:
      200:
        body:
          application/xml: ReservedDBInstanceMessage
      404:
        description: ReservedDBInstanceNotFound
/{DescribeReservedDBInstancesOfferings}:
  uriParameters:
    DescribeReservedDBInstancesOfferings?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Reserved D B Instances Offerings
  post:
    description: Lists available reserved DB instance offerings.
    displayName: Describe Reserved D B Instances Offerings
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeReservedDBInstancesOfferings
    body:
      application/xml: DescribeReservedDBInstancesOfferingsMessage
    responses:
      200:
        body:
          application/xml: ReservedDBInstancesOfferingMessage
      404:
        description: ReservedDBInstancesOfferingNotFound
/{DescribeSourceRegions}:
  uriParameters:
    DescribeSourceRegions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Source Regions
  post:
    description: Returns a list of the source AWS regions where the current AWS region can create a Read Replica or copy a DB snapshot from. This API action supports pagination.
    displayName: Describe Source Regions
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeSourceRegions
    body:
      application/xml: DescribeSourceRegionsMessage
    responses:
      200:
        body:
          application/xml: SourceRegionMessage
/{DownloadDBLogFilePortion}:
  uriParameters:
    DownloadDBLogFilePortion?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Download D BLogFile Portion
  post:
    description: Downloads all or a portion of the specified log file, up to 1 MB in size.
    displayName: Download D BLogFile Portion
    queryParameters:
      Action:
        type: string
        enum:
        - DownloadDBLogFilePortion
    body:
      application/xml: DownloadDBLogFilePortionMessage
    responses:
      200:
        body:
          application/xml: DownloadDBLogFilePortionDetails
      404:
        description: DBInstanceNotFound
/{FailoverDBCluster}:
  uriParameters:
    FailoverDBCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Failover D BCluster
  post:
    description: 'Forces a failover for a DB cluster. A failover for a DB cluster
      promotes one of the read-only instances in the DB cluster to the master DB instance
      (the cluster writer) and deletes the current primary instance. Amazon Aurora
      will automatically fail over to a read-only instance, if one exists, when the
      primary instance fails. You can force a failover when you want to simulate a
      failure of a DB instance for testing. Because each instance in a DB cluster
      has its own endpoint address, you will need to clean up and re-establish any
      existing connections that use those endpoint addresses when the failover is
      complete. For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Failover D BCluster
    queryParameters:
      Action:
        type: string
        enum:
        - FailoverDBCluster
    body:
      application/xml: FailoverDBClusterMessage
    responses:
      200:
        body:
          application/xml: FailoverDBClusterResult
      404:
        description: DBClusterNotFoundFault
      400:
        description: InvalidDBClusterStateFault
/{ListTagsForResource}:
  uriParameters:
    ListTagsForResource?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: List Tags ForResource
  post:
    description: Lists all tags on an Amazon RDS resource. For an overview on tagging an Amazon RDS resource, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Overview.Tagging.html">Tagging Amazon RDS Resources</a>.
    displayName: List Tags ForResource
    queryParameters:
      Action:
        type: string
        enum:
        - ListTagsForResource
    body:
      application/xml: ListTagsForResourceMessage
    responses:
      200:
        body:
          application/xml: TagListMessage
      404:
        description: DBInstanceNotFound
/{ModifyDBCluster}:
  uriParameters:
    ModifyDBCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify D BCluster
  post:
    description: 'Modify a setting for an Amazon Aurora DB cluster. You can change
      one or more database configuration parameters by specifying these parameters
      and the new values in the request. For more information on Amazon Aurora, see
      <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Modify D BCluster
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyDBCluster
    body:
      application/xml: ModifyDBClusterMessage
    responses:
      200:
        body:
          application/xml: ModifyDBClusterResult
      404:
        description: DBClusterNotFoundFault
      400:
        description: InvalidDBClusterStateFault
/{ModifyDBClusterParameterGroup}:
  uriParameters:
    ModifyDBClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify D BCluster Parameter Group
  post:
    description: ' Modifies the parameters of a DB cluster parameter group. To modify
      more than one parameter, submit a list of the following: <code>ParameterName</code>,
      <code>ParameterValue</code>, and <code>ApplyMethod</code>. A maximum of 20 parameters
      can be modified in a single request.  For more information on Amazon Aurora,
      see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i>  <note> Changes to dynamic
      parameters are applied immediately. Changes to static parameters require a reboot
      without failover to the DB cluster associated with the parameter group before
      the change can take effect. </note> <important> After you create a DB cluster
      parameter group, you should wait at least 5 minutes before creating your first
      DB cluster that uses that DB cluster parameter group as the default parameter
      group. This allows Amazon RDS to fully complete the create action before the
      parameter group is used as the default for a new DB cluster. This is especially
      important for parameters that are critical when creating the default database
      for a DB cluster, such as the character set for the default database defined
      by the <code>character_set_database</code> parameter. You can use the <i>Parameter
      Groups</i> option of the <a href="https://console.aws.amazon.com/rds/">Amazon
      RDS console</a> or the <a>DescribeDBClusterParameters</a> command to verify
      that your DB cluster parameter group has been created or modified. </important>'
    displayName: Modify D BCluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyDBClusterParameterGroup
    body:
      application/xml: ModifyDBClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: DBClusterParameterGroupNameMessage
      404:
        description: DBParameterGroupNotFound
      400:
        description: InvalidDBParameterGroupState
/{ModifyDBClusterSnapshotAttribute}:
  uriParameters:
    ModifyDBClusterSnapshotAttribute?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify D BCluster Snapshot Attribute
  post:
    description: Adds an attribute and values to, or removes an attribute and values from, a manual DB cluster snapshot. To share a manual DB cluster snapshot with other AWS accounts, specify <code>restore</code> as the <code>AttributeName</code> and use the <code>ValuesToAdd</code> parameter to add a list of IDs of the AWS accounts that are authorized to restore the manual DB cluster snapshot. Use the value <code>all</code> to make the manual DB cluster snapshot public, which means that it can be copied or restored by all AWS accounts. Do not add the <code>all</code> value for any manual DB cluster snapshots that contain private information that you don't want available to all AWS accounts. To view which AWS accounts have access to copy or restore a manual DB cluster snapshot, or whether a manual DB cluster snapshot public or private, use the <a>DescribeDBClusterSnapshotAttributes</a> API action. If a manual DB cluster snapshot is encrypted, it cannot be shared.
    displayName: Modify D BCluster Snapshot Attribute
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyDBClusterSnapshotAttribute
    body:
      application/xml: ModifyDBClusterSnapshotAttributeMessage
    responses:
      200:
        body:
          application/xml: ModifyDBClusterSnapshotAttributeResult
      404:
        description: DBClusterSnapshotNotFoundFault
      400:
        description: InvalidDBClusterSnapshotStateFault
/{ModifyDBInstance}:
  uriParameters:
    ModifyDBInstance?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify D BInstance
  post:
    description: Modifies settings for a DB instance. You can change one or more database configuration parameters by specifying these parameters and the new values in the request.
    displayName: Modify D BInstance
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyDBInstance
    body:
      application/xml: ModifyDBInstanceMessage
    responses:
      200:
        body:
          application/xml: ModifyDBInstanceResult
      400:
        description: InvalidDBInstanceState
      404:
        description: DBInstanceNotFound
/{ModifyDBParameterGroup}:
  uriParameters:
    ModifyDBParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify D BParameter Group
  post:
    description: ' Modifies the parameters of a DB parameter group. To modify more
      than one parameter, submit a list of the following: <code>ParameterName</code>,
      <code>ParameterValue</code>, and <code>ApplyMethod</code>. A maximum of 20 parameters
      can be modified in a single request.  <note> Changes to dynamic parameters are
      applied immediately. Changes to static parameters require a reboot without failover
      to the DB instance associated with the parameter group before the change can
      take effect. </note> <important> After you modify a DB parameter group, you
      should wait at least 5 minutes before creating your first DB instance that uses
      that DB parameter group as the default parameter group. This allows Amazon RDS
      to fully complete the modify action before the parameter group is used as the
      default for a new DB instance. This is especially important for parameters that
      are critical when creating the default database for a DB instance, such as the
      character set for the default database defined by the <code>character_set_database</code>
      parameter. You can use the <i>Parameter Groups</i> option of the <a href="https://console.aws.amazon.com/rds/">Amazon
      RDS console</a> or the <i>DescribeDBParameters</i> command to verify that your
      DB parameter group has been created or modified. </important>'
    displayName: Modify D BParameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyDBParameterGroup
    body:
      application/xml: ModifyDBParameterGroupMessage
    responses:
      200:
        body:
          application/xml: DBParameterGroupNameMessage
      404:
        description: DBParameterGroupNotFound
      400:
        description: InvalidDBParameterGroupState
/{ModifyDBSnapshotAttribute}:
  uriParameters:
    ModifyDBSnapshotAttribute?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify D BSnapshot Attribute
  post:
    description: Adds an attribute and values to, or removes an attribute and values from, a manual DB snapshot. To share a manual DB snapshot with other AWS accounts, specify <code>restore</code> as the <code>AttributeName</code> and use the <code>ValuesToAdd</code> parameter to add a list of IDs of the AWS accounts that are authorized to restore the manual DB snapshot. Uses the value <code>all</code> to make the manual DB snapshot public, which means it can be copied or restored by all AWS accounts. Do not add the <code>all</code> value for any manual DB snapshots that contain private information that you don't want available to all AWS accounts. To view which AWS accounts have access to copy or restore a manual DB snapshot, or whether a manual DB snapshot public or private, use the <a>DescribeDBSnapshotAttributes</a> API action. If the manual DB snapshot is encrypted, it cannot be shared.
    displayName: Modify D BSnapshot Attribute
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyDBSnapshotAttribute
    body:
      application/xml: ModifyDBSnapshotAttributeMessage
    responses:
      200:
        body:
          application/xml: ModifyDBSnapshotAttributeResult
      404:
        description: DBSnapshotNotFound
      400:
        description: InvalidDBSnapshotState
/{ModifyDBSubnetGroup}:
  uriParameters:
    ModifyDBSubnetGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify D BSubnet Group
  post:
    description: Modifies an existing DB subnet group. DB subnet groups must contain at least one subnet in at least two AZs in the region.
    displayName: Modify D BSubnet Group
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyDBSubnetGroup
    body:
      application/xml: ModifyDBSubnetGroupMessage
    responses:
      200:
        body:
          application/xml: ModifyDBSubnetGroupResult
      404:
        description: DBSubnetGroupNotFoundFault
      400:
        description: DBSubnetQuotaExceededFault
/{ModifyEventSubscription}:
  uriParameters:
    ModifyEventSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Event Subscription
  post:
    description: Modifies an existing RDS event notification subscription. Note that you cannot modify the source identifiers using this call; to change source identifiers for a subscription, use the <a>AddSourceIdentifierToSubscription</a> and <a>RemoveSourceIdentifierFromSubscription</a> calls. You can see a list of the event categories for a given SourceType in the <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_Events.html">Events</a> topic in the Amazon RDS User Guide or by using the <b>DescribeEventCategories</b> action.
    displayName: Modify Event Subscription
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyEventSubscription
    body:
      application/xml: ModifyEventSubscriptionMessage
    responses:
      200:
        body:
          application/xml: ModifyEventSubscriptionResult
      400:
        description: EventSubscriptionQuotaExceeded
      404:
        description: SubscriptionNotFound
/{ModifyOptionGroup}:
  uriParameters:
    ModifyOptionGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Option Group
  post:
    description: Modifies an existing option group.
    displayName: Modify Option Group
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyOptionGroup
    body:
      application/xml: ModifyOptionGroupMessage
    responses:
      200:
        body:
          application/xml: ModifyOptionGroupResult
      400:
        description: InvalidOptionGroupStateFault
      404:
        description: OptionGroupNotFoundFault
/{PromoteReadReplica}:
  uriParameters:
    PromoteReadReplica?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Promote Read Replica
  post:
    description: Promotes a Read Replica DB instance to a standalone DB instance. <note> We recommend that you enable automated backups on your Read Replica before promoting the Read Replica. This ensures that no backup is taken during the promotion process. Once the instance is promoted to a primary instance, backups are taken based on your backup settings. </note>
    displayName: Promote Read Replica
    queryParameters:
      Action:
        type: string
        enum:
        - PromoteReadReplica
    body:
      application/xml: PromoteReadReplicaMessage
    responses:
      200:
        body:
          application/xml: PromoteReadReplicaResult
      400:
        description: InvalidDBInstanceState
      404:
        description: DBInstanceNotFound
/{PromoteReadReplicaDBCluster}:
  uriParameters:
    PromoteReadReplicaDBCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Promote Read Replica D B Cluster
  post:
    description: Promotes a Read Replica DB cluster to a standalone DB cluster.
    displayName: Promote Read Replica D B Cluster
    queryParameters:
      Action:
        type: string
        enum:
        - PromoteReadReplicaDBCluster
    body:
      application/xml: PromoteReadReplicaDBClusterMessage
    responses:
      200:
        body:
          application/xml: PromoteReadReplicaDBClusterResult
      404:
        description: DBClusterNotFoundFault
      400:
        description: InvalidDBClusterStateFault
/{PurchaseReservedDBInstancesOffering}:
  uriParameters:
    PurchaseReservedDBInstancesOffering?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Purchase Reserved D B Instances Offering
  post:
    description: Purchases a reserved DB instance offering.
    displayName: Purchase Reserved D B Instances Offering
    queryParameters:
      Action:
        type: string
        enum:
        - PurchaseReservedDBInstancesOffering
    body:
      application/xml: PurchaseReservedDBInstancesOfferingMessage
    responses:
      200:
        body:
          application/xml: PurchaseReservedDBInstancesOfferingResult
      404:
        description: ReservedDBInstancesOfferingNotFound
      400:
        description: ReservedDBInstanceQuotaExceeded
/{RebootDBInstance}:
  uriParameters:
    RebootDBInstance?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Reboot D BInstance
  post:
    description: Rebooting a DB instance restarts the database engine service. A reboot also applies to the DB instance any modifications to the associated DB parameter group that were pending. Rebooting a DB instance results in a momentary outage of the instance, during which the DB instance status is set to rebooting. If the RDS instance is configured for MultiAZ, it is possible that the reboot will be conducted through a failover. An Amazon RDS event is created when the reboot is completed. If your DB instance is deployed in multiple Availability Zones, you can force a failover from one AZ to the other during the reboot. You might force a failover to test the availability of your DB instance deployment or to restore operations to the original AZ after a failover occurs. The time required to reboot is a function of the specific database engine's crash recovery process. To improve the reboot time, we recommend that you reduce database activities as much as possible during the reboot process to reduce rollback activity for in-transit transactions.
    displayName: Reboot D BInstance
    queryParameters:
      Action:
        type: string
        enum:
        - RebootDBInstance
    body:
      application/xml: RebootDBInstanceMessage
    responses:
      200:
        body:
          application/xml: RebootDBInstanceResult
      400:
        description: InvalidDBInstanceState
      404:
        description: DBInstanceNotFound
/{RemoveSourceIdentifierFromSubscription}:
  uriParameters:
    RemoveSourceIdentifierFromSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Remove Source Identifier FromSubscription
  post:
    description: Removes a source identifier from an existing RDS event notification subscription.
    displayName: Remove Source Identifier FromSubscription
    queryParameters:
      Action:
        type: string
        enum:
        - RemoveSourceIdentifierFromSubscription
    body:
      application/xml: RemoveSourceIdentifierFromSubscriptionMessage
    responses:
      200:
        body:
          application/xml: RemoveSourceIdentifierFromSubscriptionResult
      404:
        description: SubscriptionNotFound
/{RemoveTagsFromResource}:
  uriParameters:
    RemoveTagsFromResource?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Remove Tags From Resource
  post:
    description: Removes metadata tags from an Amazon RDS resource. For an overview on tagging an Amazon RDS resource, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Overview.Tagging.html">Tagging Amazon RDS Resources</a>.
    displayName: Remove Tags From Resource
    queryParameters:
      Action:
        type: string
        enum:
        - RemoveTagsFromResource
    body:
      application/xml: RemoveTagsFromResourceMessage
    responses:
      200:
        body:
          application/xml: null
      404:
        description: DBInstanceNotFound
/{ResetDBClusterParameterGroup}:
  uriParameters:
    ResetDBClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Reset D BCluster Parameter Group
  post:
    description: ' Modifies the parameters of a DB cluster parameter group to the
      default value. To reset specific parameters submit a list of the following:
      <code>ParameterName</code> and <code>ApplyMethod</code>. To reset the entire
      DB cluster parameter group, specify the <code>DBClusterParameterGroupName</code>
      and <code>ResetAllParameters</code> parameters.   When resetting the entire
      group, dynamic parameters are updated immediately and static parameters are
      set to <code>pending-reboot</code> to take effect on the next DB instance restart
      or <a>RebootDBInstance</a> request. You must call <a>RebootDBInstance</a> for
      every DB instance in your DB cluster that you want the updated static parameter
      to apply to. For more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Reset D BCluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - ResetDBClusterParameterGroup
    body:
      application/xml: ResetDBClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: DBClusterParameterGroupNameMessage
      400:
        description: InvalidDBParameterGroupState
      404:
        description: DBParameterGroupNotFound
/{ResetDBParameterGroup}:
  uriParameters:
    ResetDBParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Reset D BParameter Group
  post:
    description: ' Modifies the parameters of a DB parameter group to the engine/system
      default value. To reset specific parameters submit a list of the following:
      <code>ParameterName</code> and <code>ApplyMethod</code>. To reset the entire
      DB parameter group, specify the <code>DBParameterGroup</code> name and <code>ResetAllParameters</code>
      parameters. When resetting the entire group, dynamic parameters are updated
      immediately and static parameters are set to <code>pending-reboot</code> to
      take effect on the next DB instance restart or <code>RebootDBInstance</code>
      request. '
    displayName: Reset D BParameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - ResetDBParameterGroup
    body:
      application/xml: ResetDBParameterGroupMessage
    responses:
      200:
        body:
          application/xml: DBParameterGroupNameMessage
      400:
        description: InvalidDBParameterGroupState
      404:
        description: DBParameterGroupNotFound
/{RestoreDBClusterFromS3}:
  uriParameters:
    RestoreDBClusterFromS3?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Restore D BCluster FromS3
  post:
    description: Creates an Amazon Aurora DB cluster from data stored in an Amazon S3 bucket. Amazon RDS must be authorized to access the Amazon S3 bucket and the data must be created using the Percona XtraBackup utility as described in <a href="AmazonRDS/latest/UserGuide/Aurora.Migrate.MySQL.html#Aurora.Migrate.MySQL.S3">Migrating Data from MySQL by Using an Amazon S3 Bucket</a>.
    displayName: Restore D BCluster FromS3
    queryParameters:
      Action:
        type: string
        enum:
        - RestoreDBClusterFromS3
    body:
      application/xml: RestoreDBClusterFromS3Message
    responses:
      200:
        body:
          application/xml: RestoreDBClusterFromS3Result
      400:
        description: DBClusterAlreadyExistsFault
      403:
        description: DBClusterQuotaExceededFault
      404:
        description: DBSubnetGroupNotFoundFault
/{RestoreDBClusterFromSnapshot}:
  uriParameters:
    RestoreDBClusterFromSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Restore D BCluster FromSnapshot
  post:
    description: 'Creates a new DB cluster from a DB cluster snapshot. The target
      DB cluster is created from the source DB cluster restore point with the same
      configuration as the original source DB cluster, except that the new DB cluster
      is created with the default security group. For more information on Amazon Aurora,
      see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Restore D BCluster FromSnapshot
    queryParameters:
      Action:
        type: string
        enum:
        - RestoreDBClusterFromSnapshot
    body:
      application/xml: RestoreDBClusterFromSnapshotMessage
    responses:
      200:
        body:
          application/xml: RestoreDBClusterFromSnapshotResult
      400:
        description: DBClusterAlreadyExistsFault
      403:
        description: DBClusterQuotaExceededFault
      404:
        description: DBSubnetGroupNotFoundFault
/{RestoreDBClusterToPointInTime}:
  uriParameters:
    RestoreDBClusterToPointInTime?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Restore D BCluster To PointIn Time
  post:
    description: ' Restores a DB cluster to an arbitrary point in time. Users can
      restore to any point in time before <code>LatestRestorableTime</code> for up
      to <code>BackupRetentionPeriod</code> days. The target DB cluster is created
      from the source DB cluster with the same configuration as the original DB cluster,
      except that the new DB cluster is created with the default DB security group.  For
      more information on Amazon Aurora, see <a href="http://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/CHAP_Aurora.html">Aurora
      on Amazon RDS</a> in the <i>Amazon RDS User Guide.</i> '
    displayName: Restore D BCluster To PointIn Time
    queryParameters:
      Action:
        type: string
        enum:
        - RestoreDBClusterToPointInTime
    body:
      application/xml: RestoreDBClusterToPointInTimeMessage
    responses:
      200:
        body:
          application/xml: RestoreDBClusterToPointInTimeResult
      400:
        description: DBClusterAlreadyExistsFault
      403:
        description: DBClusterQuotaExceededFault
      404:
        description: DBSubnetGroupNotFoundFault
/{RestoreDBInstanceFromDBSnapshot}:
  uriParameters:
    RestoreDBInstanceFromDBSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Restore D BInstance FromD B Snapshot
  post:
    description: Creates a new DB instance from a DB snapshot. The target database is created from the source database restore point with the most of original configuration with the default security group and the default DB parameter group. By default, the new DB instance is created as a single-AZ deployment except when the instance is a SQL Server instance that has an option group that is associated with mirroring; in this case, the instance becomes a mirrored AZ deployment and not a single-AZ deployment. If your intent is to replace your original DB instance with the new, restored DB instance, then rename your original DB instance before you call the RestoreDBInstanceFromDBSnapshot action. RDS does not allow two DB instances with the same name. Once you have renamed your original DB instance with a different identifier, then you can pass the original name of the DB instance as the DBInstanceIdentifier in the call to the RestoreDBInstanceFromDBSnapshot action. The result is that you will replace the original DB instance with the DB instance created from the snapshot. If you are restoring from a shared manual DB snapshot, the <code>DBSnapshotIdentifier</code> must be the ARN of the shared DB snapshot.
    displayName: Restore D BInstance FromD B Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - RestoreDBInstanceFromDBSnapshot
    body:
      application/xml: RestoreDBInstanceFromDBSnapshotMessage
    responses:
      200:
        body:
          application/xml: RestoreDBInstanceFromDBSnapshotResult
      400:
        description: DBInstanceAlreadyExists
      404:
        description: DBSnapshotNotFound
/{RestoreDBInstanceToPointInTime}:
  uriParameters:
    RestoreDBInstanceToPointInTime?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Restore D BInstance To PointIn Time
  post:
    description: Restores a DB instance to an arbitrary point in time. You can restore to any point in time before the time identified by the LatestRestorableTime property. You can restore to a point up to the number of days specified by the BackupRetentionPeriod property. The target database is created with most of the original configuration, but in a system-selected availability zone, with the default security group, the default subnet group, and the default DB parameter group. By default, the new DB instance is created as a single-AZ deployment except when the instance is a SQL Server instance that has an option group that is associated with mirroring; in this case, the instance becomes a mirrored deployment and not a single-AZ deployment.
    displayName: Restore D BInstance To PointIn Time
    queryParameters:
      Action:
        type: string
        enum:
        - RestoreDBInstanceToPointInTime
    body:
      application/xml: RestoreDBInstanceToPointInTimeMessage
    responses:
      200:
        body:
          application/xml: RestoreDBInstanceToPointInTimeResult
      400:
        description: DBInstanceAlreadyExists
      404:
        description: DBInstanceNotFound
/{RevokeDBSecurityGroupIngress}:
  uriParameters:
    RevokeDBSecurityGroupIngress?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Revoke D BSecurity Group Ingress
  post:
    description: Revokes ingress from a DBSecurityGroup for previously authorized IP ranges or EC2 or VPC Security Groups. Required parameters for this API are one of CIDRIP, EC2SecurityGroupId for VPC, or (EC2SecurityGroupOwnerId and either EC2SecurityGroupName or EC2SecurityGroupId).
    displayName: Revoke D BSecurity Group Ingress
    queryParameters:
      Action:
        type: string
        enum:
        - RevokeDBSecurityGroupIngress
    body:
      application/xml: RevokeDBSecurityGroupIngressMessage
    responses:
      200:
        body:
          application/xml: RevokeDBSecurityGroupIngressResult
      404:
        description: DBSecurityGroupNotFound
      400:
        description: InvalidDBSecurityGroupState
