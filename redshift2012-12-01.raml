#%RAML 1.0
title: Amazon Redshift
version: '2012-12-01'
description: '   <b>Overview</b>   This is an interface reference for Amazon Redshift.
  It contains documentation for one of the programming or command line interfaces
  you can use to manage Amazon Redshift clusters. Note that Amazon Redshift is asynchronous,
  which means that some interfaces may require techniques, such as polling or asynchronous
  callback handlers, to determine when a command has been applied. In this reference,
  the parameter descriptions indicate whether a change is applied immediately, on
  the next instance reboot, or during the next maintenance window. For a summary of
  the Amazon Redshift cluster management interfaces, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/using-aws-sdk.html">Using
  the Amazon Redshift Management Interfaces</a>.  Amazon Redshift manages all the
  work of setting up, operating, and scaling a data warehouse: provisioning capacity,
  monitoring and backing up the cluster, and applying patches and upgrades to the
  Amazon Redshift engine. You can focus on using your data to acquire new insights
  for your business and customers.  If you are a first-time user of Amazon Redshift,
  we recommend that you begin by reading the <a href="http://docs.aws.amazon.com/redshift/latest/gsg/getting-started.html">Amazon
  Redshift Getting Started Guide</a>.  If you are a database developer, the <a href="http://docs.aws.amazon.com/redshift/latest/dg/welcome.html">Amazon
  Redshift Database Developer Guide</a> explains how to design, build, query, and
  maintain the databases that make up your data warehouse. '
mediaType:
- application/xml
uses:
  commons: https://raw.githubusercontent.com/apiregistry/commons/master/commons.raml
  extras: https://raw.githubusercontent.com/apiregistry/typesExtras/master/typeExtras.raml
types:
  AccountsWithRestoreAccessList:
    type: array
    items:
      type: AccountWithRestoreAccess
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  AvailabilityZoneList:
    type: array
    items:
      type: AvailabilityZone
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  Boolean:
    type: boolean
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  BooleanOptional:
    type: boolean
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterIamRoleList:
    type: array
    items:
      type: ClusterIamRole
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterList:
    type: array
    items:
      type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterNodesList:
    type: array
    items:
      type: ClusterNode
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterParameterGroupStatusList:
    type: array
    items:
      type: ClusterParameterGroupStatus
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterParameterStatusList:
    type: array
    items:
      type: ClusterParameterStatus
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterSecurityGroupMembershipList:
    type: array
    items:
      type: ClusterSecurityGroupMembership
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterSecurityGroupNameList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterSecurityGroups:
    type: array
    items:
      type: ClusterSecurityGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterSubnetGroups:
    type: array
    items:
      type: ClusterSubnetGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ClusterVersionList:
    type: array
    items:
      type: ClusterVersion
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  Double:
    type: number
    format: double
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  DoubleOptional:
    type: number
    format: double
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  EC2SecurityGroupList:
    type: array
    items:
      type: EC2SecurityGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  EventCategoriesList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  EventCategoriesMapList:
    type: array
    items:
      type: EventCategoriesMap
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  EventInfoMapList:
    type: array
    items:
      type: EventInfoMap
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  EventList:
    type: array
    items:
      type: Event
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  EventSubscriptionsList:
    type: array
    items:
      type: EventSubscription
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  HsmClientCertificateList:
    type: array
    items:
      type: HsmClientCertificate
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  HsmConfigurationList:
    type: array
    items:
      type: HsmConfiguration
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  IPRangeList:
    type: array
    items:
      type: IPRange
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  IamRoleArnList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ImportTablesCompleted:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ImportTablesInProgress:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ImportTablesNotStarted:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  Integer:
    type: integer
    format: int
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  IntegerOptional:
    type: integer
    format: int
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  Long:
    type: integer
    format: long
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  LongOptional:
    type: integer
    format: long
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  OrderableClusterOptionsList:
    type: array
    items:
      type: OrderableClusterOption
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ParameterApplyType:
    type: string
    enum:
    - static
    - dynamic
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ParameterGroupList:
    type: array
    items:
      type: ClusterParameterGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ParametersList:
    type: array
    items:
      type: Parameter
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RecurringChargeList:
    type: array
    items:
      type: RecurringCharge
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ReservedNodeList:
    type: array
    items:
      type: ReservedNode
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ReservedNodeOfferingList:
    type: array
    items:
      type: ReservedNodeOffering
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RestorableNodeTypeList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  SnapshotCopyGrantList:
    type: array
    items:
      type: SnapshotCopyGrant
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  SnapshotList:
    type: array
    items:
      type: Snapshot
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  SourceIdsList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  SourceType:
    type: string
    enum:
    - cluster
    - cluster-parameter-group
    - cluster-security-group
    - cluster-snapshot
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  String:
    type: string
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  SubnetIdentifierList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  SubnetList:
    type: array
    items:
      type: Subnet
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  TStamp:
    type: datetime
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  TableRestoreStatusList:
    type: array
    items:
      type: TableRestoreStatus
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  TableRestoreStatusType:
    type: string
    enum:
    - PENDING
    - IN_PROGRESS
    - SUCCEEDED
    - FAILED
    - CANCELED
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  TagKeyList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  TagList:
    type: array
    items:
      type: Tag
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  TagValueList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  TaggedResourceList:
    type: array
    items:
      type: TaggedResource
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  VpcSecurityGroupIdList:
    type: array
    items:
      type: String
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  VpcSecurityGroupMembershipList:
    type: array
    items:
      type: VpcSecurityGroupMembership
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  AccountWithRestoreAccess:
    type: object
    properties:
      AccountId?:
        type: String
        description: <p>The identifier of an AWS customer account authorized to restore a snapshot.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes an AWS customer account authorized to restore a snapshot.</p>
  AuthorizeClusterSecurityGroupIngressMessage:
    type: object
    properties:
      ClusterSecurityGroupName:
        type: String
        description: <p>The name of the security group to which the ingress rule is added.</p>
      CIDRIP?:
        type: String
        description: <p>The IP range to be added the Amazon Redshift security group.</p>
      EC2SecurityGroupName?:
        type: String
        description: <p>The EC2 security group to be added the Amazon Redshift security group.</p>
      EC2SecurityGroupOwnerId?:
        type: String
        description: '<p>The AWS account number of the owner of the security group
          specified by the <i>EC2SecurityGroupName</i> parameter. The AWS Access Key
          ID is not an acceptable value. </p> <p>Example: <code>111122223333</code>
          </p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  AuthorizeClusterSecurityGroupIngressResult:
    type: object
    properties:
      ClusterSecurityGroup?:
        type: ClusterSecurityGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  AuthorizeSnapshotAccessMessage:
    type: object
    properties:
      SnapshotIdentifier:
        type: String
        description: <p>The identifier of the snapshot the account is authorized to restore.</p>
      SnapshotClusterIdentifier?:
        type: String
        description: <p>The identifier of the cluster the snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.</p>
      AccountWithRestoreAccess:
        type: String
        description: <p>The identifier of the AWS customer account authorized to restore the specified snapshot.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  AuthorizeSnapshotAccessResult:
    type: object
    properties:
      Snapshot?:
        type: Snapshot
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  AvailabilityZone:
    type: object
    properties:
      Name?:
        type: String
        description: <p>The name of the availability zone.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes an availability zone.</p>
  Cluster:
    type: object
    properties:
      ClusterIdentifier?:
        type: String
        description: <p>The unique identifier of the cluster.</p>
      NodeType?:
        type: String
        description: <p>The node type for the nodes in the cluster.</p>
      ClusterStatus?:
        type: String
        description: <p> The current state of the cluster. Possible values are the following:</p> <ul> <li> <p> <code>available</code> </p> </li> <li> <p> <code>creating</code> </p> </li> <li> <p> <code>deleting</code> </p> </li> <li> <p> <code>final-snapshot</code> </p> </li> <li> <p> <code>hardware-failure</code> </p> </li> <li> <p> <code>incompatible-hsm</code> </p> </li> <li> <p> <code>incompatible-network</code> </p> </li> <li> <p> <code>incompatible-parameters</code> </p> </li> <li> <p> <code>incompatible-restore</code> </p> </li> <li> <p> <code>modifying</code> </p> </li> <li> <p> <code>rebooting</code> </p> </li> <li> <p> <code>renaming</code> </p> </li> <li> <p> <code>resizing</code> </p> </li> <li> <p> <code>rotating-keys</code> </p> </li> <li> <p> <code>storage-full</code> </p> </li> <li> <p> <code>updating-hsm</code> </p> </li> </ul>
      ModifyStatus?:
        type: String
        description: <p>The status of a modify operation, if any, initiated for the cluster.</p>
      MasterUsername?:
        type: String
        description: <p>The master user name for the cluster. This name is used to connect to the database that is specified in the <b>DBName</b> parameter. </p>
      DBName?:
        type: String
        description: <p>The name of the initial database that was created when the cluster was created. This same name is returned for the life of the cluster. If an initial database was not specified, a database named <code>dev</code>dev was created by default. </p>
      Endpoint?:
        type: Endpoint
        description: <p>The connection endpoint.</p>
      ClusterCreateTime?:
        type: TStamp
        description: <p>The date and time that the cluster was created.</p>
      AutomatedSnapshotRetentionPeriod?:
        type: Integer
        description: <p>The number of days that automatic cluster snapshots are retained.</p>
      ClusterSecurityGroups?:
        type: ClusterSecurityGroupMembershipList
        description: <p>A list of cluster security group that are associated with the cluster. Each security group is represented by an element that contains <code>ClusterSecurityGroup.Name</code> and <code>ClusterSecurityGroup.Status</code> subelements. </p> <p>Cluster security groups are used when the cluster is not created in an Amazon Virtual Private Cloud (VPC). Clusters that are created in a VPC use VPC security groups, which are listed by the <b>VpcSecurityGroups</b> parameter. </p>
      VpcSecurityGroups?:
        type: VpcSecurityGroupMembershipList
        description: <p>A list of Amazon Virtual Private Cloud (Amazon VPC) security groups that are associated with the cluster. This parameter is returned only if the cluster is in a VPC.</p>
      ClusterParameterGroups?:
        type: ClusterParameterGroupStatusList
        description: <p>The list of cluster parameter groups that are associated with this cluster. Each parameter group in the list is returned with its status.</p>
      ClusterSubnetGroupName?:
        type: String
        description: <p>The name of the subnet group that is associated with the cluster. This parameter is valid only when the cluster is in a VPC.</p>
      VpcId?:
        type: String
        description: <p>The identifier of the VPC the cluster is in, if the cluster is in a VPC.</p>
      AvailabilityZone?:
        type: String
        description: <p>The name of the Availability Zone in which the cluster is located.</p>
      PreferredMaintenanceWindow?:
        type: String
        description: <p>The weekly time range, in Universal Coordinated Time (UTC), during which system maintenance can occur.</p>
      PendingModifiedValues?:
        type: PendingModifiedValues
        description: <p>A value that, if present, indicates that changes to the cluster are pending. Specific pending changes are identified by subelements.</p>
      ClusterVersion?:
        type: String
        description: <p>The version ID of the Amazon Redshift engine that is running on the cluster.</p>
      AllowVersionUpgrade?:
        type: Boolean
        description: <p>A Boolean value that, if <code>true</code>, indicates that major version upgrades will be applied automatically to the cluster during the maintenance window. </p>
      NumberOfNodes?:
        type: Integer
        description: <p>The number of compute nodes in the cluster.</p>
      PubliclyAccessible?:
        type: Boolean
        description: <p>A Boolean value that, if <code>true</code>, indicates that the cluster can be accessed from a public network.</p>
      Encrypted?:
        type: Boolean
        description: <p>A Boolean value that, if <code>true</code>, indicates that data in the cluster is encrypted at rest.</p>
      RestoreStatus?:
        type: RestoreStatus
        description: <p>A value that describes the status of a cluster restore action. This parameter returns null if the cluster was not created by restoring a snapshot.</p>
      HsmStatus?:
        type: HsmStatus
        description: '<p>A value that reports whether the Amazon Redshift cluster
          has finished applying any hardware security module (HSM) settings changes
          specified in a modify cluster command.</p> <p>Values: active, applying</p>'
      ClusterSnapshotCopyStatus?:
        type: ClusterSnapshotCopyStatus
        description: <p>A value that returns the destination region and retention period that are configured for cross-region snapshot copy.</p>
      ClusterPublicKey?:
        type: String
        description: <p>The public key for the cluster.</p>
      ClusterNodes?:
        type: ClusterNodesList
        description: <p>The nodes in the cluster.</p>
      ElasticIpStatus?:
        type: ElasticIpStatus
        description: <p>The status of the elastic IP (EIP) address.</p>
      ClusterRevisionNumber?:
        type: String
        description: <p>The specific revision number of the database in the cluster.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the cluster.</p>
      KmsKeyId?:
        type: String
        description: <p>The AWS Key Management Service (AWS KMS) key ID of the encryption key used to encrypt data in the cluster.</p>
      EnhancedVpcRouting?:
        type: Boolean
        description: '<p>An option that specifies whether to create the cluster with
          enhanced VPC routing enabled. To create a cluster that uses enhanced VPC
          routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced
          VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If
          this option is <code>true</code>, enhanced VPC routing is enabled. </p>
          <p>Default: false</p>'
      IamRoles?:
        type: ClusterIamRoleList
        description: <p>A list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a cluster.</p>
  ClusterIamRole:
    type: object
    properties:
      IamRoleArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the IAM role, for example, <code>arn:aws:iam::123456789012:role/RedshiftCopyUnload</code>. </p>
      ApplyStatus?:
        type: String
        description: '<p>A value that describes the status of the IAM role''s association
          with an Amazon Redshift cluster.</p> <p>The following are possible statuses
          and descriptions.</p> <ul> <li> <p> <code>in-sync</code>: The role is available
          for use by the cluster.</p> </li> <li> <p> <code>adding</code>: The role
          is in the process of being associated with the cluster.</p> </li> <li> <p>
          <code>removing</code>: The role is in the process of being disassociated
          with the cluster.</p> </li> </ul>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>An AWS Identity and Access Management (IAM) role that can be used by the associated Amazon Redshift cluster to access other AWS services.</p>
  ClusterNode:
    type: object
    properties:
      NodeRole?:
        type: String
        description: <p>Whether the node is a leader node or a compute node.</p>
      PrivateIPAddress?:
        type: String
        description: <p>The private IP address of a node within a cluster.</p>
      PublicIPAddress?:
        type: String
        description: <p>The public IP address of a node within a cluster.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>The identifier of a node in a cluster.</p>
  ClusterParameterGroup:
    type: object
    properties:
      ParameterGroupName?:
        type: String
        description: <p>The name of the cluster parameter group.</p>
      ParameterGroupFamily?:
        type: String
        description: <p>The name of the cluster parameter group family that this cluster parameter group is compatible with.</p>
      Description?:
        type: String
        description: <p>The description of the parameter group.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the cluster parameter group.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a parameter group.</p>
  ClusterParameterGroupDetails:
    type: object
    properties:
      Parameters?:
        type: ParametersList
        description: <p>A list of <a>Parameter</a> instances. Each instance lists the parameters of one cluster parameter group. </p>
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <a>DescribeClusterParameters</a> action. </p>
  ClusterParameterGroupNameMessage:
    type: object
    properties:
      ParameterGroupName?:
        type: String
        description: <p>The name of the cluster parameter group.</p>
      ParameterGroupStatus?:
        type: String
        description: <p>The status of the parameter group. For example, if you made a change to a parameter group name-value pair, then the change could be pending a reboot of an associated cluster.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ClusterParameterGroupStatus:
    type: object
    properties:
      ParameterGroupName?:
        type: String
        description: <p>The name of the cluster parameter group.</p>
      ParameterApplyStatus?:
        type: String
        description: <p>The status of parameter updates.</p>
      ClusterParameterStatusList?:
        type: ClusterParameterStatusList
        description: <p>The list of parameter statuses.</p> <p> For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the status of a parameter group.</p>
  ClusterParameterGroupsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      ParameterGroups?:
        type: ParameterGroupList
        description: <p>A list of <a>ClusterParameterGroup</a> instances. Each instance describes one cluster parameter group. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <a>DescribeClusterParameterGroups</a> action. </p>
  ClusterParameterStatus:
    type: object
    properties:
      ParameterName?:
        type: String
        description: <p>The name of the parameter.</p>
      ParameterApplyStatus?:
        type: String
        description: '<p>The status of the parameter that indicates whether the parameter
          is in sync with the database, waiting for a cluster reboot, or encountered
          an error when being applied.</p> <p>The following are possible statuses
          and descriptions.</p> <ul> <li> <p> <code>in-sync</code>: The parameter
          value is in sync with the database.</p> </li> <li> <p> <code>pending-reboot</code>:
          The parameter value will be applied after the cluster reboots.</p> </li>
          <li> <p> <code>applying</code>: The parameter value is being applied to
          the database.</p> </li> <li> <p> <code>invalid-parameter</code>: Cannot
          apply the parameter value because it has an invalid value or syntax.</p>
          </li> <li> <p> <code>apply-deferred</code>: The parameter contains static
          property changes. The changes are deferred until the cluster reboots.</p>
          </li> <li> <p> <code>apply-error</code>: Cannot connect to the cluster.
          The parameter change will be applied after the cluster reboots.</p> </li>
          <li> <p> <code>unknown-error</code>: Cannot apply the parameter change right
          now. The change will be applied after the cluster reboots.</p> </li> </ul>'
      ParameterApplyErrorDescription?:
        type: String
        description: <p>The error that prevented the parameter from being applied to the database.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the status of a parameter group.</p>
  ClusterSecurityGroup:
    type: object
    properties:
      ClusterSecurityGroupName?:
        type: String
        description: <p>The name of the cluster security group to which the operation was applied.</p>
      Description?:
        type: String
        description: <p>A description of the security group.</p>
      EC2SecurityGroups?:
        type: EC2SecurityGroupList
        description: <p>A list of EC2 security groups that are permitted to access clusters associated with this cluster security group.</p>
      IPRanges?:
        type: IPRangeList
        description: <p>A list of IP ranges (CIDR blocks) that are permitted to access clusters associated with this cluster security group.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the cluster security group.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a security group.</p>
  ClusterSecurityGroupMembership:
    type: object
    properties:
      ClusterSecurityGroupName?:
        type: String
        description: <p>The name of the cluster security group.</p>
      Status?:
        type: String
        description: <p>The status of the cluster security group.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a cluster security group.</p>
  ClusterSecurityGroupMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      ClusterSecurityGroups?:
        type: ClusterSecurityGroups
        description: <p>A list of <a>ClusterSecurityGroup</a> instances. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ClusterSnapshotCopyStatus:
    type: object
    properties:
      DestinationRegion?:
        type: String
        description: <p>The destination region that snapshots are automatically copied to when cross-region snapshot copy is enabled.</p>
      RetentionPeriod?:
        type: Long
        description: <p>The number of days that automated snapshots are retained in the destination region after they are copied from a source region.</p>
      SnapshotCopyGrantName?:
        type: String
        description: <p>The name of the snapshot copy grant.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Returns the destination region and retention period that are configured for cross-region snapshot copy.</p>
  ClusterSubnetGroup:
    type: object
    properties:
      ClusterSubnetGroupName?:
        type: String
        description: <p>The name of the cluster subnet group.</p>
      Description?:
        type: String
        description: <p>The description of the cluster subnet group.</p>
      VpcId?:
        type: String
        description: <p>The VPC ID of the cluster subnet group.</p>
      SubnetGroupStatus?:
        type: String
        description: <p>The status of the cluster subnet group. Possible values are <code>Complete</code>, <code>Incomplete</code> and <code>Invalid</code>. </p>
      Subnets?:
        type: SubnetList
        description: <p>A list of the VPC <a>Subnet</a> elements. </p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the cluster subnet group.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a subnet group.</p>
  ClusterSubnetGroupMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      ClusterSubnetGroups?:
        type: ClusterSubnetGroups
        description: <p>A list of <a>ClusterSubnetGroup</a> instances. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <a>DescribeClusterSubnetGroups</a> action. </p>
  ClusterVersion:
    type: object
    properties:
      ClusterVersion?:
        type: String
        description: <p>The version number used by the cluster.</p>
      ClusterParameterGroupFamily?:
        type: String
        description: <p>The name of the cluster parameter group family for the cluster.</p>
      Description?:
        type: String
        description: <p>The description of the cluster version.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a cluster version, including the parameter group family and description of the version.</p>
  ClusterVersionsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      ClusterVersions?:
        type: ClusterVersionList
        description: <p>A list of <code>Version</code> elements. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <a>DescribeClusterVersions</a> action. </p>
  ClustersMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      Clusters?:
        type: ClusterList
        description: <p>A list of <code>Cluster</code> objects, where each object describes one cluster. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <a>DescribeClusters</a> action. </p>
  CopyClusterSnapshotMessage:
    type: object
    properties:
      SourceSnapshotIdentifier:
        type: String
        description: <p>The identifier for the source snapshot.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier for a valid automated snapshot whose state is <code>available</code>.</p> </li> </ul>
      SourceSnapshotClusterIdentifier?:
        type: String
        description: <p>The identifier of the cluster the source snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.</p> <p>Constraints:</p> <ul> <li> <p>Must be the identifier for a valid cluster.</p> </li> </ul>
      TargetSnapshotIdentifier:
        type: String
        description: <p>The identifier given to the new manual snapshot.</p> <p>Constraints:</p> <ul> <li> <p>Cannot be null, empty, or blank.</p> </li> <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for the AWS account that is making the request.</p> </li> </ul>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CopyClusterSnapshotResult:
    type: object
    properties:
      Snapshot?:
        type: Snapshot
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateClusterMessage:
    type: object
    properties:
      DBName?:
        type: String
        description: '<p>The name of the first database to be created when the cluster
          is created.</p> <p>To create additional databases after the cluster is created,
          connect to the cluster with a SQL client and use SQL commands to create
          a database. For more information, go to <a href="http://docs.aws.amazon.com/redshift/latest/dg/t_creating_database.html">Create
          a Database</a> in the Amazon Redshift Database Developer Guide. </p> <p>Default:
          <code>dev</code> </p> <p>Constraints:</p> <ul> <li> <p>Must contain 1 to
          64 alphanumeric characters.</p> </li> <li> <p>Must contain only lowercase
          letters.</p> </li> <li> <p>Cannot be a word that is reserved by the service.
          A list of reserved words can be found in <a href="http://docs.aws.amazon.com/redshift/latest/dg/r_pg_keywords.html">Reserved
          Words</a> in the Amazon Redshift Database Developer Guide. </p> </li> </ul>'
      ClusterIdentifier:
        type: String
        description: '<p>A unique identifier for the cluster. You use this identifier
          to refer to the cluster for any subsequent cluster operations such as deleting
          or modifying. The identifier also appears in the Amazon Redshift console.</p>
          <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric
          characters or hyphens.</p> </li> <li> <p>Alphabetic characters must be lowercase.</p>
          </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot
          end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must
          be unique for all clusters within an AWS account.</p> </li> </ul> <p>Example:
          <code>myexamplecluster</code> </p>'
      ClusterType?:
        type: String
        description: '<p>The type of the cluster. When cluster type is specified as</p>
          <ul> <li> <p> <code>single-node</code>, the <b>NumberOfNodes</b> parameter
          is not required.</p> </li> <li> <p> <code>multi-node</code>, the <b>NumberOfNodes</b>
          parameter is required.</p> </li> </ul> <p>Valid Values: <code>multi-node</code>
          | <code>single-node</code> </p> <p>Default: <code>multi-node</code> </p>'
      NodeType:
        type: String
        description: '<p>The node type to be provisioned for the cluster. For information
          about node types, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#how-many-nodes">
          Working with Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
          </p> <p>Valid Values: <code>ds1.xlarge</code> | <code>ds1.8xlarge</code>
          | <code>ds2.xlarge</code> | <code>ds2.8xlarge</code> | <code>dc1.large</code>
          | <code>dc1.8xlarge</code>. </p>'
      MasterUsername:
        type: String
        description: <p>The user name associated with the master user account for the cluster that is being created.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 - 128 alphanumeric characters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot be a reserved word. A list of reserved words can be found in <a href="http://docs.aws.amazon.com/redshift/latest/dg/r_pg_keywords.html">Reserved Words</a> in the Amazon Redshift Database Developer Guide. </p> </li> </ul>
      MasterUserPassword:
        type: String
        description: <p>The password associated with the master user account for the cluster that is being created.</p> <p>Constraints:</p> <ul> <li> <p>Must be between 8 and 64 characters in length.</p> </li> <li> <p>Must contain at least one uppercase letter.</p> </li> <li> <p>Must contain at least one lowercase letter.</p> </li> <li> <p>Must contain one number.</p> </li> <li> <p>Can be any printable ASCII character (ASCII code 33 to 126) except ' (single quote), " (double quote), \, /, @, or space.</p> </li> </ul>
      ClusterSecurityGroups?:
        type: ClusterSecurityGroupNameList
        description: '<p>A list of security groups to be associated with this cluster.</p>
          <p>Default: The default cluster security group for Amazon Redshift.</p>'
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: '<p>A list of Virtual Private Cloud (VPC) security groups to
          be associated with the cluster.</p> <p>Default: The default VPC security
          group is associated with the cluster.</p>'
      ClusterSubnetGroupName?:
        type: String
        description: <p>The name of a cluster subnet group to be associated with this cluster.</p> <p>If this parameter is not provided the resulting cluster will be deployed outside virtual private cloud (VPC).</p>
      AvailabilityZone?:
        type: String
        description: '<p>The EC2 Availability Zone (AZ) in which you want Amazon Redshift
          to provision the cluster. For example, if you have several EC2 instances
          running in a specific Availability Zone, then you might want the cluster
          to be provisioned in the same zone in order to decrease network latency.</p>
          <p>Default: A random, system-chosen Availability Zone in the region that
          is specified by the endpoint.</p> <p>Example: <code>us-east-1d</code> </p>
          <p>Constraint: The specified Availability Zone must be in the same region
          as the current endpoint.</p>'
      PreferredMaintenanceWindow?:
        type: String
        description: '<p>The weekly time range (in UTC) during which automated cluster
          maintenance can occur.</p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code>
          </p> <p> Default: A 30-minute window selected at random from an 8-hour block
          of time per region, occurring on a random day of the week. For more information
          about the time blocks for each region, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#rs-maintenance-windows">Maintenance
          Windows</a> in Amazon Redshift Cluster Management Guide.</p> <p>Valid Days:
          Mon | Tue | Wed | Thu | Fri | Sat | Sun</p> <p>Constraints: Minimum 30-minute
          window.</p>'
      ClusterParameterGroupName?:
        type: String
        description: '<p>The name of the parameter group to be associated with this
          cluster.</p> <p>Default: The default Amazon Redshift cluster parameter group.
          For information about the default parameter group, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Working
          with Amazon Redshift Parameter Groups</a> </p> <p>Constraints:</p> <ul>
          <li> <p>Must be 1 to 255 alphanumeric characters or hyphens.</p> </li> <li>
          <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a
          hyphen or contain two consecutive hyphens.</p> </li> </ul>'
      AutomatedSnapshotRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days that automated snapshots are retained.
          If the value is 0, automated snapshots are disabled. Even if automated snapshots
          are disabled, you can still create manual snapshots when you want with <a>CreateClusterSnapshot</a>.
          </p> <p>Default: <code>1</code> </p> <p>Constraints: Must be a value from
          0 to 35.</p>'
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the cluster accepts incoming connections.</p>
          <p>The cluster is accessible only via the JDBC and ODBC connection strings.
          Part of the connection string requires the port on which the cluster will
          listen for incoming connections.</p> <p>Default: <code>5439</code> </p>
          <p>Valid Values: <code>1150-65535</code> </p>'
      ClusterVersion?:
        type: String
        description: '<p>The version of the Amazon Redshift engine software that you
          want to deploy on the cluster.</p> <p>The version selected runs on all the
          nodes in the cluster.</p> <p>Constraints: Only version 1.0 is currently
          available.</p> <p>Example: <code>1.0</code> </p>'
      AllowVersionUpgrade?:
        type: BooleanOptional
        description: '<p>If <code>true</code>, major version upgrades can be applied
          during the maintenance window to the Amazon Redshift engine that is running
          on the cluster.</p> <p>When a new major version of the Amazon Redshift engine
          is released, you can request that the service automatically apply upgrades
          during the maintenance window to the Amazon Redshift engine that is running
          on your cluster.</p> <p>Default: <code>true</code> </p>'
      NumberOfNodes?:
        type: IntegerOptional
        description: '<p>The number of compute nodes in the cluster. This parameter
          is required when the <b>ClusterType</b> parameter is specified as <code>multi-node</code>.
          </p> <p>For information about determining how many nodes you need, go to
          <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#how-many-nodes">
          Working with Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
          </p> <p>If you don''t specify this parameter, you get a single-node cluster.
          When requesting a multi-node cluster, you must specify the number of nodes
          that you want in the cluster.</p> <p>Default: <code>1</code> </p> <p>Constraints:
          Value must be at least 1 and no more than 100.</p>'
      PubliclyAccessible?:
        type: BooleanOptional
        description: <p>If <code>true</code>, the cluster can be accessed from a public network. </p>
      Encrypted?:
        type: BooleanOptional
        description: '<p>If <code>true</code>, the data in the cluster is encrypted
          at rest. </p> <p>Default: false</p>'
      HsmClientCertificateIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.</p>
      HsmConfigurationIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.</p>
      ElasticIp?:
        type: String
        description: '<p>The Elastic IP (EIP) address for the cluster.</p> <p>Constraints:
          The cluster must be provisioned in EC2-VPC and publicly-accessible through
          an Internet gateway. For more information about provisioning clusters in
          EC2-VPC, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#cluster-platforms">Supported
          Platforms to Launch Your Cluster</a> in the Amazon Redshift Cluster Management
          Guide.</p>'
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
      KmsKeyId?:
        type: String
        description: <p>The AWS Key Management Service (KMS) key ID of the encryption key that you want to use to encrypt data in the cluster.</p>
      EnhancedVpcRouting?:
        type: BooleanOptional
        description: '<p>An option that specifies whether to create the cluster with
          enhanced VPC routing enabled. To create a cluster that uses enhanced VPC
          routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced
          VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If
          this option is <code>true</code>, enhanced VPC routing is enabled. </p>
          <p>Default: false</p>'
      AdditionalInfo?:
        type: String
        description: <p>Reserved.</p>
      IamRoles?:
        type: IamRoleArnList
        description: <p>A list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services. You must supply the IAM roles in their Amazon Resource Name (ARN) format. You can supply up to 10 IAM roles in a single request.</p> <p>A cluster can have up to 10 IAM roles associated with it at any time.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateClusterParameterGroupMessage:
    type: object
    properties:
      ParameterGroupName:
        type: String
        description: <p>The name of the cluster parameter group.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique withing your AWS account.</p> </li> </ul> <note> <p>This value is stored as a lower-case string.</p> </note>
      ParameterGroupFamily:
        type: String
        description: <p>The Amazon Redshift engine version to which the cluster parameter group applies. The cluster engine version determines the set of parameters.</p> <p>To get a list of valid parameter group family names, you can call <a>DescribeClusterParameterGroups</a>. By default, Amazon Redshift returns a list of all the parameter groups that are owned by your AWS account, including the default parameter groups for each Amazon Redshift engine version. The parameter group family names associated with the default parameter groups provide you the valid values. For example, a valid family name is "redshift-1.0". </p>
      Description:
        type: String
        description: <p>A description of the parameter group.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateClusterParameterGroupResult:
    type: object
    properties:
      ClusterParameterGroup?:
        type: ClusterParameterGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateClusterResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateClusterSecurityGroupMessage:
    type: object
    properties:
      ClusterSecurityGroupName:
        type: String
        description: '<p>The name for the security group. Amazon Redshift stores the
          value as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must contain
          no more than 255 alphanumeric characters or hyphens.</p> </li> <li> <p>Must
          not be "Default".</p> </li> <li> <p>Must be unique for all security groups
          that are created by your AWS account.</p> </li> </ul> <p>Example: <code>examplesecuritygroup</code>
          </p>'
      Description:
        type: String
        description: <p>A description for the security group.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateClusterSecurityGroupResult:
    type: object
    properties:
      ClusterSecurityGroup?:
        type: ClusterSecurityGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateClusterSnapshotMessage:
    type: object
    properties:
      SnapshotIdentifier:
        type: String
        description: '<p>A unique identifier for the snapshot that you are requesting.
          This identifier must be unique for all snapshots within the AWS account.</p>
          <p>Constraints:</p> <ul> <li> <p>Cannot be null, empty, or blank</p> </li>
          <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens</p>
          </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot
          end with a hyphen or contain two consecutive hyphens</p> </li> </ul> <p>Example:
          <code>my-snapshot-id</code> </p>'
      ClusterIdentifier:
        type: String
        description: <p>The cluster identifier for which you want a snapshot.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateClusterSnapshotResult:
    type: object
    properties:
      Snapshot?:
        type: Snapshot
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateClusterSubnetGroupMessage:
    type: object
    properties:
      ClusterSubnetGroupName:
        type: String
        description: '<p>The name for the subnet group. Amazon Redshift stores the
          value as a lowercase string.</p> <p>Constraints:</p> <ul> <li> <p>Must contain
          no more than 255 alphanumeric characters or hyphens.</p> </li> <li> <p>Must
          not be "Default".</p> </li> <li> <p>Must be unique for all subnet groups
          that are created by your AWS account.</p> </li> </ul> <p>Example: <code>examplesubnetgroup</code>
          </p>'
      Description:
        type: String
        description: <p>A description for the subnet group.</p>
      SubnetIds:
        type: SubnetIdentifierList
        description: <p>An array of VPC subnet IDs. A maximum of 20 subnets can be modified in a single request.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateClusterSubnetGroupResult:
    type: object
    properties:
      ClusterSubnetGroup?:
        type: ClusterSubnetGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateEventSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: <p>The name of the event subscription to be created.</p> <p>Constraints:</p> <ul> <li> <p>Cannot be null, empty, or blank.</p> </li> <li> <p>Must contain from 1 to 255 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
      SnsTopicArn:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the Amazon SNS topic used to transmit the event notifications. The ARN is created by Amazon SNS when you create a topic and subscribe to it.</p>
      SourceType?:
        type: String
        description: '<p>The type of source that will be generating the events. For
          example, if you want to be notified of events generated by a cluster, you
          would set this parameter to cluster. If this value is not specified, events
          are returned for all Amazon Redshift objects in your AWS account. You must
          specify a source type in order to specify source IDs.</p> <p>Valid values:
          cluster, cluster-parameter-group, cluster-security-group, and cluster-snapshot.</p>'
      SourceIds?:
        type: SourceIdsList
        description: '<p>A list of one or more identifiers of Amazon Redshift source
          objects. All of the objects must be of the same type as was specified in
          the source type parameter. The event subscription will return only events
          generated by the specified objects. If not specified, then events are returned
          for all objects within the source type specified.</p> <p>Example: my-cluster-1,
          my-cluster-2</p> <p>Example: my-snapshot-20131010</p>'
      EventCategories?:
        type: EventCategoriesList
        description: '<p>Specifies the Amazon Redshift event categories to be published
          by the event notification subscription.</p> <p>Values: Configuration, Management,
          Monitoring, Security</p>'
      Severity?:
        type: String
        description: '<p>Specifies the Amazon Redshift event severity to be published
          by the event notification subscription.</p> <p>Values: ERROR, INFO</p>'
      Enabled?:
        type: BooleanOptional
        description: <p>A Boolean value; set to <code>true</code> to activate the subscription, set to <code>false</code> to create the subscription but not active it. </p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateEventSubscriptionResult:
    type: object
    properties:
      EventSubscription?:
        type: EventSubscription
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateHsmClientCertificateMessage:
    type: object
    properties:
      HsmClientCertificateIdentifier:
        type: String
        description: <p>The identifier to be assigned to the new HSM client certificate that the cluster will use to connect to the HSM to use the database encryption keys.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateHsmClientCertificateResult:
    type: object
    properties:
      HsmClientCertificate?:
        type: HsmClientCertificate
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateHsmConfigurationMessage:
    type: object
    properties:
      HsmConfigurationIdentifier:
        type: String
        description: <p>The identifier to be assigned to the new Amazon Redshift HSM configuration.</p>
      Description:
        type: String
        description: <p>A text description of the HSM configuration to be created.</p>
      HsmIpAddress:
        type: String
        description: <p>The IP address that the Amazon Redshift cluster must use to access the HSM.</p>
      HsmPartitionName:
        type: String
        description: <p>The name of the partition in the HSM where the Amazon Redshift clusters will store their database encryption keys.</p>
      HsmPartitionPassword:
        type: String
        description: <p>The password required to access the HSM partition.</p>
      HsmServerPublicCertificate:
        type: String
        description: <p>The HSMs public certificate file. When using Cloud HSM, the file name is server.pem.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  CreateHsmConfigurationResult:
    type: object
    properties:
      HsmConfiguration?:
        type: HsmConfiguration
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateSnapshotCopyGrantMessage:
    type: object
    properties:
      SnapshotCopyGrantName:
        type: String
        description: <p>The name of the snapshot copy grant. This name must be unique in the region for the AWS account.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>Alphabetic characters must be lowercase.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for all clusters within an AWS account.</p> </li> </ul>
      KmsKeyId?:
        type: String
        description: <p>The unique identifier of the customer master key (CMK) to which to grant Amazon Redshift permission. If no key is specified, the default key is used.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>The result of the <code>CreateSnapshotCopyGrant</code> action.</p>
  CreateSnapshotCopyGrantResult:
    type: object
    properties:
      SnapshotCopyGrant?:
        type: SnapshotCopyGrant
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  CreateTagsMessage:
    type: object
    properties:
      ResourceName:
        type: String
        description: <p>The Amazon Resource Name (ARN) to which you want to add the tag or tags. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>. </p>
      Tags:
        type: TagList
        description: <p>One or more name/value pairs to add as tags to the specified resource. Each tag name is passed in with the parameter <code>Key</code> and the corresponding value is passed in with the parameter <code>Value</code>. The <code>Key</code> and <code>Value</code> parameters are separated by a comma (,). Separate multiple tags with a space. For example, <code>--tags "Key"="owner","Value"="admin" "Key"="environment","Value"="test" "Key"="version","Value"="1.0"</code>. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <code>CreateTags</code> action. </p>
  DefaultClusterParameters:
    type: object
    properties:
      ParameterGroupFamily?:
        type: String
        description: <p>The name of the cluster parameter group family to which the engine default parameters apply.</p>
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      Parameters?:
        type: ParametersList
        description: <p>The list of cluster default parameters.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the default cluster parameters for a parameter group family.</p>
  DeleteClusterMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: <p>The identifier of the cluster to be deleted.</p> <p>Constraints:</p> <ul> <li> <p>Must contain lowercase characters.</p> </li> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
      SkipFinalClusterSnapshot?:
        type: Boolean
        description: '<p>Determines whether a final snapshot of the cluster is created
          before Amazon Redshift deletes the cluster. If <code>true</code>, a final
          cluster snapshot is not created. If <code>false</code>, a final cluster
          snapshot is created before the cluster is deleted. </p> <note> <p>The <i>FinalClusterSnapshotIdentifier</i>
          parameter must be specified if <i>SkipFinalClusterSnapshot</i> is <code>false</code>.</p>
          </note> <p>Default: <code>false</code> </p>'
      FinalClusterSnapshotIdentifier?:
        type: String
        description: <p>The identifier of the final snapshot that is to be created immediately before deleting the cluster. If this parameter is provided, <i>SkipFinalClusterSnapshot</i> must be <code>false</code>. </p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> </ul>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteClusterParameterGroupMessage:
    type: object
    properties:
      ParameterGroupName:
        type: String
        description: <p>The name of the parameter group to be deleted.</p> <p>Constraints:</p> <ul> <li> <p>Must be the name of an existing cluster parameter group.</p> </li> <li> <p>Cannot delete a default cluster parameter group.</p> </li> </ul>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteClusterResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  DeleteClusterSecurityGroupMessage:
    type: object
    properties:
      ClusterSecurityGroupName:
        type: String
        description: <p>The name of the cluster security group to be deleted.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteClusterSnapshotMessage:
    type: object
    properties:
      SnapshotIdentifier:
        type: String
        description: '<p>The unique identifier of the manual snapshot to be deleted.</p>
          <p>Constraints: Must be the name of an existing snapshot that is in the
          <code>available</code> state.</p>'
      SnapshotClusterIdentifier?:
        type: String
        description: '<p>The unique identifier of the cluster the snapshot was created
          from. This parameter is required if your IAM user has a policy containing
          a snapshot resource element that specifies anything other than * for the
          cluster name.</p> <p>Constraints: Must be the name of valid cluster.</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteClusterSnapshotResult:
    type: object
    properties:
      Snapshot?:
        type: Snapshot
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  DeleteClusterSubnetGroupMessage:
    type: object
    properties:
      ClusterSubnetGroupName:
        type: String
        description: <p>The name of the cluster subnet group name to be deleted.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteEventSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: <p>The name of the Amazon Redshift event notification subscription to be deleted.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteHsmClientCertificateMessage:
    type: object
    properties:
      HsmClientCertificateIdentifier:
        type: String
        description: <p>The identifier of the HSM client certificate to be deleted.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteHsmConfigurationMessage:
    type: object
    properties:
      HsmConfigurationIdentifier:
        type: String
        description: <p>The identifier of the Amazon Redshift HSM configuration to be deleted.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DeleteSnapshotCopyGrantMessage:
    type: object
    properties:
      SnapshotCopyGrantName:
        type: String
        description: <p>The name of the snapshot copy grant to delete.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>The result of the <code>DeleteSnapshotCopyGrant</code> action.</p>
  DeleteTagsMessage:
    type: object
    properties:
      ResourceName:
        type: String
        description: <p>The Amazon Resource Name (ARN) from which you want to remove the tag or tags. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>. </p>
      TagKeys:
        type: TagKeyList
        description: <p>The tag key that you want to delete.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <code>DeleteTags</code> action. </p>
  DescribeClusterParameterGroupsMessage:
    type: object
    properties:
      ParameterGroupName?:
        type: String
        description: <p>The name of a specific parameter group for which to return details. By default, details about all parameter groups and the default parameter group are returned.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterParameterGroups</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching cluster parameter groups that are associated with the specified key or keys. For example, suppose that you have parameter groups that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the parameter groups that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching cluster parameter groups that are associated with the specified tag value or values. For example, suppose that you have parameter groups that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the parameter groups that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeClusterParametersMessage:
    type: object
    properties:
      ParameterGroupName:
        type: String
        description: <p>The name of a cluster parameter group for which to return details.</p>
      Source?:
        type: String
        description: '<p>The parameter types to return. Specify <code>user</code>
          to show parameters that are different form the default. Similarly, specify
          <code>engine-default</code> to show parameters that are the same as the
          default parameter group. </p> <p>Default: All parameter types returned.</p>
          <p>Valid Values: <code>user</code> | <code>engine-default</code> </p>'
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterParameters</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeClusterSecurityGroupsMessage:
    type: object
    properties:
      ClusterSecurityGroupName?:
        type: String
        description: '<p>The name of a cluster security group for which you are requesting
          details. You can specify either the <b>Marker</b> parameter or a <b>ClusterSecurityGroupName</b>
          parameter, but not both. </p> <p> Example: <code>securitygroup1</code> </p>'
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: '<p>An optional parameter that specifies the starting point to
          return a set of response records. When the results of a <a>DescribeClusterSecurityGroups</a>
          request exceed the value specified in <code>MaxRecords</code>, AWS returns
          a value in the <code>Marker</code> field of the response. You can retrieve
          the next set of response records by providing the returned marker value
          in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints:
          You can specify either the <b>ClusterSecurityGroupName</b> parameter or
          the <b>Marker</b> parameter, but not both. </p>'
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching cluster security groups that are associated with the specified key or keys. For example, suppose that you have security groups that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the security groups that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching cluster security groups that are associated with the specified tag value or values. For example, suppose that you have security groups that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the security groups that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeClusterSnapshotsMessage:
    type: object
    properties:
      ClusterIdentifier?:
        type: String
        description: <p>The identifier of the cluster for which information about snapshots is requested.</p>
      SnapshotIdentifier?:
        type: String
        description: <p>The snapshot identifier of the snapshot about which to return information.</p>
      SnapshotType?:
        type: String
        description: '<p>The type of snapshots for which you are requesting information.
          By default, snapshots of all types are returned.</p> <p>Valid Values: <code>automated</code>
          | <code>manual</code> </p>'
      StartTime?:
        type: TStamp
        description: '<p>A value that requests only snapshots created at or after
          the specified time. The time value is specified in ISO 8601 format. For
          more information about ISO 8601, go to the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601
          Wikipedia page.</a> </p> <p>Example: <code>2012-07-16T18:00:00Z</code> </p>'
      EndTime?:
        type: TStamp
        description: '<p>A time value that requests only snapshots created at or before
          the specified time. The time value is specified in ISO 8601 format. For
          more information about ISO 8601, go to the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601
          Wikipedia page.</a> </p> <p>Example: <code>2012-07-16T18:00:00Z</code> </p>'
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterSnapshots</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
      OwnerAccount?:
        type: String
        description: <p>The AWS customer account used to create or copy the snapshot. Use this field to filter the results to snapshots owned by a particular account. To describe snapshots you own, either specify your AWS customer account, or do not specify the parameter.</p>
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching cluster snapshots that are associated with the specified key or keys. For example, suppose that you have snapshots that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the snapshots that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching cluster snapshots that are associated with the specified tag value or values. For example, suppose that you have snapshots that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the snapshots that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeClusterSubnetGroupsMessage:
    type: object
    properties:
      ClusterSubnetGroupName?:
        type: String
        description: <p>The name of the cluster subnet group for which information is requested.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterSubnetGroups</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching cluster subnet groups that are associated with the specified key or keys. For example, suppose that you have subnet groups that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the subnet groups that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching cluster subnet groups that are associated with the specified tag value or values. For example, suppose that you have subnet groups that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the subnet groups that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeClusterVersionsMessage:
    type: object
    properties:
      ClusterVersion?:
        type: String
        description: '<p>The specific cluster version to return.</p> <p>Example: <code>1.0</code>
          </p>'
      ClusterParameterGroupFamily?:
        type: String
        description: <p>The name of a specific cluster parameter group family to return details for.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeClusterVersions</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeClustersMessage:
    type: object
    properties:
      ClusterIdentifier?:
        type: String
        description: <p>The unique identifier of a cluster whose properties you are requesting. This parameter is case sensitive.</p> <p>The default is that all clusters defined for an account are returned.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: '<p>An optional parameter that specifies the starting point to
          return a set of response records. When the results of a <a>DescribeClusters</a>
          request exceed the value specified in <code>MaxRecords</code>, AWS returns
          a value in the <code>Marker</code> field of the response. You can retrieve
          the next set of response records by providing the returned marker value
          in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints:
          You can specify either the <b>ClusterIdentifier</b> parameter or the <b>Marker</b>
          parameter, but not both. </p>'
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching clusters that are associated with the specified key or keys. For example, suppose that you have clusters that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the clusters that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching clusters that are associated with the specified tag value or values. For example, suppose that you have clusters that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the clusters that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeDefaultClusterParametersMessage:
    type: object
    properties:
      ParameterGroupFamily:
        type: String
        description: <p>The name of the cluster parameter group family.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeDefaultClusterParameters</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeDefaultClusterParametersResult:
    type: object
    properties:
      DefaultClusterParameters?:
        type: DefaultClusterParameters
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  DescribeEventCategoriesMessage:
    type: object
    properties:
      SourceType?:
        type: String
        description: '<p>The source type, such as cluster or parameter group, to which
          the described event categories apply.</p> <p>Valid values: cluster, cluster-snapshot,
          cluster-parameter-group, and cluster-security-group.</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeEventSubscriptionsMessage:
    type: object
    properties:
      SubscriptionName?:
        type: String
        description: <p>The name of the Amazon Redshift event notification subscription to be described.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeEventSubscriptions</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeEventsMessage:
    type: object
    properties:
      SourceIdentifier?:
        type: String
        description: <p>The identifier of the event source for which events will be returned. If this parameter is not specified, then all sources are included in the response.</p> <p>Constraints:</p> <p>If <i>SourceIdentifier</i> is supplied, <i>SourceType</i> must also be provided.</p> <ul> <li> <p>Specify a cluster identifier when <i>SourceType</i> is <code>cluster</code>.</p> </li> <li> <p>Specify a cluster security group name when <i>SourceType</i> is <code>cluster-security-group</code>.</p> </li> <li> <p>Specify a cluster parameter group name when <i>SourceType</i> is <code>cluster-parameter-group</code>.</p> </li> <li> <p>Specify a cluster snapshot identifier when <i>SourceType</i> is <code>cluster-snapshot</code>.</p> </li> </ul>
      SourceType?:
        type: SourceType
        description: <p>The event source to retrieve events for. If no value is specified, all events are returned.</p> <p>Constraints:</p> <p>If <i>SourceType</i> is supplied, <i>SourceIdentifier</i> must also be provided.</p> <ul> <li> <p>Specify <code>cluster</code> when <i>SourceIdentifier</i> is a cluster identifier.</p> </li> <li> <p>Specify <code>cluster-security-group</code> when <i>SourceIdentifier</i> is a cluster security group name.</p> </li> <li> <p>Specify <code>cluster-parameter-group</code> when <i>SourceIdentifier</i> is a cluster parameter group name.</p> </li> <li> <p>Specify <code>cluster-snapshot</code> when <i>SourceIdentifier</i> is a cluster snapshot identifier.</p> </li> </ul>
      StartTime?:
        type: TStamp
        description: '<p>The beginning of the time interval to retrieve events for,
          specified in ISO 8601 format. For more information about ISO 8601, go to
          the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a>
          </p> <p>Example: <code>2009-07-08T18:00Z</code> </p>'
      EndTime?:
        type: TStamp
        description: '<p>The end of the time interval for which to retrieve events,
          specified in ISO 8601 format. For more information about ISO 8601, go to
          the <a href="http://en.wikipedia.org/wiki/ISO_8601">ISO8601 Wikipedia page.</a>
          </p> <p>Example: <code>2009-07-08T18:00Z</code> </p>'
      Duration?:
        type: IntegerOptional
        description: '<p>The number of minutes prior to the time of the request for
          which to retrieve events. For example, if the request is sent at 18:00 and
          you specify a duration of 60, then only events which have occurred after
          17:00 will be returned.</p> <p>Default: <code>60</code> </p>'
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeEvents</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeHsmClientCertificatesMessage:
    type: object
    properties:
      HsmClientCertificateIdentifier?:
        type: String
        description: <p>The identifier of a specific HSM client certificate for which you want information. If no identifier is specified, information is returned for all HSM client certificates owned by your AWS customer account.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeHsmClientCertificates</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching HSM client certificates that are associated with the specified key or keys. For example, suppose that you have HSM client certificates that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the HSM client certificates that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching HSM client certificates that are associated with the specified tag value or values. For example, suppose that you have HSM client certificates that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the HSM client certificates that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeHsmConfigurationsMessage:
    type: object
    properties:
      HsmConfigurationIdentifier?:
        type: String
        description: <p>The identifier of a specific Amazon Redshift HSM configuration to be described. If no identifier is specified, information is returned for all HSM configurations owned by your AWS customer account.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeHsmConfigurations</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching HSM configurations that are associated with the specified key or keys. For example, suppose that you have HSM configurations that are tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with the HSM configurations that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching HSM configurations that are associated with the specified tag value or values. For example, suppose that you have HSM configurations that are tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with the HSM configurations that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeLoggingStatusMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The identifier of the cluster from which to get the logging
          status.</p> <p>Example: <code>examplecluster</code> </p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeOrderableClusterOptionsMessage:
    type: object
    properties:
      ClusterVersion?:
        type: String
        description: '<p>The version filter value. Specify this parameter to show
          only the available offerings matching the specified version.</p> <p>Default:
          All versions.</p> <p>Constraints: Must be one of the version returned from
          <a>DescribeClusterVersions</a>.</p>'
      NodeType?:
        type: String
        description: <p>The node type filter value. Specify this parameter to show only the available offerings matching the specified node type.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeOrderableClusterOptions</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeReservedNodeOfferingsMessage:
    type: object
    properties:
      ReservedNodeOfferingId?:
        type: String
        description: <p>The unique identifier for the offering.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeReservedNodeOfferings</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeReservedNodesMessage:
    type: object
    properties:
      ReservedNodeId?:
        type: String
        description: <p>Identifier for the node reservation.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: <p>An optional parameter that specifies the starting point to return a set of response records. When the results of a <a>DescribeReservedNodes</a> request exceed the value specified in <code>MaxRecords</code>, AWS returns a value in the <code>Marker</code> field of the response. You can retrieve the next set of response records by providing the returned marker value in the <code>Marker</code> parameter and retrying the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeResizeMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: <p>The unique identifier of a cluster whose resize progress you are requesting. This parameter is case-sensitive.</p> <p>By default, resize operations for all clusters defined for an AWS account are returned.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeSnapshotCopyGrantsMessage:
    type: object
    properties:
      SnapshotCopyGrantName?:
        type: String
        description: <p>The name of the snapshot copy grant.</p>
      MaxRecords?:
        type: IntegerOptional
        description: '<p>The maximum number of response records to return in each
          call. If the number of remaining response records exceeds the specified
          <code>MaxRecords</code> value, a value is returned in a <code>marker</code>
          field of the response. You can retrieve the next set of records by retrying
          the command with the returned marker value. </p> <p>Default: <code>100</code>
          </p> <p>Constraints: minimum 20, maximum 100.</p>'
      Marker?:
        type: String
        description: '<p>An optional parameter that specifies the starting point to
          return a set of response records. When the results of a <code>DescribeSnapshotCopyGrant</code>
          request exceed the value specified in <code>MaxRecords</code>, AWS returns
          a value in the <code>Marker</code> field of the response. You can retrieve
          the next set of response records by providing the returned marker value
          in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints:
          You can specify either the <b>SnapshotCopyGrantName</b> parameter or the
          <b>Marker</b> parameter, but not both. </p>'
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching resources that are associated with the specified key or keys. For example, suppose that you have resources tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with all resources that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching resources that are associated with the specified value or values. For example, suppose that you have resources tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with all resources that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>The result of the <code>DescribeSnapshotCopyGrants</code> action.</p>
  DescribeTableRestoreStatusMessage:
    type: object
    properties:
      ClusterIdentifier?:
        type: String
        description: <p>The Amazon Redshift cluster that the table is being restored to.</p>
      TableRestoreRequestId?:
        type: String
        description: <p>The identifier of the table restore request to return status for. If you don't specify a <code>TableRestoreRequestId</code> value, then <code>DescribeTableRestoreStatus</code> returns the status of all in-progress table restore requests.</p>
      MaxRecords?:
        type: IntegerOptional
        description: <p>The maximum number of records to include in the response. If more records exist than the specified <code>MaxRecords</code> value, a pagination token called a marker is included in the response so that the remaining results can be retrieved.</p>
      Marker?:
        type: String
        description: <p>An optional pagination token provided by a previous <code>DescribeTableRestoreStatus</code> request. If this parameter is specified, the response includes only records beyond the marker, up to the value specified by the <code>MaxRecords</code> parameter.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DescribeTagsMessage:
    type: object
    properties:
      ResourceName?:
        type: String
        description: <p>The Amazon Resource Name (ARN) for which you want to describe the tag or tags. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>. </p>
      ResourceType?:
        type: String
        description: '<p>The type of resource with which you want to view tags. Valid
          resource types are: </p> <ul> <li> <p>Cluster</p> </li> <li> <p>CIDR/IP</p>
          </li> <li> <p>EC2 security group</p> </li> <li> <p>Snapshot</p> </li> <li>
          <p>Cluster security group</p> </li> <li> <p>Subnet group</p> </li> <li>
          <p>HSM connection</p> </li> <li> <p>HSM certificate</p> </li> <li> <p>Parameter
          group</p> </li> <li> <p>Snapshot copy grant</p> </li> </ul> <p>For more
          information about Amazon Redshift resource types and constructing ARNs,
          go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/constructing-redshift-arn.html">Constructing
          an Amazon Redshift Amazon Resource Name (ARN)</a> in the Amazon Redshift
          Cluster Management Guide. </p>'
      MaxRecords?:
        type: IntegerOptional
        description: <p>The maximum number or response records to return in each call. If the number of remaining response records exceeds the specified <code>MaxRecords</code> value, a value is returned in a <code>marker</code> field of the response. You can retrieve the next set of records by retrying the command with the returned <code>marker</code> value. </p>
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>marker</code> parameter and retrying the command. If the <code>marker</code> field is empty, all response records have been retrieved for the request. </p>
      TagKeys?:
        type: TagKeyList
        description: <p>A tag key or keys for which you want to return all matching resources that are associated with the specified key or keys. For example, suppose that you have resources tagged with keys called <code>owner</code> and <code>environment</code>. If you specify both of these tag keys in the request, Amazon Redshift returns a response with all resources that have either or both of these tag keys associated with them.</p>
      TagValues?:
        type: TagValueList
        description: <p>A tag value or values for which you want to return all matching resources that are associated with the specified value or values. For example, suppose that you have resources tagged with values called <code>admin</code> and <code>test</code>. If you specify both of these tag values in the request, Amazon Redshift returns a response with all resources that have either or both of these tag values associated with them.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DisableLoggingMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The identifier of the cluster on which logging is to be stopped.</p>
          <p>Example: <code>examplecluster</code> </p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DisableSnapshotCopyMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The unique identifier of the source cluster that you want
          to disable copying of snapshots to a destination region.</p> <p>Constraints:
          Must be the valid name of an existing cluster that has cross-region snapshot
          copy enabled.</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  DisableSnapshotCopyResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  EC2SecurityGroup:
    type: object
    properties:
      Status?:
        type: String
        description: <p>The status of the EC2 security group.</p>
      EC2SecurityGroupName?:
        type: String
        description: <p>The name of the EC2 Security Group.</p>
      EC2SecurityGroupOwnerId?:
        type: String
        description: <p>The AWS ID of the owner of the EC2 security group specified in the <code>EC2SecurityGroupName</code> field. </p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the EC2 security group.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes an Amazon EC2 security group.</p>
  ElasticIpStatus:
    type: object
    properties:
      ElasticIp?:
        type: String
        description: <p>The elastic IP (EIP) address for the cluster.</p>
      Status?:
        type: String
        description: <p>The status of the elastic IP (EIP) address.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the status of the elastic IP (EIP) address.</p>
  EnableLoggingMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The identifier of the cluster on which logging is to be started.</p>
          <p>Example: <code>examplecluster</code> </p>'
      BucketName:
        type: String
        description: <p>The name of an existing S3 bucket where the log files are to be stored.</p> <p>Constraints:</p> <ul> <li> <p>Must be in the same region as the cluster</p> </li> <li> <p>The cluster must have read bucket and put object permissions</p> </li> </ul>
      S3KeyPrefix?:
        type: String
        description: '<p>The prefix applied to the log file names.</p> <p>Constraints:</p>
          <ul> <li> <p>Cannot exceed 512 characters</p> </li> <li> <p>Cannot contain
          spaces( ), double quotes ("), single quotes (''), a backslash (\), or control
          characters. The hexadecimal codes for invalid characters are: </p> <ul>
          <li> <p>x00 to x20</p> </li> <li> <p>x22</p> </li> <li> <p>x27</p> </li>
          <li> <p>x5c</p> </li> <li> <p>x7f or larger</p> </li> </ul> </li> </ul>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  EnableSnapshotCopyMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The unique identifier of the source cluster to copy snapshots
          from.</p> <p>Constraints: Must be the valid name of an existing cluster
          that does not already have cross-region snapshot copy enabled.</p>'
      DestinationRegion:
        type: String
        description: '<p>The destination region that you want to copy snapshots to.</p>
          <p>Constraints: Must be the name of a valid region. For more information,
          see <a href="http://docs.aws.amazon.com/general/latest/gr/rande.html#redshift_region">Regions
          and Endpoints</a> in the Amazon Web Services General Reference. </p>'
      RetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days to retain automated snapshots in the destination
          region after they are copied from the source region.</p> <p>Default: 7.</p>
          <p>Constraints: Must be at least 1 and no more than 35.</p>'
      SnapshotCopyGrantName?:
        type: String
        description: <p>The name of the snapshot copy grant to use when snapshots of an AWS KMS-encrypted cluster are copied to the destination region.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  EnableSnapshotCopyResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  Endpoint:
    type: object
    properties:
      Address?:
        type: String
        description: <p>The DNS address of the Cluster.</p>
      Port?:
        type: Integer
        description: <p>The port that the database engine is listening on.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a connection endpoint.</p>
  Event:
    type: object
    properties:
      SourceIdentifier?:
        type: String
        description: <p>The identifier for the source of the event.</p>
      SourceType?:
        type: SourceType
        description: <p>The source type for this event.</p>
      Message?:
        type: String
        description: <p>The text of this event.</p>
      EventCategories?:
        type: EventCategoriesList
        description: '<p>A list of the event categories.</p> <p>Values: Configuration,
          Management, Monitoring, Security</p>'
      Severity?:
        type: String
        description: '<p>The severity of the event.</p> <p>Values: ERROR, INFO</p>'
      Date?:
        type: TStamp
        description: <p>The date and time of the event.</p>
      EventId?:
        type: String
        description: <p>The identifier of the event.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes an event.</p>
  EventCategoriesMap:
    type: object
    properties:
      SourceType?:
        type: String
        description: <p>The source type, such as cluster or cluster-snapshot, that the returned categories belong to.</p>
      Events?:
        type: EventInfoMapList
        description: <p>The events in the event category.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes event categories.</p>
  EventCategoriesMessage:
    type: object
    properties:
      EventCategoriesMapList?:
        type: EventCategoriesMapList
        description: <p>A list of event categories descriptions.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  EventInfoMap:
    type: object
    properties:
      EventId?:
        type: String
        description: <p>The identifier of an Amazon Redshift event.</p>
      EventCategories?:
        type: EventCategoriesList
        description: <p>The category of an Amazon Redshift event.</p>
      EventDescription?:
        type: String
        description: <p>The description of an Amazon Redshift event.</p>
      Severity?:
        type: String
        description: '<p>The severity of the event.</p> <p>Values: ERROR, INFO</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes event information.</p>
  EventSubscription:
    type: object
    properties:
      CustomerAwsId?:
        type: String
        description: <p>The AWS customer account associated with the Amazon Redshift event notification subscription.</p>
      CustSubscriptionId?:
        type: String
        description: <p>The name of the Amazon Redshift event notification subscription.</p>
      SnsTopicArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the Amazon SNS topic used by the event notification subscription.</p>
      Status?:
        type: String
        description: '<p>The status of the Amazon Redshift event notification subscription.</p>
          <p>Constraints:</p> <ul> <li> <p>Can be one of the following: active | no-permission
          | topic-not-exist</p> </li> <li> <p>The status "no-permission" indicates
          that Amazon Redshift no longer has permission to post to the Amazon SNS
          topic. The status "topic-not-exist" indicates that the topic was deleted
          after the subscription was created.</p> </li> </ul>'
      SubscriptionCreationTime?:
        type: TStamp
        description: <p>The date and time the Amazon Redshift event notification subscription was created.</p>
      SourceType?:
        type: String
        description: <p>The source type of the events returned the Amazon Redshift event notification, such as cluster, or cluster-snapshot.</p>
      SourceIdsList?:
        type: SourceIdsList
        description: <p>A list of the sources that publish events to the Amazon Redshift event notification subscription.</p>
      EventCategoriesList?:
        type: EventCategoriesList
        description: '<p>The list of Amazon Redshift event categories specified in
          the event notification subscription.</p> <p>Values: Configuration, Management,
          Monitoring, Security</p>'
      Severity?:
        type: String
        description: '<p>The event severity specified in the Amazon Redshift event
          notification subscription.</p> <p>Values: ERROR, INFO</p>'
      Enabled?:
        type: Boolean
        description: <p>A Boolean value indicating whether the subscription is enabled. <code>true</code> indicates the subscription is enabled.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the event subscription.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes event subscriptions.</p>
  EventSubscriptionsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      EventSubscriptionsList?:
        type: EventSubscriptionsList
        description: <p>A list of event subscriptions.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  EventsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      Events?:
        type: EventList
        description: <p>A list of <code>Event</code> instances. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  HsmClientCertificate:
    type: object
    properties:
      HsmClientCertificateIdentifier?:
        type: String
        description: <p>The identifier of the HSM client certificate.</p>
      HsmClientCertificatePublicKey?:
        type: String
        description: <p>The public key that the Amazon Redshift cluster will use to connect to the HSM. You must register the public key in the HSM.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the HSM client certificate.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Returns information about an HSM client certificate. The certificate is stored in a secure Hardware Storage Module (HSM), and used by the Amazon Redshift cluster to encrypt data files.</p>
  HsmClientCertificateMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      HsmClientCertificates?:
        type: HsmClientCertificateList
        description: <p>A list of the identifiers for one or more HSM client certificates used by Amazon Redshift clusters to store and retrieve database encryption keys in an HSM.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  HsmConfiguration:
    type: object
    properties:
      HsmConfigurationIdentifier?:
        type: String
        description: <p>The name of the Amazon Redshift HSM configuration.</p>
      Description?:
        type: String
        description: <p>A text description of the HSM configuration.</p>
      HsmIpAddress?:
        type: String
        description: <p>The IP address that the Amazon Redshift cluster must use to access the HSM.</p>
      HsmPartitionName?:
        type: String
        description: <p>The name of the partition in the HSM where the Amazon Redshift clusters will store their database encryption keys.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the HSM configuration.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Returns information about an HSM configuration, which is an object that describes to Amazon Redshift clusters the information they require to connect to an HSM where they can store database encryption keys.</p>
  HsmConfigurationMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      HsmConfigurations?:
        type: HsmConfigurationList
        description: <p>A list of <code>HsmConfiguration</code> objects.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  HsmStatus:
    type: object
    properties:
      HsmClientCertificateIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.</p>
      HsmConfigurationIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.</p>
      Status?:
        type: String
        description: '<p>Reports whether the Amazon Redshift cluster has finished
          applying any HSM settings changes specified in a modify cluster command.</p>
          <p>Values: active, applying</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the status of changes to HSM settings.</p>
  IPRange:
    type: object
    properties:
      Status?:
        type: String
        description: <p>The status of the IP range, for example, "authorized".</p>
      CIDRIP?:
        type: String
        description: <p>The IP range in Classless Inter-Domain Routing (CIDR) notation.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the IP range.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes an IP range used in a security group.</p>
  LoggingStatus:
    type: object
    properties:
      LoggingEnabled?:
        type: Boolean
        description: <p> <code>true</code> if logging is on, <code>false</code> if logging is off.</p>
      BucketName?:
        type: String
        description: <p>The name of the S3 bucket where the log files are stored.</p>
      S3KeyPrefix?:
        type: String
        description: <p>The prefix applied to the log file names.</p>
      LastSuccessfulDeliveryTime?:
        type: TStamp
        description: <p>The last time that logs were delivered.</p>
      LastFailureTime?:
        type: TStamp
        description: <p>The last time when logs failed to be delivered.</p>
      LastFailureMessage?:
        type: String
        description: <p>The message indicating that logs failed to be delivered.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the status of logging for a cluster.</p>
  ModifyClusterIamRolesMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: <p>The unique identifier of the cluster for which you want to associate or disassociate IAM roles.</p>
      AddIamRoles?:
        type: IamRoleArnList
        description: <p>Zero or more IAM roles to associate with the cluster. The roles must be in their Amazon Resource Name (ARN) format. You can associate up to 10 IAM roles with a single cluster in a single request.</p>
      RemoveIamRoles?:
        type: IamRoleArnList
        description: <p>Zero or more IAM roles in ARN format to disassociate from the cluster. You can disassociate up to 10 IAM roles from a single cluster in a single request.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ModifyClusterIamRolesResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ModifyClusterMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The unique identifier of the cluster to be modified.</p>
          <p>Example: <code>examplecluster</code> </p>'
      ClusterType?:
        type: String
        description: '<p>The new cluster type.</p> <p>When you submit your cluster
          resize request, your existing cluster goes into a read-only mode. After
          Amazon Redshift provisions a new cluster based on your resize requirements,
          there will be outage for a period while the old cluster is deleted and your
          connection is switched to the new cluster. You can use <a>DescribeResize</a>
          to track the progress of the resize request. </p> <p>Valid Values: <code>
          multi-node | single-node </code> </p>'
      NodeType?:
        type: String
        description: '<p>The new node type of the cluster. If you specify a new node
          type, you must also specify the number of nodes parameter.</p> <p>When you
          submit your request to resize a cluster, Amazon Redshift sets access permissions
          for the cluster to read-only. After Amazon Redshift provisions a new cluster
          according to your resize requirements, there will be a temporary outage
          while the old cluster is deleted and your connection is switched to the
          new cluster. When the new connection is complete, the original access permissions
          for the cluster are restored. You can use <a>DescribeResize</a> to track
          the progress of the resize request. </p> <p>Valid Values: <code> ds1.xlarge</code>
          | <code>ds1.8xlarge</code> | <code> ds2.xlarge</code> | <code>ds2.8xlarge</code>
          | <code>dc1.large</code> | <code>dc1.8xlarge</code>.</p>'
      NumberOfNodes?:
        type: IntegerOptional
        description: '<p>The new number of nodes of the cluster. If you specify a
          new number of nodes, you must also specify the node type parameter.</p>
          <p>When you submit your request to resize a cluster, Amazon Redshift sets
          access permissions for the cluster to read-only. After Amazon Redshift provisions
          a new cluster according to your resize requirements, there will be a temporary
          outage while the old cluster is deleted and your connection is switched
          to the new cluster. When the new connection is complete, the original access
          permissions for the cluster are restored. You can use <a>DescribeResize</a>
          to track the progress of the resize request. </p> <p>Valid Values: Integer
          greater than <code>0</code>.</p>'
      ClusterSecurityGroups?:
        type: ClusterSecurityGroupNameList
        description: <p>A list of cluster security groups to be authorized on this cluster. This change is asynchronously applied as soon as possible.</p> <p>Security groups currently associated with the cluster, and not in the list of groups to apply, will be revoked from the cluster.</p> <p>Constraints:</p> <ul> <li> <p>Must be 1 to 255 alphanumeric characters or hyphens</p> </li> <li> <p>First character must be a letter</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens</p> </li> </ul>
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: <p>A list of virtual private cloud (VPC) security groups to be associated with the cluster.</p>
      MasterUserPassword?:
        type: String
        description: '<p>The new password for the cluster master user. This change
          is asynchronously applied as soon as possible. Between the time of the request
          and the completion of the request, the <code>MasterUserPassword</code> element
          exists in the <code>PendingModifiedValues</code> element of the operation
          response. </p> <note> <p>Operations never return the password, so this operation
          provides a way to regain access to the master user account for a cluster
          if the password is lost.</p> </note> <p>Default: Uses existing setting.</p>
          <p>Constraints:</p> <ul> <li> <p>Must be between 8 and 64 characters in
          length.</p> </li> <li> <p>Must contain at least one uppercase letter.</p>
          </li> <li> <p>Must contain at least one lowercase letter.</p> </li> <li>
          <p>Must contain one number.</p> </li> <li> <p>Can be any printable ASCII
          character (ASCII code 33 to 126) except '' (single quote), " (double quote),
          \, /, @, or space.</p> </li> </ul>'
      ClusterParameterGroupName?:
        type: String
        description: '<p>The name of the cluster parameter group to apply to this
          cluster. This change is applied only after the cluster is rebooted. To reboot
          a cluster use <a>RebootCluster</a>. </p> <p>Default: Uses existing setting.</p>
          <p>Constraints: The cluster parameter group must be in the same parameter
          group family that matches the cluster version.</p>'
      AutomatedSnapshotRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days that automated snapshots are retained.
          If the value is 0, automated snapshots are disabled. Even if automated snapshots
          are disabled, you can still create manual snapshots when you want with <a>CreateClusterSnapshot</a>.
          </p> <p>If you decrease the automated snapshot retention period from its
          current value, existing automated snapshots that fall outside of the new
          retention period will be immediately deleted.</p> <p>Default: Uses existing
          setting.</p> <p>Constraints: Must be a value from 0 to 35.</p>'
      PreferredMaintenanceWindow?:
        type: String
        description: '<p>The weekly time range (in UTC) during which system maintenance
          can occur, if necessary. If system maintenance is necessary during the window,
          it may result in an outage.</p> <p>This maintenance window change is made
          immediately. If the new maintenance window indicates the current time, there
          must be at least 120 minutes between the current time and end of the window
          in order to ensure that pending changes are applied.</p> <p>Default: Uses
          existing setting.</p> <p>Format: ddd:hh24:mi-ddd:hh24:mi, for example <code>wed:07:30-wed:08:00</code>.</p>
          <p>Valid Days: Mon | Tue | Wed | Thu | Fri | Sat | Sun</p> <p>Constraints:
          Must be at least 30 minutes.</p>'
      ClusterVersion?:
        type: String
        description: '<p>The new version number of the Amazon Redshift engine to upgrade
          to.</p> <p>For major version upgrades, if a non-default cluster parameter
          group is currently in use, a new cluster parameter group in the cluster
          parameter group family for the new version must be specified. The new cluster
          parameter group can be the default for that cluster parameter group family.
          For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon
          Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management
          Guide</i>.</p> <p>Example: <code>1.0</code> </p>'
      AllowVersionUpgrade?:
        type: BooleanOptional
        description: '<p>If <code>true</code>, major version upgrades will be applied
          automatically to the cluster during the maintenance window. </p> <p>Default:
          <code>false</code> </p>'
      HsmClientCertificateIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.</p>
      HsmConfigurationIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.</p>
      NewClusterIdentifier?:
        type: String
        description: '<p>The new identifier for the cluster.</p> <p>Constraints:</p>
          <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p>
          </li> <li> <p>Alphabetic characters must be lowercase.</p> </li> <li> <p>First
          character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or
          contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for all
          clusters within an AWS account.</p> </li> </ul> <p>Example: <code>examplecluster</code>
          </p>'
      PubliclyAccessible?:
        type: BooleanOptional
        description: <p>If <code>true</code>, the cluster can be accessed from a public network. Only clusters in VPCs can be set to be publicly available.</p>
      ElasticIp?:
        type: String
        description: '<p>The Elastic IP (EIP) address for the cluster.</p> <p>Constraints:
          The cluster must be provisioned in EC2-VPC and publicly-accessible through
          an Internet gateway. For more information about provisioning clusters in
          EC2-VPC, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#cluster-platforms">Supported
          Platforms to Launch Your Cluster</a> in the Amazon Redshift Cluster Management
          Guide.</p>'
      EnhancedVpcRouting?:
        type: BooleanOptional
        description: '<p>An option that specifies whether to create the cluster with
          enhanced VPC routing enabled. To create a cluster that uses enhanced VPC
          routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced
          VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If
          this option is <code>true</code>, enhanced VPC routing is enabled. </p>
          <p>Default: false</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ModifyClusterParameterGroupMessage:
    type: object
    properties:
      ParameterGroupName:
        type: String
        description: <p>The name of the parameter group to be modified.</p>
      Parameters:
        type: ParametersList
        description: <p>An array of parameters to be modified. A maximum of 20 parameters can be modified in a single request.</p> <p>For each parameter to be modified, you must supply at least the parameter name and parameter value; other name-value pairs of the parameter are optional.</p> <p>For the workload management (WLM) configuration, you must supply all the name-value pairs in the wlm_json_configuration parameter.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ModifyClusterResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ModifyClusterSubnetGroupMessage:
    type: object
    properties:
      ClusterSubnetGroupName:
        type: String
        description: <p>The name of the subnet group to be modified.</p>
      Description?:
        type: String
        description: <p>A text description of the subnet group to be modified.</p>
      SubnetIds:
        type: SubnetIdentifierList
        description: <p>An array of VPC subnet IDs. A maximum of 20 subnets can be modified in a single request.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ModifyClusterSubnetGroupResult:
    type: object
    properties:
      ClusterSubnetGroup?:
        type: ClusterSubnetGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ModifyEventSubscriptionMessage:
    type: object
    properties:
      SubscriptionName:
        type: String
        description: <p>The name of the modified Amazon Redshift event notification subscription.</p>
      SnsTopicArn?:
        type: String
        description: <p>The Amazon Resource Name (ARN) of the SNS topic to be used by the event notification subscription.</p>
      SourceType?:
        type: String
        description: '<p>The type of source that will be generating the events. For
          example, if you want to be notified of events generated by a cluster, you
          would set this parameter to cluster. If this value is not specified, events
          are returned for all Amazon Redshift objects in your AWS account. You must
          specify a source type in order to specify source IDs.</p> <p>Valid values:
          cluster, cluster-parameter-group, cluster-security-group, and cluster-snapshot.</p>'
      SourceIds?:
        type: SourceIdsList
        description: '<p>A list of one or more identifiers of Amazon Redshift source
          objects. All of the objects must be of the same type as was specified in
          the source type parameter. The event subscription will return only events
          generated by the specified objects. If not specified, then events are returned
          for all objects within the source type specified.</p> <p>Example: my-cluster-1,
          my-cluster-2</p> <p>Example: my-snapshot-20131010</p>'
      EventCategories?:
        type: EventCategoriesList
        description: '<p>Specifies the Amazon Redshift event categories to be published
          by the event notification subscription.</p> <p>Values: Configuration, Management,
          Monitoring, Security</p>'
      Severity?:
        type: String
        description: '<p>Specifies the Amazon Redshift event severity to be published
          by the event notification subscription.</p> <p>Values: ERROR, INFO</p>'
      Enabled?:
        type: BooleanOptional
        description: <p>A Boolean value indicating if the subscription is enabled. <code>true</code> indicates the subscription is enabled </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ModifyEventSubscriptionResult:
    type: object
    properties:
      EventSubscription?:
        type: EventSubscription
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  ModifySnapshotCopyRetentionPeriodMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The unique identifier of the cluster for which you want to
          change the retention period for automated snapshots that are copied to a
          destination region.</p> <p>Constraints: Must be the valid name of an existing
          cluster that has cross-region snapshot copy enabled.</p>'
      RetentionPeriod:
        type: Integer
        description: '<p>The number of days to retain automated snapshots in the destination
          region after they are copied from the source region.</p> <p>If you decrease
          the retention period for automated snapshots that are copied to a destination
          region, Amazon Redshift will delete any existing automated snapshots that
          were copied to the destination region and that fall outside of the new retention
          period.</p> <p>Constraints: Must be at least 1 and no more than 35.</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ModifySnapshotCopyRetentionPeriodResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  OrderableClusterOption:
    type: object
    properties:
      ClusterVersion?:
        type: String
        description: <p>The version of the orderable cluster.</p>
      ClusterType?:
        type: String
        description: <p>The cluster type, for example <code>multi-node</code>. </p>
      NodeType?:
        type: String
        description: <p>The node type for the orderable cluster.</p>
      AvailabilityZones?:
        type: AvailabilityZoneList
        description: <p>A list of availability zones for the orderable cluster.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes an orderable cluster option.</p>
  OrderableClusterOptionsMessage:
    type: object
    properties:
      OrderableClusterOptions?:
        type: OrderableClusterOptionsList
        description: <p>An <code>OrderableClusterOption</code> structure containing information about orderable options for the cluster.</p>
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <a>DescribeOrderableClusterOptions</a> action. </p>
  Parameter:
    type: object
    properties:
      ParameterName?:
        type: String
        description: <p>The name of the parameter.</p>
      ParameterValue?:
        type: String
        description: <p>The value of the parameter.</p>
      Description?:
        type: String
        description: <p>A description of the parameter.</p>
      Source?:
        type: String
        description: <p>The source of the parameter value, such as "engine-default" or "user".</p>
      DataType?:
        type: String
        description: <p>The data type of the parameter.</p>
      AllowedValues?:
        type: String
        description: <p>The valid range of values for the parameter.</p>
      ApplyType?:
        type: ParameterApplyType
        description: <p>Specifies how to apply the WLM configuration parameter. Some properties can be applied dynamically, while other properties require that any associated clusters be rebooted for the configuration changes to be applied. For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.</p>
      IsModifiable?:
        type: Boolean
        description: <p>If <code>true</code>, the parameter can be modified. Some parameters have security or operational implications that prevent them from being changed. </p>
      MinimumEngineVersion?:
        type: String
        description: <p>The earliest engine version to which the parameter can apply.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a parameter in a cluster parameter group.</p>
  PendingModifiedValues:
    type: object
    properties:
      MasterUserPassword?:
        type: String
        description: <p>The pending or in-progress change of the master user password for the cluster.</p>
      NodeType?:
        type: String
        description: <p>The pending or in-progress change of the cluster's node type.</p>
      NumberOfNodes?:
        type: IntegerOptional
        description: <p>The pending or in-progress change of the number of nodes in the cluster.</p>
      ClusterType?:
        type: String
        description: <p>The pending or in-progress change of the cluster type.</p>
      ClusterVersion?:
        type: String
        description: <p>The pending or in-progress change of the service version.</p>
      AutomatedSnapshotRetentionPeriod?:
        type: IntegerOptional
        description: <p>The pending or in-progress change of the automated snapshot retention period.</p>
      ClusterIdentifier?:
        type: String
        description: <p>The pending or in-progress change of the new identifier for the cluster.</p>
      PubliclyAccessible?:
        type: BooleanOptional
        description: <p>The pending or in-progress change of the ability to connect to the cluster from the public network.</p>
      EnhancedVpcRouting?:
        type: BooleanOptional
        description: '<p>An option that specifies whether to create the cluster with
          enhanced VPC routing enabled. To create a cluster that uses enhanced VPC
          routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced
          VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If
          this option is <code>true</code>, enhanced VPC routing is enabled. </p>
          <p>Default: false</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes cluster attributes that are in a pending state. A change to one or more the attributes was requested and is in progress or will be applied.</p>
  PurchaseReservedNodeOfferingMessage:
    type: object
    properties:
      ReservedNodeOfferingId:
        type: String
        description: <p>The unique identifier of the reserved node offering you want to purchase.</p>
      NodeCount?:
        type: IntegerOptional
        description: '<p>The number of reserved nodes that you want to purchase.</p>
          <p>Default: <code>1</code> </p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  PurchaseReservedNodeOfferingResult:
    type: object
    properties:
      ReservedNode?:
        type: ReservedNode
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RebootClusterMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: <p>The cluster identifier.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  RebootClusterResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RecurringCharge:
    type: object
    properties:
      RecurringChargeAmount?:
        type: Double
        description: <p>The amount charged per the period of time specified by the recurring charge frequency.</p>
      RecurringChargeFrequency?:
        type: String
        description: <p>The frequency at which the recurring charge amount is applied.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a recurring charge.</p>
  ReservedNode:
    type: object
    properties:
      ReservedNodeId?:
        type: String
        description: <p>The unique identifier for the reservation.</p>
      ReservedNodeOfferingId?:
        type: String
        description: <p>The identifier for the reserved node offering.</p>
      NodeType?:
        type: String
        description: <p>The node type of the reserved node.</p>
      StartTime?:
        type: TStamp
        description: <p>The time the reservation started. You purchase a reserved node offering for a duration. This is the start time of that duration.</p>
      Duration?:
        type: Integer
        description: <p>The duration of the node reservation in seconds.</p>
      FixedPrice?:
        type: Double
        description: <p>The fixed cost Amazon Redshift charges you for this reserved node.</p>
      UsagePrice?:
        type: Double
        description: <p>The hourly rate Amazon Redshift charges you for this reserved node.</p>
      CurrencyCode?:
        type: String
        description: <p>The currency code for the reserved cluster.</p>
      NodeCount?:
        type: Integer
        description: <p>The number of reserved compute nodes.</p>
      State?:
        type: String
        description: <p>The state of the reserved compute node.</p> <p>Possible Values:</p> <ul> <li> <p>pending-payment-This reserved node has recently been purchased, and the sale has been approved, but payment has not yet been confirmed.</p> </li> <li> <p>active-This reserved node is owned by the caller and is available for use.</p> </li> <li> <p>payment-failed-Payment failed for the purchase attempt.</p> </li> </ul>
      OfferingType?:
        type: String
        description: <p>The anticipated utilization of the reserved node, as defined in the reserved node offering.</p>
      RecurringCharges?:
        type: RecurringChargeList
        description: <p>The recurring charges for the reserved node.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a reserved node. You can call the <a>DescribeReservedNodeOfferings</a> API to obtain the available reserved node offerings. </p>
  ReservedNodeOffering:
    type: object
    properties:
      ReservedNodeOfferingId?:
        type: String
        description: <p>The offering identifier.</p>
      NodeType?:
        type: String
        description: <p>The node type offered by the reserved node offering.</p>
      Duration?:
        type: Integer
        description: <p>The duration, in seconds, for which the offering will reserve the node.</p>
      FixedPrice?:
        type: Double
        description: <p>The upfront fixed charge you will pay to purchase the specific reserved node offering.</p>
      UsagePrice?:
        type: Double
        description: <p>The rate you are charged for each hour the cluster that is using the offering is running.</p>
      CurrencyCode?:
        type: String
        description: <p>The currency code for the compute nodes offering.</p>
      OfferingType?:
        type: String
        description: <p>The anticipated utilization of the reserved node, as defined in the reserved node offering.</p>
      RecurringCharges?:
        type: RecurringChargeList
        description: <p>The charge to your account regardless of whether you are creating any clusters using the node offering. Recurring charges are only in effect for heavy-utilization reserved nodes.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a reserved node offering.</p>
  ReservedNodeOfferingsMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      ReservedNodeOfferings?:
        type: ReservedNodeOfferingList
        description: <p>A list of <code>ReservedNodeOffering</code> objects.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ReservedNodesMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      ReservedNodes?:
        type: ReservedNodeList
        description: <p>The list of <code>ReservedNode</code> objects.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ResetClusterParameterGroupMessage:
    type: object
    properties:
      ParameterGroupName:
        type: String
        description: <p>The name of the cluster parameter group to be reset.</p>
      ResetAllParameters?:
        type: Boolean
        description: '<p>If <code>true</code>, all parameters in the specified parameter
          group will be reset to their default values. </p> <p>Default: <code>true</code>
          </p>'
      Parameters?:
        type: ParametersList
        description: '<p>An array of names of parameters to be reset. If <i>ResetAllParameters</i>
          option is not used, then at least one parameter name must be supplied. </p>
          <p>Constraints: A maximum of 20 parameters can be reset in a single request.</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  ResizeProgressMessage:
    type: object
    properties:
      TargetNodeType?:
        type: String
        description: <p>The node type that the cluster will have after the resize operation is complete.</p>
      TargetNumberOfNodes?:
        type: IntegerOptional
        description: <p>The number of nodes that the cluster will have after the resize operation is complete.</p>
      TargetClusterType?:
        type: String
        description: '<p>The cluster type after the resize operation is complete.</p>
          <p>Valid Values: <code>multi-node</code> | <code>single-node</code> </p>'
      Status?:
        type: String
        description: '<p>The status of the resize operation.</p> <p>Valid Values:
          <code>NONE</code> | <code>IN_PROGRESS</code> | <code>FAILED</code> | <code>SUCCEEDED</code>
          </p>'
      ImportTablesCompleted?:
        type: ImportTablesCompleted
        description: '<p>The names of tables that have been completely imported .</p>
          <p>Valid Values: List of table names.</p>'
      ImportTablesInProgress?:
        type: ImportTablesInProgress
        description: '<p>The names of tables that are being currently imported.</p>
          <p>Valid Values: List of table names.</p>'
      ImportTablesNotStarted?:
        type: ImportTablesNotStarted
        description: '<p>The names of tables that have not been yet imported.</p>
          <p>Valid Values: List of table names</p>'
      AvgResizeRateInMegaBytesPerSecond?:
        type: DoubleOptional
        description: <p>The average rate of the resize operation over the last few minutes, measured in megabytes per second. After the resize operation completes, this value shows the average rate of the entire resize operation.</p>
      TotalResizeDataInMegaBytes?:
        type: LongOptional
        description: <p>The estimated total amount of data, in megabytes, on the cluster before the resize operation began.</p>
      ProgressInMegaBytes?:
        type: LongOptional
        description: <p>While the resize operation is in progress, this value shows the current amount of data, in megabytes, that has been processed so far. When the resize operation is complete, this value shows the total amount of data, in megabytes, on the cluster, which may be more or less than TotalResizeDataInMegaBytes (the estimated total amount of data before resize).</p>
      ElapsedTimeInSeconds?:
        type: LongOptional
        description: <p>The amount of seconds that have elapsed since the resize operation began. After the resize operation completes, this value shows the total actual time, in seconds, for the resize operation.</p>
      EstimatedTimeToCompletionInSeconds?:
        type: LongOptional
        description: <p>The estimated time remaining, in seconds, until the resize operation is complete. This value is calculated based on the average resize rate and the estimated amount of data remaining to be processed. Once the resize operation is complete, this value will be 0.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the result of a cluster resize operation.</p>
  RestoreFromClusterSnapshotMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: <p>The identifier of the cluster that will be created from restoring the snapshot.</p> <p>Constraints:</p> <ul> <li> <p>Must contain from 1 to 63 alphanumeric characters or hyphens.</p> </li> <li> <p>Alphabetic characters must be lowercase.</p> </li> <li> <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a hyphen or contain two consecutive hyphens.</p> </li> <li> <p>Must be unique for all clusters within an AWS account.</p> </li> </ul>
      SnapshotIdentifier:
        type: String
        description: '<p>The name of the snapshot from which to create the new cluster.
          This parameter isn''t case sensitive.</p> <p>Example: <code>my-snapshot-id</code>
          </p>'
      SnapshotClusterIdentifier?:
        type: String
        description: <p>The name of the cluster the source snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.</p>
      Port?:
        type: IntegerOptional
        description: '<p>The port number on which the cluster accepts connections.</p>
          <p>Default: The same port as the original cluster.</p> <p>Constraints: Must
          be between <code>1115</code> and <code>65535</code>.</p>'
      AvailabilityZone?:
        type: String
        description: '<p>The Amazon EC2 Availability Zone in which to restore the
          cluster.</p> <p>Default: A random, system-chosen Availability Zone.</p>
          <p>Example: <code>us-east-1a</code> </p>'
      AllowVersionUpgrade?:
        type: BooleanOptional
        description: '<p>If <code>true</code>, major version upgrades can be applied
          during the maintenance window to the Amazon Redshift engine that is running
          on the cluster. </p> <p>Default: <code>true</code> </p>'
      ClusterSubnetGroupName?:
        type: String
        description: <p>The name of the subnet group where you want to cluster restored.</p> <p>A snapshot of cluster in VPC can be restored only in VPC. Therefore, you must provide subnet group name where you want the cluster restored.</p>
      PubliclyAccessible?:
        type: BooleanOptional
        description: <p>If <code>true</code>, the cluster can be accessed from a public network. </p>
      OwnerAccount?:
        type: String
        description: <p>The AWS customer account used to create or copy the snapshot. Required if you are restoring a snapshot you do not own, optional if you own the snapshot.</p>
      HsmClientCertificateIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM client certificate the Amazon Redshift cluster uses to retrieve the data encryption keys stored in an HSM.</p>
      HsmConfigurationIdentifier?:
        type: String
        description: <p>Specifies the name of the HSM configuration that contains the information the Amazon Redshift cluster can use to retrieve and store keys in an HSM.</p>
      ElasticIp?:
        type: String
        description: <p>The elastic IP (EIP) address for the cluster.</p>
      ClusterParameterGroupName?:
        type: String
        description: '<p>The name of the parameter group to be associated with this
          cluster.</p> <p>Default: The default Amazon Redshift cluster parameter group.
          For information about the default parameter group, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Working
          with Amazon Redshift Parameter Groups</a>.</p> <p>Constraints:</p> <ul>
          <li> <p>Must be 1 to 255 alphanumeric characters or hyphens.</p> </li> <li>
          <p>First character must be a letter.</p> </li> <li> <p>Cannot end with a
          hyphen or contain two consecutive hyphens.</p> </li> </ul>'
      ClusterSecurityGroups?:
        type: ClusterSecurityGroupNameList
        description: '<p>A list of security groups to be associated with this cluster.</p>
          <p>Default: The default cluster security group for Amazon Redshift.</p>
          <p>Cluster security groups only apply to clusters outside of VPCs.</p>'
      VpcSecurityGroupIds?:
        type: VpcSecurityGroupIdList
        description: '<p>A list of Virtual Private Cloud (VPC) security groups to
          be associated with the cluster.</p> <p>Default: The default VPC security
          group is associated with the cluster.</p> <p>VPC security groups only apply
          to clusters in VPCs.</p>'
      PreferredMaintenanceWindow?:
        type: String
        description: '<p>The weekly time range (in UTC) during which automated cluster
          maintenance can occur.</p> <p> Format: <code>ddd:hh24:mi-ddd:hh24:mi</code>
          </p> <p> Default: The value selected for the cluster from which the snapshot
          was taken. For more information about the time blocks for each region, see
          <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#rs-maintenance-windows">Maintenance
          Windows</a> in Amazon Redshift Cluster Management Guide. </p> <p>Valid Days:
          Mon | Tue | Wed | Thu | Fri | Sat | Sun</p> <p>Constraints: Minimum 30-minute
          window.</p>'
      AutomatedSnapshotRetentionPeriod?:
        type: IntegerOptional
        description: '<p>The number of days that automated snapshots are retained.
          If the value is 0, automated snapshots are disabled. Even if automated snapshots
          are disabled, you can still create manual snapshots when you want with <a>CreateClusterSnapshot</a>.
          </p> <p>Default: The value selected for the cluster from which the snapshot
          was taken.</p> <p>Constraints: Must be a value from 0 to 35.</p>'
      KmsKeyId?:
        type: String
        description: <p>The AWS Key Management Service (KMS) key ID of the encryption key that you want to use to encrypt data in the cluster that you restore from a shared snapshot.</p>
      NodeType?:
        type: String
        description: '<p>The node type that the restored cluster will be provisioned
          with.</p> <p>Default: The node type of the cluster from which the snapshot
          was taken. You can modify this if you are using any DS node type. In that
          case, you can choose to restore into another DS node type of the same size.
          For example, you can restore ds1.8xlarge into ds2.8xlarge, or ds2.xlarge
          into ds1.xlarge. If you have a DC instance type, you must restore into that
          same instance type and size. In other words, you can only restore a dc1.large
          instance type into another dc1.large instance type. For more information
          about node types, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html#rs-about-clusters-and-nodes">
          About Clusters and Nodes</a> in the <i>Amazon Redshift Cluster Management
          Guide</i> </p>'
      EnhancedVpcRouting?:
        type: BooleanOptional
        description: '<p>An option that specifies whether to create the cluster with
          enhanced VPC routing enabled. To create a cluster that uses enhanced VPC
          routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced
          VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If
          this option is <code>true</code>, enhanced VPC routing is enabled. </p>
          <p>Default: false</p>'
      AdditionalInfo?:
        type: String
        description: <p>Reserved.</p>
      IamRoles?:
        type: IamRoleArnList
        description: <p>A list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services. You must supply the IAM roles in their Amazon Resource Name (ARN) format. You can supply up to 10 IAM roles in a single request.</p> <p>A cluster can have up to 10 IAM roles associated at any time.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  RestoreFromClusterSnapshotResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RestoreStatus:
    type: object
    properties:
      Status?:
        type: String
        description: <p>The status of the restore action. Returns starting, restoring, completed, or failed.</p>
      CurrentRestoreRateInMegaBytesPerSecond?:
        type: Double
        description: <p>The number of megabytes per second being transferred from the backup storage. Returns the average rate for a completed backup.</p>
      SnapshotSizeInMegaBytes?:
        type: Long
        description: <p>The size of the set of snapshot data used to restore the cluster.</p>
      ProgressInMegaBytes?:
        type: Long
        description: <p>The number of megabytes that have been transferred from snapshot storage.</p>
      ElapsedTimeInSeconds?:
        type: Long
        description: <p>The amount of time an in-progress restore has been running, or the amount of time it took a completed restore to finish.</p>
      EstimatedTimeToCompletionInSeconds?:
        type: Long
        description: <p>The estimate of the time remaining before the restore will complete. Returns 0 for a completed restore.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the status of a cluster restore action. Returns null if the cluster was not created by restoring a snapshot.</p>
  RestoreTableFromClusterSnapshotMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: <p>The identifier of the Amazon Redshift cluster to restore the table to.</p>
      SnapshotIdentifier:
        type: String
        description: <p>The identifier of the snapshot to restore the table from. This snapshot must have been created from the Amazon Redshift cluster specified by the <code>ClusterIdentifier</code> parameter.</p>
      SourceDatabaseName:
        type: String
        description: <p>The name of the source database that contains the table to restore from.</p>
      SourceSchemaName?:
        type: String
        description: <p>The name of the source schema that contains the table to restore from. If you do not specify a <code>SourceSchemaName</code> value, the default is <code>public</code>.</p>
      SourceTableName:
        type: String
        description: <p>The name of the source table to restore from.</p>
      TargetDatabaseName?:
        type: String
        description: <p>The name of the database to restore the table to.</p>
      TargetSchemaName?:
        type: String
        description: <p>The name of the schema to restore the table to.</p>
      NewTableName:
        type: String
        description: <p>The name of the table to create as a result of the current request.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  RestoreTableFromClusterSnapshotResult:
    type: object
    properties:
      TableRestoreStatus?:
        type: TableRestoreStatus
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RevokeClusterSecurityGroupIngressMessage:
    type: object
    properties:
      ClusterSecurityGroupName:
        type: String
        description: <p>The name of the security Group from which to revoke the ingress rule.</p>
      CIDRIP?:
        type: String
        description: <p>The IP range for which to revoke access. This range must be a valid Classless Inter-Domain Routing (CIDR) block of IP addresses. If <code>CIDRIP</code> is specified, <code>EC2SecurityGroupName</code> and <code>EC2SecurityGroupOwnerId</code> cannot be provided. </p>
      EC2SecurityGroupName?:
        type: String
        description: <p>The name of the EC2 Security Group whose access is to be revoked. If <code>EC2SecurityGroupName</code> is specified, <code>EC2SecurityGroupOwnerId</code> must also be provided and <code>CIDRIP</code> cannot be provided. </p>
      EC2SecurityGroupOwnerId?:
        type: String
        description: '<p>The AWS account number of the owner of the security group
          specified in the <code>EC2SecurityGroupName</code> parameter. The AWS access
          key ID is not an acceptable value. If <code>EC2SecurityGroupOwnerId</code>
          is specified, <code>EC2SecurityGroupName</code> must also be provided. and
          <code>CIDRIP</code> cannot be provided. </p> <p>Example: <code>111122223333</code>
          </p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  RevokeClusterSecurityGroupIngressResult:
    type: object
    properties:
      ClusterSecurityGroup?:
        type: ClusterSecurityGroup
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RevokeSnapshotAccessMessage:
    type: object
    properties:
      SnapshotIdentifier:
        type: String
        description: <p>The identifier of the snapshot that the account can no longer access.</p>
      SnapshotClusterIdentifier?:
        type: String
        description: <p>The identifier of the cluster the snapshot was created from. This parameter is required if your IAM user has a policy containing a snapshot resource element that specifies anything other than * for the cluster name.</p>
      AccountWithRestoreAccess:
        type: String
        description: <p>The identifier of the AWS customer account that can no longer restore the specified snapshot.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  RevokeSnapshotAccessResult:
    type: object
    properties:
      Snapshot?:
        type: Snapshot
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  RotateEncryptionKeyMessage:
    type: object
    properties:
      ClusterIdentifier:
        type: String
        description: '<p>The unique identifier of the cluster that you want to rotate
          the encryption keys for.</p> <p>Constraints: Must be the name of valid cluster
          that has encryption enabled.</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  RotateEncryptionKeyResult:
    type: object
    properties:
      Cluster?:
        type: Cluster
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
  Snapshot:
    type: object
    properties:
      SnapshotIdentifier?:
        type: String
        description: <p>The snapshot identifier that is provided in the request.</p>
      ClusterIdentifier?:
        type: String
        description: <p>The identifier of the cluster for which the snapshot was taken.</p>
      SnapshotCreateTime?:
        type: TStamp
        description: <p>The time (UTC) when Amazon Redshift began the snapshot. A snapshot contains a copy of the cluster data as of this exact time.</p>
      Status?:
        type: String
        description: <p>The snapshot status. The value of the status depends on the API operation used. </p> <ul> <li> <p> <a>CreateClusterSnapshot</a> and <a>CopyClusterSnapshot</a> returns status as "creating". </p> </li> <li> <p> <a>DescribeClusterSnapshots</a> returns status as "creating", "available", "final snapshot", or "failed".</p> </li> <li> <p> <a>DeleteClusterSnapshot</a> returns status as "deleted".</p> </li> </ul>
      Port?:
        type: Integer
        description: <p>The port that the cluster is listening on.</p>
      AvailabilityZone?:
        type: String
        description: <p>The Availability Zone in which the cluster was created.</p>
      ClusterCreateTime?:
        type: TStamp
        description: <p>The time (UTC) when the cluster was originally created.</p>
      MasterUsername?:
        type: String
        description: <p>The master user name for the cluster.</p>
      ClusterVersion?:
        type: String
        description: <p>The version ID of the Amazon Redshift engine that is running on the cluster.</p>
      SnapshotType?:
        type: String
        description: <p>The snapshot type. Snapshots created using <a>CreateClusterSnapshot</a> and <a>CopyClusterSnapshot</a> will be of type "manual". </p>
      NodeType?:
        type: String
        description: <p>The node type of the nodes in the cluster.</p>
      NumberOfNodes?:
        type: Integer
        description: <p>The number of nodes in the cluster.</p>
      DBName?:
        type: String
        description: <p>The name of the database that was created when the cluster was created.</p>
      VpcId?:
        type: String
        description: <p>The VPC identifier of the cluster if the snapshot is from a cluster in a VPC. Otherwise, this field is not in the output.</p>
      Encrypted?:
        type: Boolean
        description: <p>If <code>true</code>, the data in the snapshot is encrypted at rest.</p>
      KmsKeyId?:
        type: String
        description: <p>The AWS Key Management Service (KMS) key ID of the encryption key that was used to encrypt data in the cluster from which the snapshot was taken.</p>
      EncryptedWithHSM?:
        type: Boolean
        description: <p>A boolean that indicates whether the snapshot data is encrypted using the HSM keys of the source cluster. <code>true</code> indicates that the data is encrypted using HSM keys.</p>
      AccountsWithRestoreAccess?:
        type: AccountsWithRestoreAccessList
        description: <p>A list of the AWS customer accounts authorized to restore the snapshot. Returns <code>null</code> if no accounts are authorized. Visible only to the snapshot owner. </p>
      OwnerAccount?:
        type: String
        description: <p>For manual snapshots, the AWS customer account used to create or copy the snapshot. For automatic snapshots, the owner of the cluster. The owner can perform all snapshot actions, such as sharing a manual snapshot.</p>
      TotalBackupSizeInMegaBytes?:
        type: Double
        description: <p>The size of the complete set of backup data that would be used to restore the cluster.</p>
      ActualIncrementalBackupSizeInMegaBytes?:
        type: Double
        description: <p>The size of the incremental backup.</p>
      BackupProgressInMegaBytes?:
        type: Double
        description: <p>The number of megabytes that have been transferred to the snapshot backup.</p>
      CurrentBackupRateInMegaBytesPerSecond?:
        type: Double
        description: <p>The number of megabytes per second being transferred to the snapshot backup. Returns <code>0</code> for a completed backup. </p>
      EstimatedSecondsToCompletion?:
        type: Long
        description: <p>The estimate of the time remaining before the snapshot backup will complete. Returns <code>0</code> for a completed backup. </p>
      ElapsedTimeInSeconds?:
        type: Long
        description: <p>The amount of time an in-progress snapshot backup has been running, or the amount of time it took a completed backup to finish.</p>
      SourceRegion?:
        type: String
        description: <p>The source region from which the snapshot was copied.</p>
      Tags?:
        type: TagList
        description: <p>The list of tags for the cluster snapshot.</p>
      RestorableNodeTypes?:
        type: RestorableNodeTypeList
        description: <p>The list of node types that this cluster snapshot is able to restore into.</p>
      EnhancedVpcRouting?:
        type: Boolean
        description: '<p>An option that specifies whether to create the cluster with
          enhanced VPC routing enabled. To create a cluster that uses enhanced VPC
          routing, the cluster must be in a VPC. For more information, see <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/enhanced-vpc-routing.html">Enhanced
          VPC Routing</a> in the Amazon Redshift Cluster Management Guide.</p> <p>If
          this option is <code>true</code>, enhanced VPC routing is enabled. </p>
          <p>Default: false</p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a snapshot.</p>
  SnapshotCopyGrant:
    type: object
    properties:
      SnapshotCopyGrantName?:
        type: String
        description: <p>The name of the snapshot copy grant.</p>
      KmsKeyId?:
        type: String
        description: <p>The unique identifier of the customer master key (CMK) in AWS KMS to which Amazon Redshift is granted permission.</p>
      Tags?:
        type: TagList
        description: <p>A list of tag instances.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>The snapshot copy grant that grants Amazon Redshift permission to encrypt copied snapshots with the specified customer master key (CMK) from AWS KMS in the destination region.</p> <p> For more information about managing snapshot copy grants, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-db-encryption.html">Amazon Redshift Database Encryption</a> in the <i>Amazon Redshift Cluster Management Guide</i>. </p>
  SnapshotCopyGrantMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: '<p>An optional parameter that specifies the starting point to
          return a set of response records. When the results of a <code>DescribeSnapshotCopyGrant</code>
          request exceed the value specified in <code>MaxRecords</code>, AWS returns
          a value in the <code>Marker</code> field of the response. You can retrieve
          the next set of response records by providing the returned marker value
          in the <code>Marker</code> parameter and retrying the request. </p> <p>Constraints:
          You can specify either the <b>SnapshotCopyGrantName</b> parameter or the
          <b>Marker</b> parameter, but not both. </p>'
      SnapshotCopyGrants?:
        type: SnapshotCopyGrantList
        description: <p>The list of <code>SnapshotCopyGrant</code> objects.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  SnapshotMessage:
    type: object
    properties:
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
      Snapshots?:
        type: SnapshotList
        description: <p>A list of <a>Snapshot</a> instances. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Contains the output from the <a>DescribeClusterSnapshots</a> action. </p>
  Subnet:
    type: object
    properties:
      SubnetIdentifier?:
        type: String
        description: <p>The identifier of the subnet.</p>
      SubnetAvailabilityZone?:
        type: AvailabilityZone
      SubnetStatus?:
        type: String
        description: <p>The status of the subnet.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes a subnet.</p>
  TableRestoreStatus:
    type: object
    properties:
      TableRestoreRequestId?:
        type: String
        description: <p>The unique identifier for the table restore request.</p>
      Status?:
        type: TableRestoreStatusType
        description: '<p>A value that describes the current state of the table restore
          request.</p> <p>Valid Values: <code>SUCCEEDED</code>, <code>FAILED</code>,
          <code>CANCELED</code>, <code>PENDING</code>, <code>IN_PROGRESS</code> </p>'
      Message?:
        type: String
        description: <p>A description of the status of the table restore request. Status values include <code>SUCCEEDED</code>, <code>FAILED</code>, <code>CANCELED</code>, <code>PENDING</code>, <code>IN_PROGRESS</code>.</p>
      RequestTime?:
        type: TStamp
        description: <p>The time that the table restore request was made, in Universal Coordinated Time (UTC).</p>
      ProgressInMegaBytes?:
        type: LongOptional
        description: <p>The amount of data restored to the new table so far, in megabytes (MB).</p>
      TotalDataInMegaBytes?:
        type: LongOptional
        description: <p>The total amount of data to restore to the new table, in megabytes (MB).</p>
      ClusterIdentifier?:
        type: String
        description: <p>The identifier of the Amazon Redshift cluster that the table is being restored to.</p>
      SnapshotIdentifier?:
        type: String
        description: <p>The identifier of the snapshot that the table is being restored from.</p>
      SourceDatabaseName?:
        type: String
        description: <p>The name of the source database that contains the table being restored.</p>
      SourceSchemaName?:
        type: String
        description: <p>The name of the source schema that contains the table being restored.</p>
      SourceTableName?:
        type: String
        description: <p>The name of the source table being restored.</p>
      TargetDatabaseName?:
        type: String
        description: <p>The name of the database to restore the table to.</p>
      TargetSchemaName?:
        type: String
        description: <p>The name of the schema to restore the table to.</p>
      NewTableName?:
        type: String
        description: <p>The name of the table to create as a result of the table restore request.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the status of a <a>RestoreTableFromClusterSnapshot</a> operation.</p>
  TableRestoreStatusMessage:
    type: object
    properties:
      TableRestoreStatusDetails?:
        type: TableRestoreStatusList
        description: <p>A list of status details for one or more table restore requests.</p>
      Marker?:
        type: String
        description: <p>A pagination token that can be used in a subsequent <a>DescribeTableRestoreStatus</a> request.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  Tag:
    type: object
    properties:
      Key?:
        type: String
        description: <p>The key, or name, for the resource tag.</p>
      Value?:
        type: String
        description: <p>The value for the resource tag.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>A tag consisting of a name/value pair for a resource.</p>
  TaggedResource:
    type: object
    properties:
      Tag?:
        type: Tag
        description: <p>The tag for the resource.</p>
      ResourceName?:
        type: String
        description: <p>The Amazon Resource Name (ARN) with which the tag is associated. For example, <code>arn:aws:redshift:us-east-1:123456789:cluster:t1</code>.</p>
      ResourceType?:
        type: String
        description: '<p>The type of resource with which the tag is associated. Valid
          resource types are: </p> <ul> <li> <p>Cluster</p> </li> <li> <p>CIDR/IP</p>
          </li> <li> <p>EC2 security group</p> </li> <li> <p>Snapshot</p> </li> <li>
          <p>Cluster security group</p> </li> <li> <p>Subnet group</p> </li> <li>
          <p>HSM connection</p> </li> <li> <p>HSM certificate</p> </li> <li> <p>Parameter
          group</p> </li> </ul> <p>For more information about Amazon Redshift resource
          types and constructing ARNs, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/constructing-redshift-arn.html">Constructing
          an Amazon Redshift Amazon Resource Name (ARN)</a> in the Amazon Redshift
          Cluster Management Guide. </p>'
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>A tag and its associated resource.</p>
  TaggedResourceListMessage:
    type: object
    properties:
      TaggedResources?:
        type: TaggedResourceList
        description: <p>A list of tags with their associated resources.</p>
      Marker?:
        type: String
        description: <p>A value that indicates the starting point for the next set of response records in a subsequent request. If a value is returned in a response, you can retrieve the next set of records by providing this returned marker value in the <code>Marker</code> parameter and retrying the command. If the <code>Marker</code> field is empty, all response records have been retrieved for the request. </p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p/>
  VpcSecurityGroupMembership:
    type: object
    properties:
      VpcSecurityGroupId?:
        type: String
        description: <p>The identifier of the VPC security group.</p>
      Status?:
        type: String
        description: <p>The status of the VPC security group.</p>
    xml:
      namespace: http://redshift.amazonaws.com/doc/2012-12-01/
    description: <p>Describes the members of a VPC security group.</p>
/{AuthorizeClusterSecurityGroupIngress}:
  uriParameters:
    AuthorizeClusterSecurityGroupIngress?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Authorize Cluster Security Group Ingress
  post:
    description: Adds an inbound (ingress) rule to an Amazon Redshift security group. Depending on whether the application accessing your cluster is running on the Internet or an Amazon EC2 instance, you can authorize inbound access to either a Classless Interdomain Routing (CIDR)/Internet Protocol (IP) range or to an Amazon EC2 security group. You can add as many as 20 ingress rules to an Amazon Redshift security group. If you authorize access to an Amazon EC2 security group, specify <i>EC2SecurityGroupName</i> and <i>EC2SecurityGroupOwnerId</i>. The Amazon EC2 security group and Amazon Redshift cluster must be in the same AWS region.  If you authorize access to a CIDR/IP address range, specify <i>CIDRIP</i>. For an overview of CIDR blocks, see the Wikipedia article on <a href="http://en.wikipedia.org/wiki/Classless_Inter-Domain_Routing">Classless Inter-Domain Routing</a>.  You must also associate the security group with a cluster so that clients running on these IP addresses or the EC2 instance are authorized to connect to the cluster. For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Working with Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Authorize Cluster Security Group Ingress
    queryParameters:
      Action:
        type: string
        enum:
        - AuthorizeClusterSecurityGroupIngress
    body:
      application/xml: AuthorizeClusterSecurityGroupIngressMessage
    responses:
      200:
        body:
          application/xml: AuthorizeClusterSecurityGroupIngressResult
      404:
        description: ClusterSecurityGroupNotFound
      400:
        description: InvalidClusterSecurityGroupState
/{AuthorizeSnapshotAccess}:
  uriParameters:
    AuthorizeSnapshotAccess?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Authorize Snapshot Access
  post:
    description: Authorizes the specified AWS customer account to restore the specified snapshot.  For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Authorize Snapshot Access
    queryParameters:
      Action:
        type: string
        enum:
        - AuthorizeSnapshotAccess
    body:
      application/xml: AuthorizeSnapshotAccessMessage
    responses:
      200:
        body:
          application/xml: AuthorizeSnapshotAccessResult
      404:
        description: ClusterSnapshotNotFound
      400:
        description: AuthorizationAlreadyExists
/{CopyClusterSnapshot}:
  uriParameters:
    CopyClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Copy Cluster Snapshot
  post:
    description: Copies the specified automated cluster snapshot to a new manual cluster snapshot. The source must be an automated snapshot and it must be in the available state. When you delete a cluster, Amazon Redshift deletes any automated snapshots of the cluster. Also, when the retention period of the snapshot expires, Amazon Redshift automatically deletes it. If you want to keep an automated snapshot for a longer period, you can make a manual copy of the snapshot. Manual snapshots are retained until you delete them.  For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Copy Cluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - CopyClusterSnapshot
    body:
      application/xml: CopyClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: CopyClusterSnapshotResult
      400:
        description: ClusterSnapshotAlreadyExists
      404:
        description: ClusterSnapshotNotFound
/{CreateCluster}:
  uriParameters:
    CreateCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Cluster
  post:
    description: Creates a new cluster. To create the cluster in Virtual Private Cloud (VPC), you must provide a cluster subnet group name. The cluster subnet group identifies the subnets of your VPC that Amazon Redshift uses when creating the cluster. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Create Cluster
    queryParameters:
      Action:
        type: string
        enum:
        - CreateCluster
    body:
      application/xml: CreateClusterMessage
    responses:
      200:
        body:
          application/xml: CreateClusterResult
      400:
        description: ClusterAlreadyExists
      404:
        description: ClusterParameterGroupNotFound
/{CreateClusterParameterGroup}:
  uriParameters:
    CreateClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Cluster Parameter Group
  post:
    description: Creates an Amazon Redshift parameter group. Creating parameter groups is independent of creating clusters. You can associate a cluster with a parameter group when you create the cluster. You can also associate an existing cluster with a parameter group after the cluster is created by using <a>ModifyCluster</a>.  Parameters in the parameter group define specific behavior that applies to the databases you create on the cluster. For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Create Cluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateClusterParameterGroup
    body:
      application/xml: CreateClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: CreateClusterParameterGroupResult
      400:
        description: ClusterParameterGroupQuotaExceeded
/{CreateClusterSecurityGroup}:
  uriParameters:
    CreateClusterSecurityGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Cluster Security Group
  post:
    description: Creates a new Amazon Redshift security group. You use security groups to control access to non-VPC clusters.  For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Create Cluster Security Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateClusterSecurityGroup
    body:
      application/xml: CreateClusterSecurityGroupMessage
    responses:
      200:
        body:
          application/xml: CreateClusterSecurityGroupResult
      400:
        description: ClusterSecurityGroupAlreadyExists
/{CreateClusterSnapshot}:
  uriParameters:
    CreateClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Cluster Snapshot
  post:
    description: Creates a manual snapshot of the specified cluster. The cluster must be in the <code>available</code> state.   For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Create Cluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - CreateClusterSnapshot
    body:
      application/xml: CreateClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: CreateClusterSnapshotResult
      400:
        description: ClusterSnapshotAlreadyExists
      404:
        description: ClusterNotFound
/{CreateClusterSubnetGroup}:
  uriParameters:
    CreateClusterSubnetGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Cluster Subnet Group
  post:
    description: Creates a new Amazon Redshift subnet group. You must provide a list of one or more subnets in your existing Amazon Virtual Private Cloud (Amazon VPC) when creating Amazon Redshift subnet group.  For information about subnet groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-cluster-subnet-groups.html">Amazon Redshift Cluster Subnet Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Create Cluster Subnet Group
    queryParameters:
      Action:
        type: string
        enum:
        - CreateClusterSubnetGroup
    body:
      application/xml: CreateClusterSubnetGroupMessage
    responses:
      200:
        body:
          application/xml: CreateClusterSubnetGroupResult
      400:
        description: ClusterSubnetGroupAlreadyExists
/{CreateEventSubscription}:
  uriParameters:
    CreateEventSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Event Subscription
  post:
    description: Creates an Amazon Redshift event notification subscription. This action requires an ARN (Amazon Resource Name) of an Amazon SNS topic created by either the Amazon Redshift console, the Amazon SNS console, or the Amazon SNS API. To obtain an ARN with Amazon SNS, you must create a topic in Amazon SNS and subscribe to the topic. The ARN is displayed in the SNS console. You can specify the source type, and lists of Amazon Redshift source IDs, event categories, and event severities. Notifications will be sent for all events you want that match those criteria. For example, you can specify source type = cluster, source ID = my-cluster-1 and mycluster2, event categories = Availability, Backup, and severity = ERROR. The subscription will only send notifications for those ERROR events in the Availability and Backup categories for the specified clusters. If you specify both the source type and source IDs, such as source type = cluster and source identifier = my-cluster-1, notifications will be sent for all the cluster events for my-cluster-1. If you specify a source type but do not specify a source identifier, you will receive notice of the events for the objects of that type in your AWS account. If you do not specify either the SourceType nor the SourceIdentifier, you will be notified of events generated from all Amazon Redshift sources belonging to your AWS account. You must specify a source type if you specify a source ID.
    displayName: Create Event Subscription
    queryParameters:
      Action:
        type: string
        enum:
        - CreateEventSubscription
    body:
      application/xml: CreateEventSubscriptionMessage
    responses:
      200:
        body:
          application/xml: CreateEventSubscriptionResult
      400:
        description: EventSubscriptionQuotaExceeded
      404:
        description: SNSTopicArnNotFound
/{CreateHsmClientCertificate}:
  uriParameters:
    CreateHsmClientCertificate?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Hsm Client Certificate
  post:
    description: Creates an HSM client certificate that an Amazon Redshift cluster will use to connect to the client's HSM in order to store and retrieve the keys used to encrypt the cluster databases. The command returns a public key, which you must store in the HSM. In addition to creating the HSM certificate, you must create an Amazon Redshift HSM configuration that provides a cluster the information needed to store and use encryption keys in the HSM. For more information, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-HSM.html">Hardware Security Modules</a> in the Amazon Redshift Cluster Management Guide.
    displayName: Create Hsm Client Certificate
    queryParameters:
      Action:
        type: string
        enum:
        - CreateHsmClientCertificate
    body:
      application/xml: CreateHsmClientCertificateMessage
    responses:
      200:
        body:
          application/xml: CreateHsmClientCertificateResult
      400:
        description: HsmClientCertificateAlreadyExistsFault
/{CreateHsmConfiguration}:
  uriParameters:
    CreateHsmConfiguration?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Hsm Configuration
  post:
    description: Creates an HSM configuration that contains the information required by an Amazon Redshift cluster to store and use database encryption keys in a Hardware Security Module (HSM). After creating the HSM configuration, you can specify it as a parameter when creating a cluster. The cluster will then store its encryption keys in the HSM. In addition to creating an HSM configuration, you must also create an HSM client certificate. For more information, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-HSM.html">Hardware Security Modules</a> in the Amazon Redshift Cluster Management Guide.
    displayName: Create Hsm Configuration
    queryParameters:
      Action:
        type: string
        enum:
        - CreateHsmConfiguration
    body:
      application/xml: CreateHsmConfigurationMessage
    responses:
      200:
        body:
          application/xml: CreateHsmConfigurationResult
      400:
        description: HsmConfigurationAlreadyExistsFault
/{CreateSnapshotCopyGrant}:
  uriParameters:
    CreateSnapshotCopyGrant?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Snapshot Copy Grant
  post:
    description: 'Creates a snapshot copy grant that permits Amazon Redshift to use
      a customer master key (CMK) from AWS Key Management Service (AWS KMS) to encrypt
      copied snapshots in a destination region.  For more information about managing
      snapshot copy grants, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-db-encryption.html">Amazon
      Redshift Database Encryption</a> in the <i>Amazon Redshift Cluster Management
      Guide</i>. '
    displayName: Create Snapshot Copy Grant
    queryParameters:
      Action:
        type: string
        enum:
        - CreateSnapshotCopyGrant
    body:
      application/xml: CreateSnapshotCopyGrantMessage
    responses:
      200:
        body:
          application/xml: CreateSnapshotCopyGrantResult
      400:
        description: SnapshotCopyGrantAlreadyExistsFault
/{CreateTags}:
  uriParameters:
    CreateTags?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Create Tags
  post:
    description: Adds one or more tags to a specified resource. A resource can have up to 10 tags. If you try to create more than 10 tags for a resource, you will receive an error and the attempt will fail. If you specify a key that already exists for the resource, the value for that key will be updated with the new value.
    displayName: Create Tags
    queryParameters:
      Action:
        type: string
        enum:
        - CreateTags
    body:
      application/xml: CreateTagsMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: TagLimitExceededFault
      404:
        description: ResourceNotFoundFault
/{DeleteCluster}:
  uriParameters:
    DeleteCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Cluster
  post:
    description: Deletes a previously provisioned cluster. A successful response from the web service indicates that the request was received correctly. Use <a>DescribeClusters</a> to monitor the status of the deletion. The delete operation cannot be canceled or reverted once submitted. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>. If you want to shut down the cluster and retain it for future use, set <i>SkipFinalClusterSnapshot</i> to <code>false</code> and specify a name for <i>FinalClusterSnapshotIdentifier</i>. You can later restore this snapshot to resume using the cluster. If a final cluster snapshot is requested, the status of the cluster will be "final-snapshot" while the snapshot is being taken, then it's "deleting" once Amazon Redshift begins deleting the cluster.   For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Delete Cluster
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteCluster
    body:
      application/xml: DeleteClusterMessage
    responses:
      200:
        body:
          application/xml: DeleteClusterResult
      404:
        description: ClusterNotFound
      400:
        description: InvalidClusterState
/{DeleteClusterParameterGroup}:
  uriParameters:
    DeleteClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Cluster Parameter Group
  post:
    description: Deletes a specified Amazon Redshift parameter group. <note> You cannot delete a parameter group if it is associated with a cluster. </note>
    displayName: Delete Cluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteClusterParameterGroup
    body:
      application/xml: DeleteClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidClusterParameterGroupState
      404:
        description: ClusterParameterGroupNotFound
/{DeleteClusterSecurityGroup}:
  uriParameters:
    DeleteClusterSecurityGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Cluster Security Group
  post:
    description: Deletes an Amazon Redshift security group. <note> You cannot delete a security group that is associated with any clusters. You cannot delete the default security group. </note>  For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Delete Cluster Security Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteClusterSecurityGroup
    body:
      application/xml: DeleteClusterSecurityGroupMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidClusterSecurityGroupState
      404:
        description: ClusterSecurityGroupNotFound
/{DeleteClusterSnapshot}:
  uriParameters:
    DeleteClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Cluster Snapshot
  post:
    description: Deletes the specified manual snapshot. The snapshot must be in the <code>available</code> state, with no other users authorized to access the snapshot.  Unlike automated snapshots, manual snapshots are retained even after you delete your cluster. Amazon Redshift does not delete your manual snapshots. You must delete manual snapshot explicitly to avoid getting charged. If other accounts are authorized to access the snapshot, you must revoke all of the authorizations before you can delete the snapshot.
    displayName: Delete Cluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteClusterSnapshot
    body:
      application/xml: DeleteClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: DeleteClusterSnapshotResult
      400:
        description: InvalidClusterSnapshotState
      404:
        description: ClusterSnapshotNotFound
/{DeleteClusterSubnetGroup}:
  uriParameters:
    DeleteClusterSubnetGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Cluster Subnet Group
  post:
    description: Deletes the specified cluster subnet group.
    displayName: Delete Cluster Subnet Group
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteClusterSubnetGroup
    body:
      application/xml: DeleteClusterSubnetGroupMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidClusterSubnetGroupStateFault
/{DeleteEventSubscription}:
  uriParameters:
    DeleteEventSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Event Subscription
  post:
    description: Deletes an Amazon Redshift event notification subscription.
    displayName: Delete Event Subscription
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteEventSubscription
    body:
      application/xml: DeleteEventSubscriptionMessage
    responses:
      200:
        body:
          application/xml: null
      404:
        description: SubscriptionNotFound
      400:
        description: InvalidSubscriptionStateFault
/{DeleteHsmClientCertificate}:
  uriParameters:
    DeleteHsmClientCertificate?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Hsm Client Certificate
  post:
    description: Deletes the specified HSM client certificate.
    displayName: Delete Hsm Client Certificate
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteHsmClientCertificate
    body:
      application/xml: DeleteHsmClientCertificateMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidHsmClientCertificateStateFault
/{DeleteHsmConfiguration}:
  uriParameters:
    DeleteHsmConfiguration?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Hsm Configuration
  post:
    description: Deletes the specified Amazon Redshift HSM configuration.
    displayName: Delete Hsm Configuration
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteHsmConfiguration
    body:
      application/xml: DeleteHsmConfigurationMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidHsmConfigurationStateFault
/{DeleteSnapshotCopyGrant}:
  uriParameters:
    DeleteSnapshotCopyGrant?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Snapshot Copy Grant
  post:
    description: Deletes the specified snapshot copy grant.
    displayName: Delete Snapshot Copy Grant
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteSnapshotCopyGrant
    body:
      application/xml: DeleteSnapshotCopyGrantMessage
    responses:
      200:
        body:
          application/xml: null
      400:
        description: InvalidSnapshotCopyGrantStateFault
/{DeleteTags}:
  uriParameters:
    DeleteTags?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Delete Tags
  post:
    description: Deletes a tag or tags from a resource. You must provide the ARN of the resource from which you want to delete the tag or tags.
    displayName: Delete Tags
    queryParameters:
      Action:
        type: string
        enum:
        - DeleteTags
    body:
      application/xml: DeleteTagsMessage
    responses:
      200:
        body:
          application/xml: null
      404:
        description: ResourceNotFoundFault
      400:
        description: InvalidTagFault
/{DescribeClusterParameterGroups}:
  uriParameters:
    DescribeClusterParameterGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Cluster Parameter Groups
  post:
    description: Returns a list of Amazon Redshift parameter groups, including parameter groups you created and the default parameter group. For each parameter group, the response includes the parameter group name, description, and parameter group family name. You can optionally specify a name to retrieve the description of a specific parameter group.  For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>. If you specify both tag keys and tag values in the same request, Amazon Redshift returns all parameter groups that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all parameter groups that have any combination of those values are returned. If both tag keys and values are omitted from the request, parameter groups are returned regardless of whether they have tag keys or values associated with them.
    displayName: Describe Cluster Parameter Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeClusterParameterGroups
    body:
      application/xml: DescribeClusterParameterGroupsMessage
    responses:
      200:
        body:
          application/xml: ClusterParameterGroupsMessage
      404:
        description: ClusterParameterGroupNotFound
      400:
        description: InvalidTagFault
/{DescribeClusterParameters}:
  uriParameters:
    DescribeClusterParameters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Cluster Parameters
  post:
    description: Returns a detailed list of parameters contained within the specified Amazon Redshift parameter group. For each parameter the response includes information such as parameter name, description, data type, value, whether the parameter value is modifiable, and so on. You can specify <i>source</i> filter to retrieve parameters of only specific type. For example, to retrieve parameters that were modified by a user action such as from <a>ModifyClusterParameterGroup</a>, you can specify <i>source</i> equal to <i>user</i>.  For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Describe Cluster Parameters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeClusterParameters
    body:
      application/xml: DescribeClusterParametersMessage
    responses:
      200:
        body:
          application/xml: ClusterParameterGroupDetails
      404:
        description: ClusterParameterGroupNotFound
/{DescribeClusterSecurityGroups}:
  uriParameters:
    DescribeClusterSecurityGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Cluster Security Groups
  post:
    description: Returns information about Amazon Redshift security groups. If the name of a security group is specified, the response will contain only information about only that security group.  For information about managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>. If you specify both tag keys and tag values in the same request, Amazon Redshift returns all security groups that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all security groups that have any combination of those values are returned. If both tag keys and values are omitted from the request, security groups are returned regardless of whether they have tag keys or values associated with them.
    displayName: Describe Cluster Security Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeClusterSecurityGroups
    body:
      application/xml: DescribeClusterSecurityGroupsMessage
    responses:
      200:
        body:
          application/xml: ClusterSecurityGroupMessage
      404:
        description: ClusterSecurityGroupNotFound
      400:
        description: InvalidTagFault
/{DescribeClusterSnapshots}:
  uriParameters:
    DescribeClusterSnapshots?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Cluster Snapshots
  post:
    description: Returns one or more snapshot objects, which contain metadata about your cluster snapshots. By default, this operation returns information about all snapshots of all clusters that are owned by you AWS customer account. No information is returned for snapshots owned by inactive AWS customer accounts. If you specify both tag keys and tag values in the same request, Amazon Redshift returns all snapshots that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all snapshots that have any combination of those values are returned. Only snapshots that you own are returned in the response; shared snapshots are not returned with the tag key and tag value request parameters. If both tag keys and values are omitted from the request, snapshots are returned regardless of whether they have tag keys or values associated with them.
    displayName: Describe Cluster Snapshots
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeClusterSnapshots
    body:
      application/xml: DescribeClusterSnapshotsMessage
    responses:
      200:
        body:
          application/xml: SnapshotMessage
      404:
        description: ClusterSnapshotNotFound
      400:
        description: InvalidTagFault
/{DescribeClusterSubnetGroups}:
  uriParameters:
    DescribeClusterSubnetGroups?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Cluster Subnet Groups
  post:
    description: Returns one or more cluster subnet group objects, which contain metadata about your cluster subnet groups. By default, this operation returns information about all cluster subnet groups that are defined in you AWS account. If you specify both tag keys and tag values in the same request, Amazon Redshift returns all subnet groups that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all subnet groups that have any combination of those values are returned. If both tag keys and values are omitted from the request, subnet groups are returned regardless of whether they have tag keys or values associated with them.
    displayName: Describe Cluster Subnet Groups
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeClusterSubnetGroups
    body:
      application/xml: DescribeClusterSubnetGroupsMessage
    responses:
      200:
        body:
          application/xml: ClusterSubnetGroupMessage
      400:
        description: ClusterSubnetGroupNotFoundFault
/{DescribeClusterVersions}:
  uriParameters:
    DescribeClusterVersions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Cluster Versions
  post:
    description: Returns descriptions of the available Amazon Redshift cluster versions. You can call this operation even before creating any clusters to learn more about the Amazon Redshift versions. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Describe Cluster Versions
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeClusterVersions
    body:
      application/xml: DescribeClusterVersionsMessage
    responses:
      200:
        body:
          application/xml: ClusterVersionsMessage
/{DescribeClusters}:
  uriParameters:
    DescribeClusters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Clusters
  post:
    description: Returns properties of provisioned clusters including general cluster properties, cluster database properties, maintenance and backup properties, and security and access properties. This operation supports pagination. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>. If you specify both tag keys and tag values in the same request, Amazon Redshift returns all clusters that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all clusters that have any combination of those values are returned. If both tag keys and values are omitted from the request, clusters are returned regardless of whether they have tag keys or values associated with them.
    displayName: Describe Clusters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeClusters
    body:
      application/xml: DescribeClustersMessage
    responses:
      200:
        body:
          application/xml: ClustersMessage
      404:
        description: ClusterNotFound
      400:
        description: InvalidTagFault
/{DescribeDefaultClusterParameters}:
  uriParameters:
    DescribeDefaultClusterParameters?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Default Cluster Parameters
  post:
    description: Returns a list of parameter settings for the specified parameter group family.  For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Describe Default Cluster Parameters
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeDefaultClusterParameters
    body:
      application/xml: DescribeDefaultClusterParametersMessage
    responses:
      200:
        body:
          application/xml: DescribeDefaultClusterParametersResult
/{DescribeEventCategories}:
  uriParameters:
    DescribeEventCategories?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Event Categories
  post:
    description: Displays a list of event categories for all event source types, or for a specified source type. For a list of the event categories and source types, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-event-notifications.html">Amazon Redshift Event Notifications</a>.
    displayName: Describe Event Categories
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEventCategories
    body:
      application/xml: DescribeEventCategoriesMessage
    responses:
      200:
        body:
          application/xml: EventCategoriesMessage
/{DescribeEventSubscriptions}:
  uriParameters:
    DescribeEventSubscriptions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Event Subscriptions
  post:
    description: Lists descriptions of all the Amazon Redshift event notifications subscription for a customer account. If you specify a subscription name, lists the description for that subscription.
    displayName: Describe Event Subscriptions
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEventSubscriptions
    body:
      application/xml: DescribeEventSubscriptionsMessage
    responses:
      200:
        body:
          application/xml: EventSubscriptionsMessage
      404:
        description: SubscriptionNotFound
/{DescribeEvents}:
  uriParameters:
    DescribeEvents?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Events
  post:
    description: Returns events related to clusters, security groups, snapshots, and parameter groups for the past 14 days. Events specific to a particular cluster, security group, snapshot or parameter group can be obtained by providing the name as a parameter. By default, the past hour of events are returned.
    displayName: Describe Events
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeEvents
    body:
      application/xml: DescribeEventsMessage
    responses:
      200:
        body:
          application/xml: EventsMessage
/{DescribeHsmClientCertificates}:
  uriParameters:
    DescribeHsmClientCertificates?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Hsm Client Certificates
  post:
    description: Returns information about the specified HSM client certificate. If no certificate ID is specified, returns information about all the HSM certificates owned by your AWS customer account. If you specify both tag keys and tag values in the same request, Amazon Redshift returns all HSM client certificates that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all HSM client certificates that have any combination of those values are returned. If both tag keys and values are omitted from the request, HSM client certificates are returned regardless of whether they have tag keys or values associated with them.
    displayName: Describe Hsm Client Certificates
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeHsmClientCertificates
    body:
      application/xml: DescribeHsmClientCertificatesMessage
    responses:
      200:
        body:
          application/xml: HsmClientCertificateMessage
      400:
        description: HsmClientCertificateNotFoundFault
/{DescribeHsmConfigurations}:
  uriParameters:
    DescribeHsmConfigurations?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Hsm Configurations
  post:
    description: Returns information about the specified Amazon Redshift HSM configuration. If no configuration ID is specified, returns information about all the HSM configurations owned by your AWS customer account. If you specify both tag keys and tag values in the same request, Amazon Redshift returns all HSM connections that match any combination of the specified keys and values. For example, if you have <code>owner</code> and <code>environment</code> for tag keys, and <code>admin</code> and <code>test</code> for tag values, all HSM connections that have any combination of those values are returned. If both tag keys and values are omitted from the request, HSM connections are returned regardless of whether they have tag keys or values associated with them.
    displayName: Describe Hsm Configurations
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeHsmConfigurations
    body:
      application/xml: DescribeHsmConfigurationsMessage
    responses:
      200:
        body:
          application/xml: HsmConfigurationMessage
      400:
        description: HsmConfigurationNotFoundFault
/{DescribeLoggingStatus}:
  uriParameters:
    DescribeLoggingStatus?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Logging Status
  post:
    description: Describes whether information, such as queries and connection attempts, is being logged for the specified Amazon Redshift cluster.
    displayName: Describe Logging Status
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeLoggingStatus
    body:
      application/xml: DescribeLoggingStatusMessage
    responses:
      200:
        body:
          application/xml: LoggingStatus
      404:
        description: ClusterNotFound
/{DescribeOrderableClusterOptions}:
  uriParameters:
    DescribeOrderableClusterOptions?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Orderable Cluster Options
  post:
    description: Returns a list of orderable cluster options. Before you create a new cluster you can use this operation to find what options are available, such as the EC2 Availability Zones (AZ) in the specific AWS region that you can specify, and the node types you can request. The node types differ by available storage, memory, CPU and price. With the cost involved you might want to obtain a list of cluster options in the specific region and specify values when creating a cluster. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Describe Orderable Cluster Options
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeOrderableClusterOptions
    body:
      application/xml: DescribeOrderableClusterOptionsMessage
    responses:
      200:
        body:
          application/xml: OrderableClusterOptionsMessage
/{DescribeReservedNodeOfferings}:
  uriParameters:
    DescribeReservedNodeOfferings?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Reserved Node Offerings
  post:
    description: Returns a list of the available reserved node offerings by Amazon Redshift with their descriptions including the node type, the fixed and recurring costs of reserving the node and duration the node will be reserved for you. These descriptions help you determine which reserve node offering you want to purchase. You then use the unique offering ID in you call to <a>PurchaseReservedNodeOffering</a> to reserve one or more nodes for your Amazon Redshift cluster.   For more information about reserved node offerings, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/purchase-reserved-node-instance.html">Purchasing Reserved Nodes</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Describe Reserved Node Offerings
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeReservedNodeOfferings
    body:
      application/xml: DescribeReservedNodeOfferingsMessage
    responses:
      200:
        body:
          application/xml: ReservedNodeOfferingsMessage
      404:
        description: ReservedNodeOfferingNotFound
      400:
        description: UnsupportedOperation
/{DescribeReservedNodes}:
  uriParameters:
    DescribeReservedNodes?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Reserved Nodes
  post:
    description: Returns the descriptions of the reserved nodes.
    displayName: Describe Reserved Nodes
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeReservedNodes
    body:
      application/xml: DescribeReservedNodesMessage
    responses:
      200:
        body:
          application/xml: ReservedNodesMessage
      404:
        description: ReservedNodeNotFound
/{DescribeResize}:
  uriParameters:
    DescribeResize?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Resize
  post:
    description: 'Returns information about the last resize operation for the specified
      cluster. If no resize operation has ever been initiated for the specified cluster,
      a <code>HTTP 404</code> error is returned. If a resize operation was initiated
      and completed, the status of the resize remains as <code>SUCCEEDED</code> until
      the next resize.  A resize operation can be requested using <a>ModifyCluster</a>
      and specifying a different number or type of nodes for the cluster. '
    displayName: Describe Resize
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeResize
    body:
      application/xml: DescribeResizeMessage
    responses:
      200:
        body:
          application/xml: ResizeProgressMessage
      404:
        description: ClusterNotFound
/{DescribeSnapshotCopyGrants}:
  uriParameters:
    DescribeSnapshotCopyGrants?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Snapshot Copy Grants
  post:
    description: 'Returns a list of snapshot copy grants owned by the AWS account
      in the destination region.  For more information about managing snapshot copy
      grants, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-db-encryption.html">Amazon
      Redshift Database Encryption</a> in the <i>Amazon Redshift Cluster Management
      Guide</i>. '
    displayName: Describe Snapshot Copy Grants
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeSnapshotCopyGrants
    body:
      application/xml: DescribeSnapshotCopyGrantsMessage
    responses:
      200:
        body:
          application/xml: SnapshotCopyGrantMessage
      400:
        description: SnapshotCopyGrantNotFoundFault
/{DescribeTableRestoreStatus}:
  uriParameters:
    DescribeTableRestoreStatus?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Table Restore Status
  post:
    description: Lists the status of one or more table restore requests made using the <a>RestoreTableFromClusterSnapshot</a> API action. If you don't specify a value for the <code>TableRestoreRequestId</code> parameter, then <code>DescribeTableRestoreStatus</code> returns the status of all table restore requests ordered by the date and time of the request in ascending order. Otherwise <code>DescribeTableRestoreStatus</code> returns the status of the table specified by <code>TableRestoreRequestId</code>.
    displayName: Describe Table Restore Status
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeTableRestoreStatus
    body:
      application/xml: DescribeTableRestoreStatusMessage
    responses:
      200:
        body:
          application/xml: TableRestoreStatusMessage
      400:
        description: TableRestoreNotFoundFault
      404:
        description: ClusterNotFound
/{DescribeTags}:
  uriParameters:
    DescribeTags?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Describe Tags
  post:
    description: 'Returns a list of tags. You can return tags from a specific resource
      by specifying an ARN, or you can return all tags for a given type of resource,
      such as clusters, snapshots, and so on. The following are limitations for <code>DescribeTags</code>:  <ul>
      <li> You cannot specify an ARN and a resource-type value together in the same
      request. </li> <li> You cannot use the <code>MaxRecords</code> and <code>Marker</code>
      parameters together with the ARN parameter. </li> <li> The <code>MaxRecords</code>
      parameter can be a range from 10 to 50 results to return in a request. </li>
      </ul> If you specify both tag keys and tag values in the same request, Amazon
      Redshift returns all resources that match any combination of the specified keys
      and values. For example, if you have <code>owner</code> and <code>environment</code>
      for tag keys, and <code>admin</code> and <code>test</code> for tag values, all
      resources that have any combination of those values are returned. If both tag
      keys and values are omitted from the request, resources are returned regardless
      of whether they have tag keys or values associated with them.'
    displayName: Describe Tags
    queryParameters:
      Action:
        type: string
        enum:
        - DescribeTags
    body:
      application/xml: DescribeTagsMessage
    responses:
      200:
        body:
          application/xml: TaggedResourceListMessage
      404:
        description: ResourceNotFoundFault
      400:
        description: InvalidTagFault
/{DisableLogging}:
  uriParameters:
    DisableLogging?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Disable Logging
  post:
    description: Stops logging information, such as queries and connection attempts, for the specified Amazon Redshift cluster.
    displayName: Disable Logging
    queryParameters:
      Action:
        type: string
        enum:
        - DisableLogging
    body:
      application/xml: DisableLoggingMessage
    responses:
      200:
        body:
          application/xml: LoggingStatus
      404:
        description: ClusterNotFound
/{DisableSnapshotCopy}:
  uriParameters:
    DisableSnapshotCopy?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Disable Snapshot Copy
  post:
    description: 'Disables the automatic copying of snapshots from one region to another
      region for a specified cluster. If your cluster and its snapshots are encrypted
      using a customer master key (CMK) from AWS KMS, use <a>DeleteSnapshotCopyGrant</a>
      to delete the grant that grants Amazon Redshift permission to the CMK in the
      destination region. '
    displayName: Disable Snapshot Copy
    queryParameters:
      Action:
        type: string
        enum:
        - DisableSnapshotCopy
    body:
      application/xml: DisableSnapshotCopyMessage
    responses:
      200:
        body:
          application/xml: DisableSnapshotCopyResult
      404:
        description: ClusterNotFound
      400:
        description: SnapshotCopyAlreadyDisabledFault
/{EnableLogging}:
  uriParameters:
    EnableLogging?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Enable Logging
  post:
    description: Starts logging information, such as queries and connection attempts, for the specified Amazon Redshift cluster.
    displayName: Enable Logging
    queryParameters:
      Action:
        type: string
        enum:
        - EnableLogging
    body:
      application/xml: EnableLoggingMessage
    responses:
      200:
        body:
          application/xml: LoggingStatus
      404:
        description: ClusterNotFound
      400:
        description: BucketNotFoundFault
/{EnableSnapshotCopy}:
  uriParameters:
    EnableSnapshotCopy?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Enable Snapshot Copy
  post:
    description: Enables the automatic copy of snapshots from one region to another region for a specified cluster.
    displayName: Enable Snapshot Copy
    queryParameters:
      Action:
        type: string
        enum:
        - EnableSnapshotCopy
    body:
      application/xml: EnableSnapshotCopyMessage
    responses:
      200:
        body:
          application/xml: EnableSnapshotCopyResult
      400:
        description: IncompatibleOrderableOptions
      404:
        description: ClusterNotFound
/{ModifyCluster}:
  uriParameters:
    ModifyCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Cluster
  post:
    description: Modifies the settings for a cluster. For example, you can add another security or parameter group, update the preferred maintenance window, or change the master user password. Resetting a cluster password or modifying the security groups associated with a cluster do not need a reboot. However, modifying a parameter group requires a reboot for parameters to take effect. For more information about managing clusters, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>. You can also change node type and the number of nodes to scale up or down the cluster. When resizing a cluster, you must specify both the number of nodes and the node type even if one of the parameters does not change.
    displayName: Modify Cluster
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyCluster
    body:
      application/xml: ModifyClusterMessage
    responses:
      200:
        body:
          application/xml: ModifyClusterResult
      400:
        description: InvalidClusterState
      404:
        description: ClusterNotFound
/{ModifyClusterIamRoles}:
  uriParameters:
    ModifyClusterIamRoles?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Cluster IamRoles
  post:
    description: Modifies the list of AWS Identity and Access Management (IAM) roles that can be used by the cluster to access other AWS services. A cluster can have up to 10 IAM roles associated at any time.
    displayName: Modify Cluster IamRoles
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyClusterIamRoles
    body:
      application/xml: ModifyClusterIamRolesMessage
    responses:
      200:
        body:
          application/xml: ModifyClusterIamRolesResult
      400:
        description: InvalidClusterState
      404:
        description: ClusterNotFound
/{ModifyClusterParameterGroup}:
  uriParameters:
    ModifyClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Cluster Parameter Group
  post:
    description: Modifies the parameters of a parameter group.  For more information about parameters and parameter groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-parameter-groups.html">Amazon Redshift Parameter Groups</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Modify Cluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyClusterParameterGroup
    body:
      application/xml: ModifyClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: ClusterParameterGroupNameMessage
      404:
        description: ClusterParameterGroupNotFound
      400:
        description: InvalidClusterParameterGroupState
/{ModifyClusterSubnetGroup}:
  uriParameters:
    ModifyClusterSubnetGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Cluster Subnet Group
  post:
    description: Modifies a cluster subnet group to include the specified list of VPC subnets. The operation replaces the existing list of subnets with the new list of subnets.
    displayName: Modify Cluster Subnet Group
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyClusterSubnetGroup
    body:
      application/xml: ModifyClusterSubnetGroupMessage
    responses:
      200:
        body:
          application/xml: ModifyClusterSubnetGroupResult
      400:
        description: ClusterSubnetGroupNotFoundFault
/{ModifyEventSubscription}:
  uriParameters:
    ModifyEventSubscription?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Event Subscription
  post:
    description: Modifies an existing Amazon Redshift event notification subscription.
    displayName: Modify Event Subscription
    queryParameters:
      Action:
        type: string
        enum:
        - ModifyEventSubscription
    body:
      application/xml: ModifyEventSubscriptionMessage
    responses:
      200:
        body:
          application/xml: ModifyEventSubscriptionResult
      404:
        description: SubscriptionNotFound
      400:
        description: SNSInvalidTopic
/{ModifySnapshotCopyRetentionPeriod}:
  uriParameters:
    ModifySnapshotCopyRetentionPeriod?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Modify Snapshot Copy Retention Period
  post:
    description: Modifies the number of days to retain automated snapshots in the destination region after they are copied from the source region.
    displayName: Modify Snapshot Copy Retention Period
    queryParameters:
      Action:
        type: string
        enum:
        - ModifySnapshotCopyRetentionPeriod
    body:
      application/xml: ModifySnapshotCopyRetentionPeriodMessage
    responses:
      200:
        body:
          application/xml: ModifySnapshotCopyRetentionPeriodResult
      404:
        description: ClusterNotFound
      400:
        description: SnapshotCopyDisabledFault
/{PurchaseReservedNodeOffering}:
  uriParameters:
    PurchaseReservedNodeOffering?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Purchase Reserved Node Offering
  post:
    description: Allows you to purchase reserved nodes. Amazon Redshift offers a predefined set of reserved node offerings. You can purchase one or more of the offerings. You can call the <a>DescribeReservedNodeOfferings</a> API to obtain the available reserved node offerings. You can call this API by providing a specific reserved node offering and the number of nodes you want to reserve.   For more information about reserved node offerings, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/purchase-reserved-node-instance.html">Purchasing Reserved Nodes</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Purchase Reserved Node Offering
    queryParameters:
      Action:
        type: string
        enum:
        - PurchaseReservedNodeOffering
    body:
      application/xml: PurchaseReservedNodeOfferingMessage
    responses:
      200:
        body:
          application/xml: PurchaseReservedNodeOfferingResult
      404:
        description: ReservedNodeOfferingNotFound
      400:
        description: ReservedNodeQuotaExceeded
/{RebootCluster}:
  uriParameters:
    RebootCluster?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Reboot Cluster
  post:
    description: 'Reboots a cluster. This action is taken as soon as possible. It
      results in a momentary outage to the cluster, during which the cluster status
      is set to <code>rebooting</code>. A cluster event is created when the reboot
      is completed. Any pending cluster modifications (see <a>ModifyCluster</a>) are
      applied at this reboot. For more information about managing clusters, go to
      <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-clusters.html">Amazon
      Redshift Clusters</a> in the <i>Amazon Redshift Cluster Management Guide</i>. '
    displayName: Reboot Cluster
    queryParameters:
      Action:
        type: string
        enum:
        - RebootCluster
    body:
      application/xml: RebootClusterMessage
    responses:
      200:
        body:
          application/xml: RebootClusterResult
      400:
        description: InvalidClusterState
      404:
        description: ClusterNotFound
/{ResetClusterParameterGroup}:
  uriParameters:
    ResetClusterParameterGroup?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Reset Cluster Parameter Group
  post:
    description: 'Sets one or more parameters of the specified parameter group to
      their default values and sets the source values of the parameters to "engine-default".
      To reset the entire parameter group specify the <i>ResetAllParameters</i> parameter.
      For parameter changes to take effect you must reboot any associated clusters. '
    displayName: Reset Cluster Parameter Group
    queryParameters:
      Action:
        type: string
        enum:
        - ResetClusterParameterGroup
    body:
      application/xml: ResetClusterParameterGroupMessage
    responses:
      200:
        body:
          application/xml: ClusterParameterGroupNameMessage
      400:
        description: InvalidClusterParameterGroupState
      404:
        description: ClusterParameterGroupNotFound
/{RestoreFromClusterSnapshot}:
  uriParameters:
    RestoreFromClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Restore From Cluster Snapshot
  post:
    description: Creates a new cluster from a snapshot. By default, Amazon Redshift creates the resulting cluster with the same configuration as the original cluster from which the snapshot was created, except that the new cluster is created with the default cluster security and parameter groups. After Amazon Redshift creates the cluster, you can use the <a>ModifyCluster</a> API to associate a different security group and different parameter group with the restored cluster. If you are using a DS node type, you can also choose to change to another DS node type of the same size during restore. If you restore a cluster into a VPC, you must provide a cluster subnet group where you want the cluster restored.  For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Restore From Cluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - RestoreFromClusterSnapshot
    body:
      application/xml: RestoreFromClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: RestoreFromClusterSnapshotResult
      400:
        description: AccessToSnapshotDenied
      404:
        description: ClusterSnapshotNotFound
      406:
        description: InvalidRestore
/{RestoreTableFromClusterSnapshot}:
  uriParameters:
    RestoreTableFromClusterSnapshot?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Restore Table From Cluster Snapshot
  post:
    description: Creates a new table from a table in an Amazon Redshift cluster snapshot. You must create the new table within the Amazon Redshift cluster that the snapshot was taken from. You cannot use <code>RestoreTableFromClusterSnapshot</code> to restore a table with the same name as an existing table in an Amazon Redshift cluster. That is, you cannot overwrite an existing table in a cluster with a restored table. If you want to replace your original table with a new, restored table, then rename or drop your original table before you call <code>RestoreTableFromClusterSnapshot</code>. When you have renamed your original table, then you can pass the original name of the table as the <code>NewTableName</code> parameter value in the call to <code>RestoreTableFromClusterSnapshot</code>. This way, you can replace the original table with the table created from the snapshot.
    displayName: Restore Table From Cluster Snapshot
    queryParameters:
      Action:
        type: string
        enum:
        - RestoreTableFromClusterSnapshot
    body:
      application/xml: RestoreTableFromClusterSnapshotMessage
    responses:
      200:
        body:
          application/xml: RestoreTableFromClusterSnapshotResult
      404:
        description: ClusterSnapshotNotFound
      400:
        description: InProgressTableRestoreQuotaExceededFault
/{RevokeClusterSecurityGroupIngress}:
  uriParameters:
    RevokeClusterSecurityGroupIngress?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Revoke Cluster Security Group Ingress
  post:
    description: 'Revokes an ingress rule in an Amazon Redshift security group for
      a previously authorized IP range or Amazon EC2 security group. To add an ingress
      rule, see <a>AuthorizeClusterSecurityGroupIngress</a>. For information about
      managing security groups, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-security-groups.html">Amazon
      Redshift Cluster Security Groups</a> in the <i>Amazon Redshift Cluster Management
      Guide</i>. '
    displayName: Revoke Cluster Security Group Ingress
    queryParameters:
      Action:
        type: string
        enum:
        - RevokeClusterSecurityGroupIngress
    body:
      application/xml: RevokeClusterSecurityGroupIngressMessage
    responses:
      200:
        body:
          application/xml: RevokeClusterSecurityGroupIngressResult
      404:
        description: ClusterSecurityGroupNotFound
      400:
        description: InvalidClusterSecurityGroupState
/{RevokeSnapshotAccess}:
  uriParameters:
    RevokeSnapshotAccess?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Revoke Snapshot Access
  post:
    description: Removes the ability of the specified AWS customer account to restore the specified snapshot. If the account is currently restoring the snapshot, the restore will run to completion.  For more information about working with snapshots, go to <a href="http://docs.aws.amazon.com/redshift/latest/mgmt/working-with-snapshots.html">Amazon Redshift Snapshots</a> in the <i>Amazon Redshift Cluster Management Guide</i>.
    displayName: Revoke Snapshot Access
    queryParameters:
      Action:
        type: string
        enum:
        - RevokeSnapshotAccess
    body:
      application/xml: RevokeSnapshotAccessMessage
    responses:
      200:
        body:
          application/xml: RevokeSnapshotAccessResult
      400:
        description: AccessToSnapshotDenied
      404:
        description: AuthorizationNotFound
/{RotateEncryptionKey}:
  uriParameters:
    RotateEncryptionKey?:
      type: string
      enum:
      - ''
      (extras.syntetic): true
      description: This url parameter exists only to differentiate different operations on the same url
  displayName: Rotate Encryption Key
  post:
    description: Rotates the encryption keys for a cluster.
    displayName: Rotate Encryption Key
    queryParameters:
      Action:
        type: string
        enum:
        - RotateEncryptionKey
    body:
      application/xml: RotateEncryptionKeyMessage
    responses:
      200:
        body:
          application/xml: RotateEncryptionKeyResult
      404:
        description: ClusterNotFound
      400:
        description: InvalidClusterState
